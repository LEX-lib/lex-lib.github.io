---
phase: 36-mobile-performance
plan: 07
type: execute
wave: 4
depends_on: [36-01, 36-02, 36-03, 36-04, 36-05, 36-06]
files_modified:
  - index.html
  - .planning/STATE.md
autonomous: false
requirements: [PF-01, PF-09]
tags: [performance, preconnect, dns-prefetch, automated-gates, human-verify]
user_setup:
  - service: chrome-devtools-mobile-emulation
    why: "CLS visual check at mobile viewport (390×844) — same emulation path used in Phase 35 close"
    env_vars: []
    dashboard_config:
      - task: "Open DevTools → Toggle device toolbar (Ctrl+Shift+M) → iPhone 14 Pro (390×844)"
        location: "Local Chrome/Edge DevTools (no external account)"

must_haves:
  truths:
    - "index.html contains <link rel=\"preconnect\" href=\"https://lexarium-backend.fly.dev\" crossorigin /> AND <link rel=\"dns-prefetch\" href=\"https://lexarium-backend.fly.dev\" /> inserted after the favicon link and BEFORE the LOCKED viewport comment"
    - "index.html LOCKED viewport comment (line 6 in current file) and `<meta name=\"viewport\" ... viewport-fit=cover, interactive-widget=resizes-content\" />` (line 7) remain byte-intact"
    - "Post-phase `npm run analyze` shows the Wallecx route chunk gzip ≤ floor(pre-phase * 0.5) — recorded in SUMMARY against the 36-BASELINE.md number"
    - "Post-phase `npm run build` output contains 0 lines matching 'exceeds' or 'Skipping precaching' (NFR-PWA-PRECACHE-FITS)"
    - "Automated gates green: npm run type-check 0 errors, npm run test:unit ≥59 tests, npm run lint clean except grandfathered VaccinationDetail.vue:5"
    - "Grep audits pass on all NFR/CON invariants enumerated in the audit task"
    - "STATE.md `Architectural Invariants` section adds the previously-implicit `vaccinations-getFullList` requestKey as an explicit locked invariant alongside memberships/expenses/expense-budgets/expense-categories"
    - "Human-verify checkpoint records: CLS observation at 390×844 mobile viewport across all 3 tabs (cold load → skeleton → content), AND a real-device cellular session result for the PF-05 baseline if available (deferral to Phase 38 is acceptable per Phase 35 precedent)"
  artifacts:
    - path: index.html
      provides: "PocketBase origin preconnect + dns-prefetch hints"
    - path: .planning/STATE.md
      provides: "Updated Architectural Invariants — vaccinations-getFullList requestKey now explicit"
  key_links:
    - from: index.html
      to: "https://lexarium-backend.fly.dev"
      via: "<link rel=preconnect> + <link rel=dns-prefetch>"
---

<objective>
Phase 36 close: (1) add the PocketBase origin preconnect + dns-prefetch hints to `index.html` (PF-09), (2) re-run the analyzer and prove the ≥50% Wallecx route chunk reduction against the Plan 36-01 baseline (PF-01 / D-36-03), (3) run a full automated-gate + grep-audit sweep across all NFR/CON invariants Phase 36 binds (NFR-PWA-PRECACHE-FITS, NFR-REQUESTKEY-UNIQUE, CON-PB-COUNT-BUG, CON-VIEWPORT-FIT, CON-CONFIRMDIALOG-SINGLETON, CON-CARD-COLOR-NO-HASH, BR-2), (4) update STATE.md to make the newly-fixed `vaccinations-getFullList` requestKey an explicit locked invariant, then (5) human-verify checkpoint at 390×844 mobile devtools emulation for CLS + Suspense-fallback visual behavior across all 3 tabs (real-device cellular PF-05 reading deferred to Phase 38 per Phase 35 D-35-13 precedent).

Purpose: This plan is the proof-of-shipment. The pre/post visualizer comparison is the explicit gating evidence for D-36-03; the automated gates + grep audits replicate the Phase 35-06 evidence pattern; the human-verify checkpoint catches any unforeseen CLS jolt before phase close.

Output: index.html + STATE.md modifications; analyze re-run with delta vs baseline recorded in SUMMARY; 36-07-AUDIT.md grep+gate evidence file; human-approval record in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36-mobile-performance/36-CONTEXT.md
@.planning/phases/36-mobile-performance/36-RESEARCH.md
@.planning/phases/36-mobile-performance/36-PATTERNS.md
@.planning/phases/36-mobile-performance/36-BASELINE.md
@index.html
@vite.config.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: index.html — add preconnect + dns-prefetch hints (LOCKED viewport byte-intact)</name>
  <files>index.html</files>
  <read_first>
    - index.html (current file — lines 4–7: favicon, LOCKED viewport comment, viewport meta)
    - .planning/phases/36-mobile-performance/36-PATTERNS.md §"index.html (config — MODIFY)"
    - .planning/phases/36-mobile-performance/36-RESEARCH.md §"Pattern 6: Preconnect + dns-prefetch in index.html"
    - .env.production (confirm VITE_API_BASE_URL=https://lexarium-backend.fly.dev)
  </read_first>
  <action>
    Insert TWO new lines into `index.html` immediately after the favicon `<link rel="icon" ... />` line (currently line 5) and BEFORE the LOCKED viewport comment (currently line 6). The LOCKED comment AND the `<meta name="viewport">` tag MUST remain byte-intact.

    Target `<head>` shape after edit:
    ```html
    <head>
      <meta charset="UTF-8" />
      <link rel="icon" type="image/svg+xml" href="/branding_logo.svg" />
      <link rel="preconnect" href="https://lexarium-backend.fly.dev" crossorigin />
      <link rel="dns-prefetch" href="https://lexarium-backend.fly.dev" />
      <!-- LOCKED: viewport-fit=cover is required for env(safe-area-inset-*) non-zero values (CON-VIEWPORT-FIT / LT-09). Do not remove. -->
      <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, interactive-widget=resizes-content" />
      ...
    ```

    Ordering matters: `preconnect` FIRST (establishes TLS + DNS), `dns-prefetch` SECOND (fallback for browsers that ignore preconnect). The `crossorigin` attribute is required on `preconnect` because the PocketBase API is cross-origin fetch (per RESEARCH Pattern 6). No other origins are added in this phase (D-36-11 — PB origin only).

    Do NOT touch:
    - Line 6 LOCKED viewport comment string
    - Line 7 viewport meta `content="width=device-width, initial-scale=1.0, viewport-fit=cover, interactive-widget=resizes-content"`
    - The inline theme bootstrap script
    - `<meta name="color-scheme">`
    - `<title>`
    - `<body>` and the `#app` mount
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Select-String -Path index.html -Pattern 'rel=\"preconnect\".*lexarium-backend.fly.dev').Count -ge 1 -and (Select-String -Path index.html -Pattern 'rel=\"dns-prefetch\".*lexarium-backend.fly.dev').Count -ge 1 -and (Select-String -Path index.html -Pattern 'viewport-fit=cover, interactive-widget=resizes-content').Count -ge 1"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains the literal string `<link rel="preconnect" href="https://lexarium-backend.fly.dev" crossorigin />`
    - index.html contains the literal string `<link rel="dns-prefetch" href="https://lexarium-backend.fly.dev" />`
    - index.html STILL contains the LOCKED viewport comment line byte-intact (literal: `<!-- LOCKED: viewport-fit=cover is required for env(safe-area-inset-*) non-zero values (CON-VIEWPORT-FIT / LT-09). Do not remove. -->`)
    - index.html STILL contains the literal string `viewport-fit=cover, interactive-widget=resizes-content` (CON-VIEWPORT-FIT preserved)
    - index.html does NOT contain a preconnect or dns-prefetch hint for any origin OTHER than `lexarium-backend.fly.dev` (D-36-11; no scope creep)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>2 PocketBase-origin resource hints added; LOCKED viewport comment + meta byte-intact; no other origins introduced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Post-phase analyze + automated gates + grep audit</name>
  <files>.planning/phases/36-mobile-performance/36-07-AUDIT.md</files>
  <read_first>
    - .planning/phases/36-mobile-performance/36-BASELINE.md (pre-phase Wallecx route chunk gzip; reduction target)
    - .planning/phases/36-mobile-performance/36-RESEARCH.md §"Pre-Phase Baseline Measurement Procedure" (Step 5+6 — post-implementation re-run)
    - vite.config.ts (LOCKED lines 28/42/86/88 — must be checked)
  </read_first>
  <action>
    Run the post-phase analyzer + the full automated gate sweep + the grep audit. Record every result in `.planning/phases/36-mobile-performance/36-07-AUDIT.md` using the Phase 35-06 audit format as the template.

    **Step 1 — Post-phase analyze:**
    ```powershell
    npm run analyze
    ```
    Open `dist/stats.html`. Find the Wallecx route chunk; record its gzip size. Confirm `chart-js-[hash].js`, `jsbarcode-[hash].js`, `image-compression-[hash].js` appear as separate top-level chunks.

    **Step 2 — Plain build (PWA precache gate):**
    ```powershell
    npm run build
    ```
    Scan stdout for the words "exceeds" and "Skipping precaching" — both must return 0 matches.

    **Step 3 — Automated test gates:**
    ```powershell
    npm run type-check        # must exit 0
    npm run test:unit         # must report ≥59 passing
    npm run lint              # must be clean except grandfathered VaccinationDetail.vue:5
    ```

    **Step 4 — Grep audits (record each command + numeric output):**
    Run each of the following in the working directory. Record the count for each.

    | Audit | Command | Expected |
    |-------|---------|----------|
    | A1 — vite.config registerType LOCKED | `Select-String -Path vite.config.ts -Pattern 'registerType: "prompt",.*LOCKED'` | exactly 1 match (byte-intact) |
    | A2 — vite.config scope LOCKED | `Select-String -Path vite.config.ts -Pattern 'scope: "/",.*LOCKED'` | exactly 1 match |
    | A3 — vite.config maximumFileSize LOCKED | `Select-String -Path vite.config.ts -Pattern 'maximumFileSizeToCacheInBytes: 3 \* 1024 \* 1024'` | exactly 1 match |
    | A4 — vite.config navigateFallback LOCKED | `Select-String -Path vite.config.ts -Pattern 'navigateFallback: "index.html",.*LOCKED'` | exactly 1 match |
    | A5 — vite.config new chunk groups | `Select-String -Path vite.config.ts -Pattern 'chart-js|jsbarcode|image-compression'` | 3 matches |
    | A6 — index.html preconnect | `Select-String -Path index.html -Pattern 'rel="preconnect".*lexarium-backend.fly.dev'` | 1 match |
    | A7 — index.html dns-prefetch | `Select-String -Path index.html -Pattern 'rel="dns-prefetch".*lexarium-backend.fly.dev'` | 1 match |
    | A8 — viewport CON-VIEWPORT-FIT byte-intact | `Select-String -Path index.html -Pattern 'viewport-fit=cover, interactive-widget=resizes-content'` | 1 match |
    | A9 — VaccinationsTab new requestKey | `Select-String -Path src/components/projects/wallecx/VaccinationsTab.vue -Pattern "requestKey: ""vaccinations-getFullList"""` | ≥1 match |
    | A10 — Memberships requestKey preserved | `Select-String -Path src/components/projects/wallecx/MembershipsTab.vue -Pattern "memberships-getFullList"` | ≥1 match |
    | A11 — Expenses requestKey preserved | `Select-String -Path src/components/projects/wallecx/ExpensesTab.vue -Pattern "expenses-getFullList"` | ≥1 match |
    | A12 — Budgets requestKey preserved | `Select-String -Path src/components/projects/wallecx/ExpensesTab.vue -Pattern "expense-budgets-getFullList"` | ≥1 match |
    | A13 — Categories requestKey preserved | `Select-String -Path src/components/projects/wallecx/ExpensesReportsView.vue -Pattern "expense-categories-getFullList"` | ≥1 match |
    | A14 — Export-path requestKey preserved (out-of-scope path untouched) | `Select-String -Path src/components/projects/wallecx/ExpensesTab.vue -Pattern "expenses-export"` | ≥1 match |
    | A15 — CON-PB-COUNT-BUG no new getList | `Select-String -Path src/components/projects/wallecx -Pattern 'pb\.collection\(.*\)\.getList\(' -Recurse` | 0 matches (no `.getList(` introduced); if any exists pre-Phase 36, document it explicitly |
    | A16 — ConfirmDialog singleton | `Select-String -Path src/components/projects/wallecx -Pattern '<ConfirmDialog' -Recurse` | exactly 1 match (must live at WallecxApp.vue) |
    | A17 — No orphaned imageCompression calls | `Select-String -Path src/components/projects/wallecx -Pattern 'imageCompression\(' -Recurse` | 0 matches in Manage*.vue (all routed through compressToWebP) |
    | A18 — No orphaned imageCompression imports | `Select-String -Path src/components/projects/wallecx -Pattern 'import imageCompression from' -Recurse` | 0 matches (direct imports gone) |
    | A19 — ManageExpense Pitfall-4 fix | `Select-String -Path src/components/projects/wallecx/ManageExpense.vue -Pattern "type: 'image/webp'"` | ≥1 match |
    | A20 — BR-2 BarcodeDisplay untouched | `git diff --name-only HEAD~N -- src/components/projects/wallecx/BarcodeDisplay.vue` (where N is the count of phase-36 commits) | 0 lines (file not modified this phase) |

    **Step 5 — Wallecx route chunk reduction calculation:**
    Compute `post_gzip ≤ floor(pre_gzip * 0.5)` using the number recorded in 36-BASELINE.md. Record both numbers and the percent delta in the audit file.

    **Step 6 — Sample WebP size delta (optional but document if available):**
    If a sample image upload was performed during dev testing, record the pre-vs-post file size for that sample (bytes). If not available, note "deferred to Phase 38 real-device session."

    **Audit file structure** (`.planning/phases/36-mobile-performance/36-07-AUDIT.md`):
    ```markdown
    # Phase 36 — Audit (Plan 36-07)

    **Run:** YYYY-MM-DD

    ## Bundle Reduction (D-36-03)
    | Metric | Pre-phase | Post-phase | Delta |
    |--------|-----------|------------|-------|
    | Wallecx route chunk gzip | N KB | N' KB | -X% |

    Reduction target met: YES / NO (must be YES)

    New extracted chunks (gzip):
    - chart-js: N KB
    - jsbarcode: N KB
    - image-compression: N KB

    ## NFR-PWA-PRECACHE-FITS (`npm run build`)
    - "exceeds" matches: 0
    - "Skipping precaching" matches: 0

    ## Automated Gates
    | Gate | Result |
    |------|--------|
    | npm run type-check | 0 errors |
    | npm run test:unit | N/N passing |
    | npm run lint | clean except grandfathered VaccinationDetail.vue:5 |

    ## Grep Audit
    | # | Audit | Result | Pass |
    |---|-------|--------|------|
    | A1 | registerType LOCKED byte-intact | 1 match | ✓ |
    | ... | ... | ... | ... |

    All audits PASS: YES / NO
    ```
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Test-Path .planning/phases/36-mobile-performance/36-07-AUDIT.md -and (Get-Content .planning/phases/36-mobile-performance/36-07-AUDIT.md -Raw).Length -gt 500"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/36-mobile-performance/36-07-AUDIT.md` exists and contains the literal headers `Bundle Reduction`, `Automated Gates`, and `Grep Audit`
    - Bundle reduction row records a Wallecx route chunk gzip delta with `Reduction target met: YES`
    - All 20 grep audits recorded with PASS results (or explicit pre-existing exception with explanation)
    - `npm run build` 0 "exceeds" lines; 0 "Skipping precaching" lines
    - `npm run type-check` exits 0
    - `npm run test:unit` reports ≥59 tests passing
    - `npm run lint` clean except grandfathered VaccinationDetail.vue:5
  </acceptance_criteria>
  <done>Post-phase audit file with bundle delta, all automated gates green, all 20 grep audits PASS (or explicit exception); reduction target ≥50% met.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: STATE.md — add vaccinations-getFullList as explicit locked invariant</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md §"Architectural Invariants (locked)" — find the "requestKey per collection" bullet
  </read_first>
  <action>
    Update the existing `requestKey per collection` bullet in STATE.md §"Architectural Invariants (locked)" to explicitly list ALL 5 collection keys (currently 3 are listed: memberships, vaccinations, expenses; Phase 36 makes `vaccinations-getFullList` explicit and adds `expense-budgets-getFullList` + `expense-categories-getFullList` which were already in use but not in the invariants list).

    Find:
    ```
    - **requestKey per collection.** `'memberships-getFullList'` and `'vaccinations-getFullList'` and `'expenses-getFullList'` must stay distinct to prevent PocketBase auto-cancel.
    ```

    Replace with:
    ```
    - **requestKey per collection.** All five locked mount-path keys must stay distinct to prevent PocketBase auto-cancel: `'vaccinations-getFullList'` (added in Phase 36-03 to close NFR-REQUESTKEY-UNIQUE gap), `'memberships-getFullList'`, `'expenses-getFullList'`, `'expense-budgets-getFullList'`, `'expense-categories-getFullList'`. Export-path keys (e.g. `'expenses-export'`) are independent and out of scope for the mount-path invariant. Any new mount-path fetch added in future phases MUST register a sixth distinct key.
    ```

    Do NOT touch any other invariant bullet. Do NOT change the "Last updated" date — that update is owned by the post-phase commit / next session-bootstrap step.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(Select-String -Path .planning/STATE.md -Pattern 'vaccinations-getFullList.*added in Phase 36').Count -ge 1 -and (Select-String -Path .planning/STATE.md -Pattern 'expense-budgets-getFullList').Count -ge 1 -and (Select-String -Path .planning/STATE.md -Pattern 'expense-categories-getFullList').Count -ge 1"</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md contains the literal string `vaccinations-getFullList` AND the phrase `added in Phase 36`
    - STATE.md lists all 5 mount-path requestKeys in the requestKey invariant: vaccinations / memberships / expenses / expense-budgets / expense-categories
    - STATE.md notes export-path keys (e.g. expenses-export) are out of scope for the mount-path invariant
    - Other Architectural Invariant bullets in STATE.md remain byte-intact
  </acceptance_criteria>
  <done>STATE.md requestKey invariant updated to reflect Phase 36's NFR-REQUESTKEY-UNIQUE closure across all 5 mount-path collections.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify checkpoint — CLS at 390×844 across all 3 tabs + PF-05 real-device deferral acknowledgment</name>
  <what-built>
    Phase 36 delivered: 3 helper files (WallecxSkeleton, perfInstrument, compressToWebP), vite.config.ts rolldown groups (chart-js/jsbarcode/image-compression at priority 25), async-loaded tabs in WallecxApp.vue with Suspense+skeleton fallbacks, async-loaded Manage* dialogs in each tab with Suspense+skeleton fallbacks, instrumented mount-path getFullList across all 5 wallecx_* collections, the previously-missing `vaccinations-getFullList` requestKey, consolidated inline skeletons (5 surfaces), WebP image compression in all 3 Manage* upload paths, PocketBase-origin preconnect+dns-prefetch hints, ≥50% Wallecx route chunk reduction verified vs pre-phase baseline.
  </what-built>
  <how-to-verify>
    Run a 6-behavior local check at mobile devtools emulation. Phase 35 D-35-13 precedent applies: emulation approval is sufficient for in-phase close; real-device cellular PF-05 reading and real-device CLS visual final-check are formally deferred to Phase 38 UAT sweep.

    **Setup:**
    1. `npm run dev`
    2. Open the app at `http://localhost:5173/projects/wallecx`
    3. DevTools → toggle device toolbar (Ctrl+Shift+M) → iPhone 14 Pro (390×844)
    4. DevTools → Network → throttle to "Fast 3G" (proxy for mid-tier mobile cellular)
    5. DevTools → Performance → ensure layout-shift observer (or paste the CLS PerformanceObserver snippet from 36-RESEARCH.md §"CLS Measurement Procedure" into Console)

    **Behaviors to verify:**

    1. **Cold tab load — Vaccinations tab.** Hard refresh (Ctrl+F5). Watch for: brief vaccination-card skeleton (3 cards, byte-matched dimensions) → real cards swap in. EXPECTED: no jarring layout reflow; CLS observer reports no shift > 0.1 from the swap.

    2. **Cold tab load — Memberships tab.** Click Memberships tab. Watch for: brief membership-card skeleton → real cards swap in. EXPECTED: no jarring reflow; CLS ≤ 0.1.

    3. **Cold tab load — Expenses tab.** Click Expenses tab. Watch for: expense-row skeleton → real rows swap in (List view); period chart skeleton → real chart on Reports sub-tab. EXPECTED: no jarring reflow; CLS ≤ 0.1.

    4. **PF-05 console log + localStorage write.** Open DevTools Console. EXPECTED: 5 `[wallecx:perf]` lines (one per collection on first session interaction), each showing `{collection}: N records, N bytes (proxy), Nms`. Then in DevTools → Application → Local Storage → http://localhost:5173 → EXPECTED: key `wallecx:perf-baseline` contains a JSON object with 5 collection entries (rings of 1 entry each on this fresh session).

    5. **WebP upload smoke test.** Open Add Expense (or Add Vaccination/Membership) → attach an image (any JPEG/PNG you have locally). After the EXIF-strip+compression completes (toast "Location data removed."), open DevTools Network tab and Save the dialog. EXPECTED: the upload request to PocketBase shows the file part with Content-Type `image/webp`. Cancel-before-save is fine; the verification is that the FormData carries the WebP MIME.

    6. **Suspense fallback visible only on first switch.** Switch between tabs multiple times. EXPECTED: skeleton appears only on the FIRST switch to a tab (chunk download); subsequent switches show content immediately (PrimeVue Tabs default `display:none`, RESEARCH A1). If PrimeVue 4.5.5 remounts (A1 is wrong), skeleton appears on every switch — STILL ACCEPTABLE per RESEARCH (graceful degradation, not a bug), but note it in the SUMMARY for awareness.

    **Pass criteria (paste back as comma-separated yes/no list — Phase-28 D-13 paste-back convention):**
    - 1 cold-load Vaccinations CLS≤0.1: yes/no
    - 2 cold-load Memberships CLS≤0.1: yes/no
    - 3 cold-load Expenses CLS≤0.1: yes/no
    - 4 PF-05 console log + localStorage write present: yes/no
    - 5 WebP upload MIME confirmed image/webp: yes/no
    - 6 Suspense fallback only on first tab switch (or graceful re-show acceptable): yes/no

    **Deferral (explicit acknowledgment):**
    - Real-device cellular PF-05 baseline reading → Phase 38 UAT sweep (Phase 35 D-35-13 precedent for deferring real-device tests).
    - The Phase-38b conditional virtualization trigger remains pending until that real-device read.
    - Real-device CLS visual final-check → Phase 38 UAT sweep.
  </how-to-verify>
  <resume-signal>Paste back the 6-yes/no list (e.g. `yes,yes,yes,yes,yes,yes` or specific failures), confirm acknowledgment of Phase 38 deferrals, OR list specific issues to fix-forward.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → https://lexarium-backend.fly.dev | Preconnect + dns-prefetch establish TLS+DNS early; same origin every API call already uses. No new origin trust introduced. |
| index.html → browser resource hint stack | Resource hints are advisory; worst case the browser ignores them (NEGLIGIBLE risk per RESEARCH A3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-18 | Information Disclosure | preconnect leaks PB origin in HTML source | accept | PocketBase origin is already public — it appears in every fetch URL via VITE_API_BASE_URL. Adding a preconnect hint discloses nothing not already disclosed. |
| T-36-19 | Tampering | LOCKED viewport line drift during edit | mitigate | Acceptance criteria + grep audit A8 enforce byte-intact LOCKED comment + viewport meta after the preconnect insert. |
| T-36-20 | Denial of Service | Excess preconnects to many origins | mitigate | D-36-11 scopes Phase 36 to PB origin ONLY; acceptance criteria explicitly forbid adding any other origin (no scope creep). |
| T-36-21 | Information Disclosure | STATE.md updated invariant exposes internal naming | accept | Internal naming convention; STATE.md is project planning artifact, not deployed. No production surface change. |

**No HIGH-severity threats introduced.** Phase 36 close is metadata + planning artifact updates + a resource hint to an already-used origin.
</threat_model>

<verification>
- `npm run type-check` — 0 errors
- `npm run test:unit` — ≥59 tests pass
- `npm run lint` — clean except grandfathered VaccinationDetail.vue:5
- `npm run build` — 0 "exceeds" lines, 0 "Skipping precaching" lines
- `npm run analyze` — Wallecx route chunk gzip ≤ floor(baseline * 0.5); chart-js/jsbarcode/image-compression chunks visible
- 36-07-AUDIT.md exists with all 20 grep audits recorded as PASS
- STATE.md requestKey invariant lists all 5 mount-path keys
- index.html preconnect + dns-prefetch hints present; LOCKED viewport byte-intact
- Human-verify checkpoint approved with 6-yes/no paste-back
</verification>

<success_criteria>
- All 6 Phase 36 functional requirements satisfied across the 7 plans:
  - PF-01: bundle visualizer report run pre-phase (36-01) AND post-phase (36-07) with delta recorded
  - PF-02: 3 tabs + 3 Manage dialogs async-loaded; initial Wallecx chunk -≥50%
  - PF-04: WallecxSkeleton 5 variants consolidate 5 inline skeletons + serve 6 Suspense fallbacks; CLS ≤ 0.1
  - PF-05: instrumentedGetFullList wraps all 5 mount-path collections; localStorage + console.info on one-shot-per-session-per-collection gate
  - PF-07: 3 Manage* upload paths produce WebP-typed Files via shared compressToWebP helper
  - PF-09: PocketBase-origin preconnect + dns-prefetch hints in index.html
- All bound NFR/CON gates preserved (NFR-PWA-PRECACHE-FITS, CON-PB-COUNT-BUG, NFR-REQUESTKEY-UNIQUE — INCLUDING the newly-closed VaccinationsTab gap, CON-VIEWPORT-FIT, CON-CONFIRMDIALOG-SINGLETON, CON-CARD-COLOR-NO-HASH, BR-2)
- vite.config.ts LOCKED lines 28/42/86/88 byte-intact
- Human-verify checkpoint approved at 390×844 emulation
- Real-device PF-05 + final CLS check formally deferred to Phase 38 UAT sweep (per D-35-13 precedent)
</success_criteria>

<output>
After completion, create `.planning/phases/36-mobile-performance/36-07-SUMMARY.md` containing:
- Recap of 7-plan completion with per-plan one-liner
- 36-BASELINE pre vs 36-07-AUDIT post bundle numbers (Wallecx route chunk gzip + 3 new vendor chunks)
- The 6 human-verify yes/no responses with any noted issues
- WebP sample size delta if measured (else "deferred to Phase 38")
- Explicit Phase 38 carry-overs: real-device cellular PF-05 baseline + real-device CLS final-check
- Phase 38b trigger evaluation: PENDING until Phase 38 real-device read
- Then create a milestone-close-style block in ROADMAP.md marking Phase 36 complete (7/7 plans) and noting Phase 36 next: Phase 37 PWA Install + Standalone Polish.
</output>
