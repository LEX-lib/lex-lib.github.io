---
phase: 33-mobile-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: false
requirements: [FND-04]
user_setup: []

must_haves:
  truths:
    - "package.json pins vue at ^3.5.34 and primevue at ^4.5.5"
    - "@primevue/auto-import-resolver and @primevue/forms are both at ^4.5.x in lockstep with primevue"
    - "npm run type-check, npm run build, and npm run test:unit (49/49) all pass on the bumped baseline"
    - "PrimeVue Drawer, Dialog, DatePicker (touchUI), FileUpload (capture), MultiSelect, and ColorPicker direct-v-model are manually confirmed working after the bump"
    - "The v2.3 PrimeVue #7465 dark-mode override still suppresses white-flash-on-open after the minor bump"
  artifacts:
    - path: "package.json"
      provides: "Clean v4.3 version baseline (Vue 3.5.34 + PrimeVue 4.5.5 lockstep)"
      contains: '"primevue": "^4.5'
  key_links:
    - from: "package.json primevue ^4.5.5"
      to: "@primevue/auto-import-resolver ^4.5.x + @primevue/forms ^4.5.x"
      via: "lockstep version pin"
      pattern: '"@primevue/(auto-import-resolver|forms)": "\\^4\\.5'
---

<objective>
Bump Vue (3.5.18 → 3.5.34, patch) and PrimeVue (4.3.7 → 4.5.5, minor) in lockstep with `@primevue/auto-import-resolver` and `@primevue/forms`, establishing ONE clean version baseline for the entire v4.3 milestone. Posture is fix-forward (D-07): if the smoke-test surfaces a regression, fix it within this plan before merge — do NOT pin PrimeVue back or split the baseline.

This plan lands first (Wave 1) so the composable work in Plan 02 builds against the already-bumped PrimeVue/Vue, and so the milestone never carries a mixed Vue-new/PrimeVue-old baseline.

Purpose: Implements FND-04. Delivers the dependency primitive every later v4.3 phase consumes.
Output: Updated `package.json`; a passing automated gate (type-check + build + 49/49 tests + lint); a manual smoke-test confirmation covering the 6 PrimeVue surfaces + the #7465 dark-mode CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-mobile-foundation/33-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
<!-- Exact current versions from package.json (read-first). Bump these to the targets below. -->
Current (package.json):
  dependencies:    "vue": "^3.5.18", "primevue": "^4.3.7", "@primevue/forms": "^4.3.9"
  devDependencies: "@primevue/auto-import-resolver": "^4.3.7"

Targets (STACK.md "Installation" block — exact strings):
  "vue": "^3.5.34"
  "primevue": "^4.5.5"
  "@primevue/auto-import-resolver": "^4.5.5"
  "@primevue/forms": "^4.5.5"

Smoke-test surface (D-08 — must be manually verified before merge):
  Drawer, Dialog, DatePicker (touchUI), FileUpload (capture), MultiSelect, ColorPicker direct-v-model.
  HIGHEST-WATCH: v2.3 PrimeVue #7465 dark-mode override CSS — verify it still suppresses
  white-flash-on-open after the 4.3→4.5 minor bump (it targets internal PrimeVue classes that can shift between minors).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Vue + PrimeVue lockstep versions and verify the automated gate</name>
  <read_first>
    - package.json (the file being modified — current version strings)
    - .planning/research/STACK.md (§"Version Upgrades" + §"Installation" for exact target strings and rationale)
    - .planning/phases/33-mobile-foundation/33-CONTEXT.md (D-07 fix-forward posture)
  </read_first>
  <action>
    Run the four version installs from STACK.md (do them together, lockstep):
    `npm install vue@^3.5.34`
    `npm install primevue@^4.5.5 @primevue/auto-import-resolver@^4.5.5 @primevue/forms@^4.5.5`

    This bumps `vue` from ^3.5.18 to ^3.5.34 (patch) in `dependencies`, `primevue` from ^4.3.7 to ^4.5.5 (minor) in `dependencies`, `@primevue/forms` from ^4.3.9 to ^4.5.5 in `dependencies`, and `@primevue/auto-import-resolver` from ^4.3.7 to ^4.5.5 in `devDependencies`.

    Do NOT modify any other dependency. Do NOT touch `vite-plugin-pwa` (STACK.md: already current, no change). Do NOT touch `vite.config.ts` in this plan (that is Plan 03's surface — keep file ownership clean for parallel-safe waves).

    After install, run the full automated gate IN ORDER and fix-forward any failure within this plan (per D-07 — do NOT roll PrimeVue back):
    1. `npm run type-check` (vue-tsc --build) — must exit 0
    2. `npm run build` — must exit 0; scan output for 0 lines matching "exceeds" or "Skipping precaching" (NFR-PWA-PRECACHE-FITS sanity, though Phase 36 owns it)
    3. `npm run test:unit` — must report 49 passed / 49 total (regression floor; do NOT regress)
    4. `npm run lint` — must pass except the pre-existing grandfathered `VaccinationDetail.vue:5` warning (consistent with v4.2 close)

    If type-check or build surfaces a breaking change from the PrimeVue minor (e.g. a shifted internal class or a renamed prop), fix the consuming code within this plan and re-run the gate until green.
  </action>
  <verify>
    <automated>npm run type-check && npm run test:unit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "vue": "^3.5.34"
    - package.json dependencies contains "primevue": "^4.5.5"
    - package.json dependencies contains "@primevue/forms": "^4.5.5"
    - package.json devDependencies contains "@primevue/auto-import-resolver": "^4.5.5"
    - npm run type-check exits 0
    - npm run build exits 0 with 0 output lines matching "exceeds" or "Skipping precaching"
    - npm run test:unit reports 49 passed, 49 total (exits 0)
    - npm run lint passes except the pre-existing VaccinationDetail.vue:5 warning
    - vite.config.ts is unchanged by this plan (git diff shows no vite.config.ts modification)
  </acceptance_criteria>
  <done>All four version strings match the targets; type-check, build, test (49/49), and lint pass on the bumped baseline; no other file modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual PrimeVue smoke-test on bumped baseline (D-08)</name>
  <read_first>
    - .planning/phases/33-mobile-foundation/33-CONTEXT.md (D-08 — exact smoke-test surface + highest-watch item)
    - .planning/ROADMAP.md (Phase 33 Success Criterion 4)
  </read_first>
  <what-built>
    Vue 3.5.34 + PrimeVue 4.5.5 (lockstep with auto-import-resolver + forms) installed; automated gate (type-check + build + 49/49 tests + lint) green.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the Wallecx mini-app at `/projects/wallecx`. Verify each of the 6 PrimeVue surfaces works after the minor bump:
    1. **Drawer** — open MembershipDetail or VaccinationGroupPanel bottom-sheet; it opens/closes, no console errors.
    2. **Dialog** — open any Manage* dialog on a desktop-width window; renders centered, closes cleanly.
    3. **DatePicker (touchUI)** — in ManageExpense / ManageVaccination, open the date field; calendar renders and a date is selectable.
    4. **FileUpload (capture)** — in ManageExpense / ManageMembership receipt/scan affordance, the file chooser opens.
    5. **MultiSelect** — in ExpensesListView category filter, the dropdown opens, multiple options select, chips render.
    6. **ColorPicker direct-v-model** — in ManageMembership, open the card-colour picker; selecting a colour updates the bound value (PrimeVue #8135 direct-v-model workaround still works — value is NOT ignored).

    **HIGHEST-WATCH — #7465 dark-mode CSS:** toggle the site to dark mode (NavBar toggle), then open a Dialog AND a Drawer. Confirm there is NO white-flash-on-open — the override CSS that suppressed the flash in v2.3 must still apply after the 4.3→4.5 minor (internal PrimeVue classes it targets may have shifted).
  </how-to-verify>
  <acceptance_criteria>
    - All 6 PrimeVue surfaces confirmed working (paste back which were checked)
    - Dark-mode Dialog + Drawer open with NO white-flash (#7465 override intact) — confirmed explicitly
    - If any surface regressed, the regression + the fix-forward applied is recorded in the SUMMARY (per D-07; no PrimeVue pin-back)
  </acceptance_criteria>
  <resume-signal>Type "approved" (and list which surfaces you checked + dark-mode flash result), or describe issues to fix-forward.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party package code crosses into the build at install time (supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `vue` / `primevue` upgraded packages (supply chain) | mitigate | Install only the exact pinned versions from STACK.md; do not run `npm audit fix --force` or bump unrelated deps; review the lockfile diff for unexpected transitive additions before merge |
| T-33-02 | Tampering | `package-lock.json` integrity | accept | Lockfile is git-ignored in this repo (`.gitignore` excludes package-lock.json); integrity relies on npm's registry hash verification at install time — acceptable for a single-maintainer personal project |
| T-33-03 | Denial of Service | Build/runtime regression from PrimeVue minor | mitigate | Automated gate (type-check + build + 49/49 tests) + manual 6-surface smoke-test gate before merge; fix-forward within this plan (D-07) |
</threat_model>

<verification>
- `git diff package.json` shows exactly four version-string changes (vue, primevue, @primevue/forms, @primevue/auto-import-resolver) and no other dependency churn.
- Full automated gate green: `npm run type-check` (0), `npm run build` (0, no precache-skip lines), `npm run test:unit` (49/49), `npm run lint` (clean except grandfathered VaccinationDetail.vue:5).
- Manual smoke-test approved with all 6 PrimeVue surfaces + dark-mode no-flash confirmed.
</verification>

<success_criteria>
- ROADMAP Phase 33 Success Criterion 4 met: Vue patch-bump + PrimeVue minor-bump lockstep with auto-import-resolver + forms landed on `feat/wallecx` with a documented branch smoke-test confirming Drawer + Dialog + DatePicker (touchUI) + FileUpload (capture) + MultiSelect + ColorPicker direct-v-model survive.
- One clean version baseline established for the whole milestone (no mixed Vue-new/PrimeVue-old state).
</success_criteria>

<output>
After completion, create `.planning/phases/33-mobile-foundation/33-01-SUMMARY.md` documenting the version deltas, the automated gate results (test count, build status), the smoke-test outcome (which surfaces checked, dark-mode flash result), and any fix-forward changes applied.
</output>
