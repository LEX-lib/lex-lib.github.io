---
phase: 33-mobile-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - package.json
  - vite.config.ts
  - .gitignore
autonomous: true
requirements: [FND-03]
user_setup: []

must_haves:
  truths:
    - "rollup-plugin-visualizer and cross-env are devDependencies"
    - "npm run analyze runs cross-env ANALYZE=true vite build and produces a treemap report at a known path"
    - "The visualizer plugin is gated on process.env.ANALYZE === 'true' and does NOT run during a normal npm run build"
    - "The PWA registerType: 'prompt' LOCKED comment and scope: '/' in vite.config.ts are unchanged"
    - "The generated report artifact is git-ignored (regenerated on demand, not committed)"
  artifacts:
    - path: "vite.config.ts"
      provides: "ANALYZE-gated visualizer plugin wiring"
      contains: "process.env.ANALYZE"
    - path: "package.json"
      provides: "analyze script + visualizer/cross-env devDeps"
      contains: '"analyze"'
  key_links:
    - from: "package.json analyze script"
      to: "vite.config.ts visualizer plugin"
      via: "cross-env ANALYZE=true sets process.env.ANALYZE which the plugin gate reads"
      pattern: "ANALYZE=true vite build"
---

<objective>
Add `rollup-plugin-visualizer` (^7.0.1) and `cross-env` as devDependencies, wire the visualizer into `vite.config.ts` gated strictly on `process.env.ANALYZE === 'true'`, and add an `analyze` npm script (`cross-env ANALYZE=true vite build`). The visualizer treemap is the artifact Phase 36 reviews before sequencing its chunk-split work — but it must NEVER run during a normal build.

This is fully independent dev tooling (FND-03). It lands in Wave 3 because it shares `package.json` with Plan 02 (file-ownership rule: no same-wave file overlap).

Purpose: Implements FND-03.
Output: Updated `package.json` (devDeps + `analyze` script), `vite.config.ts` (gated plugin), `.gitignore` (report artifact ignored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-mobile-foundation/33-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
<!-- Exact current vite.config.ts structure (read-first). Attach the visualizer to the plugins array; keep all LOCKED comments. -->

vite.config.ts plugins array (current — append the gated visualizer; do NOT alter the VitePWA block):
```ts
export default defineConfig({
  plugins: [
    vue({ ... }),
    ...(process.env.NODE_ENV !== "production" ? [vueDevTools()] : []),
    Components({ resolvers: [PrimeVueResolver()] }),
    tailwindcss(),
    VitePWA({
      registerType: "prompt",   // LOCKED: never 'autoUpdate' — CRUD forms have unsaved state
      ...
      scope: "/",               // LOCKED: scope "/" per STATE.md — NOT "/projects/wallecx"
      ...
    }),
  ],
  ...
});
```

STACK.md exact targets:
  devDependencies add: "rollup-plugin-visualizer": "^7.0.1", "cross-env" (latest)
  package.json scripts add: "analyze": "cross-env ANALYZE=true vite build"
  Plugin gate: process.env.ANALYZE === 'true'  (keeps prod builds unaffected)
  Why cross-env: engines allow Node ^20.19.0 but contributors run Windows PowerShell where inline `ANALYZE=true vite build` fails.

.gitignore currently ignores: node_modules/, dist/, coverage/, etc. (report path must be added or live under dist/).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add visualizer + cross-env devDeps and the analyze script; gitignore the report</name>
  <read_first>
    - package.json (the file being modified — scripts + devDependencies blocks)
    - .gitignore (the file being modified — confirm dist/ + coverage/ already ignored)
    - .planning/research/STACK.md (§"Dev Dependencies" + §"Installation" — exact versions + script string)
    - .planning/phases/33-mobile-foundation/33-CONTEXT.md (FND-03 + Claude's-discretion on script name/path/gitignore)
  </read_first>
  <action>
    Install the dev tooling: `npm install -D rollup-plugin-visualizer@^7.0.1 cross-env`. This adds both to `devDependencies` in `package.json`.

    Add to the `scripts` block in `package.json`: `"analyze": "cross-env ANALYZE=true vite build"` (exact string from STACK.md). Place it near `build` / `build-only` for discoverability.

    Choose the report output path (Claude's discretion — recommend `dist/stats.html` per STACK.md §(b), which keeps it inside the already-git-ignored `dist/` and co-located with the build it describes; the report is regenerated on demand, NOT committed). If you pick a path outside `dist/`, add that exact path to `.gitignore`. If you use `dist/stats.html`, confirm `dist/` is already in `.gitignore` (it is) — no .gitignore change strictly required, but add an explicit `# bundle analyzer report` note + path if the report lands outside dist/.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.scripts.analyze) process.exit(1); if(!p.devDependencies['rollup-plugin-visualizer']) process.exit(1); if(!p.devDependencies['cross-env']) process.exit(1); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains "rollup-plugin-visualizer"
    - package.json devDependencies contains "cross-env"
    - package.json scripts.analyze equals "cross-env ANALYZE=true vite build"
    - The chosen report path is git-ignored (either lives under dist/ which is already ignored, or an explicit .gitignore entry was added)
  </acceptance_criteria>
  <done>visualizer + cross-env are devDeps; the analyze script is the exact cross-env string; the report artifact path is git-ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the ANALYZE-gated visualizer into vite.config.ts and verify gating both ways</name>
  <read_first>
    - vite.config.ts (the file being modified — plugins array + the LOCKED VitePWA block)
    - package.json (the analyze script added in Task 1)
    - .planning/research/STACK.md (§(b) — gate on process.env.ANALYZE === 'true', output to dist/stats.html)
    - .planning/research/PITFALLS.md (C-1 — registerType must stay 'prompt'; C-8 — scope stays '/')
  </read_first>
  <action>
    Import the visualizer at the top of `vite.config.ts`: `import { visualizer } from "rollup-plugin-visualizer";`.

    Append the plugin to the `plugins` array, gated so it ONLY attaches when `process.env.ANALYZE === 'true'`. Use the same spread-conditional idiom already in the file (the vueDevTools line uses `...(cond ? [plugin()] : [])`):
    `...(process.env.ANALYZE === "true" ? [visualizer({ filename: "dist/stats.html", template: "treemap", gzipSize: true, brotliSize: true })] : [])`
    (Use whatever `filename` matches the path chosen in Task 1.)

    Do NOT modify the `VitePWA({ ... })` block. The `registerType: "prompt"` LOCKED comment (line 27) and `scope: "/"` LOCKED comment (line 41) MUST remain verbatim — C-1 and C-8 are load-bearing invariants. Do NOT touch the Workbox config, the rolldown codeSplitting groups, or the resolve alias.

    Verify gating BOTH directions. The `<verify>` command below is the source of truth and is written as a cross-platform `node -e` check (NOT POSIX `test`/`&&`-chaining) because this repo is developed in a raw Windows PowerShell session where `test ! -f` / `test -f` would false-fail:
    1. `npm run build` — completes; the visualizer does NOT run (no `dist/stats.html` produced by a plain build) and build output has 0 lines matching "exceeds" / "Skipping precaching".
    2. `npm run analyze` — completes; the treemap report IS produced at the chosen path (file exists after the run).
  </action>
  <verify>
    <automated>npm run build && node -e "const fs=require('fs'); if(fs.existsSync('dist/stats.html')){console.error('FAIL: plain build produced the visualizer report');process.exit(1)} console.log('plain-build-clean')" && npm run analyze && node -e "const fs=require('fs'); if(!fs.existsSync('dist/stats.html')){console.error('FAIL: analyze did not produce the visualizer report');process.exit(1)} console.log('gating-verified')"</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains "rollup-plugin-visualizer" (import) and "visualizer(" (usage)
    - vite.config.ts contains "process.env.ANALYZE" gating the visualizer plugin
    - vite.config.ts still contains 'registerType: "prompt"' with its LOCKED comment unchanged
    - vite.config.ts still contains 'scope: "/"' with its LOCKED comment unchanged
    - A plain `npm run build` produces NO report file (the `node -e` PRESENT/ABSENT check confirms dist/stats.html is absent after build) and emits 0 lines matching "exceeds" or "Skipping precaching"
    - `npm run analyze` produces the treemap report file at the chosen path (the `node -e` check confirms dist/stats.html is present after analyze)
  </acceptance_criteria>
  <done>The visualizer attaches only when ANALYZE=true; a plain build never runs it; analyze produces the treemap; the gating is proven via a cross-platform node -e PRESENT/ABSENT check (Windows PowerShell-safe); the PWA registerType/scope LOCKED comments are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build-time devDependency | `rollup-plugin-visualizer` + `cross-env` run at build time only (never shipped to runtime) |
| Build env var `ANALYZE` → plugin activation | An environment flag gates whether the analyzer plugin attaches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Information Disclosure | `dist/stats.html` bundle report (could reveal source paths if deployed) | mitigate | Report path is git-ignored AND only generated via the opt-in `analyze` script; never produced by `npm run build`, so it cannot be accidentally deployed by the normal pipeline |
| T-33-08 | Tampering | Accidental regression of `registerType: 'prompt'` / `scope: '/'` while editing the plugins array | mitigate | Plan explicitly forbids touching the VitePWA block; acceptance criteria grep-assert both LOCKED comments remain; C-1/C-8 cited in read_first |
| T-33-09 | Tampering | visualizer + cross-env supply chain | mitigate | Install only the pinned versions from STACK.md; dev-only deps that never reach the runtime bundle |
</threat_model>

<verification>
- `rollup-plugin-visualizer` + `cross-env` present in devDependencies; `analyze` script is the exact `cross-env ANALYZE=true vite build`.
- `vite.config.ts` gates the visualizer on `process.env.ANALYZE === "true"`; a plain `npm run build` does not produce the report and shows no precache-skip lines; `npm run analyze` produces the treemap. Gating is verified with a cross-platform `node -e` PRESENT/ABSENT check (Windows PowerShell-safe).
- `registerType: "prompt"` and `scope: "/"` LOCKED comments are unchanged (C-1, C-8 upheld).
- Report artifact is git-ignored.
</verification>

<success_criteria>
- ROADMAP Phase 33 Success Criterion 3 met: `npm run analyze` produces a treemap report at a known artifact path (ANALYZE=true-gated, cross-env-wrapped for Windows), available for review before Phase 36 plan sequencing.
</success_criteria>

<output>
After completion, create `.planning/phases/33-mobile-foundation/33-03-SUMMARY.md` documenting the chosen report path, the gating idiom used, confirmation that a plain build does not run the visualizer, and confirmation that the PWA LOCKED comments (registerType/scope) are intact.
</output>
</output>
