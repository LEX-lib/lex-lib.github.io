---
phase: 34-layout-audit-touch-targets
plan: 03
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/components/projects/wallecx/ManageMembership.vue
  - src/components/projects/wallecx/ManageVaccination.vue
autonomous: false
requirements: [LT-02, LT-07]
must_haves:
  truths:
    - "On mobile, ManageMembership renders as a bottom Drawer (not a centered Dialog) with a visible drag handle"
    - "On mobile, ManageVaccination renders as a bottom Drawer with a visible drag handle, preserving the @primevue/forms Form structure and the administeredDate direct-v-model binding"
    - "On desktop, both components still render their centered Dialog (no regression)"
    - "ManageMembership preserves the ColorPicker direct-v-model (card_color no-hash) invariant in both branches"
    - "BarcodeDisplay.vue still renders black bars on white in BOTH light and dark theme after the full Phase 34 CSS sweep"
  artifacts:
    - path: "src/components/projects/wallecx/ManageMembership.vue"
      provides: "Mobile bottom-Drawer branch mirroring ManageExpense, with DragHandle + ColorPicker direct-v-model preserved"
      contains: "position=\"bottom\""
    - path: "src/components/projects/wallecx/ManageVaccination.vue"
      provides: "Mobile bottom-Drawer branch with replicated @primevue/forms Form + DragHandle"
      contains: "position=\"bottom\""
  key_links:
    - from: "ManageMembership.vue / ManageVaccination.vue Drawer #header"
      to: "DragHandle.vue"
      via: "import DragHandle from './DragHandle.vue' + <DragHandle />"
      pattern: "DragHandle"
    - from: "Dialog/Drawer conditional"
      to: "useIsMobile()"
      via: "v-if=\"!isMobile\" Dialog / v-else Drawer"
      pattern: "v-if=\"!isMobile\""
---

<objective>
Close the LT-02 gap: `ManageMembership.vue` and `ManageVaccination.vue` currently render a centered `<Dialog>` on ALL viewport sizes (no mobile bottom-sheet, no drag handle). Add the `v-if="!isMobile"` Dialog / `v-else` bottom-`<Drawer>` conditional that `ManageExpense.vue` already uses, with a `<DragHandle>` in each Drawer `#header`. Then run the mandatory BR-2 black-on-white reverify (D-34-06) as a human-verify checkpoint after the full Phase 34 CSS sweep.

Purpose: These are the last two of the seven bottom-sheet sites needing drag-handle parity. The BR-2 reverify is the regression gate required because Phase 34 touched the global CSS that the Memberships barcode surface depends on.
Output: Drawer branches added to both Manage components; BR-2 visual check recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-layout-audit-touch-targets/34-CONTEXT.md
@.planning/phases/34-layout-audit-touch-targets/34-RESEARCH.md
@.planning/phases/34-layout-audit-touch-targets/34-PATTERNS.md
@.planning/phases/34-layout-audit-touch-targets/34-01-PLAN.md

<interfaces>
<!-- Plan 01 contract (must exist before this plan runs): -->
src/components/projects/wallecx/DragHandle.vue — visual-only pill, no props:
  import DragHandle from './DragHandle.vue'
  <DragHandle />

<!-- Reference implementation — ManageExpense.vue Dialog/Drawer conditional (lines 263-267 + 393-406): -->
  <Dialog v-if="!isMobile" modal v-model:visible="visible" :header="dialogHeader"
          :style="{ width: '40vw' }" :breakpoints="{ '960px': '75vw', '641px': '92vw' }"
          :closable="!isSaving" @hide="onHide">
    <form @submit.prevent="onSubmit" class="space-y-4"> ...fields... </form>
  </Dialog>
  <Drawer v-else v-model:visible="visible" position="bottom" :show-close-icon="!isSaving" @hide="onHide">
    <template #header>
      <div class="flex flex-col items-center w-full gap-1">
        <DragHandle />
        <span class="font-semibold">{{ dialogHeader }}</span>
      </div>
    </template>
    <form @submit.prevent="onSubmit" class="space-y-4"> ...same fields... </form>
  </Drawer>

<!-- Current state (verified): both render <Dialog> only -->
  ManageMembership.vue:251  — <Dialog (no Drawer branch; does NOT import useIsMobile)
  ManageVaccination.vue:210 — <Dialog ... ; :219 <Form (no Drawer branch; does NOT import useIsMobile)

<!-- isMobile import convention (PATTERNS.md §Shared Patterns) — add to BOTH (neither currently imports it): -->
  import { useIsMobile } from '@/composables/useIsMobile'
  const isMobile = useIsMobile()

<!-- ManageMembership invariant: card_color uses ColorPicker with direct v-model="cardColor"
     (PrimeVue #8135 workaround, STATE.md locked). Ref lives in <script setup>, shared by both branches. -->
<!-- ManageVaccination invariant: administeredDate is a direct v-model ref (D-33-01-A,
     PrimeVue Forms #8191). Keep it OUT of the @primevue/forms Form in the Drawer branch too. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add mobile bottom-Drawer branch to ManageMembership.vue</name>
  <read_first>
    - src/components/projects/wallecx/ManageMembership.vue (entire file — the <Dialog> at line 251, all form fields, the cardColor ColorPicker direct v-model, the script-setup refs, dialogHeader computed, onSubmit, onHide, isSaving)
    - src/components/projects/wallecx/ManageExpense.vue lines 263-524 (Dialog/Drawer conditional reference — copy its structure)
    - src/components/projects/wallecx/DragHandle.vue (Plan 01 component)
    - .planning/phases/34-layout-audit-touch-targets/34-PATTERNS.md §"ManageMembership.vue (form overlay — ADD Drawer branch)"
  </read_first>
  <files>src/components/projects/wallecx/ManageMembership.vue</files>
  <action>
    Per D-34-03 / LT-02. ManageMembership currently renders `<Dialog>` only (line 251) and does NOT import `useIsMobile`.

    1. In `<script setup>`, add:
    ```typescript
    import { useIsMobile } from '@/composables/useIsMobile'
    import DragHandle from './DragHandle.vue'
    const isMobile = useIsMobile()
    ```

    2. Gate the existing `<Dialog>` with `v-if="!isMobile"` (keep all its existing props/attrs/`@hide`/form body unchanged).

    3. Add a sibling `<Drawer v-else v-model:visible="visible" position="bottom" :show-close-icon="!isSaving" @hide="onHide">` immediately after the Dialog, mirroring ManageExpense. Its `#header` slot uses:
    ```html
    <template #header>
      <div class="flex flex-col items-center w-full gap-1">
        <DragHandle />
        <span class="font-semibold">{{ dialogHeader }}</span>
      </div>
    </template>
    ```
    The Drawer body is a template duplication of the Dialog's `<form>` body — the SAME fields bound to the SAME script-setup refs. Use whatever the existing visible-binding name is (match the Dialog's `v-model:visible` target) and the existing submit/hide handler names.

    Critical constraints:
    - The `card_color` field MUST keep using `<ColorPicker>` with direct `v-model="cardColor"` (PrimeVue #8135 / CON-CARD-COLOR-NO-HASH — STATE.md locked invariant). Both branches share the same `cardColor` ref. Do NOT swap ColorPicker for a native picker, do NOT add/remove the `#` prefix (it is stored without `#`).
    - This is template duplication only — NO logic/handler duplication, NO new refs. Phase 35's BaseMobileDialog migration will DRY this up later.
    - Match the exact field set, labels, validation markup, and `:closable`/`:closeable`/`isSaving` gating used in the Dialog branch.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/projects/wallecx/ManageMembership.vue','utf8'); for(const s of [\"import { useIsMobile } from '@/composables/useIsMobile'\",\"import DragHandle from './DragHandle.vue'\",'v-if=\"!isMobile\"','<Drawer','v-else','position=\"bottom\"','<DragHandle']){ if(!c.includes(s)) throw new Error('missing: '+s); } if(!/v-model=\"cardColor\"|v-model='cardColor'/.test(c) && !c.includes('cardColor')) throw new Error('ColorPicker direct v-model cardColor missing'); console.log('OK')"</automated>
    <automated>npm run type-check</automated>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - ManageMembership.vue contains `import { useIsMobile } from '@/composables/useIsMobile'`, `import DragHandle from './DragHandle.vue'`, `v-if="!isMobile"`, `<Drawer`, `v-else`, `position="bottom"`, and `<DragHandle`
    - ManageMembership.vue still references the `cardColor` ColorPicker direct v-model (no-hash invariant intact)
    - `npm run type-check` exits 0; `npm run test:unit` passes (≥59 — membershipMapper.spec.ts green)
  </acceptance_criteria>
  <done>ManageMembership renders Dialog on desktop / bottom Drawer with DragHandle on mobile; ColorPicker direct-v-model preserved; tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add mobile bottom-Drawer branch to ManageVaccination.vue</name>
  <read_first>
    - src/components/projects/wallecx/ManageVaccination.vue (entire file — the <Dialog> at line 210, the <Form v-slot="$form"> at line 219, the administeredDate direct v-model ref, dateAdministeredError, resolver, initialValues, onFormSubmit/onSubmit, dialogHeader, isSaving)
    - src/components/projects/wallecx/ManageExpense.vue lines 263-524 (Dialog/Drawer conditional reference)
    - src/components/projects/wallecx/DragHandle.vue
    - .planning/phases/34-layout-audit-touch-targets/34-PATTERNS.md §"ManageVaccination.vue (form overlay — ADD Drawer branch)"
    - .planning/phases/34-layout-audit-touch-targets/34-RESEARCH.md §"Pitfall 3" + §"Open Question 2" (two <Form> instances, v-if/v-else, name-binding)
  </read_first>
  <files>src/components/projects/wallecx/ManageVaccination.vue</files>
  <action>
    Per D-34-03 / LT-02. ManageVaccination renders `<Dialog>` only (line 210) wrapping a `@primevue/forms` `<Form v-slot="$form">` (line 219). It does NOT import `useIsMobile`.

    1. In `<script setup>`, add:
    ```typescript
    import { useIsMobile } from '@/composables/useIsMobile'
    import DragHandle from './DragHandle.vue'
    const isMobile = useIsMobile()
    ```

    2. Gate the existing `<Dialog>` with `v-if="!isMobile"` (keep all existing props/attrs/form body unchanged).

    3. Add a sibling `<Drawer v-else v-model:visible="visible" position="bottom" :show-close-icon="!isSaving" @hide="onHide">` (match the Dialog's actual visible-binding + hide handler names). Its `#header` slot uses the `<DragHandle />` + `<span class="font-semibold">{{ dialogHeader }}</span>` pattern.

    4. The Drawer body replicates the Dialog's `<Form v-slot="$form" :initialValues="initialValues" :resolver="resolver" @submit="...">` VERBATIM — same field markup, same `$form.<field>?.invalid` error display, same `name="..."` bindings. Because only one of Dialog/Drawer renders at a time (v-if/v-else), the two `<Form>` instances do not conflict (each provides its own inject context; fields bind by `name`, not by ref — RESEARCH Pitfall 3 / Open Question 2).

    Critical constraints:
    - `administeredDate` MUST remain a direct `v-model="administeredDate"` ref OUTSIDE the `@primevue/forms` Form (D-33-01-A, PrimeVue Forms #8191) in the Drawer branch too. Keep the manual `dateAdministeredError` required-validation wiring identical to the Dialog branch. Do NOT fold the date field back into the Form.
    - Template duplication only — NO duplicated resolver/handler logic, NO new refs.
    - Match the exact field set, the `$form` error markup, and `isSaving`/closable gating from the Dialog branch.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/projects/wallecx/ManageVaccination.vue','utf8'); for(const s of [\"import { useIsMobile } from '@/composables/useIsMobile'\",\"import DragHandle from './DragHandle.vue'\",'v-if=\"!isMobile\"','<Drawer','v-else','position=\"bottom\"','<DragHandle','administeredDate']){ if(!c.includes(s)) throw new Error('missing: '+s); } const forms=(c.match(/<Form/g)||[]).length; if(forms<2) throw new Error('expected 2 <Form> instances (Dialog + Drawer), found '+forms); console.log('OK')"</automated>
    <automated>npm run type-check</automated>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - ManageVaccination.vue contains both imports, `v-if="!isMobile"`, `<Drawer`, `v-else`, `position="bottom"`, `<DragHandle`, and still references `administeredDate`
    - ManageVaccination.vue contains at least 2 `<Form` instances (Dialog + Drawer branches)
    - `npm run type-check` exits 0; `npm run test:unit` passes (≥59)
  </acceptance_criteria>
  <done>ManageVaccination renders Dialog on desktop / bottom Drawer with DragHandle on mobile; Form structure + administeredDate direct-v-model replicated; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BR-2 barcode black-on-white reverify + mobile bottom-sheet visual check (D-34-06)</name>
  <what-built>
    The full Phase 34 CSS/layout sweep is in place across Plans 01-03: global 44px touch-target floor, stacked sticky TabList + tab toolbars, bottom-Drawer safe-area padding, DragHandle on all 7 bottom sheets (5 swapped + 2 new branches here), and scan-overlay notch insets. This checkpoint is the mandatory BR-2 regression gate (NFR-BR-2-PRESERVED) plus a quick visual confirm of the two new mobile Drawer branches and the stacked-sticky chrome.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the Wallecx app at a ~390px mobile viewport (browser DevTools device emulation is sufficient; record any real-device note in the SUMMARY).

    1. BR-2 LIGHT MODE: Go to Memberships tab, open a card with a barcode, open the fullscreen scan overlay. Confirm the barcode renders BLACK bars on a WHITE background. Confirm the close button and barcode clear the top notch area (safe-area padding applied).
    2. BR-2 DARK MODE: Toggle site dark mode (NavBar toggle), repeat step 1. Confirm the barcode is STILL black-on-white (the overlay forces #ffffff regardless of theme).
    3. NEW DRAWERS: On mobile width, tap "Add membership" — confirm it opens as a bottom sheet (Drawer) with a visible drag handle at the top, NOT a centered dialog. Tap "Add vaccination" — confirm the same. Confirm the date field still pre-fills correctly in Edit mode (administeredDate binding intact).
    4. STACKED STICKY: On any tab with a long list, scroll down — confirm the top TabList AND the search/sort toolbar both stay pinned at the top (toolbar below tabs), no jitter.
    5. TOUCH TARGETS: Confirm icon-only buttons (e.g. clear-search ✕, Drawer close) look at least ~44px and are comfortably tappable.

    Record the BR-2 result verbatim in the SUMMARY: "BarcodeDisplay.vue: black bars on white background confirmed in light mode ✓, dark mode ✓ after Phase 34 CSS sweep."
  </how-to-verify>
  <resume-signal>Type "approved" if BR-2 is black-on-white in both themes and the two new Drawers + stacked sticky render correctly; otherwise describe the regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser markup rendering | Template-only additions (Drawer branches) reusing existing script-setup refs/handlers. No new data flow, auth path, or server input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering | Duplicated ManageMembership Drawer form body sharing the cardColor ColorPicker ref | accept | Both branches bind the same `cardColor` ref; the no-hash storage invariant (CON-CARD-COLOR-NO-HASH) is enforced by the existing schema/mapper, unchanged here. membershipMapper.spec.ts re-runs in Task 1 verify to prove round-trip integrity. |
| T-34-06 | Tampering | Duplicated ManageVaccination @primevue/forms Form in the Drawer branch | accept | Only one Form renders at a time (v-if/v-else); name-based binding + the same resolver/initialValues are reused. No bypass of validation; the manual administeredDate required-check is preserved in both branches. |

No HIGH-severity threats introduced (presentation-layer only; confirmed by 34-RESEARCH.md §Security Domain).
</threat_model>

<verification>
- `npm run type-check` exits 0
- `npm run test:unit` passes (≥59 tests; membershipMapper.spec.ts 11 tests green — card_color round-trip intact)
- `npm run build` output contains 0 lines matching "exceeds" or "Skipping precaching"
- `npm run lint` clean except the pre-existing grandfathered `VaccinationDetail.vue:5`
- Both Manage components have Dialog (`v-if="!isMobile"`) + bottom Drawer (`v-else`) branches with DragHandle
- BR-2 human-verify checkpoint approved; result recorded verbatim in SUMMARY
</verification>

<success_criteria>
- ManageMembership + ManageVaccination render bottom Drawers with drag handles on mobile, Dialogs on desktop
- ColorPicker direct-v-model (no-hash) and administeredDate direct-v-model invariants preserved
- All 7 bottom-sheet sites now have drag-handle parity (5 from Plan 02 + 2 here)
- BR-2 black-on-white confirmed in light + dark; result recorded
- All automated gates green
</success_criteria>

<output>
After completion, create `.planning/phases/34-layout-audit-touch-targets/34-03-SUMMARY.md`
</output>
