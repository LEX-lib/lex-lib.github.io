---
phase: 34-layout-audit-touch-targets
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - src/components/projects/wallecx/VaccinationsTab.vue
  - src/components/projects/wallecx/MembershipsTab.vue
  - src/components/projects/wallecx/ExpensesTab.vue
  - src/components/projects/wallecx/MembershipDetail.vue
  - src/components/projects/wallecx/ManageExpense.vue
  - src/components/projects/wallecx/ManageBudget.vue
autonomous: true
requirements: [LT-02, LT-03, LT-05, LT-07]
must_haves:
  truths:
    - "On mobile, each tab's filter/sort toolbar stays pinned below the sticky TabList while the list scrolls underneath (stacked sticky)"
    - "All 5 existing bottom-sheet Drawers render the shared <DragHandle> instead of duplicated inline pill markup"
    - "The fullscreen barcode scan overlay (MembershipDetail) respects top + bottom safe-area insets, and its close button clears the notch"
    - "Internal scroll inside the existing bottom Drawers works without a double-scroll trap on a 390px viewport"
  artifacts:
    - path: "src/components/projects/wallecx/VaccinationsTab.vue"
      provides: "Sticky toolbar wrapper + DragHandle in the reactive-position group Drawer"
      contains: "wallecx-tab-toolbar"
    - path: "src/components/projects/wallecx/MembershipsTab.vue"
      provides: "Sticky toolbar wrapper + DragHandle in the bottom Drawer"
      contains: "wallecx-tab-toolbar"
    - path: "src/components/projects/wallecx/ExpensesTab.vue"
      provides: "Sticky toolbar wrapper + DragHandle in the receipt-preview Drawer"
      contains: "wallecx-tab-toolbar"
    - path: "src/components/projects/wallecx/MembershipDetail.vue"
      provides: "Safe-area padding on the fullscreen scan overlay + notch-clearing close button"
      contains: "env(safe-area-inset-top)"
  key_links:
    - from: "tab toolbar wrappers"
      to: "wallecx-overrides.css .wallecx-tab-toolbar"
      via: ":class=\"isMobile ? 'wallecx-tab-toolbar' : ''\""
      pattern: "wallecx-tab-toolbar"
    - from: "Drawer #header slots"
      to: "DragHandle.vue"
      via: "import DragHandle from './DragHandle.vue' + <DragHandle />"
      pattern: "DragHandle"
---

<objective>
Wire the Plan-01 spine into the three tab shells and the existing overlay surfaces: wrap each tab's toolbar in the `.wallecx-tab-toolbar` sticky class (mobile only), swap all 5 existing inline drag-handle pills for the shared `<DragHandle>`, and add `env(safe-area-inset-*)` padding to the fullscreen barcode scan overlay in `MembershipDetail.vue`.

Purpose: Delivers the visible stacked-sticky chrome (LT-05), drag-handle parity on the existing bottom sheets (LT-02), scan-overlay notch safety (LT-03), and the no-double-scroll verification (LT-07).
Output: Edited VaccinationsTab, MembershipsTab, ExpensesTab, MembershipDetail, ManageExpense, ManageBudget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-layout-audit-touch-targets/34-CONTEXT.md
@.planning/phases/34-layout-audit-touch-targets/34-RESEARCH.md
@.planning/phases/34-layout-audit-touch-targets/34-PATTERNS.md
@.planning/phases/34-layout-audit-touch-targets/34-01-PLAN.md

<interfaces>
<!-- Plan 01 created these contracts (must already exist before this plan runs): -->

src/components/projects/wallecx/DragHandle.vue — visual-only pill, no props:
  import DragHandle from './DragHandle.vue'
  <DragHandle />

src/assets/wallecx-overrides.css — the sticky toolbar class (mobile-only @media ≤639px):
  .wallecx-tab-toolbar { position: sticky; top: calc(env(safe-area-inset-top) + 52px); z-index: 9; ... }

<!-- Existing pill sites to swap (verified line numbers): -->
  ExpensesTab.vue:258      — <div class="w-8 h-1 rounded-full ...">  (receipt preview Drawer)
  ManageBudget.vue:171     — same pill                                (ManageBudget Drawer)
  ManageExpense.vue:403    — same pill                                (ManageExpense Drawer)
  MembershipsTab.vue:351-355 — same pill, UNCONDITIONAL (Drawer is v-else mobile-only)
  VaccinationsTab.vue:428-432 — same pill, v-if="isMobile" (Drawer is reactive bottom/right)

<!-- Existing scan overlay (MembershipDetail.vue lines 167-203): -->
  <div v-if="showScanOverlay" class="fixed inset-0 ..." style="z-index: 9999; background: #ffffff; filter: brightness(1.4);">
    <button class="absolute top-4 right-4 w-12 h-12 ..." style="background: rgba(0,0,0,0.08);">

<!-- isMobile is ALREADY imported via useIsMobile() in all 3 tab files, ManageExpense, and ManageBudget. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sticky toolbar wrappers in the 3 tab files</name>
  <read_first>
    - src/components/projects/wallecx/VaccinationsTab.vue (the <WallecxToolbar ... /> render site + confirm useIsMobile() import at ~line 12)
    - src/components/projects/wallecx/MembershipsTab.vue (the <WallecxToolbar ... /> render site)
    - src/components/projects/wallecx/ExpensesTab.vue (the <ExpensesToolbar ... /> render site)
    - .planning/phases/34-layout-audit-touch-targets/34-PATTERNS.md §"Per-tab toolbar sticky wrapper" and the three tab-file sections
    - .planning/phases/34-layout-audit-touch-targets/34-RESEARCH.md §"Sticky Chrome Strategy"
  </read_first>
  <files>src/components/projects/wallecx/VaccinationsTab.vue, src/components/projects/wallecx/MembershipsTab.vue, src/components/projects/wallecx/ExpensesTab.vue</files>
  <action>
    Per D-34-01 / LT-05. In each of the three tab files, wrap the existing toolbar component render in a `<div>` that applies the `wallecx-tab-toolbar` class on mobile only. `isMobile` is already available via `useIsMobile()` in all three files (no new import).

    VaccinationsTab.vue and MembershipsTab.vue — wrap `<WallecxToolbar ... />`:
    ```html
    <div :class="isMobile ? 'wallecx-tab-toolbar' : ''">
      <WallecxToolbar ... />   <!-- keep all existing props/events unchanged -->
    </div>
    ```

    ExpensesTab.vue — wrap `<ExpensesToolbar ... />`:
    ```html
    <div :class="isMobile ? 'wallecx-tab-toolbar' : ''">
      <ExpensesToolbar ... />  <!-- keep all existing props/events unchanged -->
    </div>
    ```

    Constraints:
    - Do NOT change toolbar props, events, or any other markup. Only add the wrapping `<div>`.
    - The wrapper has NO margin of its own (the toolbar component keeps its own `mb-4` etc.). On desktop the class is empty string → no sticky behavior (desktop layout unchanged).
    - The actual sticky CSS lives in `wallecx-overrides.css .wallecx-tab-toolbar` from Plan 01 — this task only attaches the class.
    - ExpensesReportsView is NOT touched — its period-selector tabs are scrollable and handled by the ExpensesTab wrapper (per RESEARCH.md component structure).
  </action>
  <verify>
    <automated>node -e "for(const f of ['VaccinationsTab','MembershipsTab','ExpensesTab']){ const c=require('fs').readFileSync('src/components/projects/wallecx/'+f+'.vue','utf8'); if(!c.includes(\"isMobile ? 'wallecx-tab-toolbar'\")) throw new Error('sticky wrapper missing in '+f); } console.log('OK')"</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - VaccinationsTab.vue, MembershipsTab.vue, and ExpensesTab.vue each contain `isMobile ? 'wallecx-tab-toolbar'`
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>All three tab toolbars wrapped in the mobile-gated sticky class; desktop layout unchanged; type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the 5 existing inline pills for shared DragHandle</name>
  <read_first>
    - src/components/projects/wallecx/DragHandle.vue (the component created in Plan 01)
    - src/components/projects/wallecx/VaccinationsTab.vue lines 426-435 (pill is v-if="isMobile" — reactive-position Drawer)
    - src/components/projects/wallecx/MembershipsTab.vue lines 350-358 (pill unconditional — v-else mobile Drawer)
    - src/components/projects/wallecx/ExpensesTab.vue line 258, ManageExpense.vue line 403, ManageBudget.vue line 171 (pill markup)
    - .planning/phases/34-layout-audit-touch-targets/34-PATTERNS.md §"DragHandle.vue" usage pattern + the per-file sections
  </read_first>
  <files>src/components/projects/wallecx/VaccinationsTab.vue, src/components/projects/wallecx/MembershipsTab.vue, src/components/projects/wallecx/ExpensesTab.vue, src/components/projects/wallecx/ManageExpense.vue, src/components/projects/wallecx/ManageBudget.vue</files>
  <action>
    Per D-34-03 / LT-02. In each of the 5 files, add `import DragHandle from './DragHandle.vue'` to the `<script setup>` imports block, then replace the inline pill `<div class="w-8 h-1 rounded-full bg-gray-300 dark:bg-gray-600" aria-hidden="true">` with the `<DragHandle>` component:

    - **VaccinationsTab.vue** (lines 428-432): the Drawer is reactive-position (`bottom` on mobile, `right` on desktop) so the handle MUST keep its mobile gate → replace with `<DragHandle v-if="isMobile" />`.
    - **MembershipsTab.vue** (lines 352-355): the Drawer is `v-else` (mobile-only) so the pill is unconditional → replace with `<DragHandle />` (no v-if).
    - **ExpensesTab.vue** (line 258): receipt-preview Drawer (mobile-only) → `<DragHandle />`.
    - **ManageExpense.vue** (line 403): Drawer branch header → `<DragHandle />`.
    - **ManageBudget.vue** (line 171): Drawer branch header → `<DragHandle />`.

    Keep the surrounding `<div class="flex flex-col items-center w-full gap-1">` wrapper and the sibling `<span class="font-semibold">...</span>` title unchanged. Only the pill `<div>` becomes `<DragHandle>`.

    After this task, 0 inline `w-8 h-1 rounded-full bg-gray-300 dark:bg-gray-600` pill markup should remain in the wallecx directory (the only copy of that markup is inside DragHandle.vue).
  </action>
  <verify>
    <automated>node -e "const{execSync}=require('child_process'); let out=''; try{ out=execSync('git grep -l \"w-8 h-1 rounded-full bg-gray-300\" -- src/components/projects/wallecx/ || true').toString().trim(); }catch(e){} const files=out.split(/\\r?\\n/).filter(Boolean); const bad=files.filter(f=>!f.endsWith('DragHandle.vue')); if(bad.length) throw new Error('inline pill still present in: '+bad.join(', ')); for(const f of ['VaccinationsTab','MembershipsTab','ExpensesTab','ManageExpense','ManageBudget']){ const c=require('fs').readFileSync('src/components/projects/wallecx/'+f+'.vue','utf8'); if(!c.includes(\"import DragHandle from './DragHandle.vue'\")) throw new Error('DragHandle import missing in '+f); if(!c.includes('<DragHandle')) throw new Error('<DragHandle> not used in '+f); } console.log('OK')"</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - The only file matching `w-8 h-1 rounded-full bg-gray-300` in `src/components/projects/wallecx/` is `DragHandle.vue`
    - All 5 files (VaccinationsTab, MembershipsTab, ExpensesTab, ManageExpense, ManageBudget) contain `import DragHandle from './DragHandle.vue'` and use `<DragHandle`
    - VaccinationsTab.vue uses `<DragHandle v-if="isMobile" />` (reactive-position Drawer)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>All 5 existing pills replaced by the shared component; no duplicated pill markup remains; reactive-position gate preserved in VaccinationsTab.</done>
</task>

<task type="auto">
  <name>Task 3: Add safe-area insets to the fullscreen scan overlay (MembershipDetail.vue)</name>
  <read_first>
    - src/components/projects/wallecx/MembershipDetail.vue lines 167-203 (the scan overlay div + close button)
    - .planning/phases/34-layout-audit-touch-targets/34-PATTERNS.md §"MembershipDetail.vue (detail overlay — safe-area on scan overlay)"
    - .planning/phases/34-layout-audit-touch-targets/34-RESEARCH.md §"3. Fullscreen Scan Overlay — top + bottom insets"
    - src/components/projects/wallecx/BarcodeDisplay.vue lines 1-10 (confirm BARCODE_FOREGROUND #000000 / BARCODE_BACKGROUND #ffffff — NOT to be touched)
  </read_first>
  <files>src/components/projects/wallecx/MembershipDetail.vue</files>
  <action>
    Per D-34-04 / LT-03. Edit ONLY the scan overlay markup. Use direct `env()` strings in the inline `style` — no `useMobileEnv` import is needed (the browser resolves `env()` natively).

    1. On the overlay `<div v-if="showScanOverlay" class="fixed inset-0 ...">`, extend the existing inline `style` to add top + bottom safe-area padding (keep the existing `z-index`, `background`, `filter` declarations):
    ```
    style="z-index: 9999; background: #ffffff; filter: brightness(1.4);
           padding-top: env(safe-area-inset-top);
           padding-bottom: env(safe-area-inset-bottom);"
    ```

    2. On the close button, change `class="absolute top-4 right-4 ..."` to drop `top-4` and add an inline `top` calc so it clears the notch (keep `absolute right-4 w-12 h-12 ...` classes and the existing `background: rgba(0,0,0,0.08)`):
    ```
    class="absolute right-4 w-12 h-12 flex items-center justify-center rounded-full"
    style="top: calc(1rem + env(safe-area-inset-top)); background: rgba(0,0,0,0.08);"
    ```

    Constraints:
    - Do NOT touch `BarcodeDisplay.vue`, the `#ffffff` overlay background, or the barcode constants — BR-2 must stay black-on-white (D-34-06 / NFR-BR-2-PRESERVED). The `#ffffff` overlay background and `filter: brightness(1.4)` stay exactly as-is.
    - Do NOT change `role`, `aria-modal`, `aria-label`, or the `@keydown.esc` / `@click` handlers.
    - No script change, no new import.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/components/projects/wallecx/MembershipDetail.vue','utf8'); if(!c.includes('padding-top: env(safe-area-inset-top)')) throw new Error('overlay top inset missing'); if(!c.includes('padding-bottom: env(safe-area-inset-bottom)')) throw new Error('overlay bottom inset missing'); if(!c.includes('calc(1rem + env(safe-area-inset-top))')) throw new Error('close-button notch calc missing'); if(!c.includes('background: #ffffff')) throw new Error('BR-2 white overlay background regressed'); console.log('OK')"</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - MembershipDetail.vue scan overlay contains `padding-top: env(safe-area-inset-top)` and `padding-bottom: env(safe-area-inset-bottom)`
    - Close button contains `top: calc(1rem + env(safe-area-inset-top))`
    - The overlay still contains `background: #ffffff` (BR-2 white background not regressed)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>Scan overlay pads top + bottom for the notch/home-indicator; close button clears the notch; barcode white background untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser CSS/markup rendering | Template-only edits (sticky wrapper div, component swap, inline env() padding). No new data flow, auth path, or input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | Sticky `z-index: 9` toolbar wrapper over scrolling content | accept | Sticky chrome only pins the search/sort toolbar; it sits below modal Drawer/Dialog masks and never overlays the ConfirmDialog or auth UI. No clickjacking surface introduced. |
| T-34-04 | Information Disclosure | Fullscreen scan overlay padding change | accept | Padding only repositions the centered barcode and close button; the `#ffffff` background + `filter` are unchanged. No data exposure; BR-2 black-on-white preserved (verified in Plan 03 checkpoint). |

No HIGH-severity threats introduced (presentation-layer only; confirmed by 34-RESEARCH.md §Security Domain).
</threat_model>

<verification>
- `npm run type-check` exits 0
- `npm run test:unit` passes (≥59 tests — regression floor)
- `npm run build` output contains 0 lines matching "exceeds" or "Skipping precaching"
- `npm run lint` clean except the pre-existing grandfathered `VaccinationDetail.vue:5`
- No inline pill markup remains outside DragHandle.vue
- Scan overlay safe-area insets + sticky toolbar wrappers present per task acceptance criteria
- LT-07 manual confirmation: on a 390px viewport, swiping inside a bottom Drawer scrolls the form, not the page (record in SUMMARY)
</verification>

<success_criteria>
- 3 tab toolbars wrapped in mobile-gated `.wallecx-tab-toolbar` (stacked sticky below TabList)
- 5 existing pills swapped for shared `<DragHandle>`; VaccinationsTab keeps `v-if="isMobile"`
- Scan overlay has top + bottom safe-area padding; close button clears the notch
- BR-2 white background untouched
- All automated gates green
</success_criteria>

<output>
After completion, create `.planning/phases/34-layout-audit-touch-targets/34-02-SUMMARY.md`
</output>
