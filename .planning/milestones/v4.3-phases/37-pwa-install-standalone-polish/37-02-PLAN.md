---
phase: 37-pwa-install-standalone-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/OfflineBanner.vue
  - src/App.vue
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PWA-07]
must_haves:
  truths:
    - "A site-wide offline banner shows when navigator goes offline and auto-clears when it goes back online"
    - "The banner has NO retry button (D-37-11)"
    - "The banner sits below the iOS status bar (paddingTop env(safe-area-inset-top))"
    - "ROADMAP §Phase 37 SC#4 and REQUIREMENTS PWA-07 say 'banner auto-clears when navigator goes back online (useOnline reactive)' instead of 'retry affordance'"
  artifacts:
    - path: "src/components/OfflineBanner.vue"
      provides: "Site-wide offline banner, useOnline-driven, Teleport to body"
      min_lines: 20
      contains: "useOnline"
    - path: "src/App.vue"
      provides: "OfflineBanner mount above CustomNavBar"
      contains: "OfflineBanner"
  key_links:
    - from: "src/components/OfflineBanner.vue"
      to: "@vueuse/core useOnline"
      via: "import { useOnline } from '@vueuse/core'"
      pattern: "from\\s+['\"]@vueuse/core['\"]"
    - from: "src/App.vue"
      to: "src/components/OfflineBanner.vue"
      via: "import OfflineBanner from '@/components/OfflineBanner.vue'"
      pattern: "OfflineBanner"
---

<objective>
Ship the site-wide offline banner (PWA-07 / D-37-09/10/11) by creating `src/components/OfflineBanner.vue`, mounting it in `App.vue`, and rewording `.planning/ROADMAP.md` §Phase 37 SC#4 + `.planning/REQUIREMENTS.md` PWA-07 to drop the obsolete "retry affordance" wording in favor of the useOnline-driven design (D-37-12).

Purpose: Honest offline feedback. `useOnline` reactivity IS the retry mechanism — a button cannot make the network return. Banner shows when `navigator.onLine === false`, auto-clears when it flips back, sits at the top of the viewport amber, coexisting with the bottom-fixed install banner without z-index collision.
Output: 1 new component file, 1 modified shell file (`App.vue`), 2 modified documentation files (ROADMAP.md, REQUIREMENTS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md
@.planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md
@.planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md
@.planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md
@src/App.vue
@src/components/projects/wallecx/PwaInstallBanner.vue
@src/assets/base.css

<interfaces>
<!-- @vueuse/core useOnline (already direct dep since Phase 33 FND-01) -->
<!-- Source: https://vueuse.org/core/useonline/ -->

import { useOnline } from '@vueuse/core'
// useOnline(): Ref<boolean>
// Returns true when navigator.onLine; reactively flips on window 'online'/'offline' events.
// Synchronously seeded from navigator.onLine at call time — no race.

<!-- Teleport-to-body fixed banner pattern (PATTERNS §Shared Patterns) -->
<!-- Existing analog: src/components/projects/wallecx/PwaInstallBanner.vue lines 43-75 -->

<!-- CSS tokens from src/assets/base.css -->
- --color-status-warning: #e89820 (amber — light AND dark mode per UI-SPEC; status tokens theme-independent)
- --color-surface-page (dark): #0d1117  // used as offline banner text color for AA contrast on amber

<!-- App.vue current template (5 lines, lines 40-45) -->
<template>
  <CustomNavBar class="mb-1" :style="{ paddingTop: 'env(safe-area-inset-top)' }" />
  <RouterView />
  <Toaster />
  <SpeedInsights v-if="isProd" />
</template>

<!-- App.vue current script (lines 1-37) — App.vue ALREADY imports setInstallPromptEvent/clearInstallPromptEvent and registers beforeinstallprompt/appinstalled listeners. DO NOT change script logic. Only add OfflineBanner import + template mount. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OfflineBanner.vue (useOnline-driven, fixed top, amber, no retry button)</name>
  <files>src/components/OfflineBanner.vue</files>
  <read_first>
    - src/components/projects/wallecx/PwaInstallBanner.vue (existing Teleport+fixed-banner analog; copy structural pattern lines 43-75)
    - .planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md §"src/components/OfflineBanner.vue (component, event-driven) — NEW" (the complete template pattern)
    - .planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md §"Color Details by Surface — OfflineBanner.vue"
    - .planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md D-37-09/D-37-10/D-37-11
    - src/assets/base.css (confirm `--color-status-warning: #e89820` is declared in `@theme` AND has the same value in `.my-app-dark` — status tokens are theme-independent per UI-SPEC §Color)
  </read_first>
  <behavior>
    - Test 1: When `navigator.onLine === false` at mount, the banner renders with `role="status"`, copy "You're offline. Changes will resume when you reconnect."
    - Test 2: When `navigator.onLine === true` at mount, the banner does NOT render (v-if="!isOnline" -> false)
    - Test 3: Banner template root has `paddingTop: 'env(safe-area-inset-top)'` and z-index 50
    - Test 4: Banner has NO retry button — no `<button>` element inside the offline div (D-37-11)
    - Test 5: Banner uses Teleport to body (matches PwaInstallBanner.vue stacking-context pattern)
  </behavior>
  <action>
    Create `src/components/OfflineBanner.vue` using Vue 3 Composition API `<script setup lang="ts">`. Script imports ONLY `useOnline` from `@vueuse/core` and binds `const isOnline = useOnline()`. No other refs, no onMounted, no watchers — the reactive `isOnline` ref drives `v-if="!isOnline"` directly.

    Template uses `<Teleport to="body">` wrapping a single `<div>` with:
    - `v-if="!isOnline"`
    - Tailwind classes: `fixed top-0 left-0 right-0 z-50 flex items-center justify-center px-4 py-2`
    - Inline `:style` binding for dynamic env() value and color tokens:
      - `backgroundColor: 'var(--color-status-warning)'` (resolves to `#e89820` in both light and dark; per UI-SPEC §Color Details by Surface OfflineBanner.vue)
      - `paddingTop: 'env(safe-area-inset-top)'` (top-fixed safe-area, NOT `calc(...+0.75rem)` — that pattern is for bottom-fixed only)
      - `color: '#0d1117'` (dark text on amber — white text would wash; UI-SPEC explicitly locks `#0d1117`)
    - `role="status"` and `aria-live="polite"` (UI-SPEC §Color Details)
    - Body text: `You're offline. Changes will resume when you reconnect.` (D-37-11 / UI-SPEC §Copywriting Contract — EXACT string, including apostrophe in "You're")
    - NO retry button, NO dismiss button — none whatsoever (D-37-11)

    Component is presentational with no props/emits. Path alias `@` not needed inside the component itself. Follow project convention `<script setup lang="ts">` per CLAUDE.md.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/OfflineBanner.vue','utf8');const errs=[];if(!c.includes(\"from '@vueuse/core'\")&&!c.includes('from \"@vueuse/core\"'))errs.push('useOnline import missing');if(!c.includes('useOnline()'))errs.push('useOnline() call missing');if(!c.includes('Teleport to=\"body\"')&&!c.includes(\"Teleport to='body'\"))errs.push('Teleport missing');if(!c.includes(\"v-if=\\\"!isOnline\\\"\"))errs.push('v-if=!isOnline missing');if(!c.includes(\"role=\\\"status\\\"\"))errs.push('role=status missing');if(!c.includes('aria-live=\"polite\"'))errs.push('aria-live=polite missing');if(!c.includes('env(safe-area-inset-top)'))errs.push('safe-area-inset-top missing');if(!c.includes('z-50'))errs.push('z-50 missing');if(!c.includes('top-0'))errs.push('top-0 missing');if(!c.includes(\"You're offline. Changes will resume when you reconnect.\"))errs.push('exact copy missing');if(!c.includes('--color-status-warning'))errs.push('--color-status-warning token missing');if(!c.includes('#0d1117'))errs.push('#0d1117 text color missing');if(c.includes('<button'))errs.push('FORBIDDEN: <button> element present (no retry button per D-37-11)');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK');"</automated>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/OfflineBanner.vue` exists with Vue 3 `<script setup lang="ts">`
    - Imports `useOnline` from `@vueuse/core` (exact import path)
    - Calls `useOnline()` and binds the result to a local `isOnline` ref
    - Template wraps everything in `<Teleport to="body">`
    - Single child div has `v-if="!isOnline"`
    - Div has classes including `fixed`, `top-0`, `z-50`, `role="status"`, `aria-live="polite"`
    - `:style` binding contains `paddingTop: 'env(safe-area-inset-top)'`, `backgroundColor: 'var(--color-status-warning)'`, `color: '#0d1117'`
    - Body text EXACTLY matches: `You're offline. Changes will resume when you reconnect.`
    - No `<button>` element anywhere in the template (grep `<button` returns 0)
    - No props/emits defined (component is presentational)
    - `npm run type-check` exits 0
    - `npm run test:unit` exits 0 (regression floor 59/59 preserved; no new tests required for this presentational component)
  </acceptance_criteria>
  <done>
    OfflineBanner.vue is a self-contained presentational component ready to mount in App.vue.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount OfflineBanner in App.vue + reword ROADMAP.md and REQUIREMENTS.md PWA-07 (drop "retry affordance", lock useOnline reactive design per D-37-12)</name>
  <files>src/App.vue, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - src/App.vue (current 45 lines — script imports + template structure)
    - src/components/OfflineBanner.vue (created in Task 1 — must exist)
    - .planning/ROADMAP.md §"Phase 37" Success Criteria block (find the SC#4 line containing "retry affordance" / "useOnline")
    - .planning/REQUIREMENTS.md PWA-07 line (search for "retry affordance" / "useOnline")
    - .planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md D-37-12 (exact reword wording)
    - .planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md §"src/App.vue (provider, event-driven) — EXTEND"
  </read_first>
  <action>
    EDIT 1 — `src/App.vue` (template-only edit; script unchanged per A-43-5 / Phase 33 invariant):
    - Add `import OfflineBanner from "@/components/OfflineBanner.vue";` to the existing script imports block (after the existing `setInstallPromptEvent` / `clearInstallPromptEvent` import, BEFORE the `const isProd` line so imports stay grouped at top).
    - In the template, add `<OfflineBanner />` as the FIRST sibling element, BEFORE `<CustomNavBar />`. Since OfflineBanner internally uses `<Teleport to="body">`, the DOM ordering at the template level does not affect stacking context — the teleported element is governed by `z-index: 50` (per PATTERNS §"App.vue (provider, event-driven) — EXTEND"). Place it first for readability.
    - Do NOT modify the existing `beforeinstallprompt` capture logic (lines 14-37 must be byte-intact — App.vue is Phase 33 substrate, FND-02). Do NOT modify `<CustomNavBar>`, `<RouterView>`, `<Toaster>`, or `<SpeedInsights>` JSX.

    EDIT 2 — `.planning/ROADMAP.md`:
    - Locate the line in §"Phase 37: PWA Install + Standalone Polish" -> "Success Criteria" containing the phrase `retry affordance` (SC#4 — the bullet about going offline). The current text reads approximately: `4. Going offline (airplane mode) inside the installed PWA shows a clear offline banner with a retry affordance; reconnecting clears the banner; existing NetworkOnly /api/* toast behavior remains intact`
    - Rewrite SC#4 to: `4. Going offline (airplane mode) inside the installed PWA shows a clear offline banner; the banner auto-clears when navigator goes back online (useOnline reactive); existing NetworkOnly /api/* toast behavior remains intact`
    - The change removes "with a retry affordance" and "reconnecting clears the banner" and replaces with "the banner auto-clears when navigator goes back online (useOnline reactive)" per D-37-12.

    EDIT 3 — `.planning/REQUIREMENTS.md`:
    - Locate the PWA-07 bullet (currently: `**PWA-07**: Offline banner backed by useOnline shows when navigator goes offline (in addition to existing NetworkOnly /api/* toast behavior); retry affordance offered on reconnect`).
    - Rewrite to: `**PWA-07**: Offline banner backed by useOnline shows when navigator goes offline (in addition to existing NetworkOnly /api/* toast behavior); banner auto-clears when navigator goes back online (useOnline reactive)` per D-37-12.

    Do NOT change any other line in ROADMAP.md or REQUIREMENTS.md. Do NOT update the Progress table or Traceability table (PWA-07 stays Pending until ship).
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>node -e "const fs=require('fs');const app=fs.readFileSync('src/App.vue','utf8');const errs=[];if(!app.includes('import OfflineBanner'))errs.push('OfflineBanner import missing in App.vue');if(!app.includes('@/components/OfflineBanner.vue'))errs.push('OfflineBanner path missing');if(!app.includes('<OfflineBanner'))errs.push('<OfflineBanner /> mount missing in template');if(!app.includes('handleBeforeInstallPrompt'))errs.push('REGRESSION: Phase 33 beforeinstallprompt handler removed');if(!app.includes('setInstallPromptEvent'))errs.push('REGRESSION: setInstallPromptEvent import removed');const rm=fs.readFileSync('.planning/ROADMAP.md','utf8');if(rm.includes('retry affordance'))errs.push('ROADMAP still contains \"retry affordance\" — reword incomplete');if(!rm.includes('useOnline reactive'))errs.push('ROADMAP missing \"useOnline reactive\" wording');const rq=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');if(rq.includes('retry affordance offered on reconnect'))errs.push('REQUIREMENTS PWA-07 still contains \"retry affordance\"');if(!rq.includes('useOnline reactive'))errs.push('REQUIREMENTS missing \"useOnline reactive\" wording');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK');"</automated>
    <automated>npm run test:unit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.vue` script section contains `import OfflineBanner from "@/components/OfflineBanner.vue";`
    - `src/App.vue` template contains `<OfflineBanner />` as a sibling of `<CustomNavBar />` (NOT nested inside CustomNavBar)
    - `src/App.vue` STILL contains `handleBeforeInstallPrompt`, `setInstallPromptEvent`, `clearInstallPromptEvent`, `handleAppInstalled`, `window.addEventListener("beforeinstallprompt", ...)` — Phase 33 FND-02 substrate byte-intact
    - `.planning/ROADMAP.md` contains the string `useOnline reactive` in §Phase 37 SC#4
    - `.planning/ROADMAP.md` does NOT contain the string `retry affordance` (per D-37-12 — full removal)
    - `.planning/REQUIREMENTS.md` PWA-07 line contains `useOnline reactive` and does NOT contain `retry affordance offered on reconnect`
    - `npm run type-check` exits 0
    - `npm run test:unit` exits 0 (59/59 floor preserved)
    - `npm run build` exits 0 with 0 lines containing "exceeds" or "Skipping precaching" (NFR-PWA-PRECACHE-FITS)
  </acceptance_criteria>
  <done>
    OfflineBanner mounted site-wide; ROADMAP + REQUIREMENTS docs align with the useOnline-driven design contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser navigator.onLine -> Vue reactive | Browser event stream is trusted; no user input crosses |
| App.vue mount -> all routes | OfflineBanner is site-wide and renders on every route including unauthenticated pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Spoofing | navigator.onLine | accept | Browser API; cannot be spoofed by client code. Worst case: spoofed offline banner shown — purely cosmetic |
| T-37-02-02 | Information Disclosure | OfflineBanner copy | accept | Static string; no PII, no session data, no error details |
| T-37-02-03 | Denial of Service | banner z-index collision with PrimeVue overlays | mitigate | z-50 matches install banner; verified above CustomNavBar in PATTERNS.md key-link; if PrimeVue Modal needs >50, that is per-modal concern not banner concern |
| T-37-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (`@vueuse/core@^13.9.0` already direct dep since Phase 33; RESEARCH Package Legitimacy Audit [OK]) |
</threat_model>

<verification>
- `npm run type-check` passes
- `npm run test:unit` passes (regression floor 59/59 intact)
- `npm run build` passes with 0 "exceeds"/"Skipping precaching" lines
- `<button` count inside OfflineBanner.vue == 0 (no retry button, D-37-11)
- ROADMAP.md grep `retry affordance` == 0 matches (full reword)
- REQUIREMENTS.md grep `retry affordance offered on reconnect` == 0 matches
</verification>

<success_criteria>
- OfflineBanner.vue exists, useOnline-driven, Teleport to body, fixed top, amber bg, no retry button, exact copy locked
- App.vue mounts <OfflineBanner /> while preserving all Phase 33 beforeinstallprompt logic byte-intact
- ROADMAP.md SC#4 and REQUIREMENTS.md PWA-07 align with D-37-12 reword
- All gates green
</success_criteria>

<output>
Create `.planning/phases/37-pwa-install-standalone-polish/37-02-SUMMARY.md` documenting:
- OfflineBanner.vue final structure (file:line refs)
- App.vue diff scope (1 import line, 1 template line; Phase 33 logic unchanged confirmation)
- ROADMAP.md SC#4 before/after text
- REQUIREMENTS.md PWA-07 before/after text
- Gate results (type-check, test:unit, build)
</output>
</content>
</invoke>