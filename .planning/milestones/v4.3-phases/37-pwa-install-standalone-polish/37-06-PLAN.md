---
phase: 37-pwa-install-standalone-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/useMobileEnv.ts
  - src/composables/__tests__/useMobileEnv.spec.ts
  - src/components/projects/wallecx/WallecxApp.vue
autonomous: true
requirements: [PWA-04, PWA-09]
gap_closure: true

must_haves:
  truths:
    - "isStandalone flips to true reactively when display-mode:standalone matches mid-session (D-37-08), not only at construction time"
    - "pendingAction is reset to null after the deep-link action is dispatched, so tab remount with { immediate: true } watchers does not replay the action without a user gesture"
  artifacts:
    - path: "src/composables/useMobileEnv.ts"
      provides: "Reactive standalone propagation via watch(standaloneMatch, ...)"
      contains: "watch(standaloneMatch"
    - path: "src/components/projects/wallecx/WallecxApp.vue"
      provides: "pendingAction reset after dispatch"
      contains: "pendingAction.value = null"
  key_links:
    - from: "src/composables/useMobileEnv.ts standaloneMatch"
      to: "isStandalone ref"
      via: "watch with one-directional false->true"
      pattern: "watch\\(standaloneMatch"
    - from: "src/components/projects/wallecx/WallecxApp.vue dispatch block"
      to: "pendingAction ref"
      via: "await nextTick then null assignment after router.replace"
      pattern: "pendingAction\\.value = null"
---

<objective>
Close the two BLOCKER gaps from 37-VERIFICATION.md (CR-01, CR-02). Both are correctness defects in already-shipped Phase 37 code; neither adds new surface.

Purpose: Make the Phase 37 must-haves "isStandalone === true ALWAYS suppresses banner branches (D-37-08)" and "deep-link dispatch does not replay on tab remount" fully true.
Output: A reactive standalone watcher in useMobileEnv.ts (+ regression test) and a pendingAction reset in WallecxApp.vue.

Scope guard: Do NOT touch the other Phase 37 files (PwaInstallBanner.vue, OfflineBanner.vue, App.vue, index.html, vite.config.ts, the three tab components, guard.spec.ts). They are VERIFIED. Do NOT address WR-02/WR-03/WR-04/IN-03 warnings/info items — out of gap-closure scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-pwa-install-standalone-polish/37-VERIFICATION.md

<interfaces>
<!-- Contracts the executor must respect. Already in the codebase — do not redefine. -->

src/composables/useMobileEnv.ts (current relevant lines):
- Imports: `import { ref, type Ref } from 'vue'` and `import { useMediaQuery } from '@vueuse/core'`. `watch` is NOT yet imported.
- Line 104: `const standaloneMatch = useMediaQuery('(display-mode: standalone)')` — reactive Ref<boolean>.
- Line 105: `const isStandalone = ref(detectStandalone())` — seeded once via detectStandalone() (covers iOS navigator.standalone seed).
- Lines 106-109: the bare `if (standaloneMatch.value) { isStandalone.value = true }` one-shot read that must be REPLACED by a reactive watcher.
- The comment on lines 102-103 already claims "Kept reactive via the same media query so an install during the session flips it." — the fix makes that comment true.

Consumers whose contract must be preserved (DO NOT EDIT):
- src/components/projects/wallecx/PwaInstallBanner.vue — Android branch `v-else-if="installPromptEvent && !isStandalone && !_dismissed"` hard-gates on isStandalone; relies on it flipping true when the PWA enters standalone.
- src/components/projects/wallecx/WallecxApp.vue line 76 — eviction copy branch reads `isStandalone.value`.

src/components/projects/wallecx/WallecxApp.vue (current relevant lines):
- `nextTick` is already imported on line 2 (`import { ref, onMounted, watch, defineAsyncComponent, nextTick } from "vue"`).
- Lines 86-92 — the dispatch block: sets `activeTab.value`, `await nextTick()`, `pendingAction.value = action`, `router.replace({ query: {} })`. The reset must be appended after `router.replace`.

Tab consumers whose contract must be respected (DO NOT EDIT):
- ExpensesTab.vue line 19 `defineProps<{ pendingAction?: string | null }>()`, lines 113-117 `watch(() => props.pendingAction, (action) => { if (action === 'add-expense') ... }, { immediate: true })`.
- VaccinationsTab.vue and MembershipsTab.vue — same `{ immediate: true }` watcher shape on `props.pendingAction`. They must tolerate the prop returning to `null` after dispatch (a `null`/undefined action is a no-op in every existing watcher branch — verify, do not modify).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make isStandalone reactive to mid-session display-mode flip (CR-01)</name>
  <files>src/composables/useMobileEnv.ts, src/composables/__tests__/useMobileEnv.spec.ts</files>
  <read_first>
    - src/composables/useMobileEnv.ts (full file — current imports on line 1, the one-shot block lines 104-109, the seeding comment lines 102-103)
    - src/composables/__tests__/useMobileEnv.spec.ts (full file — the keyed matchMedia mock lines 19-51 whose addEventListener is currently a no-op vi.fn(); the 'standalone detection' describe block lines 116-128 is the place to extend)
    - src/components/projects/wallecx/PwaInstallBanner.vue (consumer — confirm the `!isStandalone` hard-gate semantics so the one-directional fix is correct: standalone only ever flips false->true, never back)
  </read_first>
  <behavior>
    - Regression test: after useMobileEnv() returns with the display-mode mock initially false (isStandalone reads false), simulating the media query matching true at runtime (a 'change' to standalone) flips isStandalone.value to true.
    - The watcher is one-directional: once isStandalone is true, a later display-mode change back to non-standalone leaves isStandalone === true (matches the iOS navigator.standalone seed-preservation contract; standalone never un-installs mid-session in practice).
    - Existing synchronous-seeding test (M-6 guard) and all breakpoint-tier tests still pass unchanged.
  </behavior>
  <action>
    In src/composables/useMobileEnv.ts: add `watch` to the existing `import { ref, type Ref } from 'vue'` line (it is not currently imported — confirm before editing). Replace the one-shot `if (standaloneMatch.value) { isStandalone.value = true }` block (lines 106-109) with a reactive watcher: `watch(standaloneMatch, (matched) => { if (matched) isStandalone.value = true }, { immediate: false })`. Keep the synchronous `const isStandalone = ref(detectStandalone())` seed on line 105 exactly as-is so the iOS navigator.standalone path and the standalone-on-launch case stay correct. Keep `{ immediate: false }` so the seed is the single source for the initial value and the watcher only handles subsequent flips. Update the inline comment so it accurately describes the watcher (one-directional false->true, preserves the navigator.standalone seed).

    In src/composables/__tests__/useMobileEnv.spec.ts: the current mock's `addEventListener: vi.fn()` discards the change handler, so a 'change' cannot be dispatched. Upgrade the mock so the standalone media-query object captures its registered 'change' listeners and exposes a way to invoke them (e.g. store handlers in a module-level array keyed to the standalone query and add a helper that mutates `state.standalone = true` then calls each captured listener). Then add a test in the 'standalone detection' describe block: construct useMobileEnv() with `state.standalone = false` (assert isStandalone.value === false), flip the mock to standalone and fire the captured change listener(s), then await a tick (`await nextTick()` from 'vue', or `await Promise.resolve()`) and assert `isStandalone.value === true`. Add a second assertion that firing a change back to non-standalone leaves isStandalone.value === true (one-directional). If, after a genuine attempt, the VueUse useMediaQuery + jsdom mock cannot reliably deliver the change event, fall back to a watcher-presence assertion only (do NOT delete the attempt — leave it skipped with a comment) and rely on the source `contains` check plus type-check. Do not weaken or remove any existing test.
  </action>
  <verify>
    <automated>npm run test:unit -- src/composables/__tests__/useMobileEnv.spec.ts</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - src/composables/useMobileEnv.ts contains `watch(standaloneMatch` (the reactive watcher).
    - src/composables/useMobileEnv.ts no longer contains the bare one-shot `if (standaloneMatch.value)` block.
    - `watch` is imported from 'vue' in useMobileEnv.ts.
    - `const isStandalone = ref(detectStandalone())` seed line is unchanged.
    - npm run test:unit (the useMobileEnv spec) exits 0 — existing tests still pass and the new reactive-flip test passes (or is explicitly skipped with a comment if jsdom cannot deliver the change event).
    - npm run type-check exits 0.
  </acceptance_criteria>
  <done>
    isStandalone reactively flips false->true when the display-mode media query matches mid-session, one-directional, with the synchronous seed preserved; regression test added; type-check clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reset pendingAction after deep-link dispatch (CR-02)</name>
  <files>src/components/projects/wallecx/WallecxApp.vue</files>
  <read_first>
    - src/components/projects/wallecx/WallecxApp.vue (full file — the dispatch block lines 86-92; confirm `nextTick` is already imported on line 2; confirm `pendingAction` ref on line 20 typed `ref<string | null>(null)`)
    - src/components/projects/wallecx/ExpensesTab.vue (consumer — the `{ immediate: true }` watcher lines 113-117; confirm a `null`/undefined action is a no-op in every branch so resetting to null does not trigger spurious behavior)
    - src/components/projects/wallecx/VaccinationsTab.vue and src/components/projects/wallecx/MembershipsTab.vue (consumers — confirm same null-tolerant watcher shape)
  </read_first>
  <action>
    In src/components/projects/wallecx/WallecxApp.vue, inside the `onMounted` dispatch block (lines 86-92), after `pendingAction.value = action` and `router.replace({ query: {} })`, append `await nextTick(); pendingAction.value = null;`. This second nextTick lets the tab watchers consume the current action value (they already ran via the first nextTick + Suspense resolution) before the ref is cleared, so the action fires exactly once and a later tab remount with `{ immediate: true }` sees `null` (a no-op). `nextTick` is already imported — do not add a duplicate import. Do NOT change the existing first `await nextTick()` on line 89, the `activeTab.value` assignment, or the `router.replace({ query: {} })` call. Do NOT await router.replace (WR-03 is out of scope). Do not modify any tab component.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/projects/wallecx/WallecxApp.vue contains `pendingAction.value = null` positioned after `router.replace({ query: {} })` inside the dispatch block.
    - A second `await nextTick()` precedes the `pendingAction.value = null` line (separate from the existing nextTick on line 89).
    - No duplicate `nextTick` import added; no tab component modified; no change to the eviction copy or SW-toast logic.
    - npm run type-check exits 0.
    - npm run test:unit exits 0 (full suite — existing tests still pass).
  </acceptance_criteria>
  <done>
    After a deep-link action is dispatched and the URL query cleared, pendingAction is reset to null on the next tick, preventing replay of openManage/sub-tab switch on Suspense tab remount without a user gesture.
  </done>
</task>

</tasks>

<verification>
- Both blocker gaps from 37-VERIFICATION.md closed: CR-01 (isStandalone reactivity) and CR-02 (pendingAction reset).
- `npm run test:unit` passes (full suite) — no regression in useMobileEnv spec, PwaInstallBanner spec, or guard spec.
- `npm run type-check` exits 0.
- Grep confirms: `watch(standaloneMatch` present in useMobileEnv.ts; `pendingAction.value = null` present after the dispatch block in WallecxApp.vue.
- No edits to the VERIFIED Phase 37 files outside the two declared targets.
</verification>

<success_criteria>
- useMobileEnv.ts: isStandalone is reactive (one-directional false->true) to runtime display-mode changes; synchronous seed preserved; regression test added.
- WallecxApp.vue: pendingAction reset to null after dispatch on the next tick; no replay on tab remount.
- Type-check and unit tests green; no scope creep beyond the two BLOCKER gaps.
</success_criteria>

<output>
Create `.planning/phases/37-pwa-install-standalone-polish/37-06-SUMMARY.md` when done.
</output>
