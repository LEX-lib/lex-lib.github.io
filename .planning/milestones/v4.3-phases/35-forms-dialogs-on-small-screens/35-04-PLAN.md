---
phase: 35-forms-dialogs-on-small-screens
plan: 04
type: execute
wave: 4
depends_on: ["35-01", "35-02", "35-03"]
files_modified:
  - src/components/projects/wallecx/ManageMembership.vue
autonomous: true
requirements: [FD-07, FD-03, FD-04, FD-05, FD-09, LT-08, FD-06]
must_haves:
  truths:
    - "ManageMembership renders through a single BaseMobileDialog instance — its Phase-34 Dialog/Drawer branches are collapsed to one"
    - "ColorPicker stays a direct v-model='cardColor' in ManageMembership (#8135) — BaseMobileDialog never owns the color ref"
    - "card_color is saved WITHOUT a '#' prefix and loaded back without acquiring one (CON-CARD-COLOR-NO-HASH)"
    - "membershipMapper.spec.ts 11 tests still pass after migration"
    - "Expiry DatePicker uses :inline=isMobile; card_image upload offers Take photo + Choose from gallery (images only, no PDF)"
  artifacts:
    - path: "src/components/projects/wallecx/ManageMembership.vue"
      provides: "ManageMembership migrated to BaseMobileDialog with ColorPicker + card_color invariants preserved"
      contains: "BaseMobileDialog"
  key_links:
    - from: "ManageMembership ColorPicker"
      to: "cardColor ref (direct v-model)"
      via: "v-model='cardColor' in child #default slot"
      pattern: "v-model=\"cardColor\""
    - from: "ManageMembership save"
      to: "formData card_color (no-hash)"
      via: "formData.append('card_color', cardColor.value)"
      pattern: "card_color"
---

<objective>
Migrate `ManageMembership.vue` (3rd in the locked order A-43-2 — HIGHEST-RISK so far) onto `BaseMobileDialog`. This dialog carries two locked invariants that MUST survive: the ColorPicker direct-v-model workaround (PrimeVue #8135) and the `card_color` no-hash round-trip (CON-CARD-COLOR-NO-HASH). The migration collapses the Phase-34 branches to one `<BaseMobileDialog>`, applies FD-03/04/05/09, and re-runs `membershipMapper.spec.ts` (11 tests) to prove the mapper is unaffected.

Purpose: Prove the wrapper does not disturb the ColorPicker / card_color invariants before the final (ManageVaccination) migration. The ColorPicker ref and the `{ immediate: true }` record watcher stay entirely in the child.

Output: ManageMembership.vue migrated; membershipMapper.spec.ts green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-forms-dialogs-on-small-screens/35-RESEARCH.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-UI-SPEC.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-PATTERNS.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-02-SUMMARY.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-03-SUMMARY.md

<interfaces>
<BaseMobileDialog v-model:visible :title :is-dirty :is-saving ref="baseDialogRef">
  <!-- #default: form body --> <template #actions><!-- Save/Cancel --></template>
</BaseMobileDialog>
baseDialogRef.value?.closeWithoutGuard()

INVARIANTS (must not change):
- <ColorPicker v-model="cardColor" ...>  — direct v-model, child-owned (#8135)
- cardColor stored/loaded WITHOUT '#' prefix (CON-CARD-COLOR-NO-HASH)
- record watcher with { immediate: true } seeds cardColor — do not remove (Pitfall 3)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate ManageMembership.vue to BaseMobileDialog, preserving ColorPicker + card_color invariants</name>
  <files>src/components/projects/wallecx/ManageMembership.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageMembership.vue (entire file — ColorPicker direct v-model ~41-47/304-310/428-435; card_color no-hash load ~63-65, save ~208; record watcher {immediate:true} ~56-80; EXIF path ~115-173; Dialog/Drawer branches; expiry DatePicker ~323-325/449-451; card_image FileUpload accept ~353-360)
    - src/components/projects/wallecx/ManageExpense.vue (migrated reference — BaseMobileDialog usage + onRawFileChange bridge)
    - src/lib/pocketbase/__tests__/membershipMapper.spec.ts (the 11 tests — read to understand what card_color round-trip they assert)
    - src/lib/pocketbase/membershipMapper.ts (mapToUpdateMembership — must stay untouched)
    - 35-PATTERNS.md §ManageMembership.vue (ColorPicker invariant, card_color round-trip, record watcher preserve, EXIF, FD-05 images-only accept, FD-04, dirty snapshot, FD-03) + §Invariant Cross-Reference
    - 35-UI-SPEC.md Contract 5 (camera — images only for membership), Contract 7 ManageMembership table, Open Question 1 (ColorPicker dark-mode in Drawer)
  </read_first>
  <action>
**Composable swap (Pattern S-2):** `useIsMobile` → `const { isMobile } = useMobileEnv()`.

**Collapse branches (Pattern S-1):** Replace `<Dialog v-if="!isMobile">...</Dialog>` + `<Drawer v-else>...</Drawer>` with ONE `<BaseMobileDialog v-model:visible="visible" :title="dialogHeader" :is-dirty="isDirty" :is-saving="isSaving" ref="baseDialogRef">`. Form body in `#default` inside `<form id="manage-membership-form" @submit.prevent="onSubmit" class="space-y-4">`. Save/Cancel in `<template #actions>` per Contract 2 (Save label: `isEditMode ? 'Save Changes' : 'Add Membership'`). Remove this file's duplicated dark-mode `:deep` scoped block.

**INVARIANT — ColorPicker (#8135, Pitfall 3):** The `<ColorPicker v-model="cardColor" aria-label="Card colour picker" :disabled="isSaving" />` stays in the `#default` slot content with its DIRECT v-model. The `cardColor` ref stays declared in ManageMembership.vue (`const cardColor = ref<string>('002244')` — navy default, no '#'). The preview swatch `<span :style="{ backgroundColor: '#' + cardColor }">` stays. BaseMobileDialog NEVER binds or owns cardColor.

**INVARIANT — record watcher (Pitfall 3):** Preserve the `watch(() => record.value, (rec) => {...}, { immediate: true })` block VERBATIM, including the `cardColor.value = rec.card_color ?? '002244'` seed and the else-branch reset to `'002244'`. Do NOT remove `{ immediate: true }`.

**INVARIANT — card_color no-hash (CON-CARD-COLOR-NO-HASH):** Do NOT touch the save logic `formData.append('card_color', cardColor.value)` — it must remain WITHOUT a `'#' +` prefix. Do NOT touch the load logic. The mapper file is not modified.

**Import + ref:** `import BaseMobileDialog from './BaseMobileDialog.vue'`; `const baseDialogRef = ref<InstanceType<typeof BaseMobileDialog> | null>(null)`.

**Close paths (Pitfall 2):** onSubmit success → `baseDialogRef.value?.closeWithoutGuard()`; add `function onCancel(): void { baseDialogRef.value?.closeWithoutGuard() }`. Keep existing onHide reset (pendingFile reset) running on close.

**Dirty snapshot (FD-09):** Per 35-PATTERNS.md ManageMembership block — snapshot on `visible` rising edge, `isDirty` computed comparing cardName/barcodeType/barcodeValue/cardNumber/cardColor/expiryDate(ISO)/issuer/notes and `pendingFile.value !== null`. cardColor IS part of the dirty comparison (read-only compare — the ref still lives in the child).

**FD-04 (expiry DatePicker):** single instance after collapse → `<DatePicker v-model="expiryDate" :inline="isMobile" fluid dateFormat="dd M yy" showButtonBar :disabled="isSaving" />`.

**FD-05 two-affordance (Contract 5 — IMAGES ONLY, no PDF for membership):** Replace the card_image FileUpload with `<template v-if="isMobile">` two label-wrapped hidden inputs — camera (`accept="image/jpeg,image/png,image/webp" capture="environment"`) + gallery (`accept="image/jpeg,image/png,image/webp"` — NO application/pdf). Desktop `v-else` keeps existing FileUpload (images-only accept). Add the `onRawFileChange` bridge calling the existing `onFileSelect` (preserve the EXIF `URL.createObjectURL`/`Image` pipeline verbatim, including its size + MIME validation).

**FD-03 (Contract 7 ManageMembership):** Card Name `inputmode="text" enterkeyhint="next"`; Barcode Value `inputmode="text" enterkeyhint="next"`; Card Number `inputmode="numeric" enterkeyhint="next"`; Issuer `inputmode="text" enterkeyhint="next"`; Notes Textarea `inputmode="text" enterkeyhint="done"` (last text field); `autocomplete="off"` on all. Barcode Type Select / ColorPicker / DatePicker get no inputmode (non-text).
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `ManageMembership.vue` imports `BaseMobileDialog` and contains exactly one `<BaseMobileDialog` — `grep -c "<BaseMobileDialog" ManageMembership.vue` returns 1
    - `ManageMembership.vue` does NOT contain `<Dialog ` and does NOT contain `<Drawer `
    - `ManageMembership.vue` ColorPicker still uses direct `v-model="cardColor"` (grep `v-model="cardColor"` present)
    - `ManageMembership.vue` save path still `formData.append('card_color', cardColor.value)` with NO `'#' +` prefix
    - `ManageMembership.vue` record watcher still contains `{ immediate: true }`
    - `ManageMembership.vue` gallery input accept does NOT contain `application/pdf` (images only); camera input has `capture="environment"`
    - `ManageMembership.vue` contains `useMobileEnv`, `:inline="isMobile"`, `inputmode="numeric"`, `enterkeyhint="done"`, `closeWithoutGuard`
    - `src/lib/pocketbase/membershipMapper.ts` is unchanged (not in the diff)
    - `npm run test:unit` passes with membershipMapper's 11 tests green and total >=59
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>ManageMembership renders through one BaseMobileDialog; ColorPicker direct v-model + card_color no-hash + {immediate:true} watcher preserved; FD-03/04/05/09 applied; membershipMapper 11 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device camera/gallery → card_image input | User-supplied image crosses into upload pipeline |
| cardColor → PocketBase card_color | Hex string written to wallecx_memberships |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Tampering | card_color field | mitigate | Existing Zod `[0-9a-fA-F]{6}` validation + no-hash storage logic preserved unchanged; the migration does not touch save/load. membershipMapper.spec.ts re-run proves round-trip integrity. |
| T-35-10 | Tampering | onRawFileChange card_image bridge | mitigate | Routes through unchanged onFileSelect (size cap + image-only MIME allowlist + EXIF strip). accept attrs are hints only. |
| T-35-11 | Information Disclosure | card_image EXIF | mitigate | Existing EXIF-strip (URL.createObjectURL/Image re-encode) preserved — strips location metadata before upload. |

No HIGH-severity threats. card_color validation + EXIF strip + image-only accept all preserved; dirty guard does not bypass save validation.
</threat_model>

<verification>
- `npm run test:unit` → membershipMapper 11 green, total >=59
- `npm run type-check` → 0 errors
- `grep -c "<BaseMobileDialog" ManageMembership.vue` → 1
- `v-model="cardColor"` present; `formData.append('card_color'` present with no `'#' +`
</verification>

<success_criteria>
ManageMembership migrated to one BaseMobileDialog with all three invariants intact (ColorPicker direct v-model, card_color no-hash, {immediate:true} watcher), FD-03/04/05/09 applied, membershipMapper 11 tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/35-forms-dialogs-on-small-screens/35-04-SUMMARY.md`
</output>
