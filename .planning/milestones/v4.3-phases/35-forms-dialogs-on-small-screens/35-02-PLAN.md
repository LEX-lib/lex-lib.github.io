---
phase: 35-forms-dialogs-on-small-screens
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - src/components/projects/wallecx/ManageExpense.vue
  - src/components/projects/wallecx/ExpensesToolbar.vue
  - src/components/projects/wallecx/ExpensesReportsView.vue
autonomous: true
requirements: [FD-07, FD-03, FD-04, FD-05, FD-06, FD-09, LT-08]
must_haves:
  truths:
    - "ManageExpense renders through a single BaseMobileDialog instance — its Phase-34 Dialog/Drawer branches are collapsed to one"
    - "ManageExpense Amount uses inputmode=decimal; Notes (last field) uses enterkeyhint=done"
    - "ManageExpense date and all 4 non-dialog DatePicker sites (ExpensesToolbar From/To, ExpensesReportsView Custom From/To) use :inline=isMobile"
    - "ManageExpense offers two mobile upload affordances (Take photo with capture=environment + Choose from gallery) feeding the existing EXIF/compression pipeline"
    - "Successful save and explicit Cancel close via closeWithoutGuard() so the dirty guard does not fire on those paths"
  artifacts:
    - path: "src/components/projects/wallecx/ManageExpense.vue"
      provides: "ManageExpense migrated to BaseMobileDialog with FD-03/04/05 attributes and dirty snapshot"
      contains: "BaseMobileDialog"
    - path: "src/components/projects/wallecx/ExpensesToolbar.vue"
      provides: "From/To DatePickers with :inline=isMobile (FD-04)"
      contains: ":inline=\"isMobile\""
    - path: "src/components/projects/wallecx/ExpensesReportsView.vue"
      provides: "Custom range DatePickers with :inline=isMobile (FD-04)"
      contains: ":inline=\"isMobile\""
  key_links:
    - from: "ManageExpense.vue"
      to: "BaseMobileDialog.vue"
      via: "single <BaseMobileDialog> with #default + #actions slots"
      pattern: "BaseMobileDialog"
    - from: "ManageExpense onRawFileChange"
      to: "onFileSelect EXIF/compression pipeline"
      via: "raw <input type=file> @change bridge"
      pattern: "onRawFileChange"
---

<objective>
Migrate `ManageExpense.vue` (the reference dialog, first in the locked order A-43-2) onto `BaseMobileDialog`, and apply FD-04 `:inline="isMobile"` to the two NON-dialog DatePicker sites that also need it (`ExpensesToolbar` From/To, `ExpensesReportsView` Custom range). ManageExpense gets the full Phase-35 treatment: collapse its Phase-34 v-if/v-else branches to one `<BaseMobileDialog>`, add FD-03 input attributes, FD-04 inline DatePicker, FD-05 two-affordance camera/gallery, and the FD-09 dirty snapshot + `closeWithoutGuard()` wiring.

Purpose: ManageExpense is the proven pattern that ManageBudget/ManageMembership/ManageVaccination follow. Doing the two standalone toolbar/view DatePicker sites here keeps all expense-tab DatePicker edits in one plan (disjoint files from the other migrations).

Output: ManageExpense.vue migrated; ExpensesToolbar.vue + ExpensesReportsView.vue DatePickers made inline-on-mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-forms-dialogs-on-small-screens/35-RESEARCH.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-UI-SPEC.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-PATTERNS.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-01-SUMMARY.md

<interfaces>
<!-- BaseMobileDialog contract from Plan 01 (consume directly, no exploration) -->
<BaseMobileDialog v-model:visible :title :is-dirty :is-saving ref="baseDialogRef">
  <!-- #default: form body (rendered once) -->
  <template #actions> <!-- Save/Cancel buttons --> </template>
</BaseMobileDialog>
baseDialogRef.value?.closeWithoutGuard()  // call on save success + explicit cancel

FD-04 inline DatePicker shape:
<DatePicker v-model="x" :inline="isMobile" fluid dateFormat="dd M yy" showButtonBar :disabled="isSaving" />
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate ManageExpense.vue to BaseMobileDialog + FD-03/04/05/09</name>
  <files>src/components/projects/wallecx/ManageExpense.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageExpense.vue (entire file — the Dialog branch ~265-392 and Drawer branch ~395-526 are identical forms to collapse; onFileSelect EXIF pipeline ~127-173; onHide reset ~175-177; onSubmit close paths)
    - src/components/projects/wallecx/BaseMobileDialog.vue (the wrapper from Plan 01 — slot contract + closeWithoutGuard)
    - 35-PATTERNS.md §ManageExpense.vue (collapse strategy, onRawFileChange bridge, two-affordance template, dirty snapshot, FD-04 change, onSubmit close, FD-03 additions) + §Shared Patterns S-1..S-7
    - 35-UI-SPEC.md Contract 2 (button layout), Contract 5 (camera two-affordance + file preview), Contract 7 ManageExpense table, Contract 4 (inline DatePicker field group)
    - 35-RESEARCH.md §8 ManageExpense migration map
  </read_first>
  <action>
**Composable swap (Pattern S-2):** change `import { useIsMobile } from '@/composables/useIsMobile'` / `const isMobile = useIsMobile()` to `import { useMobileEnv } from '@/composables/useMobileEnv'` / `const { isMobile } = useMobileEnv()`.

**Collapse branches (Pattern S-1):** Replace the entire `<Dialog v-if="!isMobile">...</Dialog>` + `<Drawer v-else>...</Drawer>` template with ONE `<BaseMobileDialog v-model:visible="visible" :title="dialogHeader" :is-dirty="isDirty" :is-saving="isSaving || isLoadingCategories" ref="baseDialogRef">`. Put the `<form @submit.prevent="onSubmit" id="manage-expense-form" class="space-y-4">` with all fields in the `#default` slot (rendered ONCE). Put Save/Cancel in `<template #actions>` per UI-SPEC Contract 2:
```html
<template #actions>
  <div class="flex gap-2">
    <Button type="button" label="Cancel" severity="secondary" fluid :disabled="isSaving" @click="onCancel" />
    <Button type="submit" form="manage-expense-form" :label="isEditMode ? 'Save Changes' : 'Add Expense'" fluid :loading="isSaving || isLoadingCategories" :disabled="isSaving || isLoadingCategories" />
  </div>
</template>
```
Remove the duplicated dark-mode `:deep(.my-app-dark .p-dialog/.p-drawer)` scoped block from this file's `<style scoped>` — it now lives in BaseMobileDialog (do not re-declare).

**Import BaseMobileDialog:** `import BaseMobileDialog from './BaseMobileDialog.vue'` and add `const baseDialogRef = ref<InstanceType<typeof BaseMobileDialog> | null>(null)`.

**Close paths (Pitfall 2):** In onSubmit's success path, replace `visible.value = false` with `baseDialogRef.value?.closeWithoutGuard()`. Add `function onCancel(): void { baseDialogRef.value?.closeWithoutGuard() }`. Keep the existing onHide reset (`pendingFile.value = null`) — BaseMobileDialog relays `@hide` through the Drawer/Dialog, but since onHide logic is component-specific, ensure it still runs: the cleanest path is to keep ManageExpense's own reset triggered by a `watch(visible, (v) => { if (!v) pendingFile.value = null })`.

**Dirty snapshot (FD-09, Pattern from PATTERNS.md):** Add `ExpenseSnapshot` interface, `snapshot` ref, `watch(visible, isOpen => { if (isOpen) snapshot.value = {...} })`, and `isDirty` computed comparing amount/expenseDate(ISO)/category/description/notes/pendingFile per 35-PATTERNS.md ManageExpense dirty snapshot block.

**FD-04 DatePicker:** change the expense date DatePicker (now single in #default) to `<DatePicker v-model="expenseDate" :inline="isMobile" fluid dateFormat="dd M yy" showButtonBar :disabled="isSaving" />`.

**FD-05 two-affordance upload (Contract 5 / PATTERNS template):** Replace the single FileUpload with `<template v-if="isMobile">` two `<label class="p-button p-button-outlined p-button-secondary p-button-sm min-h-[44px] cursor-pointer flex items-center gap-2 flex-1">`-wrapped hidden `<input type="file">` — camera (`accept="image/jpeg,image/png,image/webp" capture="environment"`) + gallery (`accept="image/jpeg,image/png,image/webp,application/pdf"`), both `@change="onRawFileChange"`. Desktop `v-else` keeps the existing `<FileUpload>`. Add the bridge:
```typescript
async function onRawFileChange(event: Event): Promise<void> {
  const input = event.target as HTMLInputElement
  const file = input.files?.[0]
  if (!file) return
  input.value = ''
  await onFileSelect({ files: [file] })
}
```
Do NOT modify the `onFileSelect` EXIF/compression pipeline body — it keeps its existing 10 MB + allowedTypes validation (security-relevant; must remain).

**FD-03 attributes (Contract 7 ManageExpense):** add to existing inputs — Amount InputNumber `inputmode="decimal" enterkeyhint="next" autocomplete="off"`; Category Select `inputmode="text"` (where applicable) `enterkeyhint="next"`; Description InputText `inputmode="text" enterkeyhint="next" autocomplete="off"`; Notes Textarea `inputmode="text" enterkeyhint="done" autocomplete="off"` (last field = done).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `ManageExpense.vue` imports `BaseMobileDialog` and contains exactly one `<BaseMobileDialog` (branches collapsed) — `grep -c "<BaseMobileDialog" ManageExpense.vue` returns 1
    - `ManageExpense.vue` does NOT contain `<Dialog ` and does NOT contain `<Drawer ` (the shell now lives in the wrapper)
    - `ManageExpense.vue` contains `useMobileEnv` (not `useIsMobile`)
    - `ManageExpense.vue` contains `inputmode="decimal"`, `enterkeyhint="done"`, `:inline="isMobile"`, `capture="environment"`, `onRawFileChange`, `closeWithoutGuard`
    - `ManageExpense.vue` retains its `onFileSelect` validation (`10 * 1024 * 1024` size check + `allowedTypes`) unchanged
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>ManageExpense renders through one BaseMobileDialog; FD-03/04/05/09 applied; EXIF validation untouched; type-check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply :inline="isMobile" to ExpensesToolbar + ExpensesReportsView DatePickers (FD-04)</name>
  <files>src/components/projects/wallecx/ExpensesToolbar.vue, src/components/projects/wallecx/ExpensesReportsView.vue</files>
  <read_first>
    - src/components/projects/wallecx/ExpensesToolbar.vue (entire file — From/To DatePickers ~67-83; note it currently has NO composable import in script setup)
    - src/components/projects/wallecx/ExpensesReportsView.vue (Custom From ~412-417 / Custom To ~421-426 DatePickers; check existing script setup for a mobile composable already imported)
    - 35-PATTERNS.md §ExpensesToolbar.vue + §ExpensesReportsView.vue (exact FD-04 edits, @update:model-value cast preservation)
    - 35-UI-SPEC.md Contract 4 (the 5 DatePicker sites table; placeholder harmless on inline)
  </read_first>
  <action>
**ExpensesToolbar.vue:** Add `import { useMobileEnv } from '@/composables/useMobileEnv'` and `const { isMobile } = useMobileEnv()` to script setup (file currently has props/emits only). Add `:inline="isMobile"` to BOTH DatePicker sites (From, To). PRESERVE the existing `@update:model-value="emit('update:dateFrom', ($event instanceof Date ? $event : null))"` cast pattern verbatim on each — inline DatePicker still emits `Date | null`. Keep `placeholder`, `dateFormat="dd M yy"`, `class="w-32 min-h-[44px]"`, `showButtonBar`.

**ExpensesReportsView.vue:** Ensure `const { isMobile } = useMobileEnv()` is available (add the `useMobileEnv` import if not present; if the file already imports `useIsMobile`, either reuse it or add `useMobileEnv` — prefer `useMobileEnv().isMobile` for the single source of truth, matching Pattern S-2). Add `:inline="isMobile"` and `showButtonBar` to both `customFrom` and `customTo` DatePickers. Preserve `v-model`, `placeholder`, `dateFormat="dd M yy"`, `class="w-full min-h-[44px]"`.

Do NOT change any period-selector / sessionStorage / comparison logic — FD-04 is a DatePicker-attribute-only change.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `ExpensesToolbar.vue` contains `useMobileEnv` and `:inline="isMobile"` appears 2 times (From + To)
    - `ExpensesToolbar.vue` still contains both `@update:model-value` emit casts (`$event instanceof Date`)
    - `ExpensesReportsView.vue` contains `:inline="isMobile"` appearing 2 times (customFrom + customTo) and `isMobile` is sourced from `useMobileEnv`
    - Neither file contains a `<DatePicker` with a `touchUI` prop (D-35-13 — prop does not exist in PrimeVue 4.5.5)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>All 4 non-dialog DatePicker sites use :inline=isMobile; cast patterns preserved; no touchUI; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device camera/gallery → file input | User-supplied file crosses into the upload pipeline |
| file → PocketBase write | Pending file uploaded to wallecx_expenses receipt field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Tampering | onRawFileChange raw <input> bridge | mitigate | Bridge routes the file through the UNCHANGED `onFileSelect`, which enforces the existing 10 MB size cap + `allowedTypes` MIME allowlist. The two raw inputs also carry `accept=` attributes as a client hint, but server-side `onFileSelect` validation is the real gate and remains intact. |
| T-35-05 | Spoofing | accept/capture attributes | accept | `accept` and `capture` are UX hints only; a malicious file is still caught by onFileSelect validation. No trust placed in the attribute. |
| T-35-06 | Information Disclosure | EXIF stripping | mitigate | Existing canvas re-encode EXIF-strip path is preserved (strips GPS/location metadata before upload). Not modified by this plan. |

No HIGH-severity threats. Camera input keeps the existing accept-type + 10 MB validation; dirty guard does not bypass save validation.
</threat_model>

<verification>
- `npm run type-check` → 0 errors
- `grep -c "<BaseMobileDialog" ManageExpense.vue` → 1
- `grep -c ":inline=\"isMobile\"" ExpensesToolbar.vue` → 2; same for ExpensesReportsView.vue
- No `touchUI` anywhere in the 3 modified files
</verification>

<success_criteria>
ManageExpense renders through one BaseMobileDialog with collapsed branches, full FD-03/04/05/09 treatment, and preserved EXIF validation. All 4 standalone expense DatePicker sites render inline on mobile. type-check passes.
</success_criteria>

<output>
After completion, create `.planning/phases/35-forms-dialogs-on-small-screens/35-02-SUMMARY.md`
</output>
