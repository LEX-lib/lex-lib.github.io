---
phase: 35-forms-dialogs-on-small-screens
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified:
  - src/components/projects/wallecx/ManageBudget.vue
autonomous: true
requirements: [FD-07, FD-03, FD-09, LT-08, FD-06]
must_haves:
  truths:
    - "ManageBudget renders through a single BaseMobileDialog instance — its Phase-34 Dialog/Drawer branches are collapsed to one"
    - "Per-row budget amount InputNumbers use inputmode=decimal; the last row uses enterkeyhint=done"
    - "Backdrop-tap on a budget form with edited rows prompts 'Discard changes?'; Save/Cancel bypass via closeWithoutGuard()"
    - "The existing localRows rebuild-on-open watch is preserved verbatim"
  artifacts:
    - path: "src/components/projects/wallecx/ManageBudget.vue"
      provides: "ManageBudget migrated to BaseMobileDialog with FD-03 attrs + JSON-snapshot dirty detection"
      contains: "BaseMobileDialog"
  key_links:
    - from: "ManageBudget.vue"
      to: "BaseMobileDialog.vue"
      via: "single <BaseMobileDialog> with #default + #actions slots"
      pattern: "BaseMobileDialog"
    - from: "ManageBudget isDirty"
      to: "localRows JSON snapshot"
      via: "JSON.stringify comparison vs on-open snapshot"
      pattern: "JSON.stringify"
---

<objective>
Migrate `ManageBudget.vue` (2nd in the locked order A-43-2, the simplest because it has NO file upload) onto `BaseMobileDialog`. Apply the now-proven ManageExpense pattern: collapse the Phase-34 v-if/v-else branches to one `<BaseMobileDialog>`, add FD-03 input attributes to the per-row amount inputs, and add the FD-09 dirty snapshot (JSON-stringify of `localRows`) + `closeWithoutGuard()` wiring. There is NO camera/gallery work here (D-35-11 — ManageBudget has no upload).

Purpose: Second migration, confirming the pattern works for a multi-row form with array-based dirty detection before tackling the two highest-risk dialogs.

Output: ManageBudget.vue migrated to BaseMobileDialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-forms-dialogs-on-small-screens/35-RESEARCH.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-UI-SPEC.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-PATTERNS.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-01-SUMMARY.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-02-SUMMARY.md

<interfaces>
<BaseMobileDialog v-model:visible :title :is-dirty :is-saving ref="baseDialogRef">
  <!-- #default: form body --> <template #actions><!-- Save/Cancel --></template>
</BaseMobileDialog>
baseDialogRef.value?.closeWithoutGuard()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate ManageBudget.vue to BaseMobileDialog + FD-03/09</name>
  <files>src/components/projects/wallecx/ManageBudget.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageBudget.vue (entire file — Dialog/Drawer branches ~110-238; localRows rebuild watch ~40-50; onHide reset ~56-58; per-row InputNumber ~138-146/207-213; Save button label ~163/232)
    - src/components/projects/wallecx/ManageExpense.vue (the just-migrated reference from Plan 02 — copy the BaseMobileDialog usage shape)
    - src/components/projects/wallecx/BaseMobileDialog.vue (slot contract + closeWithoutGuard)
    - 35-PATTERNS.md §ManageBudget.vue (collapse, localRows watch preserve, JSON snapshot dirty, per-row enterkeyhint, Save Budgets label) + S-1/S-2/S-3/S-6
    - 35-UI-SPEC.md Contract 7 ManageBudget table, Contract 2 button layout, Open Question 2 (confirm existing "Save Budget(s)" label verbatim)
  </read_first>
  <action>
**Composable swap (Pattern S-2):** `useIsMobile` → `const { isMobile } = useMobileEnv()`.

**Collapse branches (Pattern S-1):** Replace `<Dialog v-if="!isMobile">...</Dialog>` + `<Drawer v-else>...</Drawer>` with ONE `<BaseMobileDialog v-model:visible="visible" :title="dialogHeader" :is-dirty="isDirty" :is-saving="isSaving" ref="baseDialogRef">`. Form body (the per-category rows + budget-type SelectButton) goes in `#default` inside `<form id="manage-budget-form" @submit.prevent="onSubmit" class="space-y-4">`. Save/Cancel go in `<template #actions>` per Contract 2, using the EXISTING save label verbatim (confirm it — likely `Save Budgets`; do NOT rename to "Save Budget"):
```html
<template #actions>
  <div class="flex gap-2">
    <Button type="button" label="Cancel" severity="secondary" fluid :disabled="isSaving" @click="onCancel" />
    <Button type="submit" form="manage-budget-form" label="<EXISTING_LABEL_VERBATIM>" fluid :loading="isSaving" :disabled="isSaving" />
  </div>
</template>
```
Remove this file's duplicated dark-mode `:deep(.my-app-dark .p-dialog/.p-drawer)` scoped block (now in BaseMobileDialog).

**Import + ref:** `import BaseMobileDialog from './BaseMobileDialog.vue'`; `const baseDialogRef = ref<InstanceType<typeof BaseMobileDialog> | null>(null)`.

**Preserve the localRows rebuild watch verbatim** (PATTERNS §ManageBudget — fires on `visible` rising edge to rebuild rows from `props.categories` + `budgetMap`). Do NOT alter its logic. Keep the existing onHide reset (`isSaving.value = false`); ensure it still runs on close (relayed via a `watch(visible, v => { if (!v) isSaving.value = false })` if needed).

**Dirty snapshot (FD-09):** Add an `openSnapshot` ref(string). In the SAME `visible` rising-edge watch (AFTER localRows is rebuilt), set `openSnapshot.value = JSON.stringify(localRows.value)`. Add `const isDirty = computed<boolean>(() => JSON.stringify(localRows.value) !== openSnapshot.value)`.

**Close paths (Pitfall 2):** In onSubmit success path replace `visible.value = false` with `baseDialogRef.value?.closeWithoutGuard()`. Add `function onCancel(): void { baseDialogRef.value?.closeWithoutGuard() }`.

**FD-03 (Contract 7 ManageBudget):** On the per-row amount `<InputNumber>`, add `inputmode="decimal"`, `autocomplete="off"`, and `:enterkeyhint="idx === localRows.length - 1 ? 'done' : 'next'"` (use the existing row index variable name).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `ManageBudget.vue` imports `BaseMobileDialog` and contains exactly one `<BaseMobileDialog` — `grep -c "<BaseMobileDialog" ManageBudget.vue` returns 1
    - `ManageBudget.vue` does NOT contain `<Dialog ` and does NOT contain `<Drawer `
    - `ManageBudget.vue` contains `useMobileEnv`, `inputmode="decimal"`, `JSON.stringify`, `closeWithoutGuard`
    - `ManageBudget.vue` contains `enterkeyhint` with the `'done' : 'next'` last-row ternary
    - `ManageBudget.vue` contains NO `capture="environment"` and NO `<FileUpload` (no upload site per D-35-11)
    - The existing save-button label string is unchanged (verbatim carry-forward)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>ManageBudget renders through one BaseMobileDialog; FD-03/09 applied; localRows watch preserved; no upload added; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → budget rows | Per-category amounts cross into the bulk-upsert write path |
| dirty guard → dismissal | Guard governs dismissal only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | ManageBudget bulk-upsert | accept | This plan does not touch the existing Promise.all create/update/delete write logic or its validation; only the shell + dirty UX change. Existing per-row validation remains. |
| T-35-08 | Denial of Service | JSON.stringify isDirty on every keystroke | accept | localRows is ~5-15 rows for a personal vault; stringify is trivially fast (per RESEARCH Open Question 3). No DoS surface. |

No HIGH-severity threats. No upload, no new data flow; dirty guard does not bypass save validation.
</threat_model>

<verification>
- `npm run type-check` → 0 errors
- `grep -c "<BaseMobileDialog" ManageBudget.vue` → 1
- No `<FileUpload` / `capture` in ManageBudget.vue
</verification>

<success_criteria>
ManageBudget renders through one BaseMobileDialog with collapsed branches, FD-03 per-row attributes, JSON-snapshot dirty detection, and preserved localRows rebuild. type-check passes.
</success_criteria>

<output>
After completion, create `.planning/phases/35-forms-dialogs-on-small-screens/35-03-SUMMARY.md`
</output>
