---
phase: 17-mobile-bottom-sheets-view-toggle
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/components/projects/wallecx/VaccinationsTab.vue
autonomous: true
requirements:
  - UX-01
  - UX-03
  - UX-04
  - MOB-09

must_haves:
  truths:
    - "On phones (< 640px), tapping a vaccination group card opens a Drawer that slides up from the bottom of the screen with the group's records and edit/delete actions; no right-side drawer appears."
    - "On desktop (≥ 640px), tapping a vaccination group card opens the SAME right-side Drawer as before — unchanged in width, animation direction, and behavior."
    - "On phones (< 640px), the grid/list view toggle is not rendered in WallecxToolbar."
    - "On phones (< 640px), the vaccination card grid renders in list layout (`grid grid-cols-1 gap-4`) regardless of the value persisted in sessionStorage under `wallecx:view-mode`."
    - "When the user resizes the window from < 640px to ≥ 640px, the toggle reappears and the card grid returns to the user's stored `viewMode` preference."
    - "The mobile bottom Drawer displays a centered drag handle pill (~32×4px rounded grey bar) above the title; the desktop right-side Drawer shows only the title."
    - "The mobile bottom Drawer is dismissible by tapping the backdrop OR the Drawer's built-in close button (PrimeVue Drawer default)."
  artifacts:
    - path: "src/components/projects/wallecx/VaccinationsTab.vue"
      provides: "Reactive Drawer position, effectiveViewMode computed, isMobile-aware showToggle, drag handle pill in #header slot"
      contains: "useIsMobile"
  key_links:
    - from: "src/components/projects/wallecx/VaccinationsTab.vue"
      to: "src/composables/useIsMobile.ts"
      via: "import { useIsMobile } from '@/composables/useIsMobile'"
      pattern: "from\\s+['\\\"]@/composables/useIsMobile['\\\"]"
    - from: "VaccinationsTab.vue <Drawer>"
      to: "isMobile ref"
      via: ":position binding"
      pattern: ":position=\"isMobile \\?"
    - from: "VaccinationsTab.vue <WallecxToolbar>"
      to: "isMobile ref"
      via: ":show-toggle binding with && !isMobile"
      pattern: "&& !isMobile"
    - from: "VaccinationsTab.vue gridClass computed"
      to: "effectiveViewMode computed"
      via: "effectiveViewMode.value === 'list'"
      pattern: "effectiveViewMode\\.value === 'list'"
---

<objective>
Wire reactive mobile detection into `VaccinationsTab.vue` to deliver four behaviors at once:
1. **UX-01:** Make the existing `<Drawer>`'s `:position` reactive (`isMobile ? 'bottom' : 'right'`) so the vaccination group detail slides up from the bottom on phones.
2. **UX-03:** Provide a drag handle pill via a custom `#header` slot on the Drawer (mobile-only via `v-if`); preserve PrimeVue's built-in close button and backdrop tap dismissal.
3. **UX-04:** Keep desktop (≥ 640px) behavior identical — Drawer slides from right, no pill, same width, same dismissal.
4. **MOB-09:** Hide the view toggle on mobile (`&& !isMobile` on the existing `showToggle` prop) and force list-layout grid via a new `effectiveViewMode` computed — without touching the persisted `viewMode` ref or its sessionStorage watch.

Purpose: Implement CONTEXT decisions D-04, D-05, D-11, D-12, D-13 in a single tightly-scoped file change. The `viewMode` ref and `sessionStorage` watch are EXPLICITLY unchanged (D-12, D-13) so desktop preference round-trips correctly across resizes.

Output: Modified `src/components/projects/wallecx/VaccinationsTab.vue` covering UX-01, UX-03 (vaccination side), UX-04 (vaccination side), and MOB-09.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-mobile-bottom-sheets-view-toggle/17-CONTEXT.md
@.planning/phases/17-mobile-bottom-sheets-view-toggle/17-PATTERNS.md
@.planning/phases/17-mobile-bottom-sheets-view-toggle/17-01-SUMMARY.md
@./CLAUDE.md
@src/components/projects/wallecx/VaccinationsTab.vue

<interfaces>
<!-- Composable created in plan 17-01 (Wave 1) -->
From src/composables/useIsMobile.ts:
```ts
export function useIsMobile(breakpoint = 639): Ref<boolean>
```
Returns a reactive Ref<boolean>; `.value` is `true` when viewport width ≤ 639px.

<!-- Existing VaccinationsTab.vue current state (key fragments to modify in-place) -->
Imports (lines 1–11):
```ts
import { ref, onMounted, onUnmounted, computed, watch } from "vue";
import dayjs from "dayjs";
import { toast } from "vue-sonner";
import { useConfirm } from "primevue/useconfirm";
import { pb } from "@/lib/pocketbase";
import type { Vaccinations } from "@/types/wallecx/vaccinations/types";
import ManageVaccination from "./ManageVaccination.vue";
import VaccinationGroupCard from "./VaccinationGroupCard.vue";
import VaccinationGroupPanel from "./VaccinationGroupPanel.vue";
import WallecxToolbar from './WallecxToolbar.vue';
```

State refs (lines 13–30): includes `const viewMode = ref<'grid' | 'list'>('grid');` on line 30 — DO NOT touch this ref or the sessionStorage watch (D-12).

Existing gridClass (lines 122–126):
```ts
const gridClass = computed(() =>
  viewMode.value === 'list'
    ? 'grid grid-cols-1 gap-4'
    : 'grid grid-cols-1 sm:grid-cols-2 gap-4',
);
```

Existing viewMode watch (lines 170–176) — DO NOT MODIFY:
```ts
watch(viewMode, (next) => {
  try {
    sessionStorage.setItem(VIEW_MODE_STORAGE_KEY, next);
  } catch { /* non-fatal */ }
});
```

Existing WallecxToolbar binding (line 344):
```html
:show-toggle="!isLoading && records.length > 0 && displayedGroups.length > 0"
```

Existing Drawer markup (lines 409–425):
```html
<Drawer
  v-model:visible="showGroupPanel"
  position="right"
  :header="selectedGroup?.vaccineType ?? ''"
  :style="{ width: '30rem' }"
  :breakpoints="{ '641px': '92vw' }"
  @hide="selectedGroup = null"
>
  <VaccinationGroupPanel
    v-if="selectedGroup"
    :records="selectedGroup.records"
    :list-token="listToken"
    @view="openDetail"
    @edit="openManage"
    @delete="openDelete"
  />
</Drawer>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire useIsMobile + effectiveViewMode into VaccinationsTab.vue script</name>
  <files>src/components/projects/wallecx/VaccinationsTab.vue</files>
  <read_first>
    - src/components/projects/wallecx/VaccinationsTab.vue (current state — lines 1–176 cover the script changes; lines 122–126 and 170–176 are the key sections)
    - src/composables/useIsMobile.ts (created in plan 17-01; confirms the export signature)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-CONTEXT.md (D-04, D-09, D-11, D-12, D-13)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-PATTERNS.md (§VaccinationsTab.vue section — exact insertion points)
  </read_first>
  <behavior>
    - `useIsMobile` is imported via the `@/composables/useIsMobile` path alias.
    - A new `isMobile` ref is instantiated in the script setup alongside other refs (after `viewMode` is fine).
    - A new `effectiveViewMode` computed is added that returns `'list'` when `isMobile.value` is true and `viewMode.value` otherwise.
    - The existing `gridClass` computed reads from `effectiveViewMode.value` instead of `viewMode.value`.
    - The existing `viewMode` ref declaration on line 30 is unchanged.
    - The existing `watch(viewMode, ...)` on lines 170–176 is unchanged (sessionStorage is never written with `'list'` due to mobile forcing — D-12).
    - The `VIEW_MODE_STORAGE_KEY` constant on line 13 is unchanged.
    - The onMounted sessionStorage read on lines 134–143 is unchanged.
  </behavior>
  <action>
    Edit `src/components/projects/wallecx/VaccinationsTab.vue` script setup block (lines 1–313) as follows. The file uses double quotes and trailing semicolons — match that style (PATTERNS.md §TypeScript style notes).

    **Edit 1 — Add import (after line 11):** Insert a new import line after the existing `WallecxToolbar` import:

    Replace this block:
    ```ts
    import WallecxToolbar from './WallecxToolbar.vue';

    const VIEW_MODE_STORAGE_KEY = 'wallecx:view-mode';
    ```
    With:
    ```ts
    import WallecxToolbar from './WallecxToolbar.vue';
    import { useIsMobile } from "@/composables/useIsMobile";

    const VIEW_MODE_STORAGE_KEY = 'wallecx:view-mode';
    ```

    **Edit 2 — Add isMobile ref:** Just after the existing `const viewMode = ref<'grid' | 'list'>('grid');` line (line 30 currently), insert:
    ```ts
    const isMobile = useIsMobile();
    ```

    So the surrounding context becomes:
    ```ts
    const sortMode = ref<string>('type-asc');
    const viewMode = ref<'grid' | 'list'>('grid');
    const isMobile = useIsMobile();
    ```

    **Edit 3 — Add effectiveViewMode computed AND update gridClass to use it.** Replace the existing `gridClass` block (lines 122–126):
    ```ts
    const gridClass = computed(() =>
      viewMode.value === 'list'
        ? 'grid grid-cols-1 gap-4'
        : 'grid grid-cols-1 sm:grid-cols-2 gap-4',
    );
    ```

    With:
    ```ts
    // Phase 17 D-12: read-only layer above viewMode that forces 'list' on mobile.
    // viewMode ref and its sessionStorage watch are unchanged — desktop preference
    // is preserved across resizes (D-13).
    const effectiveViewMode = computed(() => isMobile.value ? 'list' : viewMode.value);

    const gridClass = computed(() =>
      effectiveViewMode.value === 'list'
        ? 'grid grid-cols-1 gap-4'
        : 'grid grid-cols-1 sm:grid-cols-2 gap-4',
    );
    ```

    **DO NOT touch:**
    - The `viewMode` ref declaration.
    - The `onMounted` sessionStorage read block (lines 134–143).
    - The `watch(viewMode, ...)` block (lines 170–176).
    - The `VIEW_MODE_STORAGE_KEY` constant.
    - Any unrelated function (`openDetail`, `openManage`, `openGroupPanel`, `exportJson`, `onCreated`, `onUpdated`, `openDelete`, `deleteRecord`, etc.).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `VaccinationsTab.vue` contains the exact string `import { useIsMobile } from "@/composables/useIsMobile";` in the script imports block.
    - `VaccinationsTab.vue` contains the exact string `const isMobile = useIsMobile();`.
    - `VaccinationsTab.vue` contains the exact string `const effectiveViewMode = computed(() => isMobile.value ? 'list' : viewMode.value);` (or a Prettier-formatted equivalent that still includes the substring `effectiveViewMode = computed`).
    - `VaccinationsTab.vue` contains the string `effectiveViewMode.value === 'list'` inside `gridClass`.
    - `VaccinationsTab.vue` still contains the exact string `const viewMode = ref<'grid' | 'list'>('grid');` (the underlying ref is unchanged).
    - `VaccinationsTab.vue` still contains the string `sessionStorage.setItem(VIEW_MODE_STORAGE_KEY, next);` inside a `watch(viewMode` block (the watch is unchanged).
    - `VaccinationsTab.vue` does NOT contain the line `viewMode.value === 'list'` anymore (it has been replaced by `effectiveViewMode.value === 'list'`); grep for `viewMode\.value === 'list'` returns ZERO matches.
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>
    Script setup uses `isMobile` ref and `effectiveViewMode` computed; `viewMode` and its watch are untouched. Type-check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make Drawer position reactive, add drag handle pill, hide toggle on mobile</name>
  <files>src/components/projects/wallecx/VaccinationsTab.vue</files>
  <read_first>
    - src/components/projects/wallecx/VaccinationsTab.vue (current template — lines 339–425 cover the toolbar binding and Drawer markup)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-CONTEXT.md (D-03, D-04, D-05, D-11, §Specific Ideas drag handle pill markup)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-PATTERNS.md (§VaccinationsTab.vue — Existing Drawer markup, Custom header slot, Existing WallecxToolbar showToggle binding)
  </read_first>
  <behavior>
    - The `<WallecxToolbar>` `:show-toggle` binding adds `&& !isMobile` so the grid/list toggle is hidden when `isMobile.value` is true (MOB-09 / D-11).
    - The `<Drawer>` for `VaccinationGroupPanel`:
      * Has its `position` prop made reactive: `:position="isMobile ? 'bottom' : 'right'"` (D-04).
      * The `:breakpoints="{ '641px': '92vw' }"` prop is REMOVED entirely (superseded by reactive position — D-04).
      * The `:header="selectedGroup?.vaccineType ?? ''"` prop is REMOVED and replaced with a custom `<template #header>` slot containing the drag handle pill (only when `isMobile`) and the title (always) — D-05.
    - The `:style="{ width: '30rem' }"` prop is KEPT (only takes effect when position is right; bottom-position Drawer fills width automatically).
    - The `v-model:visible="showGroupPanel"` binding and `@hide="selectedGroup = null"` handler are UNCHANGED.
    - The `<VaccinationGroupPanel>` child component inside the Drawer is UNCHANGED (props, events, v-if guard).
  </behavior>
  <action>
    Edit `src/components/projects/wallecx/VaccinationsTab.vue` template as follows. The file uses double-quoted HTML attributes — match that style.

    **Edit 1 — Hide toggle on mobile.** Replace the `<WallecxToolbar>` block at lines 339–345:
    ```html
        <WallecxToolbar
          v-model:search-query="searchQuery"
          v-model:sort-mode="sortMode"
          v-model:view-mode="viewMode"
          :sort-options="vaccinationSortOptions"
          :show-toggle="!isLoading && records.length > 0 && displayedGroups.length > 0"
        />
    ```
    With:
    ```html
        <WallecxToolbar
          v-model:search-query="searchQuery"
          v-model:sort-mode="sortMode"
          v-model:view-mode="viewMode"
          :sort-options="vaccinationSortOptions"
          :show-toggle="!isLoading && records.length > 0 && displayedGroups.length > 0 && !isMobile"
        />
    ```

    Note: `v-model:view-mode="viewMode"` is unchanged. The toolbar still binds to the underlying `viewMode` ref so the user's desktop selection still round-trips to sessionStorage. Only the toggle's *visibility* is suppressed on mobile (D-11). The card grid layout itself uses `effectiveViewMode` via `gridClass` (Task 1).

    **Edit 2 — Reactive Drawer position + drag handle pill header.** Replace the entire `<Drawer>` block at lines 409–425:
    ```html
        <!-- Drawer for group detail panel (GROUP-06, D-01: slides from right) -->
        <Drawer
          v-model:visible="showGroupPanel"
          position="right"
          :header="selectedGroup?.vaccineType ?? ''"
          :style="{ width: '30rem' }"
          :breakpoints="{ '641px': '92vw' }"
          @hide="selectedGroup = null"
        >
          <VaccinationGroupPanel
            v-if="selectedGroup"
            :records="selectedGroup.records"
            :list-token="listToken"
            @view="openDetail"
            @edit="openManage"
            @delete="openDelete"
          />
        </Drawer>
    ```
    With:
    ```html
        <!-- Drawer for group detail panel.
             Phase 17 D-04: position is reactive (`bottom` on mobile, `right` on desktop).
             Phase 17 D-05: drag handle pill renders in custom #header slot only on mobile.
             :breakpoints removed — superseded by reactive position switch.
             :style width '30rem' only takes effect in right-position (desktop). -->
        <Drawer
          v-model:visible="showGroupPanel"
          :position="isMobile ? 'bottom' : 'right'"
          :style="{ width: '30rem' }"
          @hide="selectedGroup = null"
        >
          <template #header>
            <div class="flex flex-col items-center w-full gap-1">
              <div
                v-if="isMobile"
                class="w-8 h-1 rounded-full bg-surface-300 dark:bg-surface-600"
                aria-hidden="true"
              ></div>
              <span class="font-semibold">{{ selectedGroup?.vaccineType ?? '' }}</span>
            </div>
          </template>
          <VaccinationGroupPanel
            v-if="selectedGroup"
            :records="selectedGroup.records"
            :list-token="listToken"
            @view="openDetail"
            @edit="openManage"
            @delete="openDelete"
          />
        </Drawer>
    ```

    Key removals (must be gone after this edit):
    - The static `position="right"` attribute (replaced with reactive `:position`).
    - The `:header="selectedGroup?.vaccineType ?? ''"` prop (replaced by `#header` slot — required because the prop and the slot are mutually exclusive in PrimeVue Drawer).
    - The `:breakpoints="{ '641px': '92vw' }"` prop (D-04: superseded).

    **Anti-regression checks for this task:**
    - The `<Dialog>` block for `VaccinationDetail` at lines 436–449 is UNCHANGED.
    - The `<ManageVaccination>` block at lines 428–433 is UNCHANGED.
    - `WallecxToolbar.vue` itself is NOT modified (D-15 — the toolbar's `showToggle` prop is the control point; the toolbar component code does not change).
    - The `<style scoped></style>` block at line 453 stays empty — the 85dvh content cap is in `wallecx-overrides.css` (added by plan 17-01), not here.
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - `VaccinationsTab.vue` contains the exact string `:show-toggle="!isLoading && records.length > 0 && displayedGroups.length > 0 && !isMobile"`.
    - `VaccinationsTab.vue` contains the exact string `:position="isMobile ? 'bottom' : 'right'"`.
    - `VaccinationsTab.vue` contains the strings `<template #header>` AND `w-8 h-1 rounded-full bg-surface-300 dark:bg-surface-600` AND `aria-hidden="true"` AND `v-if="isMobile"` within ~12 lines of each other (the new `#header` slot block).
    - `VaccinationsTab.vue` contains the string `{{ selectedGroup?.vaccineType ?? '' }}` inside a `<span class="font-semibold">`.
    - `VaccinationsTab.vue` does NOT contain the string `position="right"` (the static prop is replaced by reactive binding).
    - `VaccinationsTab.vue` does NOT contain the string `:header="selectedGroup?.vaccineType ?? ''"` (replaced by #header slot).
    - `VaccinationsTab.vue` does NOT contain the string `:breakpoints="{ '641px': '92vw' }"` (D-04: removed).
    - `VaccinationsTab.vue` still contains the string `@hide="selectedGroup = null"` (handler unchanged).
    - `VaccinationsTab.vue` still contains the unchanged `<Dialog v-model:visible="showDetail"` block for `VaccinationDetail` and the unchanged `<ManageVaccination v-model:visible="showManage"` block.
    - `npm run type-check` exits 0.
    - `npm run build-only` exits 0.
    - `src/components/projects/wallecx/WallecxToolbar.vue` is NOT in `git status` modified files for this plan (anti-regression: D-15).
  </acceptance_criteria>
  <done>
    The vaccination Drawer slides from bottom on mobile with a drag handle pill, slides from right on desktop with title only. The toggle is hidden on mobile. WallecxToolbar.vue is not modified.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run type-check` — exits 0.
2. Run `npm run build-only` — exits 0.
3. Run `npm run lint` — exits 0 (no new lint errors).
4. Grep verification:
   - `import { useIsMobile } from "@/composables/useIsMobile";` present in VaccinationsTab.vue.
   - `const isMobile = useIsMobile();` present.
   - `effectiveViewMode = computed` present.
   - `:position="isMobile ? 'bottom' : 'right'"` present.
   - `&& !isMobile` present in the WallecxToolbar `:show-toggle` binding.
   - `viewMode.value === 'list'` NOT present anywhere (replaced by `effectiveViewMode.value === 'list'`).
   - `:breakpoints="{ '641px': '92vw' }"` NOT present anywhere in this file (it was only on the Drawer).
   - `position="right"` (with static double-quoted attribute) NOT present.
5. Anti-regression: `git diff` shows only `VaccinationsTab.vue` modified. `WallecxToolbar.vue`, `VaccinationGroupPanel.vue`, `MembershipsTab.vue`, and `MembershipDetail.vue` are untouched by this plan.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 17 Success Criteria:
- SC-1: ✅ On phones, tapping a vaccination group card opens a bottom-sliding Drawer (no right-side drawer).
- SC-3: ✅ Bottom sheet dismissible via backdrop tap or PrimeVue's built-in close button (preserved by default Drawer behavior; no custom close logic needed).
- SC-4 (vaccination side): ✅ On desktop, vaccination group detail opens as a right-side drawer — identical to pre-phase behavior.
- SC-5: ✅ View toggle hidden on mobile (`&& !isMobile`); vaccination tab renders list layout (`grid grid-cols-1 gap-4`) regardless of sessionStorage value (via `effectiveViewMode`); on resize to desktop, toggle reappears and stored preference re-applies.
</success_criteria>

<output>
After completion, create `.planning/phases/17-mobile-bottom-sheets-view-toggle/17-02-SUMMARY.md` per the SUMMARY template, recording:
- Files modified (`VaccinationsTab.vue`)
- Decisions implemented: D-04 (reactive position), D-05 (mobile-only pill), D-11 (toggle hidden on mobile), D-12 (effectiveViewMode layer), D-13 (no sessionStorage reset on resize)
- Decisions preserved (anti-regression): D-15 (WallecxToolbar untouched), viewMode ref + sessionStorage watch untouched (D-12)
- Removed: `position="right"`, `:header="selectedGroup?.vaccineType ?? ''"`, `:breakpoints="{ '641px': '92vw' }"`
- Pitfall avoided: keeping `v-model:view-mode="viewMode"` binding (not switching to effectiveViewMode) so user's desktop selection still round-trips to sessionStorage when they're on desktop
</output>
