---
phase: 17-mobile-bottom-sheets-view-toggle
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/components/projects/wallecx/MembershipsTab.vue
autonomous: true
requirements:
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "On phones (< 640px), tapping a membership card opens a Drawer that slides up from the bottom of the screen showing the card details, barcode, and edit/delete actions; no centered modal dialog appears."
    - "On desktop (≥ 640px), tapping a membership card opens the SAME centered Dialog as before — unchanged in width, modality, and behavior."
    - "On phones, the bottom Drawer displays a centered drag handle pill (~32×4px rounded grey bar) above the title 'Membership Card'."
    - "The bottom Drawer is dismissible by tapping the backdrop OR the Drawer's built-in close button."
    - "When the bottom Drawer is dismissed, the `@hide` handler runs identically to the Dialog's — clearing `selectedRecord` and `fileToken`."
    - "Edit and Delete actions inside MembershipDetail behave identically whether the wrapper is the Dialog (desktop) or the Drawer (mobile) — they close the detail view first, then open the manage/confirm dialog."
  artifacts:
    - path: "src/components/projects/wallecx/MembershipsTab.vue"
      provides: "v-if/v-else Dialog/Drawer split around MembershipDetail driven by isMobile"
      contains: "useIsMobile"
  key_links:
    - from: "src/components/projects/wallecx/MembershipsTab.vue"
      to: "src/composables/useIsMobile.ts"
      via: "import { useIsMobile } from '@/composables/useIsMobile'"
      pattern: "from\\s+['\\\"]@/composables/useIsMobile['\\\"]"
    - from: "MembershipsTab.vue <Dialog>"
      to: "isMobile ref (negated)"
      via: "v-if=\"!isMobile\""
      pattern: "v-if=\"!isMobile\""
    - from: "MembershipsTab.vue <Drawer>"
      to: "isMobile ref"
      via: "v-else position=\"bottom\""
      pattern: "v-else[\\s\\S]*?position=\"bottom\""
---

<objective>
Add a `v-if !isMobile` Dialog / `v-else` Drawer-bottom split around `<MembershipDetail>` in `MembershipsTab.vue`. The Dialog branch is the existing markup (unchanged) and serves desktop. The new Drawer branch serves mobile with a drag handle pill in a custom `#header` slot. Both wrappers share the same `v-model:visible`, the same `@hide` handler, the same child `<MembershipDetail>` props, and the same edit/delete event wiring.

Purpose: Implement CONTEXT decisions D-06, D-07, D-08 in a single file change. `MembershipDetail.vue` is NOT modified (only its wrapper changes). MOB-09 does NOT apply here — `MembershipsTab.vue` already passes `:show-toggle="false"` (D-14), so no toolbar change is needed.

Output: Modified `src/components/projects/wallecx/MembershipsTab.vue` covering UX-02, UX-03 (membership side), and UX-04 (membership side).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-mobile-bottom-sheets-view-toggle/17-CONTEXT.md
@.planning/phases/17-mobile-bottom-sheets-view-toggle/17-PATTERNS.md
@.planning/phases/17-mobile-bottom-sheets-view-toggle/17-01-SUMMARY.md
@./CLAUDE.md
@src/components/projects/wallecx/MembershipsTab.vue

<interfaces>
<!-- Composable created in plan 17-01 (Wave 1) -->
From src/composables/useIsMobile.ts:
```ts
export function useIsMobile(breakpoint = 639): Ref<boolean>
```

<!-- Existing MembershipsTab.vue state (key fragments to modify in-place) -->
Imports (lines 1–10) — single quotes, no semicolons:
```ts
import { ref, onMounted, computed, watch } from 'vue'
import { toast } from 'vue-sonner'
import { pb } from '@/lib/pocketbase'
import type { Memberships } from '@/types/wallecx/memberships/types'
import MembershipCard from './MembershipCard.vue'
import MembershipDetail from './MembershipDetail.vue'
import { useConfirm } from 'primevue/useconfirm'   // explicit — NOT auto-resolved by PrimeVueResolver
import ManageMembership from './ManageMembership.vue'
import WallecxToolbar from './WallecxToolbar.vue'
```

State refs (lines 12–19): includes `selectedRecord`, `showDetail`, `fileToken`.

Existing Dialog markup (lines 253–269) — the desktop branch:
```html
<Dialog
  v-model:visible="showDetail"
  modal
  header="Membership Card"
  :style="{ width: '40rem' }"
  :breakpoints="{ '960px': '75vw', '641px': '92vw' }"
  @hide="selectedRecord = null; fileToken = ''"
>
  <MembershipDetail
    v-if="selectedRecord"
    :record="selectedRecord"
    :token="fileToken"
    @edit="openEdit(selectedRecord!)"
    @delete="openDelete(selectedRecord!)"
  />
</Dialog>
```

Existing WallecxToolbar binding (lines 180–186) — DO NOT MODIFY (D-14):
```html
<WallecxToolbar
  v-model:search-query="searchQuery"
  v-model:sort-mode="sortMode"
  :view-mode="'grid'"
  :sort-options="membershipSortOptions"
  :show-toggle="false"
/>
```

openEdit (lines 122–126) — DO NOT MODIFY; already closes detail before opening manage:
```ts
function openEdit(record: Memberships): void {
  manageRecord.value = record
  showDetail.value = false      // close detail dialog first
  showManage.value = true       // then open manage dialog
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Import useIsMobile and add isMobile ref</name>
  <files>src/components/projects/wallecx/MembershipsTab.vue</files>
  <read_first>
    - src/components/projects/wallecx/MembershipsTab.vue (current state — lines 1–19)
    - src/composables/useIsMobile.ts (created in plan 17-01)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-CONTEXT.md (D-09)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-PATTERNS.md (§MembershipsTab.vue — Imports pattern, Insertion point for isMobile ref)
  </read_first>
  <behavior>
    - `useIsMobile` is imported via the `@/composables/useIsMobile` path alias using the file's single-quote / no-semicolon style.
    - A new `isMobile` ref is instantiated alongside other refs (after `confirm` on line 19 is the recommended slot).
    - No other state, computed, function, lifecycle hook, or template element changes in this task.
  </behavior>
  <action>
    Edit `src/components/projects/wallecx/MembershipsTab.vue`. Style: single quotes, NO trailing semicolons (match the file's existing style — PATTERNS.md §TypeScript style notes).

    **Edit 1 — Add import.** Insert a new import line after the existing `WallecxToolbar` import (currently line 10):

    Replace:
    ```ts
    import WallecxToolbar from './WallecxToolbar.vue'

    const records = ref<Memberships[]>([])
    ```
    With:
    ```ts
    import WallecxToolbar from './WallecxToolbar.vue'
    import { useIsMobile } from '@/composables/useIsMobile'

    const records = ref<Memberships[]>([])
    ```

    **Edit 2 — Add isMobile ref.** Insert `const isMobile = useIsMobile()` immediately after the existing `const confirm = useConfirm()` declaration (currently line 19).

    Replace:
    ```ts
    const confirm = useConfirm()

    const SORT_MODE_STORAGE_KEY = 'wallecx:memberships-sort-mode';
    ```
    With:
    ```ts
    const confirm = useConfirm()
    const isMobile = useIsMobile()

    const SORT_MODE_STORAGE_KEY = 'wallecx:memberships-sort-mode';
    ```

    **DO NOT touch** anything else in the script setup block in this task — `displayedMemberships`, `watch(sortMode, ...)`, `onMounted`, `openDetail`, `openEdit`, `openManage`, `openDelete`, `deleteCard`, `onCreated`, `onUpdated` are all unchanged.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `MembershipsTab.vue` contains the exact string `import { useIsMobile } from '@/composables/useIsMobile'` (single quotes, no semicolon).
    - `MembershipsTab.vue` contains the exact string `const isMobile = useIsMobile()` (single quotes, no semicolon).
    - `MembershipsTab.vue` still contains the unchanged `const confirm = useConfirm()` line.
    - `MembershipsTab.vue` still contains the unchanged WallecxToolbar block with `:show-toggle="false"` (D-14 — toolbar binding is untouched).
    - `npm run type-check` exits 0.
  </acceptance_criteria>
  <done>
    `isMobile` ref is available in MembershipsTab.vue. No other behavior changes yet.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Split MembershipDetail wrapper into v-if Dialog / v-else Drawer-bottom</name>
  <files>src/components/projects/wallecx/MembershipsTab.vue</files>
  <read_first>
    - src/components/projects/wallecx/MembershipsTab.vue (current template — lines 253–269 cover the Dialog wrapper)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-CONTEXT.md (D-06, D-07, D-08, §Specific Ideas: MembershipsTab v-if/v-else structure)
    - .planning/phases/17-mobile-bottom-sheets-view-toggle/17-PATTERNS.md (§MembershipsTab.vue — Existing Dialog markup, Action per D-06/D-07/D-08, full v-if/v-else snippet)
  </read_first>
  <behavior>
    - The existing `<Dialog>` wrapping `<MembershipDetail>` becomes the desktop-only branch via `v-if="!isMobile"`. Its props (modal, header, style, breakpoints), `v-model:visible`, `@hide` handler, and the `<MembershipDetail>` child block (with its `v-if="selectedRecord"` guard and `@edit` / `@delete` events) are UNCHANGED.
    - A new `<Drawer v-else>` is added as the mobile-only branch:
      * `v-model:visible="showDetail"` — same as Dialog (only one renders at a time, no double-binding risk per PATTERNS.md note).
      * `position="bottom"` — static value (the v-else branch only renders when isMobile is true, so the position is always "bottom" here).
      * `@hide="selectedRecord = null; fileToken = ''"` — identical to the Dialog's @hide (D-08).
      * `<template #header>` slot containing a centered drag handle pill (unconditional in this branch — the Drawer only renders on mobile) and the title text "Membership Card".
      * Inside the Drawer: the SAME `<MembershipDetail v-if="selectedRecord" :record="selectedRecord" :token="fileToken" @edit="openEdit(selectedRecord!)" @delete="openDelete(selectedRecord!)" />` as inside the Dialog. The `v-if="selectedRecord"` guard is preserved.
    - The Dialog's `:style="{ width: '40rem' }"` and `:breakpoints="{ '960px': '75vw', '641px': '92vw' }"` are KEPT on the Dialog branch only. The Drawer branch does NOT need width/breakpoints (bottom-position Drawer fills width automatically; height is capped at 85dvh by the `wallecx-overrides.css` rule from plan 17-01).
    - The `<ManageMembership>` block below (lines 272–278) is UNCHANGED.
    - The `<WallecxToolbar>` block above (lines 180–186) is UNCHANGED (D-14).
  </behavior>
  <action>
    Edit `src/components/projects/wallecx/MembershipsTab.vue` template.

    **Replace the entire existing Dialog block at lines 253–269:**
    ```html
        <!-- MembershipDetail Dialog -->
        <Dialog
          v-model:visible="showDetail"
          modal
          header="Membership Card"
          :style="{ width: '40rem' }"
          :breakpoints="{ '960px': '75vw', '641px': '92vw' }"
          @hide="selectedRecord = null; fileToken = ''"
        >
          <MembershipDetail
            v-if="selectedRecord"
            :record="selectedRecord"
            :token="fileToken"
            @edit="openEdit(selectedRecord!)"
            @delete="openDelete(selectedRecord!)"
          />
        </Dialog>
    ```

    **With:**
    ```html
        <!-- MembershipDetail wrapper.
             Phase 17 D-06: desktop renders Dialog, mobile renders bottom Drawer.
             Phase 17 D-07: 85dvh content cap is applied via `wallecx-overrides.css`
                            (`.p-drawer-bottom .p-drawer-content` rule).
             Phase 17 D-08: identical @hide handler on both wrappers; openEdit/openDelete
                            inside MembershipDetail already close the detail before opening
                            the manage/confirm dialog (see openEdit fn). -->
        <!-- Desktop: centered Dialog -->
        <Dialog
          v-if="!isMobile"
          v-model:visible="showDetail"
          modal
          header="Membership Card"
          :style="{ width: '40rem' }"
          :breakpoints="{ '960px': '75vw', '641px': '92vw' }"
          @hide="selectedRecord = null; fileToken = ''"
        >
          <MembershipDetail
            v-if="selectedRecord"
            :record="selectedRecord"
            :token="fileToken"
            @edit="openEdit(selectedRecord!)"
            @delete="openDelete(selectedRecord!)"
          />
        </Dialog>

        <!-- Mobile: bottom Drawer -->
        <Drawer
          v-else
          v-model:visible="showDetail"
          position="bottom"
          @hide="selectedRecord = null; fileToken = ''"
        >
          <template #header>
            <div class="flex flex-col items-center w-full gap-1">
              <div
                class="w-8 h-1 rounded-full bg-surface-300 dark:bg-surface-600"
                aria-hidden="true"
              ></div>
              <span class="font-semibold">Membership Card</span>
            </div>
          </template>
          <MembershipDetail
            v-if="selectedRecord"
            :record="selectedRecord"
            :token="fileToken"
            @edit="openEdit(selectedRecord!)"
            @delete="openDelete(selectedRecord!)"
          />
        </Drawer>
    ```

    Notes:
    - The Dialog branch retains the `<Dialog v-if="!isMobile" ...>` attribute as the FIRST attribute on the Dialog (Vue requires `v-if`/`v-else` to be on sibling elements with no intervening non-element nodes; placing them as the first conditional makes the v-else linkage unambiguous).
    - The Drawer's pill is UNCONDITIONAL (no `v-if="isMobile"`) — unlike the Vaccinations Drawer, this Drawer only renders in the `v-else` branch, which is already gated on mobile (PATTERNS.md §MembershipsTab.vue — "Pill on Drawer header has no v-if here").
    - `<Drawer>` is auto-imported by PrimeVueResolver (same mechanism as `<Dialog>`); no explicit import needed.
    - The `<MembershipDetail>` child component itself is NOT modified — only its wrapper changes (CONTEXT.md §Files Untouched).

    **DO NOT** modify:
    - The `<WallecxToolbar>` block at lines 180–186 (D-14).
    - The `<ManageMembership>` block at lines 272–278.
    - `MembershipDetail.vue` (CONTEXT.md §Files Untouched).
    - Any function in `<script setup>` (`openDetail`, `openEdit`, `openManage`, `openDelete`, `deleteCard`, `onCreated`, `onUpdated` — all unchanged).
  </action>
  <verify>
    <automated>npm run type-check &amp;&amp; npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - `MembershipsTab.vue` contains the exact string `<Dialog` immediately followed (within ~3 lines) by `v-if="!isMobile"` (the Dialog is now gated on `!isMobile`).
    - `MembershipsTab.vue` contains the exact string `<Drawer` followed within ~3 lines by both `v-else` AND `position="bottom"`.
    - `MembershipsTab.vue` contains TWO occurrences of the exact string `@hide="selectedRecord = null; fileToken = ''"` (one on the Dialog, one on the Drawer — D-08).
    - `MembershipsTab.vue` contains TWO occurrences of the exact string `<MembershipDetail` (one inside each wrapper) — verified by grep count = 2.
    - `MembershipsTab.vue` contains the strings `w-8 h-1 rounded-full bg-surface-300 dark:bg-surface-600` AND `aria-hidden="true"` AND `<span class="font-semibold">Membership Card</span>` within the `#header` slot of the Drawer.
    - `MembershipsTab.vue` still contains the unchanged WallecxToolbar block with `:show-toggle="false"` (anti-regression: D-14).
    - `MembershipsTab.vue` still contains the unchanged `<ManageMembership v-model:visible="showManage"` block.
    - `src/components/projects/wallecx/MembershipDetail.vue` is NOT in `git status` modified files for this plan (anti-regression).
    - `src/components/projects/wallecx/WallecxToolbar.vue` is NOT in `git status` modified files for this plan (anti-regression: D-15).
    - `npm run type-check` exits 0.
    - `npm run build-only` exits 0.
  </acceptance_criteria>
  <done>
    The MembershipDetail wrapper now splits into Dialog (desktop) and Drawer-bottom (mobile) branches with identical event wiring. MembershipDetail.vue, WallecxToolbar.vue, and ManageMembership are not modified.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run type-check` — exits 0.
2. Run `npm run build-only` — exits 0.
3. Run `npm run lint` — exits 0 (no new lint errors).
4. Grep verification on `MembershipsTab.vue`:
   - `import { useIsMobile } from '@/composables/useIsMobile'` present.
   - `const isMobile = useIsMobile()` present.
   - `<Dialog` with `v-if="!isMobile"` present.
   - `<Drawer` with `v-else` and `position="bottom"` present.
   - `<MembershipDetail` appears EXACTLY twice (one per branch).
   - `@hide="selectedRecord = null; fileToken = ''"` appears EXACTLY twice.
   - `:show-toggle="false"` still present on `<WallecxToolbar>` (D-14 preserved).
5. Anti-regression: `git diff` shows only `MembershipsTab.vue` modified by this plan. `MembershipDetail.vue`, `WallecxToolbar.vue`, `VaccinationsTab.vue` (modified by 17-02 — separate plan), `VaccinationGroupPanel.vue`, `ManageMembership.vue`, `ManageVaccination.vue` are untouched by this plan.
</verification>

<success_criteria>
Maps to ROADMAP §Phase 17 Success Criteria:
- SC-2: ✅ On phones, tapping a membership card opens a bottom-sliding Drawer (no centered modal dialog).
- SC-3: ✅ Bottom sheet dismissible via backdrop tap or PrimeVue's built-in close button (preserved by default Drawer behavior).
- SC-4 (membership side): ✅ On desktop, membership card detail opens as a centered Dialog — identical to pre-phase behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/17-mobile-bottom-sheets-view-toggle/17-03-SUMMARY.md` per the SUMMARY template, recording:
- Files modified (`MembershipsTab.vue`)
- Decisions implemented: D-06 (v-if Dialog / v-else Drawer split), D-07 (85dvh cap via shared CSS), D-08 (identical @hide handler on both branches)
- Decisions preserved (anti-regression): D-14 (no toolbar change in MembershipsTab — toggle was never shown here), D-15 (WallecxToolbar.vue untouched), MembershipDetail.vue untouched
- Pitfall avoided: pill is unconditional in the membership Drawer (no `v-if="isMobile"` needed) because the entire Drawer is the v-else mobile branch
</output>
