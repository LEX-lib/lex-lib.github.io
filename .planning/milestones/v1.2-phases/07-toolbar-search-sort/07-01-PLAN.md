---
phase: 07-toolbar-search-sort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/wallecx/WallecxToolbar.vue
autonomous: true
requirements:
  - SEARCH-01
  - SORT-01

must_haves:
  truths:
    - "WallecxToolbar.vue exists as a new file at the expected path"
    - "Search input renders with a left pi-search icon and accepts text"
    - "When searchQuery is non-empty, a pi-times clear icon is rendered inside the IconField"
    - "Sort Select renders with 4 options (Type A-Z, Type Z-A, Name A-Z, Name Z-A)"
    - "Component emits update:searchQuery and update:sortMode upward — no local state held"
  artifacts:
    - path: "src/components/projects/wallecx/WallecxToolbar.vue"
      provides: "Toolbar presentational component with search input and sort select"
      exports: ["defineProps<{ searchQuery: string; sortMode: string }>", "defineEmits<{ 'update:searchQuery': ...; 'update:sortMode': ... }>"]
  key_links:
    - from: "WallecxApp.vue"
      to: "WallecxToolbar.vue"
      via: "v-model:search-query and v-model:sort-mode bindings (Plan 2)"
      pattern: "v-model:search-query"
---

<objective>
Create `src/components/projects/wallecx/WallecxToolbar.vue` — a purely presentational component that renders the search input and sort control in a single toolbar row.

Purpose: Provides the UI surface for SEARCH-01 (search input with icon + inline clear) and SORT-01 (4-option sort Select). All state lives in the parent (WallecxApp.vue). This component only emits upward via v-model.

Output: `WallecxToolbar.vue` — complete, type-correct, ready for Plan 2 to import.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WallecxToolbar.vue</name>
  <files>src/components/projects/wallecx/WallecxToolbar.vue</files>

  <read_first>
    - src/components/projects/wallecx/VaccinationGroupCard.vue — defineProps/defineEmits typed pattern to follow (lines 6–15); no-PrimeVue-manual-import pattern to follow
    - .planning/phases/07-toolbar-search-sort/07-UI-SPEC.md — §1 "WallecxToolbar.vue — New Component" is the authoritative template; copy verbatim
  </read_first>

  <action>
Create the file `src/components/projects/wallecx/WallecxToolbar.vue` with the EXACT content below. Do not alter Unicode characters (en-dash `–` U+2013 in sort labels, ellipsis `…` U+2026 in placeholder). Do NOT add any manual imports for PrimeVue components (IconField, InputIcon, InputText, Select, Button) — PrimeVueResolver auto-imports them.

```vue
<script setup lang="ts">
const props = defineProps<{
  searchQuery: string;
  sortMode: string;
}>();

const emit = defineEmits<{
  'update:searchQuery': [value: string];
  'update:sortMode': [value: string];
}>();

const sortOptions = [
  { value: 'type-asc',  label: 'Type A–Z' },
  { value: 'type-desc', label: 'Type Z–A' },
  { value: 'name-asc',  label: 'Name A–Z' },
  { value: 'name-desc', label: 'Name Z–A' },
];
</script>

<template>
  <div class="flex items-center gap-2 mb-4">
    <IconField class="flex-1">
      <InputIcon class="pi pi-search" />
      <InputText
        :value="searchQuery"
        placeholder="Search by name or type…"
        class="w-full"
        @input="emit('update:searchQuery', ($event.target as HTMLInputElement).value)"
      />
      <InputIcon
        v-if="searchQuery"
        class="pi pi-times cursor-pointer"
        @click="emit('update:searchQuery', '')"
      />
    </IconField>
    <Select
      :model-value="sortMode"
      :options="sortOptions"
      option-label="label"
      option-value="value"
      class="w-36"
      @update:model-value="emit('update:sortMode', $event)"
    />
  </div>
</template>
```

Key implementation notes:
- `defineProps` and `defineEmits` are compiler macros — no Vue import needed.
- `:value` + `@input` on InputText (NOT `v-model`) — the ref lives in the parent. Using `v-model` here would attempt to mutate a prop (Vue 3 runtime warning).
- `InputIcon v-if="searchQuery"` on the right side is the inline × clear button (D-06). Only rendered when searchQuery is non-empty.
- `Select` uses `:model-value` + `@update:model-value` (NOT `v-model`) for the same reason — emits upward, does not hold local state.
- No `<style>` block needed — all styling via Tailwind and PrimeVue Aura.
  </action>

  <verify>
    Run: `npm run type-check`
    Expected: exits 0 with no errors related to WallecxToolbar.vue.

    Also confirm file contents:
    - `WallecxToolbar.vue` contains `defineProps<{` with `searchQuery: string` and `sortMode: string`
    - `WallecxToolbar.vue` contains `defineEmits<{` with `'update:searchQuery'` and `'update:sortMode'`
    - `WallecxToolbar.vue` contains `sortOptions` array with exactly 4 entries (`type-asc`, `type-desc`, `name-asc`, `name-desc`)
    - `WallecxToolbar.vue` contains `<IconField` and `<InputIcon` and `<InputText` and `<Select` (no manual script imports for these)
    - `WallecxToolbar.vue` does NOT contain `import { IconField }` or `import { Select }` or `import { InputText }` in the script block
    - `WallecxToolbar.vue` does NOT contain `v-model` on `InputText` (must use `:value` + `@input`)
    - `WallecxToolbar.vue` does NOT contain `v-html`
  </verify>

  <acceptance_criteria>
    - File exists at `src/components/projects/wallecx/WallecxToolbar.vue`
    - File contains: `defineProps<{` with `searchQuery: string;` and `sortMode: string;`
    - File contains: `defineEmits<{` with `'update:searchQuery': [value: string];` and `'update:sortMode': [value: string];`
    - File contains: `{ value: 'type-asc'` and `{ value: 'type-desc'` and `{ value: 'name-asc'` and `{ value: 'name-desc'`
    - File contains: `<IconField class="flex-1">` and `<InputIcon class="pi pi-search"` and `<InputText` and `<Select`
    - File contains: `:value="searchQuery"` on InputText (not `v-model="searchQuery"`)
    - File contains: `v-if="searchQuery"` on the second `<InputIcon` (the × clear button)
    - File contains: `@input="emit('update:searchQuery', ($event.target as HTMLInputElement).value)"`
    - File contains: `@update:model-value="emit('update:sortMode', $event)"`
    - File does NOT contain `import { IconField` or `import { InputIcon` or `import { Select` or `import { InputText` in script
    - File does NOT contain `v-html`
    - `npm run type-check` exits 0
  </acceptance_criteria>

  <done>WallecxToolbar.vue exists, is fully type-correct, emits upward for both searchQuery and sortMode, and has no manual PrimeVue component imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → searchQuery | User-typed text enters the Vue reactivity system via emit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Injection | WallecxToolbar.vue — searchQuery echoed in no-results message | accept | searchQuery is only ever rendered via `{{ searchQuery }}` mustache interpolation in WallecxApp.vue (Plan 2). Vue's default text interpolation escapes HTML entities. No v-html is used anywhere. The toolbar component itself does not render the query back to the user. |
| T-07-02 | Information Disclosure | WallecxToolbar.vue — no PocketBase calls | accept | Component is purely presentational. No auth tokens, no PocketBase calls, no network requests. Zero disclosure surface. |
</threat_model>

<verification>
After Plan 1 execution, the following must hold:
- `src/components/projects/wallecx/WallecxToolbar.vue` exists
- `npm run type-check` passes (0 errors)
- No manual PrimeVue component imports exist in WallecxToolbar.vue script
- The component exposes the correct v-model API: `update:searchQuery` and `update:sortMode` emits
</verification>

<success_criteria>
- WallecxToolbar.vue is created and type-checks cleanly
- Component is ready to be imported and used by Plan 2 (WallecxApp.vue wiring)
- All constraints from 07-UI-SPEC.md §Constraint Checklist are satisfied for this component
</success_criteria>

<output>
After completion, create `.planning/phases/07-toolbar-search-sort/07-01-SUMMARY.md`
</output>
