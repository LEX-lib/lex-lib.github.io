---
phase: 21-mini-app-dark-mode-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/api-playground/ApiPlaygroundApp.vue
autonomous: true
requirements:
  - THEME-12
tags:
  - dark-mode
  - api-playground
  - vue3

must_haves:
  truths:
    - "With dark mode active, API Playground (`/projects/api-playground`) renders with dark backgrounds and legible text across request panel, response display, headers/params/body tables, tabs, toolbar, and history list"
    - "The JSON-highlighted response body (`<pre class='json-pre'>` + `.json-key`/`.json-string`/`.json-number`/`.json-boolean`/`.json-null`) remains readable in dark mode — the existing navy-dark `json-pre` palette already contrasts cleanly on a dark page, so no syntax-color change is required (D-15 audit confirms; if any class loses contrast against the dark page background, add a `.my-app-dark`-scoped override in the same `<style>` block)"
    - "Every hardcoded light hex (`#fff`, `#ffffff`, `#f0f0f0`, `#fafafa`, `#e0e0e0`, etc.) used as a chrome surface in the `<style>` block has a paired `.my-app-dark` override (or has been converted to a CSS variable that switches via `.my-app-dark`)"
    - "No new dependency is added to `package.json` (executor INVESTIGATED first per D-15 and confirmed the JSON highlighter is custom-inline, not a third-party library)"
    - "The consolidated phase UAT doc `21-HUMAN-UAT.md` exists with checklist sections for each of the 4 mini-apps (LexTrack, Larga, MonitoX, API Playground), per D-21"
    - "Anti-regression files (main.ts, useTheme.ts, useIsMobile.ts, base.css, wallecx-overrides.css, wallecx/**, views/**, lextrack/**, larga/**, gift-exchange/**) are NOT modified"
    - "`npm run type-check` exits 0"
    - "`npm run build-only` exits 0"
  artifacts:
    - path: "src/components/projects/api-playground/ApiPlaygroundApp.vue"
      provides: "API Playground with dark-mode chrome overrides for hardcoded light hex / inline-style backgrounds; JSON highlighter remains readable (already dark-themed by design)"
      contains: "my-app-dark"
    - path: ".planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md"
      provides: "Single consolidated phase-level UAT doc covering all 4 mini-apps (per D-21)"
      contains: "THEME-09"
    - path: ".planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md"
      provides: "Plan SUMMARY documenting highlighter investigation outcome, overrides applied, UAT doc generation"
      contains: "THEME-12"
  key_links:
    - from: "ApiPlaygroundApp.vue chrome (toolbar, panels, tables, tabs)"
      to: ".my-app-dark on <html>"
      via: "`.my-app-dark` selector rules added to the file's `<style>` block targeting existing light-mode classes"
      pattern: "\\.my-app-dark"
    - from: "ApiPlaygroundApp.vue JSON viewer (`.json-pre`, `.json-key`, etc.)"
      to: "(intentionally dark in both themes — D-15 investigation outcome)"
      via: "existing `background: #001122` + `color: #d0dbe8` palette"
      pattern: "json-pre"
---

<objective>
Investigate API Playground's structure (D-15), then add `.my-app-dark` overrides in the file's `<style>` block to make the chrome around the response viewer (toolbar, tabs, request/response panels, headers/params/body tables, history list, status pills) legible in dark mode. The JSON response viewer is ALREADY navy-dark in both themes by design — no syntax-color change is needed unless audit reveals contrast loss against the dark page background.

Finally, generate the consolidated phase-level UAT doc `21-HUMAN-UAT.md` covering all 4 mini-apps (D-21).

Purpose: Closes THEME-12 (API Playground dark mode) and contributes to ROADMAP §Phase 21 SC 4 ("API Playground renders correctly in dark mode — request panel, response display, headers tables; syntax-highlighted body should remain readable"). Also produces the cross-plan deliverable required by D-21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md
@.planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md
@.planning/phases/18-dark-mode-fixes/18-CONTEXT.md
@.planning/phases/20-site-shell-non-app-pages/20-CONTEXT.md

<interfaces>
<!-- Planner-confirmed file structure (ApiPlaygroundApp.vue, ~2048 lines): -->
<!-- - imports: axios, vue, dompurify, pocketbase, auth store, vue-sonner — NO Prism / Highlight.js / Shiki / hljs / Monaco / CodeMirror -->
<!-- - JSON highlighter: CUSTOM inline `highlightedJson` computed (lines ~135-159) that wraps regex matches in <span class="json-key|json-string|json-number|json-boolean|json-null"> — sanitized with DOMPurify, rendered via v-html in `<pre class="json-pre">` -->
<!-- - Existing JSON viewer palette (lines ~1989-2017) is INTENTIONALLY DARK: bg #001122, color #d0dbe8, key #7eb8f7, string #a8d4ff, number #e89820, boolean #ff8c69, null #7a96b0 — works on both light and dark page backgrounds because the `<pre>` provides its own contrasting surface -->
<!-- - 184 color references in the file: 151 `background:` / `background-color:` / `color:` / `border-color:` in CSS rules, 4 dynamic inline `style="{ color: ... }"` for method-color tokens (theme-independent) -->
<!-- - The file has 0 light-only Tailwind grays (`bg-white`, `text-gray-*`, etc.) — work is exclusively in the `<style>` block -->
<!-- - The file does NOT currently use `:global(.my-app-dark)` or `.my-app-dark` selectors (0 occurrences) -->

So the D-15 INVESTIGATION outcome is pre-confirmed: NO third-party syntax highlighter exists; the JSON highlighter is custom inline. The executor must still re-confirm by reading lines ~135-159 and the `<style>` block themselves, but should NOT install a Prism/Shiki/hljs library.

Approach:
1. Read the file's `<style>` and `<style scoped>` block(s) end-to-end
2. Identify rules whose declarations contain LIGHT hex values (`#fff`, `#ffffff`, `#fafafa`, `#f0f0f0`, `#eee`, `#e0e0e0`, `rgba(255,...)`, `white`) used as chrome surface backgrounds/borders/text
3. For each, add a paired `.my-app-dark`-prefixed rule (in the same `<style>` block) substituting an appropriate dark-mode value. Prefer `var(--color-surface-card)`, `var(--color-surface-page)`, `var(--color-typo-body)`, `var(--color-surface-divider)` from the dark-aware @theme tokens; fall back to navy hexes consistent with the existing `#001122` JSON viewer palette where a token doesn't apply.
4. Do NOT touch `.json-pre`, `.json-key`, `.json-string`, `.json-number`, `.json-boolean`, `.json-null` rules UNLESS the audit reveals one of them loses contrast against the (now dark) page background. Justify any change in the SUMMARY.
5. Do NOT change the dynamic inline `style="{ color: methodColor }"` bindings — those are theme-independent semantic method colors (GET/POST/PUT/DELETE/etc.).

`:deep()` usage in the existing file (lines ~2003-2017): the file uses `:deep(.json-key)` etc. because the `v-html` content is rendered outside scoped CSS's reach. Use the same `:deep()` form when adding dark overrides for the same classes IF needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate ApiPlaygroundApp.vue structure (D-15 confirmation)</name>
  <files></files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (D-14, D-15, D-16, D-18)
    - src/components/projects/api-playground/ApiPlaygroundApp.vue (entire file — 2048 lines; read in 2-3 ranges if needed to cover template + script + styles)
  </read_first>
  <action>
This is a READ-ONLY investigation task. No file modifications.

1. Confirm the import list at the top of the file (~lines 1-20) contains NO third-party syntax highlighter (Prism / Highlight.js / hljs / Shiki / Monaco / CodeMirror).
2. Confirm the JSON highlighter implementation: read the `highlightedJson` computed (~lines 135-159). Verify it's a custom inline regex → `<span class="json-*">` wrapper using DOMPurify for sanitization, NOT a library call.
3. Read the file's `<style>` block (starts around line 1500-1700; ends at line 2048) in full. Enumerate:
   - Class selectors whose declarations contain light hex backgrounds / borders / text (`#fff`, `#ffffff`, `#fafafa`, `#f5f5f5`, `#f0f0f0`, `#eee`, `#e0e0e0`, `rgba(255,...)`, `white`)
   - Class selectors that already use dark hex values (these are likely intentional, e.g. `.json-pre { background: #001122 }`)
   - Whether any `.my-app-dark` rules already exist in the file (planner confirms 0)
4. Compile a list of the candidate rules for dark-mode pairing into the SUMMARY draft. This list seeds Task 2's diff.
5. Decide: if a third-party highlighter IS unexpectedly present, the planner predicted wrong — pivot to swap its theme stylesheet using Option A or B from CONTEXT.md §Specific Ideas D-15 and document the pivot. If NO highlighter is present (expected outcome), proceed to Task 2 with the chrome-only override plan.

Document findings as a short note appended to the task's output. This note becomes section 1 of the SUMMARY in Task 3.
  </action>
  <verify>
    <automated>grep -E "import .* from .*(prism|highlight|shiki|hljs|monaco|codemirror)" src/components/projects/api-playground/ApiPlaygroundApp.vue ; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep` for `prism|highlight|shiki|hljs|monaco|codemirror` in the file's import lines returns ZERO matches (confirms custom inline highlighter)
    - Executor produces a written list of candidate dark-mode override rules (selectors + current light values) — captured in scratch notes / SUMMARY draft, NOT yet committed to a file
    - No file modification this task — `git diff --name-only` empty
  </acceptance_criteria>
  <done>Investigation complete; custom-inline highlighter confirmed (no library to swap); candidate override list compiled for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Apply mechanical sweep — add `.my-app-dark` overrides for chrome surfaces</name>
  <files>src/components/projects/api-playground/ApiPlaygroundApp.vue</files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (D-14, D-16, D-17)
    - src/components/projects/api-playground/ApiPlaygroundApp.vue (full `<style>` block from previous task's investigation)
    - .planning/phases/20-site-shell-non-app-pages/20-CONTEXT.md §decisions D-02 (`:global(.my-app-dark)` scoped override pattern) and D-08 (`!important` matching specificity)
  </read_first>
  <action>
For each candidate rule identified in Task 1, append a paired `.my-app-dark`-prefixed override in the SAME `<style>` block, immediately following the original rule (group dark overrides at the bottom of the `<style>` block, OR alongside their light counterparts — executor's call, document the choice in SUMMARY).

Override patterns:

**Pattern A — class with light background:**
```css
/* Existing light-mode rule (unchanged) */
.request-panel {
  background: #ffffff;
  border: 1px solid #e0e0e0;
}

/* Phase 21 dark-mode override */
.my-app-dark .request-panel {
  background: var(--color-surface-card);
  border-color: var(--color-surface-divider);
}
```

**Pattern B — class with light text:**
```css
.tab-label {
  color: #333;
}

.my-app-dark .tab-label {
  color: var(--color-typo-body);
}
```

**Pattern C — `!important` match (D-08 from Phase 20):** If the original rule uses `!important`, the override must also use `!important` to win specificity.

**Pattern D — `:deep()` for `v-html` content:** Use `:deep(.my-app-dark .json-key)` IF (and only if) audit shows a JSON viewer class loses contrast against the dark page background. The planner predicts this is unnecessary — the `.json-pre` provides its own dark surface.

Constraints:
- Touch ONLY `src/components/projects/api-playground/ApiPlaygroundApp.vue`
- Do NOT install new dependencies — `git diff package.json` must be empty
- Do NOT swap the JSON viewer to a third-party theme — the custom highlighter stays
- Do NOT modify dynamic inline `style="{ color: methodColor }"` bindings (lines ~510, 547, 569, 575) — theme-independent semantic colors
- Do NOT modify the `.json-pre` background `#001122` or any `.json-{key,string,number,boolean,null}` color UNLESS audit shows a specific contrast failure on the dark page background (justify in SUMMARY)
- Do NOT add `.my-app-dark .p-{component}` overrides for PrimeVue components — Phase 18 variant alignment handles them; UAT-driven only if specific bleed observed

Prefer `var(--color-surface-card)` / `var(--color-surface-page)` / `var(--color-typo-body)` / `var(--color-typo-heading)` / `var(--color-typo-muted)` / `var(--color-surface-divider)` over hardcoded hexes — these tokens are already defined in `base.css` and auto-switch via Phase 18 (so technically the override could even be a single `:root`-level rule, but per-class overrides match Phase 20 D-02's pattern and remain explicit).

When NO appropriate token exists (e.g. a navy decorative band), use navy hexes consistent with the existing `#001122` JSON viewer palette.
  </action>
  <verify>
    <automated>cd "C:/GitRepos/lex-lib.github.io" &amp;&amp; npm run type-check &amp;&amp; npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` lists `src/components/projects/api-playground/ApiPlaygroundApp.vue` (and possibly the UAT/SUMMARY from later tasks)
    - `git diff --name-only` does NOT include: src/main.ts, src/composables/useTheme.ts, src/composables/useIsMobile.ts, src/assets/base.css, src/assets/wallecx-overrides.css, any file under src/components/projects/wallecx/, src/components/projects/lextrack/, src/components/projects/larga/, src/components/projects/gift-exchange/, src/views/**, index.html, package.json, package-lock.json
    - `grep -c "\.my-app-dark" src/components/projects/api-playground/ApiPlaygroundApp.vue` returns a number ≥ 1 (at least one dark override was added)
    - No new import statement was added to the file
    - No `import .* (prism|highlight|shiki|hljs|monaco|codemirror)` in the file's imports
    - `npm run type-check` exit 0
    - `npm run build-only` exit 0
  </acceptance_criteria>
  <done>API Playground chrome has paired `.my-app-dark` overrides for each light-hex surface identified in Task 1; build green; no new dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Generate consolidated `21-HUMAN-UAT.md` covering all 4 mini-apps (D-21)</name>
  <files>.planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (D-21 UAT structure, D-18 anti-regression list)
    - .planning/phases/20-site-shell-non-app-pages/20-HUMAN-UAT.md (if exists — template format reference)
    - .planning/phases/18-dark-mode-fixes/18-HUMAN-UAT.md (if exists — template format reference)
    - .planning/ROADMAP.md §Phase 21 (SC 1-4 wording)
  </read_first>
  <action>
Create `.planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md` — ONE consolidated UAT doc for the entire phase (per D-21; mirrors Phase 20's UAT structure).

Required structure:

```markdown
# Phase 21: Mini-App Dark Mode Sweep — Human UAT

**Generated:** {today's date}
**Scope:** THEME-09 (LexTrack) + THEME-10 (Larga) + THEME-11 (Gift Exchange / MonitoX) + THEME-12 (API Playground)
**Verification approach:** Open each route on a 375px viewport and on ≥640px viewport, in BOTH light and dark mode (toggle via NavBar sun/moon button delivered in Phase 19), and check each item below.

## How to enter / leave dark mode
- Click the sun/moon toggle in CustomNavBar (Phase 19 delivery)
- Or, in DevTools: select `<html>` and toggle the `my-app-dark` class

## §1 — LexTrack (THEME-09, ROADMAP SC 1)

Route: `/projects/lextrack` (requires login)

### Light mode (≥ 640px)
- [ ] Task list shows white card faces, dark text — legible
- [ ] Meeting list shows white card faces, dark text — legible
- [ ] Support list shows white card faces, dark text — legible
- [ ] AddMeeting dialog opens with white surface, dark form labels
- [ ] ManageTask / ManageMeeting / ManageSupport dialogs open with white surface, dark form labels

### Dark mode (≥ 640px)
- [ ] Task list shows dark card faces (no white bleed), light text — legible
- [ ] Meeting list shows dark card faces, light text
- [ ] Support list shows dark card faces, light text
- [ ] AddMeeting dialog opens with dark surface, light form labels
- [ ] ManageTask / ManageMeeting / ManageSupport dialogs — same
- [ ] Status badges (success/warning/error/info) remain in their semantic colors (theme-independent — D-13)

### 375px viewport — both themes
- [ ] No horizontal scroll on any LexTrack screen
- [ ] All dialogs reachable / dismissible

## §2 — Larga (THEME-10, ROADMAP SC 2)

Route: `/projects/larga`

### Light mode
- [ ] Map renders with OSM tiles (light)
- [ ] Route picker, geocoder input, route info panels — legible

### Dark mode
- [ ] Page chrome (route picker, panels, controls) renders with dark backgrounds, light text — legible
- [ ] Geocoder input is legible (either dark or light input — judgment per D-09; document outcome)
- [ ] Map TILES stay light — this is intentional per D-07; verify map content itself is still readable
- [ ] No light panel bleed against the dark page surface

## §3 — Gift Exchange / MonitoX (THEME-11, ROADMAP SC 3)

Routes: `/projects/gift-exchange`, `/projects/gift-exchange/join`, `/projects/gift-exchange/draw`, `/projects/gift-exchange/manage`

### Each sub-route, dark mode
- [ ] Landing (`GiftExchange.vue`) — dark surface, legible heading + body + buttons
- [ ] Join (`GiftExchangeJoin.vue`) — dark surface, legible form fields, status messages
- [ ] Draw (`GiftExchangeDraw.vue`) — dark surface, legible participant list, reveal mechanism
- [ ] Manage (`GiftExchangeManage.vue`) — dark surface, legible participant/admin controls, status chips
- [ ] Colored chips (red/yellow/green/blue) use subtle dark backgrounds with lighter foreground (`bg-color-900/30 text-color-300` pattern)
- [ ] Status colors (success/warning/error/info from custom theme) stay in their fixed semantic palette (D-13)

### 375px viewport, dark mode
- [ ] All 4 sub-routes usable; no horizontal scroll
- [ ] CTA buttons reachable

## §4 — API Playground (THEME-12, ROADMAP SC 4)

Route: `/projects/api-playground`

### Light mode
- [ ] Toolbar, tabs, request panel, response panel — light surfaces, dark text
- [ ] JSON response body — navy `<pre>` background, readable JSON syntax colors

### Dark mode
- [ ] Toolbar, tabs, request panel, response panel — dark surfaces, light text
- [ ] Headers / params / body input tables — dark cells, legible content
- [ ] History list legible against dark page
- [ ] JSON response body — STILL the navy `<pre>` palette (intentional both-theme palette); JSON syntax colors (key/string/number/boolean/null) remain readable against `#001122`
- [ ] Method-color text (GET green / POST orange / DELETE red etc.) unchanged in both themes (theme-independent)

### 375px viewport, both themes
- [ ] Request panel scrolls; response panel scrolls; no horizontal page scroll

## Failure-mode catalog

If anything fails the above, file it as a gap with:
- Mini-app + sub-route
- Viewport (375 / ≥ 640)
- Theme (light / dark)
- Symptom (light bleed / contrast / clipping / etc.)
- Suggested fix (`dark:` Tailwind pairing / scoped `:global(.my-app-dark)` override / `.my-app-dark` CSS rule)

## Sign-off

- [ ] All §1 items pass — THEME-09 approved
- [ ] All §2 items pass — THEME-10 approved
- [ ] All §3 items pass — THEME-11 approved
- [ ] All §4 items pass — THEME-12 approved
- [ ] No anti-regression files touched (verify with `git log --oneline {phase-start-commit}..HEAD -- src/main.ts src/composables/useTheme.ts src/composables/useIsMobile.ts src/assets/base.css src/assets/wallecx-overrides.css src/components/projects/wallecx/ src/views/`)

---
*Phase 21 UAT generated as the last deliverable of plan 21-04 per D-21.*
```

Fill in the actual today's date in the `**Generated:**` line. Otherwise the doc is verbatim.
  </action>
  <verify>
    <automated>test -f .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md &amp;&amp; grep -q "THEME-09" .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md &amp;&amp; grep -q "THEME-10" .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md &amp;&amp; grep -q "THEME-11" .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md &amp;&amp; grep -q "THEME-12" .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md` exists
    - File contains the four §1-§4 sections (one per mini-app)
    - Each section references its corresponding THEME-09/10/11/12 requirement and ROADMAP SC
    - Both light + dark mode checklists present per section
    - Both 375px and ≥640px viewport checks present
    - Sign-off block at the end with one tick per mini-app + anti-regression verification
  </acceptance_criteria>
  <done>Consolidated phase UAT doc generated; covers all 4 mini-apps with light/dark + viewport checks; ready for human runtime UAT.</done>
</task>

<task type="auto">
  <name>Task 4: SUMMARY for plan 21-04</name>
  <files>.planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md
    - src/components/projects/api-playground/ApiPlaygroundApp.vue (current state)
    - .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md (just generated)
  </read_first>
  <action>
Generate `.planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md` following the SUMMARY template. Required sections:

1. **D-15 highlighter investigation outcome** — confirm: NO third-party syntax highlighter is imported; the `highlightedJson` computed (lines ~135-159) is a CUSTOM inline regex implementation using DOMPurify; no library swap was needed
2. **Files modified** — exact list (`ApiPlaygroundApp.vue` + `21-HUMAN-UAT.md` + this SUMMARY)
3. **Overrides applied** — enumerate each `.my-app-dark .{selector}` rule added: selector, new value, rationale (which @theme token or navy hex was used, and why)
4. **Status of JSON viewer (`.json-pre` family)** — explicit statement: the existing navy palette is left as-is because it provides its own surface contrasting cleanly with the dark page; if the audit revealed any contrast issue, that decision is documented here
5. **No-dependency confirmation** — `package.json` / `package-lock.json` unchanged (`git diff package.json package-lock.json` empty)
6. **Anti-regression confirmation** — `git diff --name-only` includes ONLY `src/components/projects/api-playground/ApiPlaygroundApp.vue` plus phase planning docs (UAT + SUMMARY); no file from the locked list
7. **THEME-12 trace** — connect request panel / response display / headers tables / syntax-highlighted body to specific class selectors and override rules added
8. **D-21 deliverable** — explicit statement: `21-HUMAN-UAT.md` was generated as part of this plan, covers all 4 mini-apps, ready for human runtime UAT
9. **Verification results** — `npm run type-check` PASS, `npm run build-only` PASS
10. **Phase-level next steps** — note that after all 4 plan SUMMARYs land, the human runs `21-HUMAN-UAT.md` per its instructions; any failure becomes a gap-closure phase
  </action>
  <verify>
    <automated>test -f .planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md &amp;&amp; grep -q "THEME-12" .planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md &amp;&amp; grep -q "21-HUMAN-UAT" .planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md &amp;&amp; grep -q "type-check" .planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md` exists
    - Contains the 10 required sections above
    - References THEME-12, ROADMAP §Phase 21 SC 4, D-15 investigation, D-21 UAT deliverable
    - Explicitly states no dependency was added
  </acceptance_criteria>
  <done>SUMMARY committed; documents highlighter investigation, override list, UAT deliverable, no-dependency-added confirmation.</done>
</task>

</tasks>

<verification>
- `ApiPlaygroundApp.vue` contains paired `.my-app-dark` overrides for each light-mode chrome surface
- JSON highlighter is unchanged (custom inline, intentionally dark in both themes — D-15 audit outcome documented in SUMMARY)
- No new dependency in `package.json`
- `npm run type-check` exits 0
- `npm run build-only` exits 0
- `git diff --name-only` contains ONLY `src/components/projects/api-playground/ApiPlaygroundApp.vue` plus `21-HUMAN-UAT.md` and `21-04-SUMMARY.md`
- Locked anti-regression files NOT in `git diff --name-only` (same list as plan 21-01; also excludes lextrack/larga/gift-exchange directories)
- `21-HUMAN-UAT.md` exists and covers all 4 mini-apps (D-21 deliverable)
</verification>

<success_criteria>
- THEME-12 structurally satisfied (chrome surfaces have `.my-app-dark` overrides; JSON viewer remains readable)
- D-21 deliverable produced (consolidated phase-level UAT doc)
- Plan touches only API Playground source file + phase planning docs
- Plan SUMMARY exists and traces to ROADMAP §Phase 21 SC 4
- Build is green; no new dependency
- Visible runtime UAT begins after this plan ships (per `21-HUMAN-UAT.md`)
</success_criteria>

<output>
After completion, create `.planning/phases/21-mini-app-dark-mode-sweep/21-04-SUMMARY.md` (Task 4). The consolidated UAT doc `21-HUMAN-UAT.md` is generated in Task 3.
</output>
