---
phase: 21-mini-app-dark-mode-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/gift-exchange/GiftExchange.vue
  - src/components/projects/gift-exchange/GiftExchangeJoin.vue
  - src/components/projects/gift-exchange/GiftExchangeDraw.vue
  - src/components/projects/gift-exchange/GiftExchangeManage.vue
autonomous: true
requirements:
  - THEME-11
tags:
  - dark-mode
  - gift-exchange
  - monitox
  - tailwind
  - vue3

must_haves:
  truths:
    - "With dark mode active, MonitoX (Gift Exchange) renders with dark backgrounds and legible text across all four sub-routes: index/landing (GiftExchange.vue), join (GiftExchangeJoin.vue), draw (GiftExchangeDraw.vue), manage (GiftExchangeManage.vue)"
    - "Every existing light-only Tailwind utility (`bg-white`, `text-gray-*`, `border-gray-*`, `bg-gray-*`) in the 4 gift-exchange files has a paired `dark:` variant"
    - "Colored chips/badges (red/yellow/green/blue/etc.) use `dark:bg-{color}-900/30 dark:text-{color}-300` pattern where present (D-11)"
    - "Custom @theme tokens (`bg-surface-card`, `text-typo-body`, `border-surface-divider`) are used as `dark:` targets where applicable (D-11)"
    - "MonitoX is NOT refactored to a fully semantic-token structure — pair-only, no redesign (D-12)"
    - "`bg-status-*` and `text-status-*` utilities are NOT paired with `dark:` variants (D-13 lock, BR-2 invariant)"
    - "Anti-regression files (main.ts, useTheme.ts, useIsMobile.ts, base.css, wallecx-overrides.css, wallecx/**, views/**, lextrack/**, larga/**, api-playground/**) are NOT modified"
    - "`npm run type-check` exits 0"
    - "`npm run build-only` exits 0"
  artifacts:
    - path: "src/components/projects/gift-exchange/GiftExchange.vue"
      provides: "Gift Exchange landing/index with paired dark Tailwind utilities (~30 instances)"
      contains: "dark:"
    - path: "src/components/projects/gift-exchange/GiftExchangeJoin.vue"
      provides: "Join sub-route with paired dark Tailwind utilities (~24 instances)"
      contains: "dark:"
    - path: "src/components/projects/gift-exchange/GiftExchangeDraw.vue"
      provides: "Draw sub-route with paired dark Tailwind utilities (~18 instances)"
      contains: "dark:"
    - path: "src/components/projects/gift-exchange/GiftExchangeManage.vue"
      provides: "Manage sub-route with paired dark Tailwind utilities (~31 instances)"
      contains: "dark:"
  key_links:
    - from: "All 4 gift-exchange files"
      to: ".my-app-dark on <html>"
      via: "Tailwind `dark:` variants (aligned via Phase 18 @custom-variant rule)"
      pattern: "dark:bg-|dark:text-|dark:border-"
---

<objective>
Pair every light-only Tailwind utility in the 4 MonitoX (Gift Exchange) source files with an appropriate `dark:` variant so all four sub-routes render correctly when `.my-app-dark` is active on `<html>`. Strict mechanical sweep — no redesign, no semantic-token refactor (D-12).

Purpose: Closes THEME-11 (Gift Exchange dark mode) and contributes to ROADMAP §Phase 21 SC 3 ("Gift Exchange renders correctly in dark mode across all sub-routes (join, draw, manage, result)"). MonitoX is currently the most visibly broken in dark mode (~103 light-only utility instances).

Output: 4 modified `.vue` files plus 1 plan SUMMARY documenting per-file replacements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md
@.planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md
@.planning/phases/18-dark-mode-fixes/18-CONTEXT.md
@.planning/phases/20-site-shell-non-app-pages/20-CONTEXT.md

<interfaces>
<!-- Custom @theme tokens already auto-switch via Phase 18's `.my-app-dark` block in base.css. -->
<!-- Only Tailwind scale utilities (bg-white, text-gray-700, border-gray-200, colored chips) need explicit `dark:` pairing. -->

Pairing table (same as plan 21-01):

| Light utility | Add `dark:` variant |
|---|---|
| `bg-white` | `dark:bg-surface-card` |
| `bg-gray-50` / `bg-gray-100` | `dark:bg-surface-page` |
| `bg-gray-200` | `dark:bg-surface-card` (or `dark:bg-gray-700` if context demands) |
| `text-gray-900` / `text-gray-800` | `dark:text-typo-heading` |
| `text-gray-700` / `text-gray-600` | `dark:text-typo-body` |
| `text-gray-500` / `text-gray-400` | `dark:text-typo-muted` |
| `border-gray-200` / `border-gray-300` | `dark:border-surface-divider` |
| `bg-red-100 text-red-700` | `dark:bg-red-900/30 dark:text-red-300` |
| `bg-yellow-100 text-yellow-700` | `dark:bg-yellow-900/30 dark:text-yellow-300` |
| `bg-green-100 text-green-700` | `dark:bg-green-900/30 dark:text-green-300` |
| `bg-blue-100 text-blue-700` | `dark:bg-blue-900/30 dark:text-blue-300` |

DO NOT pair:
- `bg-status-*` / `text-status-*` utilities (D-13 lock — these are theme-independent per BR-2)
- Anything inside `:deep()` selectors targeting PrimeVue components (Phase 18 handles them)

DO NOT:
- Refactor existing utilities to semantic tokens (D-12: pair-only, no redesign)
- Add `.my-app-dark .p-{component}` PrimeVue overrides (Phase 18 variant alignment handles them)
- Add dark overrides for `--color-status-*` tokens (D-13)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GiftExchange.vue — mechanical dark-variant sweep (~30 instances)</name>
  <files>src/components/projects/gift-exchange/GiftExchange.vue</files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (D-10, D-11, D-12, D-13, D-18)
    - src/components/projects/gift-exchange/GiftExchange.vue (full file)
  </read_first>
  <action>
Mechanical sweep of `GiftExchange.vue`. For every occurrence of `bg-white`, `text-gray-N`, `border-gray-N`, `bg-gray-N`, and colored chips (`bg-red-100 text-red-700`, etc.), append the corresponding `dark:` variant per the pairing table in `<interfaces>` above.

Mechanical before/after (verbatim from 21-CONTEXT.md §Specific Ideas):

Before:
```html
<div class="bg-white border border-gray-200 p-4">
  <h2 class="text-gray-800 font-bold">Heading</h2>
  <p class="text-gray-600">Body</p>
</div>
```

After:
```html
<div class="bg-white dark:bg-surface-card border border-gray-200 dark:border-surface-divider p-4">
  <h2 class="text-gray-800 dark:text-typo-heading font-bold">Heading</h2>
  <p class="text-gray-600 dark:text-typo-body">Body</p>
</div>
```

Colored chip:

Before:
```html
<span class="bg-red-100 text-red-700 px-2 py-1 rounded">Expired</span>
```

After:
```html
<span class="bg-red-100 dark:bg-red-900/30 text-red-700 dark:text-red-300 px-2 py-1 rounded">Expired</span>
```

Constraints:
- Touch ONLY `src/components/projects/gift-exchange/GiftExchange.vue`
- Do NOT modify markup structure, props, scripts, computeds, methods, or imports
- Do NOT refactor existing utilities to semantic tokens (D-12)
- Skip `bg-status-*` / `text-status-*` (D-13)
  </action>
  <verify>
    <automated>cd "C:/GitRepos/lex-lib.github.io" &amp;&amp; npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` lists exactly `src/components/projects/gift-exchange/GiftExchange.vue` (plus other gift-exchange files from later tasks)
    - `git diff --name-only` does NOT include anti-regression files (see plan 21-01)
    - `grep -E "\bbg-white\b" src/components/projects/gift-exchange/GiftExchange.vue | grep -v "dark:bg-"` returns ZERO lines
    - `grep -E "\btext-gray-[0-9]+" src/components/projects/gift-exchange/GiftExchange.vue | grep -v "dark:text-"` returns ZERO lines
    - `grep -E "\bborder-gray-[0-9]+" src/components/projects/gift-exchange/GiftExchange.vue | grep -v "dark:border-"` returns ZERO lines
    - `grep -E "\bbg-gray-[0-9]+" src/components/projects/gift-exchange/GiftExchange.vue | grep -v "dark:bg-"` returns ZERO lines
    - `grep -E "(bg|text)-status-" src/components/projects/gift-exchange/GiftExchange.vue | grep "dark:"` returns ZERO lines
    - `npm run type-check` exit 0
  </acceptance_criteria>
  <done>GiftExchange.vue's ~30 light-only utilities each have a paired `dark:` variant; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: GiftExchangeJoin.vue — mechanical dark-variant sweep (~24 instances)</name>
  <files>src/components/projects/gift-exchange/GiftExchangeJoin.vue</files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (D-10, D-11, D-13)
    - src/components/projects/gift-exchange/GiftExchangeJoin.vue (full file)
  </read_first>
  <action>
Same mechanical sweep as Task 1, applied to `GiftExchangeJoin.vue`. Same pairing table, same constraints. ~24 instances expected.

Touch ONLY `src/components/projects/gift-exchange/GiftExchangeJoin.vue`. Skip `bg-status-*` / `text-status-*` (D-13). No semantic-token refactor (D-12).
  </action>
  <verify>
    <automated>cd "C:/GitRepos/lex-lib.github.io" &amp;&amp; npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` includes `src/components/projects/gift-exchange/GiftExchangeJoin.vue`
    - `git diff --name-only` does NOT include anti-regression files
    - `grep -E "\b(bg-white|text-gray-[0-9]+|border-gray-[0-9]+|bg-gray-[0-9]+)\b" src/components/projects/gift-exchange/GiftExchangeJoin.vue | grep -v "dark:"` returns ZERO lines
    - `grep -E "(bg|text)-status-" src/components/projects/gift-exchange/GiftExchangeJoin.vue | grep "dark:"` returns ZERO lines
    - `npm run type-check` exit 0
  </acceptance_criteria>
  <done>GiftExchangeJoin.vue's ~24 light-only utilities each have a paired `dark:` variant; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 3: GiftExchangeDraw.vue — mechanical dark-variant sweep (~18 instances)</name>
  <files>src/components/projects/gift-exchange/GiftExchangeDraw.vue</files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (D-10, D-11, D-13)
    - src/components/projects/gift-exchange/GiftExchangeDraw.vue (full file)
  </read_first>
  <action>
Same mechanical sweep as Task 1, applied to `GiftExchangeDraw.vue`. ~18 instances expected.

Touch ONLY `src/components/projects/gift-exchange/GiftExchangeDraw.vue`. Skip `bg-status-*` / `text-status-*` (D-13). No semantic-token refactor (D-12).
  </action>
  <verify>
    <automated>cd "C:/GitRepos/lex-lib.github.io" &amp;&amp; npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` includes `src/components/projects/gift-exchange/GiftExchangeDraw.vue`
    - `grep -E "\b(bg-white|text-gray-[0-9]+|border-gray-[0-9]+|bg-gray-[0-9]+)\b" src/components/projects/gift-exchange/GiftExchangeDraw.vue | grep -v "dark:"` returns ZERO lines
    - `grep -E "(bg|text)-status-" src/components/projects/gift-exchange/GiftExchangeDraw.vue | grep "dark:"` returns ZERO lines
    - `npm run type-check` exit 0
  </acceptance_criteria>
  <done>GiftExchangeDraw.vue's ~18 light-only utilities each have a paired `dark:` variant; type-check passes.</done>
</task>

<task type="auto">
  <name>Task 4: GiftExchangeManage.vue — mechanical dark-variant sweep (~31 instances) + final build</name>
  <files>src/components/projects/gift-exchange/GiftExchangeManage.vue</files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (D-10, D-11, D-12, D-13)
    - src/components/projects/gift-exchange/GiftExchangeManage.vue (full file — largest of the four, ~31 instances)
  </read_first>
  <action>
Same mechanical sweep as Task 1, applied to `GiftExchangeManage.vue`. ~31 instances expected (largest file).

Touch ONLY `src/components/projects/gift-exchange/GiftExchangeManage.vue`. Skip `bg-status-*` / `text-status-*` (D-13). No semantic-token refactor (D-12).

After this file is done, run BOTH `npm run type-check` AND `npm run build-only` to validate the entire plan's changes compile and bundle cleanly.
  </action>
  <verify>
    <automated>cd "C:/GitRepos/lex-lib.github.io" &amp;&amp; npm run type-check &amp;&amp; npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` after this task includes all 4 gift-exchange files
    - `git diff --name-only` does NOT include any anti-regression file
    - `grep -E "\b(bg-white|text-gray-[0-9]+|border-gray-[0-9]+|bg-gray-[0-9]+)\b" src/components/projects/gift-exchange/GiftExchangeManage.vue | grep -v "dark:"` returns ZERO lines
    - `grep -E "(bg|text)-status-" src/components/projects/gift-exchange/*.vue | grep "dark:"` returns ZERO lines (across all 4 files — no status-color was paired)
    - `npm run type-check` exit 0
    - `npm run build-only` exit 0
  </acceptance_criteria>
  <done>GiftExchangeManage.vue's ~31 light-only utilities have paired dark variants; full plan build is green.</done>
</task>

<task type="auto">
  <name>Task 5: SUMMARY for plan 21-03</name>
  <files>.planning/phases/21-mini-app-dark-mode-sweep/21-03-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md
    - All 4 gift-exchange files (current modified state) to enumerate pairings made
  </read_first>
  <action>
Generate `.planning/phases/21-mini-app-dark-mode-sweep/21-03-SUMMARY.md` following the SUMMARY template. Required sections (at minimum):

1. **Files modified** — exact list with line-count delta per file
2. **Pairings applied** — per file, count of `bg-white`, `text-gray-*`, `border-gray-*`, `bg-gray-*`, colored-chip pairings; balance of custom theme tokens vs Tailwind scale fallback
3. **D-12 / D-13 invariants confirmed** — explicit statement that no existing utility was refactored to a semantic token (pair-only) and no status-color utility was paired
4. **Anti-regression confirmation** — explicit statement that `git diff --name-only` includes only gift-exchange files plus the SUMMARY
5. **THEME-11 trace** — bullet list connecting each sub-route (landing, join, draw, manage) to its now-paired source file; reference ROADMAP §Phase 21 SC 3
6. **Verification results** — `npm run type-check` PASS, `npm run build-only` PASS
7. **Open items / UAT pending** — note visible runtime UAT deferred to consolidated `21-HUMAN-UAT.md` (plan 21-04)
  </action>
  <verify>
    <automated>test -f .planning/phases/21-mini-app-dark-mode-sweep/21-03-SUMMARY.md &amp;&amp; grep -q "THEME-11" .planning/phases/21-mini-app-dark-mode-sweep/21-03-SUMMARY.md &amp;&amp; grep -q "type-check" .planning/phases/21-mini-app-dark-mode-sweep/21-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/21-mini-app-dark-mode-sweep/21-03-SUMMARY.md` exists
    - Contains the 7 required sections above
    - References THEME-11 and ROADMAP §Phase 21 SC 3 explicitly
    - States D-12 (no refactor) and D-13 (no status-color pairing) compliance explicitly
  </acceptance_criteria>
  <done>SUMMARY committed; documents per-file pairings and invariant compliance.</done>
</task>

</tasks>

<verification>
- All 4 gift-exchange `.vue` files contain paired `dark:` variants for every light-only Tailwind utility
- `npm run type-check` exits 0
- `npm run build-only` exits 0
- `git diff --name-only` contains ONLY files under `src/components/projects/gift-exchange/` plus the plan's SUMMARY
- Locked anti-regression files NOT in `git diff --name-only` (same list as plan 21-01)
- No `bg-status-*` or `text-status-*` utility has been paired with a `dark:` variant (D-13)
- No existing utility was refactored to a semantic token (D-12 — pair-only)
</verification>

<success_criteria>
- THEME-11 structurally satisfied (every light-only utility in 4 files has a `dark:` counterpart)
- Plan touches only gift-exchange files
- Plan SUMMARY exists and traces to ROADMAP §Phase 21 SC 3
- Build is green
- Visible runtime UAT deferred to consolidated `21-HUMAN-UAT.md` (plan 21-04)
</success_criteria>

<output>
After completion, create `.planning/phases/21-mini-app-dark-mode-sweep/21-03-SUMMARY.md` (Task 5).
</output>
