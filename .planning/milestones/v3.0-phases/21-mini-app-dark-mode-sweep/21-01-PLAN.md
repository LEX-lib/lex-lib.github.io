---
phase: 21-mini-app-dark-mode-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/lextrack/LexTrackApp.vue
  - src/components/projects/lextrack/ActivityCard.vue
  - src/components/projects/lextrack/AddMeeting.vue
  - src/components/projects/lextrack/ManageMeeting.vue
  - src/components/projects/lextrack/ManageSupport.vue
  - src/components/projects/lextrack/ManageTask.vue
autonomous: true
requirements:
  - THEME-09
tags:
  - dark-mode
  - lextrack
  - tailwind
  - vue3

must_haves:
  truths:
    - "With dark mode active, LexTrackApp.vue task list / meeting list / support list surfaces render with dark backgrounds and legible light text"
    - "With dark mode active, every CRUD form dialog (AddMeeting, ManageMeeting, ManageTask, ManageSupport) renders with dark backgrounds, dark form chrome, and legible labels"
    - "Every existing light-only Tailwind utility (`bg-white`, `text-gray-*`, `border-gray-*`, `bg-gray-*`) in the 5 LexTrack files has a paired `dark:` variant"
    - "Anti-regression files (main.ts, useTheme.ts, useIsMobile.ts, base.css, wallecx-overrides.css, wallecx/**, views/**, larga/**, gift-exchange/**, api-playground/**) are NOT modified by this plan"
    - "`npm run type-check` exits 0"
    - "`npm run build-only` exits 0"
  artifacts:
    - path: "src/components/projects/lextrack/LexTrackApp.vue"
      provides: "LexTrack shell with paired dark Tailwind utilities (~34 instances)"
      contains: "dark:bg-surface-card OR dark:bg-gray-"
    - path: "src/components/projects/lextrack/AddMeeting.vue"
      provides: "AddMeeting dialog with paired dark Tailwind utilities (~5 instances)"
      contains: "dark:"
    - path: "src/components/projects/lextrack/ManageMeeting.vue"
      provides: "ManageMeeting dialog with paired dark Tailwind utilities (~5 instances)"
      contains: "dark:"
    - path: "src/components/projects/lextrack/ManageSupport.vue"
      provides: "ManageSupport dialog with paired dark Tailwind utilities (~4 instances)"
      contains: "dark:"
    - path: "src/components/projects/lextrack/ManageTask.vue"
      provides: "ManageTask dialog with paired dark Tailwind utilities (~5 instances)"
      contains: "dark:"
  key_links:
    - from: "LexTrackApp.vue"
      to: ".my-app-dark on <html>"
      via: "Tailwind `dark:` variants (aligned via Phase 18 @custom-variant rule)"
      pattern: "dark:bg-|dark:text-|dark:border-"
    - from: "All 5 LexTrack files"
      to: "@theme custom tokens (bg-surface-card, text-typo-body, border-surface-divider)"
      via: "Phase 18 token overrides — preferred over Tailwind gray scale where applicable"
      pattern: "dark:bg-surface-|dark:text-typo-|dark:border-surface-"
---

<objective>
Pair every light-only Tailwind utility in the 6 LexTrack source files with an appropriate `dark:` variant so LexTrack renders correctly when `.my-app-dark` is active on `<html>`. Strict mechanical sweep — no redesign, no new components, no semantic-token refactor beyond pairing.

Purpose: Closes THEME-09 (LexTrack dark mode) and contributes to ROADMAP §Phase 21 SC 1 ("LexTrack renders correctly in dark mode — task lists, meeting forms, support requests, dialogs, and any tables/charts are readable").

Output:
- 5 modified LexTrack `.vue` files (LexTrackApp + 4 manage/add dialogs) with paired `dark:` utilities
- 1 file (ActivityCard.vue) verified — `git diff` may show zero changes if no light-only utilities found
- 1 plan SUMMARY documenting per-file replacements
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@./CLAUDE.md
@.planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md
@.planning/phases/18-dark-mode-fixes/18-CONTEXT.md
@.planning/phases/20-site-shell-non-app-pages/20-CONTEXT.md

<interfaces>
<!-- The Phase 18 @custom-variant alignment in base.css makes Tailwind `dark:` utilities fire when `.my-app-dark` is on <html>. -->
<!-- Custom @theme tokens (bg-surface-card, text-typo-body, etc.) AUTO-SWITCH via base.css's `.my-app-dark` block — no `dark:` prefix needed there. -->
<!-- Only Tailwind scale utilities (bg-white, text-gray-700, border-gray-200) need explicit `dark:` pairing. -->

Available custom @theme tokens (already dark-aware via Phase 18):
- `bg-surface-page`, `bg-surface-card` — surface backgrounds
- `text-typo-heading`, `text-typo-body`, `text-typo-muted` — typography
- `border-surface-divider` — dividers
- `text-status-success`, `text-status-warning`, `text-status-error`, `text-status-info` (and `bg-status-*`) — THEME-INDEPENDENT (D-13 lock); do NOT add `dark:` variants

PrimeVue components auto-switch via the variant alignment (Phase 18): Dialog, DataTable, Button, InputText, Dropdown, etc. — do NOT pre-emptively add `.my-app-dark .p-*` overrides.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LexTrackApp.vue — mechanical dark-variant sweep</name>
  <files>src/components/projects/lextrack/LexTrackApp.vue</files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (full file — D-04 sweep pattern, D-13 status-color lock, D-18 anti-regression list)
    - .planning/phases/18-dark-mode-fixes/18-CONTEXT.md §decisions D-01 (@custom-variant alignment) — explains WHY `dark:` utilities now fire
    - src/components/projects/lextrack/LexTrackApp.vue (full file, ~34 light-only utility instances expected)
    - src/assets/base.css (DO NOT MODIFY — read only, to see which @theme tokens are dark-aware)
  </read_first>
  <action>
For every occurrence in LexTrackApp.vue, pair the light-only Tailwind utility with a `dark:` variant. Prefer custom-theme tokens (auto-switching via Phase 18) over numeric Tailwind grays. Pairing table:

| Light utility | Add `dark:` variant |
|---|---|
| `bg-white` | `dark:bg-surface-card` |
| `bg-gray-50` / `bg-gray-100` | `dark:bg-surface-page` |
| `bg-gray-200` | `dark:bg-surface-card` (or `dark:bg-gray-700` if context demands) |
| `text-gray-900` / `text-gray-800` | `dark:text-typo-heading` |
| `text-gray-700` / `text-gray-600` | `dark:text-typo-body` |
| `text-gray-500` / `text-gray-400` | `dark:text-typo-muted` |
| `border-gray-200` / `border-gray-300` | `dark:border-surface-divider` |
| `border-gray-100` | `dark:border-surface-divider` |
| Colored chips (`bg-red-100 text-red-700`, etc.) | `dark:bg-red-900/30 dark:text-red-300` (and equivalents for green/yellow/blue/amber per CONTEXT.md §Specific Ideas) |

DO NOT pair:
- `bg-status-*` or `text-status-*` utilities — theme-independent per D-13
- Anything inside `:deep()` selectors targeting PrimeVue components — Phase 18 handles them

Mechanical before/after (verbatim from 21-CONTEXT.md §Specific Ideas):

Before:
```html
<div class="bg-white border border-gray-200 p-4">
  <h2 class="text-gray-800 font-bold">Task</h2>
  <p class="text-gray-600">Description</p>
</div>
```

After:
```html
<div class="bg-white dark:bg-surface-card border border-gray-200 dark:border-surface-divider p-4">
  <h2 class="text-gray-800 dark:text-typo-heading font-bold">Task</h2>
  <p class="text-gray-600 dark:text-typo-body">Description</p>
</div>
```

Per D-04 (decision): use custom theme tokens (`dark:bg-surface-card`, `dark:text-typo-body`) over `dark:bg-gray-800` / `dark:text-gray-300` wherever the semantic token applies. Fall back to Tailwind scale (`dark:bg-yellow-900/30`) ONLY for non-semantic colored chips/badges where no matching token exists.

Touch ONLY `src/components/projects/lextrack/LexTrackApp.vue`. Do NOT modify markup structure, props, scripts, or any non-class attribute. Do NOT touch any other file.
  </action>
  <verify>
    <automated>cd "C:/GitRepos/lex-lib.github.io" &amp;&amp; npm run type-check &amp;&amp; npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` lists exactly `src/components/projects/lextrack/LexTrackApp.vue` (plus any subsequent task files, not yet committed)
    - `git diff --name-only` does NOT include: src/main.ts, src/composables/useTheme.ts, src/composables/useIsMobile.ts, src/assets/base.css, src/assets/wallecx-overrides.css, any file under src/components/projects/wallecx/, src/components/projects/larga/, src/components/projects/gift-exchange/, src/components/projects/api-playground/, src/views/**, index.html
    - `grep -E "\bbg-white\b" src/components/projects/lextrack/LexTrackApp.vue | grep -v "dark:bg-"` returns ZERO lines (every `bg-white` is paired)
    - `grep -E "\btext-gray-[0-9]+" src/components/projects/lextrack/LexTrackApp.vue | grep -v "dark:text-"` returns ZERO lines (every `text-gray-N` is paired)
    - `grep -E "\bborder-gray-[0-9]+" src/components/projects/lextrack/LexTrackApp.vue | grep -v "dark:border-"` returns ZERO lines
    - `grep -E "(bg|text)-status-" src/components/projects/lextrack/LexTrackApp.vue | grep "dark:"` returns ZERO lines (status colors NOT paired, per D-13)
    - `npm run type-check` exit 0
    - `npm run build-only` exit 0
  </acceptance_criteria>
  <done>LexTrackApp.vue's ~34 light-only utilities are each paired with a `dark:` variant using custom theme tokens where applicable; build passes; no anti-regression files touched.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityCard + AddMeeting + ManageMeeting + ManageSupport + ManageTask — mechanical dark-variant sweep</name>
  <files>
    src/components/projects/lextrack/ActivityCard.vue,
    src/components/projects/lextrack/AddMeeting.vue,
    src/components/projects/lextrack/ManageMeeting.vue,
    src/components/projects/lextrack/ManageSupport.vue,
    src/components/projects/lextrack/ManageTask.vue
  </files>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md (pairing table, D-04, D-13)
    - src/components/projects/lextrack/ActivityCard.vue (verify; CONTEXT estimates 0 instances — may be no-op)
    - src/components/projects/lextrack/AddMeeting.vue (~5 instances)
    - src/components/projects/lextrack/ManageMeeting.vue (~5 instances)
    - src/components/projects/lextrack/ManageSupport.vue (~4 instances)
    - src/components/projects/lextrack/ManageTask.vue (~5 instances)
  </read_first>
  <action>
Same mechanical sweep as Task 1, applied to each of the 5 files. Pairing table is identical (see Task 1 `<action>`). Total expected: ~19 paired utilities across these files.

Per file:
1. Read the file
2. Identify every `bg-white`, `text-gray-N`, `border-gray-N`, `bg-gray-N` utility
3. Add a `dark:` companion using a custom theme token (preferred) or Tailwind scale fallback (chips/badges)
4. Skip `bg-status-*` / `text-status-*` (D-13 lock)
5. Move to next file

If ActivityCard.vue truly has zero light-only utilities, leave it unmodified; document in the SUMMARY that the audit confirmed zero changes needed.

Do NOT modify anything outside the 5 listed files. Do NOT touch PrimeVue prop bindings (e.g. `pt:`, `dt:` overrides) or scripts.
  </action>
  <verify>
    <automated>cd "C:/GitRepos/lex-lib.github.io" &amp;&amp; npm run type-check &amp;&amp; npm run build-only</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only` after this task includes at most: src/components/projects/lextrack/LexTrackApp.vue, src/components/projects/lextrack/ActivityCard.vue, src/components/projects/lextrack/AddMeeting.vue, src/components/projects/lextrack/ManageMeeting.vue, src/components/projects/lextrack/ManageSupport.vue, src/components/projects/lextrack/ManageTask.vue (ActivityCard may be absent if no changes needed)
    - `git diff --name-only` does NOT include anti-regression files (see Task 1)
    - `grep -E "\b(bg-white|text-gray-[0-9]+|border-gray-[0-9]+|bg-gray-[0-9]+)\b" src/components/projects/lextrack/AddMeeting.vue src/components/projects/lextrack/ManageMeeting.vue src/components/projects/lextrack/ManageSupport.vue src/components/projects/lextrack/ManageTask.vue | grep -v "dark:"` returns ZERO lines
    - `grep -E "(bg|text)-status-" src/components/projects/lextrack/*.vue | grep "dark:"` returns ZERO lines
    - `npm run type-check` exit 0
    - `npm run build-only` exit 0
  </acceptance_criteria>
  <done>All 5 dialog/card files have paired dark variants for every light-only Tailwind utility; ActivityCard either has paired variants or is confirmed clean; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: SUMMARY for plan 21-01</name>
  <files>.planning/phases/21-mini-app-dark-mode-sweep/21-01-SUMMARY.md</files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY template)
    - .planning/phases/21-mini-app-dark-mode-sweep/21-CONTEXT.md
    - All 6 LexTrack files (in current modified state) to enumerate per-file pairings made
  </read_first>
  <action>
Generate `.planning/phases/21-mini-app-dark-mode-sweep/21-01-SUMMARY.md` following the SUMMARY template. Required sections (at minimum):

1. **Files modified** — exact list with line-count delta per file
2. **Pairings applied** — per file, count of `bg-white`, `text-gray-*`, `border-gray-*`, `bg-gray-*`, and colored-chip pairings added; whether custom theme tokens (`dark:bg-surface-*`) or Tailwind scale (`dark:bg-gray-*`) was used
3. **Anti-regression confirmation** — explicit statement that `git diff --name-only` includes only LexTrack files and NO file from the locked list
4. **THEME-09 trace** — bullet list connecting "task lists/meeting forms/support requests/dialogs" (ROADMAP SC 1) to the specific files now paired
5. **Verification results** — `npm run type-check` PASS, `npm run build-only` PASS (with exit codes captured)
6. **Open items / UAT pending** — note that visible runtime UAT is deferred to the consolidated `21-HUMAN-UAT.md` (delivered in plan 21-04)
7. **ActivityCard outcome** — whether audit found light-only utilities or confirmed zero changes
  </action>
  <verify>
    <automated>test -f .planning/phases/21-mini-app-dark-mode-sweep/21-01-SUMMARY.md &amp;&amp; grep -q "THEME-09" .planning/phases/21-mini-app-dark-mode-sweep/21-01-SUMMARY.md &amp;&amp; grep -q "type-check" .planning/phases/21-mini-app-dark-mode-sweep/21-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/21-mini-app-dark-mode-sweep/21-01-SUMMARY.md` exists
    - Contains the 7 required sections above
    - References THEME-09 and ROADMAP §Phase 21 SC 1 explicitly
    - Anti-regression section lists each locked file by path
  </acceptance_criteria>
  <done>SUMMARY committed alongside the LexTrack changes; documents what was paired and confirms anti-regression scope.</done>
</task>

</tasks>

<verification>
- All 6 LexTrack `.vue` files contain paired `dark:` variants for every light-only Tailwind utility (or are confirmed zero-change for ActivityCard.vue)
- `npm run type-check` exits 0
- `npm run build-only` exits 0
- `git diff --name-only` contains ONLY files under `src/components/projects/lextrack/` plus the plan's SUMMARY
- Locked anti-regression files are NOT in `git diff --name-only`:
  - `src/main.ts`
  - `src/composables/useTheme.ts`
  - `src/composables/useIsMobile.ts`
  - `src/assets/base.css`
  - `src/assets/wallecx-overrides.css`
  - Any file under `src/components/projects/wallecx/`
  - Any file under `src/components/projects/larga/`, `src/components/projects/gift-exchange/`, `src/components/projects/api-playground/`
  - Any file under `src/views/`
  - `index.html`
- No `bg-status-*` or `text-status-*` utility has been paired with a `dark:` variant (D-13)
</verification>

<success_criteria>
- THEME-09 structurally satisfied (every light-only utility in the 6 files has a `dark:` counterpart)
- Plan touches only LexTrack files
- Plan SUMMARY exists and traces to ROADMAP §Phase 21 SC 1
- Build is green
- Visible runtime UAT deferred to consolidated `21-HUMAN-UAT.md` (plan 21-04)
</success_criteria>

<output>
After completion, create `.planning/phases/21-mini-app-dark-mode-sweep/21-01-SUMMARY.md` (Task 3).
</output>
