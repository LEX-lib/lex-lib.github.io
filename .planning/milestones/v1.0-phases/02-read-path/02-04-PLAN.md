---
phase: 02-read-path
plan: 04
type: execute
wave: 3
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/components/projects/wallecx/WallecxApp.vue
autonomous: true
requirements:
  - READ-04
  - READ-05
  - READ-07

must_haves:
  truths:
    - "Navigating to /projects/wallecx immediately shows skeleton rows — never a blank page or 'no records' flash"
    - "After data loads, the vaccination list appears with real records or the empty state"
    - "A network error during getFullList surfaces a vue-sonner toast: 'Failed to load vaccination records. Refresh to try again.'"
    - "Clicking 'View Record' on a list row opens the detail Dialog with all fields and the attachment preview"
    - "The detail Dialog shows the correct record — selectedRecord is set before showDetail becomes true"
    - "A separate listToken is generated alongside getFullList for thumbnail auth; fileToken is generated when the dialog opens"
    - "A token fetch failure in openDetail shows a toast and still opens the dialog (graceful degradation)"
    - "Closing the Dialog clears selectedRecord and fileToken"
    - "The Dialog has header 'Vaccination Record', width 40rem, and responsive breakpoints"
    - "The record-count placeholder paragraph is removed; VaccinationList replaces it"
  artifacts:
    - path: "src/components/projects/wallecx/WallecxApp.vue"
      provides: "Wired shell — VaccinationList + Dialog + VaccinationDetail + listToken + fileToken + openDetail"
      contains: "listToken"
  key_links:
    - from: "WallecxApp.vue"
      to: "VaccinationList.vue"
      via: "<VaccinationList :records='records' :is-loading='isLoading' :list-token='listToken' @view='openDetail' />"
      pattern: "VaccinationList"
    - from: "WallecxApp.vue"
      to: "pb.files.getToken()"
      via: "listToken in onMounted + fileToken in openDetail"
      pattern: "pb\\.files\\.getToken"
    - from: "WallecxApp.vue"
      to: "VaccinationDetail.vue"
      via: "<VaccinationDetail v-if='selectedRecord' :record='selectedRecord' :token='fileToken' />"
      pattern: "VaccinationDetail"
---

<objective>
Wire `WallecxApp.vue` to complete the Phase 2 read path: add new state refs, extend onMounted to generate a listToken, add the openDetail function with view-time fileToken generation, replace the record-count placeholder with `<VaccinationList>`, and add the Dialog overlay containing `<VaccinationDetail>`.

Purpose: This is the final wiring step that makes Phase 2 functional end-to-end. VaccinationList and VaccinationDetail are already built (Plans 01–03); this plan connects them through WallecxApp's state management. Token generation follows READ-07: listToken at page load for thumbnails, fileToken at dialog open for the detail view.

Output:
- `src/components/projects/wallecx/WallecxApp.vue` — extended shell with listToken/fileToken/selectedRecord/showDetail refs, openDetail function, VaccinationList + Dialog + VaccinationDetail in template
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-path/02-CONTEXT.md
@.planning/phases/02-read-path/02-RESEARCH.md
@.planning/phases/02-read-path/02-PATTERNS.md
@.planning/phases/02-read-path/02-UI-SPEC.md
@.planning/phases/02-read-path/02-02-SUMMARY.md
@.planning/phases/02-read-path/02-03-SUMMARY.md

<interfaces>
<!-- Current WallecxApp.vue state (read this file before modifying) -->
<!--
  Existing imports: ref, onMounted (from "vue"), toast (from "vue-sonner"), pb (from "@/lib/pocketbase"), Vaccinations type
  Existing state refs: records ref<Vaccinations[]>([]), isLoading ref(false)
  Existing onMounted: pb.collection("wallecx_vaccinations").getFullList with sort: "-date_administered"
  Existing template: <Card #content> with <h1> header + placeholder <div> with p tag showing record count
-->

<!-- VaccinationList.vue props interface (created in Plan 02) -->
<!--
  defineProps<{
    records: Vaccinations[];
    isLoading: boolean;
    listToken: string;
  }>()
  defineEmits<{
    view: [record: Vaccinations];
    edit: [record: Vaccinations];
    remove: [record: Vaccinations];
  }>()
  — Usage: <VaccinationList :records="records" :is-loading="isLoading" :list-token="listToken" @view="openDetail" @edit="() => {}" @remove="() => {}" />
-->

<!-- VaccinationDetail.vue props interface (created in Plan 03) -->
<!--
  defineProps<{
    record: Vaccinations;
    token: string;
  }>()
  — Usage: <VaccinationDetail v-if="selectedRecord" :record="selectedRecord" :token="fileToken" />
-->

<!-- PocketBase token API -->
<!--
  pb.files.getToken(): Promise<string>  — short-lived (~2 min) file access token
  Must be called:
    - in onMounted after getFullList → stored in listToken (for thumbnail auth in the list)
    - in openDetail when record.card is truthy → stored in fileToken (for detail view auth)
  NEVER call pb.files.getToken() only once for both list and detail — they must be separate refs
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WallecxApp.vue — add state refs, openDetail function, and listToken to onMounted</name>
  <files>src/components/projects/wallecx/WallecxApp.vue</files>

  <read_first>
    - src/components/projects/wallecx/WallecxApp.vue — read the FULL current file before modifying; understand the exact current state: existing imports, STATE section, LOGIC section (onMounted), and template structure
    - src/components/projects/lextrack/ManageTask.vue — Dialog usage pattern (lines 27–66): v-model:visible, :style, modal prop, header prop
    - .planning/phases/02-read-path/02-RESEARCH.md — Pattern 2 (openDetail + fileToken), Pattern 6 (WallecxApp wiring template), Pitfall 1 resolution (listToken alongside getFullList)
    - .planning/phases/02-read-path/02-UI-SPEC.md — Section 1 (WallecxApp spec): exact template structure, Dialog props (width 40rem, breakpoints, @hide handler), toast copy, interaction flow
    - .planning/phases/02-read-path/02-PATTERNS.md — WallecxApp section: exact diffs — what to add and what to preserve
  </read_first>

  <action>
Modify `src/components/projects/wallecx/WallecxApp.vue` in place. Read the current file first to understand its exact current state, then apply the following changes.

**PRESERVE:** The existing import block, the `// --- STATE ---` and `// --- LOGIC ---` section comments, the existing `records` and `isLoading` refs, and the entire `<Card #content>` + `<h1>` header structure.

**CHANGE 1 — Extend the STATE section.** After `const isLoading = ref(false);`, add:

```typescript
const selectedRecord = ref<Vaccinations | null>(null);
const showDetail = ref(false);
const fileToken = ref<string>("");
const listToken = ref<string>("");
```

**CHANGE 2 — Extend onMounted.** After the `records.value = await pb...getFullList(...)` line succeeds (inside the try block, after the records assignment), add the listToken fetch:

```typescript
listToken.value = await pb.files.getToken();
```

This must be inside the try block so a token failure is caught by the existing catch handler. The existing `toast.error("Failed to load vaccination records.")` message is preserved for any fetch error including token generation failure.

**CHANGE 3 — Add openDetail function.** After the closing brace of the `onMounted` call (still in `// --- LOGIC ---`), add:

```typescript
async function openDetail(record: Vaccinations): Promise<void> {
  selectedRecord.value = record;
  if (record.card) {
    try {
      fileToken.value = await pb.files.getToken();
    } catch (e: unknown) {
      toast.error("Failed to load attachment. Refresh to try again.");
      console.error("WallecxApp: getToken failed", e);
    }
  }
  showDetail.value = true;
}
```

Note: `showDetail.value = true` is placed AFTER the try/catch — the dialog opens even if token generation fails (graceful degradation: user can still see text fields; attachment shows its error fallback).

**CHANGE 4 — Replace template content.** Remove the placeholder `<div>` containing the record count paragraph:

```html
<!-- REMOVE this: -->
<div>
  <p v-if="isLoading">Loading...</p>
  <p v-else>{{ records.length }} vaccination record{{ records.length === 1 ? "" : "s" }}</p>
</div>
```

Replace it with `<VaccinationList>` + the Dialog overlay:

```html
<VaccinationList
  :records="records"
  :is-loading="isLoading"
  :list-token="listToken"
  @view="openDetail"
  @edit="() => {}"
  @remove="() => {}"
/>

<Dialog
  v-model:visible="showDetail"
  modal
  header="Vaccination Record"
  :style="{ width: '40rem' }"
  :breakpoints="{ '960px': '75vw', '641px': '92vw' }"
  @hide="selectedRecord = null; fileToken = ''"
>
  <VaccinationDetail
    v-if="selectedRecord"
    :record="selectedRecord"
    :token="fileToken"
  />
</Dialog>
```

**CRITICAL constraints:**
- `listToken` is generated in `onMounted` alongside `getFullList` — this is the Pitfall 1 resolution so list thumbnails render (READ-07 intent preserved: it is fresh per page load, not stale embedded HTML)
- `fileToken` is generated in `openDetail` only when the dialog opens — this is the READ-07 core requirement (token at view time)
- `showDetail.value = true` must come AFTER the try/catch block in openDetail — the dialog still opens on token failure
- The `@hide` handler resets both `selectedRecord` and `fileToken` — use `selectedRecord = null; fileToken = ''` (without `.value` since this is an inline template expression; or use a dedicated `closeDetail` function if cleaner)
- The Dialog `header` is exactly `"Vaccination Record"` (per UI-SPEC copywriting contract)
- The Dialog `:style` is exactly `{ width: '40rem' }` (not `'40vw'` — per UI-SPEC Section 5)
- Do NOT import VaccinationList, VaccinationDetail, or Dialog from their paths — they are auto-resolved (PrimeVue via PrimeVueResolver, Wallecx components via unplugin-vue-components)
- The empty handler `@edit="() => {}"` and `@remove="() => {}"` are intentional stubs — Phase 3 will replace them
  </action>

  <verify>
    <automated>grep -n "listToken" src/components/projects/wallecx/WallecxApp.vue && grep -n "fileToken" src/components/projects/wallecx/WallecxApp.vue && grep -n "openDetail" src/components/projects/wallecx/WallecxApp.vue && grep -n "VaccinationList" src/components/projects/wallecx/WallecxApp.vue && grep -n "VaccinationDetail" src/components/projects/wallecx/WallecxApp.vue && grep -n "Vaccination Record" src/components/projects/wallecx/WallecxApp.vue && ! grep -n "records.length.*vaccination record" src/components/projects/wallecx/WallecxApp.vue && npm run type-check 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep "listToken" src/components/projects/wallecx/WallecxApp.vue` returns at least 3 hits (ref declaration, assignment in onMounted, :list-token binding in template)
    - `grep "fileToken" src/components/projects/wallecx/WallecxApp.vue` returns at least 3 hits (ref declaration, assignment in openDetail, :token binding in template)
    - `grep "selectedRecord" src/components/projects/wallecx/WallecxApp.vue` returns at least 4 hits (ref declaration, assignment in openDetail, v-if in template, @hide reset)
    - `grep "openDetail" src/components/projects/wallecx/WallecxApp.vue` returns at least 2 hits (function declaration + @view binding)
    - `grep "VaccinationList" src/components/projects/wallecx/WallecxApp.vue` returns a hit in the template
    - `grep "VaccinationDetail" src/components/projects/wallecx/WallecxApp.vue` returns a hit in the template
    - `grep "Vaccination Record" src/components/projects/wallecx/WallecxApp.vue` returns a hit (Dialog header)
    - `grep "width: '40rem'" src/components/projects/wallecx/WallecxApp.vue` returns a hit (Dialog style — not 40vw)
    - `grep "960px.*75vw" src/components/projects/wallecx/WallecxApp.vue` returns a hit (breakpoints configured)
    - `grep "pb.files.getToken" src/components/projects/wallecx/WallecxApp.vue` returns at least 2 hits (onMounted listToken + openDetail fileToken — separate calls)
    - `grep "records.length.*vaccination record" src/components/projects/wallecx/WallecxApp.vue` returns zero hits (placeholder removed)
    - `npm run type-check` exits 0
  </acceptance_criteria>

  <done>WallecxApp.vue has all four new refs (selectedRecord, showDetail, fileToken, listToken), openDetail function with try/catch and graceful degradation, listToken generated in onMounted, placeholder paragraph removed, VaccinationList and Dialog+VaccinationDetail in template. TypeScript checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PocketBase API → WallecxApp | getFullList and getToken make authenticated API calls; tokens flow into state refs and down to child components as props |
| WallecxApp → VaccinationList | listToken is passed as a prop; list thumbnails use it for protected file access |
| WallecxApp → VaccinationDetail | fileToken is passed as a prop; detail attachment preview uses it for protected file access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | `listToken` generated at list load | accept | The listToken is intentionally generated at page load (onMounted) to enable thumbnail rendering — this is the Pitfall 1 resolution. READ-07's intent (no stale embedded tokens) is preserved: the token is fresh per page navigation, not baked into static markup. Token scope is limited to the authenticated user's files. |
| T-02-04-02 | Information Disclosure | `fileToken` generated in openDetail | mitigate | fileToken is generated only when the detail dialog opens (view time) per READ-07; it is stored in a local Vue ref (not in localStorage, sessionStorage, or the DOM); it is cleared on `@hide` — minimising the window it exists in memory |
| T-02-04-03 | Denial of Service | Token fetch failure in openDetail | mitigate | A try/catch wraps `pb.files.getToken()` in openDetail; on failure, `toast.error("Failed to load attachment. Refresh to try again.")` is shown AND `showDetail.value = true` still executes — the dialog opens with text fields visible even when attachment preview is unavailable (graceful degradation) |
| T-02-04-04 | Information Disclosure | selectedRecord left in state after dialog close | mitigate | The `@hide` handler resets `selectedRecord = null` and `fileToken = ''` — no residual record data or token persists in component state after the dialog is dismissed |
| T-02-04-05 | Elevation of Privilege | @edit and @remove handlers active in Phase 2 | mitigate | Both handlers are bound as `() => {}` (no-op stubs); the VaccinationList component also marks those buttons as `:disabled="true"` — double protection; Phase 3 replaces stubs with validated write-path handlers |
</threat_model>

<verification>
After the task completes:

1. `npm run type-check` exits 0 — entire project type-checks cleanly with all new components
2. `grep "listToken" src/components/projects/wallecx/WallecxApp.vue` returns 3+ hits
3. `grep "pb.files.getToken" src/components/projects/wallecx/WallecxApp.vue` returns 2+ hits (separate list + detail calls)
4. `grep "VaccinationList" src/components/projects/wallecx/WallecxApp.vue` returns 1+ hits
5. `grep "VaccinationDetail" src/components/projects/wallecx/WallecxApp.vue` returns 1+ hits
6. `grep "records.length.*vaccination record" src/components/projects/wallecx/WallecxApp.vue` returns 0 hits (placeholder gone)

Full phase check:
7. `git grep "v-html" src/components/projects/wallecx` returns zero hits (READ-02 — no v-html in any Wallecx component)
8. `grep "worker-src 'self' blob:" index.html` returns 1 hit (READ-06)
9. `grep "script-src 'self'" index.html` returns 1 hit (script-src unchanged)
10. `grep "defineAsyncComponent" src/components/projects/wallecx/AttachmentPreview.vue` returns 1 hit (READ-03 lazy load)
</verification>

<success_criteria>
- WallecxApp.vue has listToken/fileToken/selectedRecord/showDetail refs, openDetail function with graceful degradation on token failure, listToken generated in onMounted, placeholder paragraph removed, VaccinationList wired with @view/edit/remove, Dialog overlay with VaccinationDetail inside
- `npm run type-check` passes
- `git grep "v-html" src/components/projects/wallecx` returns zero hits across all four Wallecx components
- `grep "worker-src 'self' blob:" index.html` returns 1 hit
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-path/02-04-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
