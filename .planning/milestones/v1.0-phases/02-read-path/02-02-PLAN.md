---
phase: 02-read-path
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/wallecx/VaccinationList.vue
autonomous: true
requirements:
  - READ-01
  - READ-04
  - READ-05

must_haves:
  truths:
    - "During data fetch, the DataTable area shows 3 skeleton rows across all 5 columns — no layout shift"
    - "When the user has zero records, a centered needle-off icon and 'No vaccination records yet.' message appear"
    - "When the user has records, a striped DataTable shows vaccine name, formatted date, dose number, and a thumbnail or placeholder icon per row"
    - "Records with an attached card show a 48x48 thumbnail served at 100x100 from PocketBase using the listToken"
    - "Records without an attached card show an mdi:image-off icon in the thumbnail column"
    - "The View Record button emits 'view' with the row's record; Edit and Remove emit their events but are disabled"
    - "The component accepts records, isLoading, and listToken props; emits view, edit, remove"
  artifacts:
    - path: "src/components/projects/wallecx/VaccinationList.vue"
      provides: "DataTable list with skeleton, empty, and data states; emits view/edit/remove"
      min_lines: 80
  key_links:
    - from: "VaccinationList.vue"
      to: "Vaccinations[]"
      via: "defineProps<{ records: Vaccinations[] }>"
      pattern: "defineProps"
    - from: "VaccinationList.vue"
      to: "pb.files.getURL"
      via: "thumbUrl(data) inside #body slot"
      pattern: "pb\\.files\\.getURL"
    - from: "VaccinationList.vue"
      to: "parent (WallecxApp.vue)"
      via: "defineEmits view/edit/remove"
      pattern: "defineEmits"
---

<objective>
Create `VaccinationList.vue` — the date-sorted vaccination list component with three states: skeleton loading, empty state, and data state.

Purpose: Provides the primary read surface. The skeleton state (READ-05) prevents any empty-state flash before data arrives. The data state (READ-01) shows the user their vaccination records with thumbnails. The empty state (READ-04) handles the zero-records case gracefully. This component is Wave 1 — it has no dependency on other new Phase 2 files, only on existing types and the pb singleton.

Output:
- `src/components/projects/wallecx/VaccinationList.vue` — accepts `records: Vaccinations[]`, `isLoading: boolean`, `listToken: string`; emits `view`, `edit`, `remove`; renders DataTable in three states
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-read-path/02-CONTEXT.md
@.planning/phases/02-read-path/02-RESEARCH.md
@.planning/phases/02-read-path/02-PATTERNS.md
@.planning/phases/02-read-path/02-UI-SPEC.md

<interfaces>
<!-- From src/types/wallecx/vaccinations/types.d.ts -->
<!--
  interface Vaccinations extends RecordModel {
    user: string
    vaccine_name: string
    date_administered: string   // ISO date string — format as "DD MMM YYYY" in list
    dose_number?: number        // optional number — display as record.dose_number ?? '—'
    lot_number: string
    location: string
    manufacturer: string
    notes: string
    card: string                // plain filename; empty string when no file attached
  }
-->

<!-- From src/lib/pocketbase/index.ts -->
<!--
  pb.files.getURL(record, filename, options): string
    options.thumb?: string  — e.g. "100x100"
    options.token?: string  — short-lived file access token
  Combined for list thumbnail:
    pb.files.getURL(data, data.card, { thumb: "100x100", token: listToken })
-->

<!-- From src/components/projects/lextrack/ActivityCard.vue — emit pattern analog -->
<!--
  const emit = defineEmits<{
    update: [index: number]
    remove: [index: number]
  }>();
  // VaccinationList adaptation: payload is record, not index
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VaccinationList.vue — three-state DataTable component</name>
  <files>src/components/projects/wallecx/VaccinationList.vue</files>

  <read_first>
    - src/types/wallecx/vaccinations/types.d.ts — confirm all Vaccinations fields; particularly `card: string` (not nullable), `dose_number?: number` (optional)
    - src/lib/pocketbase/index.ts — confirm `pb` export path for import
    - src/components/projects/lextrack/ActivityCard.vue — emit pattern and iconify-icon usage (lines 44–49 for icon, lines 94–128 for emit helpers)
    - .planning/phases/02-read-path/02-RESEARCH.md — Pattern 1 (DataTable skeleton), Pitfall 6 (empty skeletonRows), MIME-sniff is NOT needed here (thumbnails only)
    - .planning/phases/02-read-path/02-UI-SPEC.md — Section 2 (VaccinationList spec): column widths, skeleton config per column, empty-state copy, button labels, striped-rows
  </read_first>

  <action>
Create `src/components/projects/wallecx/VaccinationList.vue` as a new file with `<script setup lang="ts">`.

Do NOT import PrimeVue components (DataTable, Column, Skeleton, Button) — they are auto-imported via PrimeVueResolver. Do NOT import iconify-icon — it is a custom element registered globally.

**`<script setup lang="ts">`:**

```typescript
import dayjs from "dayjs";
import { pb } from "@/lib/pocketbase";
import type { Vaccinations } from "@/types/wallecx/vaccinations/types";

const props = defineProps<{
  records: Vaccinations[];
  isLoading: boolean;
  listToken: string;
}>();

const emit = defineEmits<{
  view: [record: Vaccinations];
  edit: [record: Vaccinations];
  remove: [record: Vaccinations];
}>();

// Skeleton placeholder array — must have length > 0 or DataTable renders no body rows (Pitfall 6)
const skeletonRows = Array.from({ length: 3 }, (_, i) => ({ id: String(i) }));

function thumbUrl(record: Vaccinations): string {
  return pb.files.getURL(record, record.card, { thumb: "100x100", token: props.listToken });
}

function displayDate(iso: string): string {
  return dayjs(iso).format("DD MMM YYYY");
}
```

**`<template>` — three mutually exclusive branches:**

**Branch 1: Loading state** (`v-if="isLoading"`)

DataTable fed `skeletonRows` with Skeleton in every `#body` slot. Column widths from UI-SPEC:

```html
<DataTable v-if="isLoading" :value="skeletonRows" table-style="min-width: 30rem">
  <Column header="Card" style="width: 4rem">
    <template #body>
      <Skeleton shape="rectangle" width="3rem" height="3rem" />
    </template>
  </Column>
  <Column header="Vaccine">
    <template #body><Skeleton /></template>
  </Column>
  <Column header="Date" style="width: 8rem">
    <template #body><Skeleton /></template>
  </Column>
  <Column header="Dose" style="width: 4rem">
    <template #body><Skeleton /></template>
  </Column>
  <Column header="" style="width: 10rem">
    <template #body><Skeleton width="8rem" /></template>
  </Column>
</DataTable>
```

**Branch 2: Empty state** (`v-else-if="records.length === 0"`)

Rendered as a standalone `<div>`, NOT inside a DataTable:

```html
<div v-else-if="records.length === 0" class="flex flex-col items-center py-12 gap-3">
  <iconify-icon
    icon="mdi:needle-off"
    width="48"
    height="48"
    style="color: var(--color-brand-primary)"
  ></iconify-icon>
  <p class="text-sm" style="color: var(--color-typo-heading)">No vaccination records yet.</p>
</div>
```

**Branch 3: Data state** (`v-else`)

DataTable with `striped-rows` prop and real `records` as `:value`:

```html
<DataTable v-else :value="records" table-style="min-width: 30rem" striped-rows>
  <Column header="Card" style="width: 4rem">
    <template #body="{ data }">
      <img
        v-if="data.card"
        :src="thumbUrl(data)"
        :alt="`${data.vaccine_name} vaccination card thumbnail`"
        class="w-12 h-12 object-cover rounded"
      />
      <iconify-icon
        v-else
        icon="mdi:image-off"
        width="32"
        height="32"
        style="color: var(--color-typo-muted)"
      ></iconify-icon>
    </template>
  </Column>
  <Column field="vaccine_name" header="Vaccine" />
  <Column header="Date" style="width: 8rem">
    <template #body="{ data }">{{ displayDate(data.date_administered) }}</template>
  </Column>
  <Column header="Dose" style="width: 5rem">
    <template #body="{ data }">{{ data.dose_number ?? '—' }}</template>
  </Column>
  <Column header="" style="width: 10rem">
    <template #body="{ data }">
      <div class="flex gap-1">
        <Button size="small" label="View Record" @click="emit('view', data)" />
        <Button
          size="small"
          severity="secondary"
          label="Edit"
          :disabled="true"
          @click="emit('edit', data)"
        />
        <Button
          size="small"
          severity="danger"
          label="Remove"
          :disabled="true"
          @click="emit('remove', data)"
        />
      </div>
    </template>
  </Column>
</DataTable>
```

**CRITICAL constraints:**
- The `skeletonRows` array MUST have length 3 — `Array.from({ length: 3 }, ...)` with dummy objects containing at least an `id` field
- The data-state DataTable MUST include `striped-rows` prop (per UI-SPEC)
- Button label for view action is `"View Record"` (not "View") — per UI-SPEC copywriting contract
- Edit and Remove buttons MUST have `:disabled="true"` — write-path is Phase 3
- `thumbUrl(data)` is called only when `data.card` is truthy — the `v-if="data.card"` guard is MANDATORY
- NEVER use `v-html`
- Do NOT import DataTable, Column, Skeleton, Button from PrimeVue — they are auto-imported
  </action>

  <verify>
    <automated>grep -n "striped-rows" src/components/projects/wallecx/VaccinationList.vue && grep -n "skeletonRows" src/components/projects/wallecx/VaccinationList.vue && grep -n "mdi:needle-off" src/components/projects/wallecx/VaccinationList.vue && grep -n "defineEmits" src/components/projects/wallecx/VaccinationList.vue && ! grep -n "v-html" src/components/projects/wallecx/VaccinationList.vue && npm run type-check 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `src/components/projects/wallecx/VaccinationList.vue`
    - `grep "striped-rows" src/components/projects/wallecx/VaccinationList.vue` returns a hit (data-state DataTable has striped-rows)
    - `grep "skeletonRows" src/components/projects/wallecx/VaccinationList.vue` returns at least 2 hits (declaration + template usage)
    - `grep "Array.from" src/components/projects/wallecx/VaccinationList.vue` returns a hit with `length: 3`
    - `grep "mdi:needle-off" src/components/projects/wallecx/VaccinationList.vue` returns a hit (empty state icon)
    - `grep "No vaccination records yet." src/components/projects/wallecx/VaccinationList.vue` returns a hit (exact empty-state copy)
    - `grep "View Record" src/components/projects/wallecx/VaccinationList.vue` returns a hit (correct button label)
    - `grep "defineEmits" src/components/projects/wallecx/VaccinationList.vue` returns a hit with view/edit/remove declared
    - `grep ":disabled=\"true\"" src/components/projects/wallecx/VaccinationList.vue` returns 2 hits (Edit and Remove buttons)
    - `grep "v-html" src/components/projects/wallecx/VaccinationList.vue` returns zero hits
    - `grep "import DataTable\|import Column\|import Skeleton\|import Button" src/components/projects/wallecx/VaccinationList.vue` returns zero hits (no manual PrimeVue imports)
    - `npm run type-check` exits 0
  </acceptance_criteria>

  <done>VaccinationList.vue exists with three DataTable states (skeleton/empty/data), striped-rows on data state, skeletonRows array of length 3, mdi:needle-off empty icon, View Record/Edit/Remove buttons, and zero v-html usage. TypeScript checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PocketBase API → Browser | `getFullList` returns user-owned records; thumbnail URLs include short-lived listToken |
| User data → Template rendering | vaccine_name, lot_number, and other string fields are rendered into the DataTable — must never use v-html |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | `VaccinationList.vue` — user string fields in DataTable | mitigate | All field rendering uses `{{ }}` mustache interpolation via `field` prop or `#body` slots — no `v-html`; verified by grep returning zero hits |
| T-02-02-02 | Information Disclosure | Thumbnail URL in `<img :src>` | mitigate | `thumbUrl()` always includes `{ thumb: "100x100", token: props.listToken }` — the token is short-lived and per-page-load; without it the 100x100 thumb returns 403 from PocketBase |
| T-02-02-03 | Information Disclosure | `v-if="data.card"` missing — empty-string URL in img src | mitigate | The `v-if="data.card"` guard on the `<img>` tag prevents passing an empty filename to `pb.files.getURL()`, which would generate a malformed URL pointing at an unintended resource |
| T-02-02-04 | Elevation of Privilege | Edit/Remove buttons enabled prematurely | mitigate | Both buttons carry `:disabled="true"` — Phase 3 will remove this flag only after the write path and its validation/confirm-dialog are implemented |
</threat_model>

<verification>
After the task completes:

1. `npm run type-check` exits 0
2. `grep "striped-rows" src/components/projects/wallecx/VaccinationList.vue` returns a hit
3. `grep "mdi:needle-off" src/components/projects/wallecx/VaccinationList.vue` returns a hit
4. `grep "v-html" src/components/projects/wallecx/VaccinationList.vue` returns zero hits
5. `grep "defineEmits" src/components/projects/wallecx/VaccinationList.vue` returns a hit
6. `grep "Array.from.*length.*3" src/components/projects/wallecx/VaccinationList.vue` returns a hit
</verification>

<success_criteria>
- `VaccinationList.vue` exists with three-state DataTable (isLoading skeleton → records.length === 0 empty → data), striped-rows on data state, 3-row skeleton array, mdi:needle-off empty icon, correct button labels, disabled Edit/Remove, no v-html
- `npm run type-check` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-read-path/02-02-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
