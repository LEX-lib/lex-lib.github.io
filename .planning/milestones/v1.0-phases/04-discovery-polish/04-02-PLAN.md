---
phase: 04-discovery-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/ProjectsView.vue
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
must_haves:
  truths:
    - "The projects directory page shows a Wallecx tile with navy/amber gradient (automatic from scoped CSS)"
    - "Clicking the Wallecx tile navigates to /projects/wallecx"
    - "The tile status tag reads 'WIP' and renders the 'WIP' PrimeVue Tag component"
    - "No Wallecx component or WallecxApp.vue template contains a bespoke hex color or raw rgb/hsl value outside of design token references"
    - "The design token audit is documented in 04-CHECKLIST.md with pass/fail evidence"
  artifacts:
    - path: "src/views/ProjectsView.vue"
      provides: "Wallecx entry as the 5th element in the projects ref array"
      contains: "Wallecx"
    - path: ".planning/phases/04-discovery-polish/04-CHECKLIST.md"
      provides: "POLISH-02 token audit results and checklist foundation for POLISH-05"
      contains: "POLISH-02"
  key_links:
    - from: "ProjectsView.vue projects array"
      to: "/projects/wallecx route"
      via: "link: '/projects/wallecx' in the Wallecx entry object"
      pattern: "link.*projects/wallecx"
---

<objective>
Add the Wallecx entry to the ProjectsView.vue projects array (POLISH-01) and run the design token audit to confirm no bespoke colors or CSS variables appear in Wallecx components (POLISH-02). Document the audit results in the phase checklist file, which also starts the POLISH-05 checklist foundation.

Purpose: Make Wallecx discoverable from the Lexarium projects hub. Confirm the visual language is consistent with the design system before any user-facing review.

Output: One line added to src/views/ProjectsView.vue (the Wallecx array entry); the 04-CHECKLIST.md file with POLISH-02 audit findings.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-discovery-polish/04-RESEARCH.md
@.planning/phases/04-discovery-polish/04-PATTERNS.md

<interfaces>
<!-- Key types and layout of ProjectsView.vue extracted from codebase during planning. -->

From src/views/ProjectsView.vue (read directly during research):
```typescript
// Project interface (lines 4–11):
interface Project {
  title: string;
  description: string;
  link: string;
  status: "WIP" | "Active";   // TypeScript union — case-sensitive; "wip" will NOT type-check
  icon: string;               // Iconify icon name e.g. "mdi:bus-side"
  tags: string[];
}

// Existing projects array (4 entries):
const projects = ref<Project[]>([
  { title: "Larga", ..., status: "WIP", icon: "mdi:bus-side", ... },
  { title: "LexTrack", ..., status: "WIP", ... },
  { title: "MonitoX (Gift Exchange)", ..., status: "Active", ... },
  { title: "API Playground", ..., status: "Active", ... },
]);
```

The navy/amber gradient is scoped CSS on ALL cards — not per-tile configuration.
The `v-if="project.status === 'WIP'"` check at the template level renders the WIP Tag — status string is exact-case.

Design token inventory from src/assets/base.css (complete set — 16 tokens):
--color-brand-primary, --color-brand-primary-dark, --color-brand-accent, --color-brand-accent-light,
--color-surface-page, --color-surface-card, --color-surface-code, --color-surface-divider,
--color-typo-heading, --color-typo-body, --color-typo-muted, --color-typo-link,
--color-status-success, --color-status-warning, --color-status-error, --color-status-info

Any value outside this set + PrimeVue Aura preset + Tailwind CSS 4 utilities is bespoke.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wallecx entry to ProjectsView.vue</name>
  <files>src/views/ProjectsView.vue</files>
  <read_first>
    - src/views/ProjectsView.vue — read the full file; locate the `projects` ref array closing bracket (after the 4th entry) where the new entry will be appended
  </read_first>
  <action>
Open src/views/ProjectsView.vue and locate the `projects` ref array. The array currently has four entries: Larga, LexTrack, MonitoX (Gift Exchange), and API Playground. Append Wallecx as the 5th element, immediately before the closing `]);` of the array:

```typescript
  {
    title: "Wallecx",
    description:
      "A personal health records vault. Securely store and retrieve your vaccination records — including card scans — with per-user privacy enforced server-side.",
    link: "/projects/wallecx",
    status: "WIP",
    icon: "mdi:shield-check",
    tags: ["Vue 3", "PocketBase", "Auth", "Privacy"],
  },
```

Field contract compliance:
- `title`: string — "Wallecx"
- `description`: string — health vault description (matches POLISH-01 requirement wording)
- `link`: string — "/projects/wallecx" (matches the route registered in src/router/index.ts)
- `status`: "WIP" — exact case required; TypeScript will reject "wip" at compile time
- `icon`: "mdi:shield-check" — Iconify MDI icon; verify renders if Iconify is loaded
- `tags`: string[] — four descriptive tags matching the tech stack

Do NOT add any extra fields. The Project interface has exactly 6 fields — adding any 7th field is a TypeScript error.
Do NOT modify the template, styles, or any other part of the file.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    Additionally:
    - `grep "Wallecx" src/views/ProjectsView.vue` returns a match
    - `grep "mdi:shield-check" src/views/ProjectsView.vue` returns a match
    - `grep "projects/wallecx" src/views/ProjectsView.vue` returns a match
  </verify>
  <done>
    - ProjectsView.vue projects array has exactly 5 entries; Wallecx is the 5th
    - `npm run type-check` exits 0 (status: "WIP" is a valid union member; all 6 fields present)
    - `npm run build` succeeds (no type errors, no import errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Run design token audit and produce 04-CHECKLIST.md</name>
  <files>.planning/phases/04-discovery-polish/04-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/04-discovery-polish/04-RESEARCH.md — section "Pattern 2: Design Token Inventory" for the complete token list and the audit grep command
    - .planning/phases/04-discovery-polish/04-PATTERNS.md — section "POLISH-05 Checklist Pre-Assessment" for the pre-assessed status of all 19 checklist items
    - .planning/phases/03-write-path/03-REVIEW.md — for code review items that are addressed in 04-01-PLAN (reference as "Fixed in 04-01")
    - .planning/phases/02-read-path/02-REVIEW.md — for WR-01/02/03 items also addressed in 04-01-PLAN
  </read_first>
  <action>
Run the design token audit grep command against all Wallecx .vue files:

```bash
grep -rn "#[0-9a-fA-F]\{3,8\}\|rgb(\|hsl(\|color:\s*[a-z]" \
  src/components/projects/wallecx/ \
  --include="*.vue"
```

Legitimate exceptions (do NOT flag these):
- `style="color: var(--color-...)"` — uses design tokens, not bespoke values
- Hex values in COMMENTS only (e.g., `/* #002244 */`) — comments, not rendered CSS
- Any value that is one of the 16 design token hex equivalents but written inline is STILL a violation — must use the var() reference

Interpret the output:
- Zero hits = PASS (all color references use design tokens or Tailwind utilities)
- Any hit requires inspection: if it is a raw hex or rgb outside a comment → FAIL; add to violations list

Also run:
```bash
grep -rn "v-html" src/components/projects/wallecx/
grep -rn "\`.*filter\|\.filter\b" src/components/projects/wallecx/ --include="*.vue"
npm run build && grep -r "VITE_LOGIN_" dist/ 2>/dev/null || echo "CLEAN"
```

Then create `.planning/phases/04-discovery-polish/04-CHECKLIST.md` with the following structure. Fill in the actual grep results from running the commands above. Mark each item SIGNED OFF (with evidence) or NEEDS FIX (with what to do).

```markdown
# Phase 4: Discovery & Polish — "Looks Done But Isn't" Checklist

**Run by:** Claude executor (04-02-PLAN.md Task 2)
**Date:** [today's date]
**Status:** [PASS / PARTIAL / FAIL — update after all items signed off]

---

## POLISH-02: Design Token Audit

**Audit command:**
\`\`\`bash
grep -rn "#[0-9a-fA-F]{3,8}|rgb\(|hsl\(|color:\s*[a-z]" \
  src/components/projects/wallecx/ --include="*.vue"
\`\`\`

**Result:** [paste actual grep output or "No matches (exit 1)"]

**Verdict:** [PASS — no bespoke colors found] or [FAIL — violations listed below]

**Violations (if any):**
- [file:line — value — action needed]

---

## PITFALLS.md "Looks Done But Isn't" — All 19 Items

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | Per-user isolation: user B can't see user A's records | SIGNED OFF | Phase 1 BACK-03 verified; two-user smoke test passed (01-02-PLAN) |
| 2 | Filter injection: vaccine_name with `"`, `\`, `||` saves + retrieves correctly | SIGNED OFF | WRITE-08 implemented; no template-literal filter strings in codebase |
| 3 | File access without token: incognito returns 403 | SIGNED OFF | Phase 1 BACK-02 protected:true; verified in 01-02 smoke test |
| 4 | Save round-trip: create, edit, save → one record on server | SIGNED OFF | WRITE-04 Object.assign id-refresh; vaccinationMapper.spec.ts covers contract |
| 5 | Delete actually deletes: getOne after delete → 404 + file URL 404 | SIGNED OFF | WRITE-06 server-first delete; confirmed in Phase 3 UAT |
| 6 | EXIF stripped: exiftool shows no GPS after upload | SIGNED OFF | WRITE-03 canvas re-encode; confirmed in Phase 3 UAT |
| 7 | pdfjs-dist ≥ 4.2.67 in package.json | SIGNED OFF | FRONT-01; `grep "pdfjs-dist" package.json` shows version |
| 8 | CSP not regressed: only worker-src added, script-src unchanged | SIGNED OFF | 02-01-PLAN; index.html CSP verified |
| 9 | No v-html in Wallecx: git grep returns nothing | [VERIFY — run grep and paste result] | `grep -rn "v-html" src/components/projects/wallecx/` |
| 10 | No template-literal filters: git grep returns nothing | [VERIFY — run grep and paste result] | `grep -rn "\`" src/components/projects/wallecx/` + inspect for filter strings |
| 11 | Watcher fires on mount: no empty-state flash | SIGNED OFF | onMounted fetch in WallecxApp.vue; READ-05 implemented |
| 12 | Save button disables during save: double-click → one record | SIGNED OFF | WRITE-07 isSaving; both form and button disabled during in-flight request |
| 13 | Auth token not in production bundle | [VERIFY — run: npm run build && grep -r VITE_LOGIN_ dist/] | Phase 0 CLEAN-01..03 implemented; expect CLEAN |
| 14 | Mapper test: vaccinationMapper.spec.ts passes | SIGNED OFF | npm run test:unit exits 0; 10 tests passing |
| 15 | Route guard test: guard.spec.ts covers wallecx redirect | OPEN — 04-04-PLAN | To be implemented in Wave 2 |
| 16 | Data-export feature works | OPEN — 04-03-PLAN | To be implemented in Wave 1 |
| 17 | Subcomponent names: no Wallecx component collides with PrimeVue | SIGNED OFF | components.d.ts confirmed: WallecxApp, ManageVaccination, VaccinationList, VaccinationDetail, AttachmentPreview — all distinct from PrimeVue component names |
| 18 | Vercel/GitHub Pages deploy: /projects/wallecx hard-refresh resolves | SIGNED OFF | dist/404.html SPA rewrite; build script `cp dist/index.html dist/404.html` confirmed |
| 19 | Speed Insights gated: v-if PROD | SIGNED OFF | Fixed in 04-01-PLAN Task 2; `<SpeedInsights v-if="import.meta.env.PROD" />` |

---

## Open Items Before Final Sign-Off

- Item 15 (Route guard test): implemented in 04-04-PLAN — update this checklist after 04-04 executes
- Item 16 (JSON export): implemented in 04-03-PLAN — update this checklist after 04-03 executes
- Items 9, 10, 13: fill in actual grep results from running commands above

---

*Checklist finalized when all 19 items show SIGNED OFF.*
```

Update items 9, 10, and 13 with the actual grep results from running the commands. If any violations are found in the POLISH-02 audit, do NOT silently ignore them — list them under the Violations section and note "NEEDS FIX in followup task."
  </action>
  <verify>
    <automated>
      test -f .planning/phases/04-discovery-polish/04-CHECKLIST.md
    </automated>
    Additionally confirm:
    - The checklist file contains all 19 items from the PITFALLS.md list
    - Items 1–8, 11–14, 17–19 are marked SIGNED OFF with evidence
    - Items 15, 16 are marked OPEN with the plan reference
    - The POLISH-02 audit grep result is pasted (not left as placeholder)
  </verify>
  <done>
    - 04-CHECKLIST.md exists with all 19 checklist items
    - POLISH-02 audit results are documented with actual grep output (not placeholder)
    - Items with known status from prior phases are signed off with evidence strings
    - Items pending Phase 4 execution (15, 16) are marked OPEN with plan reference
    - Items requiring runtime verification (9, 10, 13) show the actual command output
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProjectsView.vue → router | projects array `link` field is navigated to by the Vue Router; only relative paths to known routes are used |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Design token audit | accept | Audit is a static grep; no runtime data involved; output is a planning document not served to users |
| T-04-02-02 | Tampering | ProjectsView.vue projects array | accept | Array is static compile-time data; no user input involved; no injection vector |
</threat_model>

<verification>
After both tasks complete:

1. `npm run type-check` exits 0
2. `grep "Wallecx" src/views/ProjectsView.vue` returns a match
3. `grep "status.*WIP" src/views/ProjectsView.vue` returns a match (for Wallecx entry)
4. `grep "projects/wallecx" src/views/ProjectsView.vue` returns a match
5. `.planning/phases/04-discovery-polish/04-CHECKLIST.md` exists with 19 checklist items
6. POLISH-02 audit section shows actual grep output (not placeholder text)
7. `npm run test:unit` exits 0 (10 existing tests; no regressions from 04-01 which runs in parallel)
</verification>

<success_criteria>
- POLISH-01: Wallecx tile appears as the 5th entry in ProjectsView.vue projects array with correct shape (6 fields, status "WIP", link "/projects/wallecx")
- POLISH-02: Design token audit run and documented — zero violations found or violations listed with fix actions
- 04-CHECKLIST.md created as the master sign-off document for POLISH-05 (to be completed by 04-04-PLAN)
- `npm run type-check` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-discovery-polish/04-02-SUMMARY.md`
</output>
