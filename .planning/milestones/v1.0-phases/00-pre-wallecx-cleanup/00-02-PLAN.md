---
phase: 00-pre-wallecx-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - CLEAN-02

must_haves:
  truths:
    - "The developer has confirmed that credentials stored in local.jsonc and any .env* files have been rotated out-of-band"
    - "local.jsonc is not modified as part of this phase's code changes"
  artifacts: []
  key_links: []
---

<objective>
Confirm that any credentials previously stored in `local.jsonc` and `.env*` files have been rotated out-of-band by the developer. This is a human-action checkpoint — no code change is made. The phase is not complete until the developer confirms rotation.

Purpose: CLEAN-02 requires credential rotation as a separate, non-automated step. The risk register (Pitfall 13, CRITICAL operational) flags plaintext credentials in `local.jsonc` as a critical issue. Rotation must happen before the Wallecx health-data surface exists.
Output: Developer's written confirmation that credentials are rotated.
</objective>

<execution_context>
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/00-pre-wallecx-cleanup/00-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm credential rotation out-of-band</name>
  <read_first>
    - .planning/phases/00-pre-wallecx-cleanup/00-CONTEXT.md — re-read the D-02 decision and CLEAN-02 requirement to understand what rotation means for this phase
  </read_first>
  <what-to-do>
    Rotate the credentials that previously existed in `local.jsonc` and any `.env*` files (e.g., `VITE_LOGIN_EMAIL` / `VITE_LOGIN_PASSWORD` values) by:

    1. Logging in to the relevant service (e.g., PocketBase admin panel or whichever identity provider these credentials belonged to).
    2. Changing the password / revoking the account token so the old credential values are no longer valid.
    3. Updating `local.jsonc` and/or `.env.local` with the new credentials if you still need them for local development.

    Important constraints (per D-02):
    - `local.jsonc` is gitignored — do NOT commit it.
    - You do NOT need to delete or blank out `local.jsonc` — the file content cleanup is not in scope.
    - This rotation is entirely out-of-band; no code changes are required or expected.
  </what-to-do>
  <how-to-verify>
    After rotating, verify the old credentials no longer work:
    1. Attempt to sign in to the relevant service (PocketBase admin / app login) using the old email/password combination.
    2. Confirm the login fails (401 / "Invalid credentials" response).
    3. Confirm login still works with the new credentials via `npm run dev` → navigate to `/login` → sign in.
  </how-to-verify>
  <resume-signal>
    Type "credentials rotated" or describe what was rotated (e.g., "PocketBase user password changed; local.jsonc updated with new password") to continue.

    If credentials were already rotated prior to this phase or the local.jsonc values were never valid production credentials, type "confirmed — not applicable" with a brief explanation.
  </resume-signal>
  <acceptance_criteria>
    - Developer has provided written confirmation in the resume signal
    - The response either confirms rotation was performed OR explains why rotation is not applicable
    - No `local.jsonc` changes are committed to git (file must remain gitignored and untracked)
  </acceptance_criteria>
  <done>Developer has confirmed in writing that credentials from local.jsonc/.env* are either rotated or confirmed not applicable. Phase 0 CLEAN-02 requirement is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local.jsonc → developer machine | Contains plaintext credentials; gitignored so never reaches the remote, but rotation is required to invalidate any credential that may have been observed |
| Old credentials → authentication service | If old credentials remain valid, any party who observed them (e.g., from a local.jsonc copy) could still authenticate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04 | Information Disclosure | local.jsonc plaintext credentials | mitigate | Rotate credentials so observed values become invalid; this checkpoint confirms that mitigation has been performed |
| T-00-05 | Elevation of Privilege | Old credential still valid after cleanup | mitigate | Rotation (this task) invalidates the credential at the identity provider level, not just at the file level |
</threat_model>

<verification>
This plan has no automated verification — it is a human confirmation checkpoint.

The verification is the developer's written confirmation in the resume signal:
- "credentials rotated" — explicit confirmation
- "confirmed — not applicable" + explanation — acceptable if credentials were never real or already rotated

Do NOT mark Phase 0 as complete until this confirmation is received and recorded in the summary.
</verification>

<success_criteria>
- Developer has confirmed (in writing) that credentials previously in local.jsonc / .env* are rotated or confirmed not applicable
- No credential values appear in any committed file (local.jsonc remains gitignored and untracked)
- The login flow still works after rotation (developer confirmed via manual smoke test per D-03)
</success_criteria>

<output>
After the developer provides the resume signal, create `.planning/phases/00-pre-wallecx-cleanup/00-02-SUMMARY.md` using the summary template at `@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/templates/summary.md`. Record the developer's confirmation statement verbatim in the summary.
</output>
