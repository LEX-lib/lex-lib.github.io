---
phase: 31-recreate-budgets-collection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [BUG-01]
requirements_addressed: [BUG-01]
tags: [pocketbase, admin-ui, schema, budget, recovery, smoke-verify]
user_setup:
  - service: pocketbase
    why: "wallecx_expense_budgets collection missing in production PB — Phase 28-01 trust-based checkpoint never landed; re-create it manually."
    dashboard_config:
      - task: "Re-create collection wallecx_expense_budgets with the locked 4 fields + 5 per-user rules"
        location: "PocketBase Admin UI → Collections → New collection (Base)"

must_haves:
  truths:
    - "PocketBase Admin UI shows a collection named exactly wallecx_expense_budgets (lowercase, underscores)"
    - "Collection has 4 fields with correct types/constraints: user (Relation→users required max=1 cascade=false), category (Text required min=1 max=200), budget_type (Text required pattern ^(monthly|yearly)$), amount (Number required min=0)"
    - "Collection has all 5 API rules — list/view/update/delete = `user = @request.auth.id`; create = `@request.auth.id != \"\" && @request.body.user = @request.auth.id` (v0.29.3 @request.body.user syntax)"
    - "pb.collection('wallecx_expense_budgets').getList({ page: 1, perPage: 1 }) from the live Wallecx app (DevTools console) returns 200 OK with items: [] (NOT 404 Missing collection context)"
    - "Refreshing the Expenses tab no longer fires any error toast on mount"
  artifacts:
    - path: ".planning/phases/31-recreate-budgets-collection/31-01-SUMMARY.md"
      provides: "Pre-flight 404 evidence + paste-back schema/rules + 200+empty smoke evidence + no-toast confirmation + Phase 28-01 root-cause cross-ref"
      contains: "Pre-flight evidence"
    - path: "live:pocketbase://collections/wallecx_expense_budgets"
      provides: "Live PocketBase collection with locked schema + rules"
      contains: "4 fields + 5 rules"
  key_links:
    - from: "src/components/projects/wallecx/ExpensesTab.vue"
      to: "wallecx_expense_budgets collection"
      via: "pb.collection('wallecx_expense_budgets').getFullList in onMounted (line ~71)"
      pattern: "wallecx_expense_budgets"
    - from: "DevTools console one-liner"
      to: "src/lib/pocketbase/index.ts pb singleton"
      via: "dynamic import of /src/lib/pocketbase/index.ts"
      pattern: "import.*pocketbase"
---

<objective>
Land the locked Phase 28-01 `wallecx_expense_budgets` schema into the production PocketBase instance, then verify with a code-side smoke read that the live Wallecx app can `getList` from it without the `404 Missing collection context` error. Closes BUG-01.

Purpose: Phase 28-01 Task 1 was a trust-based human-action checkpoint that silently no-op'd — the production PocketBase never received the collection, causing `ExpensesTab.vue` onMounted to throw and fire `'Failed to load expenses...'` on every Expenses tab mount. Phase 31 lands the same schema with a STRUCTURAL paste-back gate so the silent-no-op failure mode cannot recur.

Output: (1) The live `wallecx_expense_budgets` PocketBase collection with the locked schema/rules; (2) `31-01-SUMMARY.md` containing pre-flight 404 evidence + paste-back schema/rules + 200-empty smoke evidence + no-toast confirmation + Phase 28-01 root-cause cross-reference; (3) NEW project-level architectural invariant injected into `.planning/STATE.md`.

Phase 31 makes ZERO code modifications. `ExpensesTab.vue` is NOT touched here — that's Phase 32 (BUG-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-recreate-budgets-collection/31-CONTEXT.md
@.planning/milestones/v4.1-phases/28-budget-tracking/28-01-PLAN.md
@src/components/projects/wallecx/ExpensesTab.vue
@src/lib/pocketbase/index.ts

<interfaces>
<!-- Locked schema source — verbatim from .planning/milestones/v4.1-phases/28-budget-tracking/28-01-PLAN.md §Task 1 -->
<!-- Executor SHOULD diff Task 2 paste-back against THIS block, not by re-reading 28-01-PLAN.md -->

Collection name (exact, case-sensitive): `wallecx_expense_budgets`
Base type: **Base** collection (NOT auth, NOT view)

Fields (in this order):
| # | Name        | Type      | Required | Constraints                                                        |
|---|-------------|-----------|----------|--------------------------------------------------------------------|
| 1 | user        | Relation  | true     | related collection = `users`, max select = 1, cascade delete = false |
| 2 | category    | Text      | true     | min = 1, max = 200                                                 |
| 3 | budget_type | Text      | true     | pattern = `^(monthly\|yearly)$`                                    |
| 4 | amount      | Number    | true     | min = 0                                                            |

API Rules (exact text — PocketBase v0.29.3 syntax; `@request.body.user` NOT `@request.data.user`):
```
List rule:   user = @request.auth.id
View rule:   user = @request.auth.id
Create rule: @request.auth.id != "" && @request.body.user = @request.auth.id
Update rule: user = @request.auth.id
Delete rule: user = @request.auth.id
```

Smoke probe (used in Task 1 pre-flight AND Task 3 post-fix):
```js
await (await import('/src/lib/pocketbase/index.ts')).default.collection('wallecx_expense_budgets').getList({ page: 1, perPage: 1 })
```
- Pre-flight expected response: HTTP 404 with body `{ "code": 404, "message": "Missing collection context.", ... }` (or thrown ClientResponseError with status 404).
- Post-fix expected response: `{ page: 1, perPage: 1, totalItems: 0, totalPages: 0, items: [] }` (HTTP 200).

Note on PocketBase v0.29.x status semantics (from MEMORY feedback_pocketbase_v029_rule_syntax):
- A 404 here means the collection itself does not exist (NOT a per-record filter miss).
- An authenticated `listRule = user = @request.auth.id` with zero matching rows returns 200 + `items: []` (filter false, not 403/404). That's the post-fix success signal.
</interfaces>

<src_singleton>
<!-- Current contents of src/lib/pocketbase/index.ts — the singleton the console one-liner imports -->
```typescript
import PocketBase from "pocketbase";
const baseUrl = import.meta.env.VITE_API_BASE_URL;
export const pb = new PocketBase(baseUrl);
```
The `pb` singleton is the DEFAULT export pattern used by the dynamic import: `(await import('/src/lib/pocketbase/index.ts')).default`. Wait — note that `pb` is a NAMED export, not default. The Task 1 and Task 3 console one-liners therefore use the `.pb` accessor, NOT `.default`. See Task 1/3 action text for the exact command.
</src_singleton>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Pre-flight probe — confirm collection truly missing (404)</name>
  <read_first>
    - .planning/phases/31-recreate-budgets-collection/31-CONTEXT.md (§decisions D-05, D-06, D-07 — pre-flight contract + divergent-schema branch)
    - ~/.claude/projects/C--GitRepos-lex-lib-github-io/memory/feedback_pocketbase_v029_rule_syntax.md (v0.29.x status code semantics — 404 means collection missing, NOT a rule violation; rule violation on filter returns 200 + empty)
    - src/lib/pocketbase/index.ts (pb is a NAMED export, not default — the console one-liner uses .pb, not .default)
  </read_first>
  <what-built>Nothing modified — this is a read-only probe against live PocketBase to confirm the collection is genuinely missing before we re-create it (per D-05). This step DEFENDS against the "collection exists but rules are typo'd" scenario, which would require fix-in-place, not delete-and-recreate (per D-06).</what-built>
  <how-to-verify>
    1. Ensure the Wallecx dev server is running: `npm run dev` in a terminal (if not already up).
    2. Open Chrome (or your usual browser) to `http://localhost:5173/projects/wallecx` and log in.
    3. Open DevTools (F12) → Console tab.
    4. Paste EXACTLY this one-liner and press Enter:
       ```js
       try { const m = await import('/src/lib/pocketbase/index.ts'); const r = await m.pb.collection('wallecx_expense_budgets').getList({ page: 1, perPage: 1 }); console.log('UNEXPECTED 200:', JSON.stringify(r)); } catch (e) { console.log('STATUS:', e?.status, 'BODY:', JSON.stringify(e?.response ?? e?.data ?? { message: e?.message })); }
       ```
    5. Copy the console output (the line starting with `STATUS:` OR `UNEXPECTED 200:`).
    6. Paste the FULL output as plain text into chat. Do NOT just say "got 404" — paste the literal string the console printed.

    **Branch logic (the executor evaluates the paste-back):**
    - **404 with `Missing collection context`** → green light, proceed to Task 2 (re-create from scratch).
    - **200 with response body** → STOP. Collection unexpectedly exists. Executor switches to the fix-in-place sub-flow (D-06): user captures actual schema+rules from Admin UI, executor diffs against the locked spec in the `<interfaces>` block, user edits divergent fields/rules via Admin UI (not delete-and-recreate), then re-runs this probe until 200 + `items: []` is observed. Skip Task 2.
    - **Any other status** (401, 403, 500, network error, etc.) → STOP and surface to user verbatim. Do NOT proceed.

  </how-to-verify>
  <acceptance_criteria>
    - User has pasted the literal console output as text into chat (NOT a screenshot, NOT a summary, NOT "approved" alone).
    - The pasted output matches one of: (a) `STATUS: 404 BODY: {"...","message":"Missing collection context"...}` → proceed to Task 2; (b) `UNEXPECTED 200: {...,"items":[...]}` → switch to fix-in-place sub-flow per D-06; (c) other → STOP.
    - The full console output text is appended verbatim to `31-01-SUMMARY.md` §"Pre-flight evidence" (per D-07, D-11).
    - Plain acknowledgment ("approved", "done", "ran it") is EXPLICITLY INSUFFICIENT — the paste-back text is the gate.
  </acceptance_criteria>
  <resume-signal>Paste the literal console output (status + body) as text in chat. Executor reads it, confirms branch, and explicitly says "Proceeding to Task 2" (or "Switching to fix-in-place" / "Stopping — unexpected response").</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Re-create wallecx_expense_budgets collection via Admin UI (paste-back gated)</name>
  <read_first>
    - .planning/milestones/v4.1-phases/28-budget-tracking/28-01-PLAN.md (§Task 1 — verbatim Admin UI steps; this is the LOCKED spec Phase 31 lands)
    - .planning/phases/31-recreate-budgets-collection/31-CONTEXT.md (§decisions D-08, D-09, D-10 — single-plan structure, paste-back gate, rollback path)
    - ~/.claude/projects/C--GitRepos-lex-lib-github-io/memory/feedback_pocketbase_v029_rule_syntax.md (createRule MUST use @request.body.user — @request.data.user causes 403 in v0.29.3)
    - .planning/STATE.md (§Accumulated Context → Architectural Invariants → "Server-side per-user isolation is the auth boundary" — why all 5 rules must land, not just listRule)
  </read_first>
  <what-built>Nothing automated — this task requires the user to operate the PocketBase Admin UI manually (no CLI exists for collection schema creation against this PocketBase instance). The paste-back gate replaces Phase 28-01's trust-based "approved" signal, which is what caused BUG-01.</what-built>
  <how-to-verify>
    1. Open the PocketBase Admin UI (same instance hosting `wallecx_expenses`, `wallecx_expense_categories`, etc.).
    2. Click "New collection" → select **Base** (NOT auth, NOT view).
    3. Name it exactly: `wallecx_expense_budgets` (lowercase, underscores — case-sensitive).
    4. Add the 4 fields in this order — values MUST match the `<interfaces>` block in the plan context verbatim:
       - **user** — type **Relation**, required = ✓, max select = 1, related collection = `users`, cascade delete = ✗ (false)
       - **category** — type **Text**, required = ✓, min = 1, max = 200
       - **budget_type** — type **Text**, required = ✓, pattern = `^(monthly|yearly)$`
       - **amount** — type **Number**, required = ✓, min = 0
    5. Open the **API Rules** tab. Paste EXACTLY these strings (no leading/trailing whitespace, exact `@request.body.user` syntax — v0.29.3 requirement):
       - **List rule:**   `user = @request.auth.id`
       - **View rule:**   `user = @request.auth.id`
       - **Create rule:** `@request.auth.id != "" && @request.body.user = @request.auth.id`
       - **Update rule:** `user = @request.auth.id`
       - **Delete rule:** `user = @request.auth.id`
    6. Save the collection. Verify it appears in the sidebar with an empty data view.

    **PASTE-BACK GATE (per D-09 — this is the BUG-01 structural fix; "approved" alone is INSUFFICIENT):**

    Paste the following two artifacts back into chat as plain text. The executor will literal-string-compare against the locked spec; ANY deviation stops the task and the user must edit Admin UI + re-paste.

    **(a) Field configs — paste as a markdown table:**
    ```
    | # | Name        | Type     | Required | Constraints                                  |
    |---|-------------|----------|----------|----------------------------------------------|
    | 1 | user        | Relation | true     | related=users, max select=1, cascade=false   |
    | 2 | category    | Text     | true     | min=1, max=200                               |
    | 3 | budget_type | Text     | true     | pattern=^(monthly|yearly)$                   |
    | 4 | amount      | Number   | true     | min=0                                        |
    ```
    (Fill in the row values from what you actually configured in Admin UI; do not copy this table blindly. If a value differs in your UI, paste the actual value — the executor will catch the divergence.)

    **(b) Rules — paste as a code block:**
    ```
    List rule:   user = @request.auth.id
    View rule:   user = @request.auth.id
    Create rule: @request.auth.id != "" && @request.body.user = @request.auth.id
    Update rule: user = @request.auth.id
    Delete rule: user = @request.auth.id
    ```
    (Copy directly from each rule input in the Admin UI — do not retype.)

    **Rollback path (per D-10):** If Task 3 smoke verify fails, delete the entire `wallecx_expense_budgets` collection via Admin UI (Settings → Delete collection) and re-run Task 2 from step 1. Zero production rows exist, so deletion is cheap.

  </how-to-verify>
  <acceptance_criteria>
    - User has pasted BOTH artifacts (field table + rules code block) as plain text into chat.
    - Executor performs LITERAL string compare against the `<interfaces>` block:
      - Field table: every row's Name + Type + Required + Constraints match exactly (e.g., `Relation`, not `relation`; `min=1, max=200`, not `min:1`).
      - Rules code block: every rule string matches character-for-character including `@request.body.user` (NOT `@request.data.user`), the `&&`, the `!= ""`, and the `=` (single, not `==`).
    - On ANY divergence: executor surfaces the diff, user edits Admin UI, re-pastes. Loop until exact match.
    - "approved", "done", "verified", or any acknowledgment-only resume signal WITHOUT the paste-back is EXPLICITLY INSUFFICIENT and must be rejected by the executor.
    - On exact match: executor appends both pasted artifacts verbatim to `31-01-SUMMARY.md` §"Configured schema (paste-back)" and §"Configured rules (paste-back)" (per D-11).
  </acceptance_criteria>
  <resume-signal>Paste the field table AND the rules code block as plain text in chat. Executor verifies literal match against the locked spec and explicitly says "Schema and rules match — proceeding to Task 3" (or surfaces the specific diff).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Code-side smoke verify (200 + items:[] from live app) and STATE.md invariant injection</name>
  <read_first>
    - .planning/phases/31-recreate-budgets-collection/31-CONTEXT.md (§decisions D-01, D-02, D-03, D-04 — dual-pass-signal contract; read-only verify only)
    - src/components/projects/wallecx/ExpensesTab.vue (lines 61–82 — the onMounted that previously threw; this is the app-flow signal source)
    - src/lib/pocketbase/index.ts (`pb` named export — Task 3 console one-liner uses `.pb`, not `.default`)
    - ~/.claude/projects/C--GitRepos-lex-lib-github-io/memory/feedback_pocketbase_v029_rule_syntax.md (post-fix 200 + items:[] is the correct signal for "collection exists, user authenticated, zero rows match filter")
    - .planning/STATE.md (§Accumulated Context — for D-13 invariant injection target location)
  </read_first>
  <what-built>Nothing modified in code — verification only. Two parallel signals (per D-03) prove BUG-01 closed: (a) zero on-mount toast when the user lands on the Expenses tab; (b) explicit DevTools console getList returning 200 + items:[]. After both pass, executor injects the new D-13 architectural invariant into STATE.md.</what-built>
  <how-to-verify>
    **Part A — App-flow signal (per D-01, D-03):**
    1. Ensure dev server is running: `npm run dev`.
    2. Open a FRESH tab to `http://localhost:5173/projects/wallecx` (close prior tabs to ensure onMounted re-runs fresh; or hard-refresh with Ctrl+Shift+R).
    3. Log in if prompted.
    4. Click the **Expenses** tab in the WallecxApp tab shell.
    5. Observe: NO error toast should fire on mount. Specifically, neither `'Failed to load expenses. Pull to refresh or reload the page.'` (line 77 in ExpensesTab.vue) nor `'Failed to refresh budgets. Please reload the page.'` (line 34) should appear.
    6. Paste back into chat: `App-flow: no on-mount toast observed` — explicit confirmation.

    **Part B — Console one-liner signal (per D-02, D-03):**
    7. With the Expenses tab still open and logged in, open DevTools → Console.
    8. Paste EXACTLY this one-liner and press Enter (same as Task 1 but expecting different result):
       ```js
       try { const m = await import('/src/lib/pocketbase/index.ts'); const r = await m.pb.collection('wallecx_expense_budgets').getList({ page: 1, perPage: 1 }); console.log('SUCCESS:', JSON.stringify(r)); } catch (e) { console.log('FAIL STATUS:', e?.status, 'BODY:', JSON.stringify(e?.response ?? e?.data ?? { message: e?.message })); }
       ```
    9. Copy the FULL console output and paste it as plain text into chat.

    **Dual-pass evaluation (per D-03):**
    - **Both signals must pass:**
      - Part A: literal text `App-flow: no on-mount toast observed` in chat from the user.
      - Part B: console output starting with `SUCCESS:` AND containing `"items":[]` AND `"totalItems":0` (and `"page":1`, `"perPage":1`).
    - **If EITHER fails** → invoke rollback path from Task 2 D-10: user deletes the collection via Admin UI, re-runs Task 2 from scratch. Phase 31 is NOT done until BOTH signals pass.
    - Read-only verify only (per D-04) — do NOT probe create/update/delete in this task.

    **Part C — STATE.md architectural invariant injection (per D-13):**
    10. Once both signals pass, executor (Claude) appends a new bullet to `.planning/STATE.md` §"Accumulated Context" → "Architectural Invariants (locked)" section. Use the Edit tool to add this bullet (exact text, after the last existing invariant in that block):
        ```
        - **Admin-UI checkpoints require text paste-back + downstream smoke verify.** When a phase configures a live external artifact (PocketBase collection schema, rules, env vars, dashboard settings), the checkpoint MUST require the user to paste back the actual configured values as text AND a code-side smoke verification must run against the live system. Acknowledgment-only signals ("approved", "done") are insufficient. See BUG-01 post-mortem: Phase 28-01 Task 1 → Phase 31.
        ```
    11. After Edit, confirm by re-reading the modified `.planning/STATE.md` block and verifying the bullet appears.

    **Part D — Write 31-01-SUMMARY.md (per D-11, D-12):**
    12. Executor creates `.planning/phases/31-recreate-budgets-collection/31-01-SUMMARY.md` with the 6-section structure described in `<output>` below.

  </how-to-verify>
  <acceptance_criteria>
    - **Part A:** User pasted the literal string `App-flow: no on-mount toast observed` in chat. ("Looks good" / "approved" alone is insufficient.)
    - **Part B:** User pasted full console output. Output contains the substring `SUCCESS:` AND `"items":[]` AND `"totalItems":0`. (FAIL output → invoke rollback.)
    - **Part C:** `.planning/STATE.md` contains the new D-13 invariant bullet under §"Architectural Invariants (locked)" — verifiable via `grep -c "Admin-UI checkpoints require text paste-back"` returning ≥ 1.
    - **Part D:** `.planning/phases/31-recreate-budgets-collection/31-01-SUMMARY.md` exists with all 6 required sections (Pre-flight evidence, Configured schema paste-back, Configured rules paste-back, Smoke verification, App-flow verification, Root cause cross-reference).
    - All 5 ROADMAP §Phase 31 success criteria are observably TRUE after this task closes.
    - Read-only verify only — no create/update/delete probes against the live collection.
  </acceptance_criteria>
  <resume-signal>Paste Part A confirmation text AND Part B console output in chat. Executor then performs Part C (STATE.md edit) + Part D (summary write) autonomously, and confirms "Phase 31 complete".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PocketBase API | Authenticated Wallecx user submits read/write on `wallecx_expense_budgets` |
| PocketBase rules → DB | Server-side rules filter cross-user reads and block cross-user writes |
| Human checkpoint → live artifact | User claims Admin UI state via paste-back text; executor diffs against locked spec |
| Phase planning artifact → live system | Trust-based acknowledgment ("approved") can silently no-op real configuration steps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | `wallecx_expense_budgets` listRule | mitigate | `listRule: user = @request.auth.id` filters at query time (Task 2 field 1). Verified by Task 1 pre-flight 404 probe (proves the rule doesn't exist yet, ruling out a leaky misconfig) + Task 3 post-fix `items: []` (proves the rule is filtering correctly for the authenticated user). |
| T-31-02 | Tampering | `wallecx_expense_budgets` createRule | mitigate | `createRule: @request.auth.id != "" && @request.body.user = @request.auth.id` blocks cross-user creates (Task 2 rules block). v0.29.3 `@request.body.user` syntax mandatory — `@request.data.user` returns 403 (per MEMORY feedback note). Verified by Task 2 paste-back literal-string compare. Write-path runtime verification deferred to next organic Manage Budgets use (per D-04). |
| T-31-03 | Tampering | `budget_type` field accepts arbitrary strings | mitigate | PocketBase Text pattern `^(monthly|yearly)$` enforced server-side (Task 2 field 3). Verified by Task 2 paste-back exact pattern match. |
| T-31-04 | Tampering | `amount` field accepts negative values | mitigate | PocketBase Number `min = 0` enforced server-side (Task 2 field 4). Verified by Task 2 paste-back exact `min=0` match. |
| T-31-05 | **Process — Trust-based checkpoint silent no-op** | Admin-UI human-action checkpoints across the GSD workflow | mitigate | **NEW threat class — process/workflow layer, not protocol layer.** Severity HIGH because this exact threat directly caused BUG-01 (Phase 28-01 Task 1 was accepted on "approved" alone; collection was never created; ExpensesTab.vue threw on mount in production for 2+ days post-ship). Phase 31 mitigates structurally via: (a) Task 2 paste-back gate (D-09) — `approved` alone is insufficient; user MUST paste actual configured values; executor MUST literal-string-compare; (b) Task 3 downstream code-side smoke verify (D-01, D-02, D-03) — the live app must successfully exercise the artifact; (c) D-13 STATE.md invariant injection (Task 3 Part C) — makes the paste-back + downstream-verify pattern a project-wide rule binding all future GSD admin-UI checkpoints. |

</threat_model>

<verification>
- All 3 tasks complete; user has pasted required artifacts at every gate (NOT acknowledgment-only).
- Task 1 paste-back shows 404 `Missing collection context` (or branch to fix-in-place per D-06).
- Task 2 paste-back of field table + rules code block matches locked spec literal-string.
- Task 3 Part A: `App-flow: no on-mount toast observed` confirmation.
- Task 3 Part B: console `SUCCESS:` + `"items":[]` + `"totalItems":0`.
- Task 3 Part C: `.planning/STATE.md` contains new D-13 invariant bullet (grep verifies).
- Task 3 Part D: `31-01-SUMMARY.md` exists with all 6 required sections.
- ZERO code modifications under `src/`, `tests/`, or any build artifact (this is the per-D scope guarantee — Phase 32 owns code-side decoupling).
- BUG-01 acceptance from REQUIREMENTS.md satisfied: collection exists with locked schema + 5 rules; smoke read returns 200 + items:[] from the running app.
</verification>

<success_criteria>
All 5 ROADMAP.md §Phase 31 success criteria observably TRUE:
1. PocketBase Admin UI shows collection named exactly `wallecx_expense_budgets` (paste-back gate enforced this).
2. Collection has 4 fields with locked types/constraints (Task 2 paste-back literal-match).
3. Collection has all 5 API rules with v0.29.3 syntax (Task 2 paste-back literal-match).
4. `pb.collection('wallecx_expense_budgets').getList({ page: 1, perPage: 1 })` from the live app returns 200 + items:[] (Task 3 Part B).
5. Refreshing Expenses tab fires no error toast on mount (Task 3 Part A — note: Phase 32 has not yet shipped, so this signal directly proves BUG-01 closed, not BUG-02).

Additionally: a new project-level architectural invariant ("Admin-UI checkpoints require text paste-back + downstream smoke verify") is locked into STATE.md, preventing recurrence of T-31-05 across all future GSD admin-UI tasks in this project.
</success_criteria>

<output>
After completion, executor creates `.planning/phases/31-recreate-budgets-collection/31-01-SUMMARY.md` with the following 6-section structure (per D-11, D-12; text-only, no screenshots):

1. **Pre-flight evidence** — Task 1 probe one-liner verbatim + the user's pasted-back console response (the 404 `Missing collection context` smoking gun). Include timestamp.

2. **Configured schema (paste-back)** — Markdown table of all 4 field configs (Name | Type | Required | Constraints), exactly as the user pasted from Admin UI in Task 2.

3. **Configured rules (paste-back)** — Code block with all 5 rule strings exact text, exactly as the user pasted from Admin UI in Task 2.

4. **Smoke verification** — Task 3 Part B console one-liner verbatim + the user's pasted-back `SUCCESS: {...,"items":[],"totalItems":0,...}` response. Include timestamp.

5. **App-flow verification** — Task 3 Part A confirmation: `App-flow: no on-mount toast observed` + brief note ("Opened /projects/wallecx, navigated to Expenses tab; neither 'Failed to load expenses...' nor 'Failed to refresh budgets...' toast fired"). Include timestamp.

6. **Root cause (Phase 28-01 retrospective)** — Cross-reference section:
   - Link to `.planning/milestones/v4.1-phases/28-budget-tracking/28-01-SUMMARY.md`
   - Link to `.planning/milestones/v4.1-phases/28-budget-tracking/28-01-PLAN.md` §Task 1
   - 1-paragraph explanation: Phase 28-01 Task 1 was a `checkpoint:human-action` with a trust-based "approved" resume signal. The user typed "approved" but the actual Admin UI step was not performed (or was performed against a different PB instance / a typo'd collection name). The collection never landed in production. ExpensesTab.vue onMounted threw `404 Missing collection context` on every Expenses tab mount post-v4.1-ship. Phase 31 lands the same locked schema with a STRUCTURAL paste-back gate (D-09) plus downstream code-side smoke verify (D-01–D-03), eliminating the silent-no-op failure mode. D-13 generalizes this gate as a project-wide invariant in STATE.md so all future GSD admin-UI checkpoints inherit the protection.

Then: STATE.md updates by executor: (i) §"Architectural Invariants (locked)" — new D-13 bullet (already added in Task 3 Part C); (ii) §"Current Position" — Phase 31 marked complete with date; (iii) progress block updated to `1/2 phases`. Roadmap also updates with Phase 31 checkbox + plan list.
</output>
