---
phase: 30-uat-sweep
plan: 06
type: execute
wave: 6
depends_on: []
files_modified:
  - .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md
  - .planning/phases/30-uat-sweep/30-UAT-REPORT.md
autonomous: false
requirements: [QA-01]
requirements_addressed: [QA-01]
tags: [uat, qa, human-verify, phase-21, dark-mode, mini-apps]

must_haves:
  truths:
    - "Each of 4 mini-apps (LexTrack, Larga, Gift Exchange/MonitoX, API Playground) walked through in light + dark + 375px"
    - "Critical theme-independence rules verified (semantic status badges remain in palette regardless of theme; Leaflet map tiles stay light per D-07)"
    - "Phase 30 UAT Results appendix at bottom of 21-HUMAN-UAT.md"
    - "Phase 21 roll-up in 30-UAT-REPORT.md"
    - "npm run test:unit still passes"
  artifacts:
    - path: ".planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md"
      provides: "Phase 30 UAT Results appendix"
      contains: "Phase 30 UAT Results"
    - path: ".planning/phases/30-uat-sweep/30-UAT-REPORT.md"
      provides: "Phase 21 rows"
      contains: "Phase 21"
---

<objective>
Walk through the Phase 21 mini-app dark-mode sweep covering 4 mini-apps:
- §1 LexTrack (THEME-09) — task/meeting/support tracker; requires login
- §2 Larga (THEME-10) — PUV transit helper with Leaflet map
- §3 Gift Exchange / MonitoX (THEME-11) — Secret Santa app
- §4 API Playground (THEME-12) — Postman-like tool

Each mini-app is tested in light + dark at desktop and 375px. Theme-independence rules from Phase 21 (semantic status badges, Leaflet map tiles) must be verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-uat-sweep/30-CONTEXT.md
@.planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk through Phase 21 mini-app dark-mode UAT</name>
  <read_first>
    - .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md (full §1–§4 mini-app checklists)
  </read_first>
  <how-to-verify>
    Setup:
    - `npm run dev` and open `http://localhost:5173/`
    - Toggle theme via NavBar sun/moon
    - DevTools Device Toolbar: 375px ready
    - For LexTrack: need a logged-in dev account (route is auth-guarded)
    - For Gift Exchange: may need to walk multiple sub-routes (join / draw / manage)

    For each mini-app:

    1. **§1 LexTrack (/projects/lextrack):**
       - Light desktop: page shell white, section headings dark, list rows white with dark/mid-slate text, dividers subtle; AddMeeting / ManageTask / ManageMeeting / ManageSupport dialogs with white surfaces; DatePicker indigo highlight; TabView indigo underline
       - Dark desktop: page shell `#0d1117`, main card `#1a1a2e`, section headings `#f5f7fa`, list rows dark with light text, indigo accent shifts to `#a5b4fc` (light variant)
       - **Critical:** Meeting duration pill uses `bg-indigo-900/30 text-indigo-300` (paired dark) in dark mode; status badges STAY in semantic palette (theme-independent — D-13)
       - 375px both themes: no horizontal scroll, all dialogs reachable + dismissible, tap targets ≥ 44px
       - Spot-check `ActivityCard.vue` inline-style icons (out-of-scope but flag if visibly bad in dark)
       - Record pass/fail per section

    2. **§2 Larga (/projects/larga):**
       - Light desktop: OSM default (light) tiles, route picker buttons visible, geocoder input light bg + dark text, route info panel light
       - Dark desktop: page chrome dark, PrimeVue Card dark navy, route picker recognizable, geocoder input DARK bg + light text + mid-slate placeholder (D-09 verbatim override)
       - **Critical:** Map tiles STAY light in dark mode (D-07 intentional — no dark tile provider swap)
       - 375px: no horizontal page scroll, map fills width, route picker stacks acceptably
       - Record pass/fail per section

    3. **§3 Gift Exchange / MonitoX (/projects/gift-exchange/*):**
       - Walk sub-routes: join, draw, manage (and root)
       - Light + dark + 375px at each sub-route
       - Verify dark backgrounds, light text, no white bleed
       - Specific to verify per the original UAT — read 21-HUMAN-UAT.md §3 for the exact checklist
       - Record pass/fail per sub-route

    4. **§4 API Playground (/projects/api-playground):**
       - Postman-like interface — input + headers + body + response areas
       - Light + dark + 375px
       - Verify request/response panels render correctly in both themes
       - Record pass/fail per section

    Report per mini-app:
    ```
    §1 LexTrack: passed (semantic badges + indigo accents verified)
    §2 Larga: passed (geocoder dark override fires; map tiles stay light)
    §3 Gift Exchange: failed — join page shows white card in dark mode
    §4 API Playground: passed
    Overall: PARTIAL
    ```
  </how-to-verify>
  <acceptance_criteria>
    - All 4 mini-apps walked through
    - Theme-independence rules (status badges, Leaflet tiles) explicitly verified
  </acceptance_criteria>
  <resume-signal>Type "approved: pass / partial — list / fail — list".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Phase 30 results to 21-HUMAN-UAT.md and update 30-UAT-REPORT.md</name>
  <action>
    Per CONTEXT D-05:
    1. Append `## Phase 30 UAT Results` to bottom of 21-HUMAN-UAT.md with a 4-row table (one per mini-app), pass/fail/notes
    2. Update 30-UAT-REPORT.md with Phase 21 Summary row + 4 mini-app Per-Scenario rows
    3. Commit: `test(30-06): record Phase 21 mini-app UAT results`
  </action>
  <acceptance_criteria>
    - 21-HUMAN-UAT.md has Phase 30 appendix
    - 30-UAT-REPORT.md has 4 mini-app rows
    - test:unit passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-30-06-01 | Information Disclosure | accept | LexTrack auth boundary already enforced by route guard |
</threat_model>

<verification>
- 21-HUMAN-UAT.md has Phase 30 appendix
- 30-UAT-REPORT.md has Phase 21 entries
- test:unit passes
</verification>

<success_criteria>
- All 4 mini-apps walked through
- Theme-independence rules verified
</success_criteria>

<output>
Create `30-06-SUMMARY.md` with per-mini-app breakdown + note: next plan is 30-07 (Phase 22 Wallecx audit — 6 vectors).
</output>
