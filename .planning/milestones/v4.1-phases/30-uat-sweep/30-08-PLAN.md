---
phase: 30-uat-sweep
plan: 08
type: execute
wave: 8
depends_on: []
files_modified:
  - .planning/milestones/v4.0-phases/25-read-path-list-view/25-HUMAN-UAT.md
  - .planning/phases/30-uat-sweep/30-UAT-REPORT.md
autonomous: false
requirements: [QA-01]
requirements_addressed: [QA-01]
tags: [uat, qa, human-verify, phase-25, expenses, archived]

must_haves:
  truths:
    - "All 7 pending scenarios in 25-HUMAN-UAT.md have non-pending results"
    - "Summary pending count equals 0 in the archived UAT file"
    - "Phase 25 row in 30-UAT-REPORT.md with per-scenario detail"
    - "npm run test:unit still passes"
  artifacts:
    - path: ".planning/milestones/v4.0-phases/25-read-path-list-view/25-HUMAN-UAT.md"
      provides: "Filled-in pass/fail results for Phase 25's 7 scenarios (archived path per CONTEXT D-06)"
      contains: "result: passed"
    - path: ".planning/phases/30-uat-sweep/30-UAT-REPORT.md"
      provides: "Phase 25 rows in master report"
      contains: "Phase 25"
---

<objective>
Walk through the 7 pending UAT scenarios for Phase 25 (Read-Path List View — Expenses tab toolbar filtering + receipt preview + sort persistence). The HUMAN-UAT.md for Phase 25 is archived at `.planning/milestones/v4.0-phases/25-read-path-list-view/25-HUMAN-UAT.md` per CONTEXT D-06 — the executor must update that archived path.

Phase 25 shipped the comprehensive Expenses list filtering: receipt MIME-branched preview (image/PDF/download), responsive Dialog/Drawer wrapper, sessionStorage sort persistence (`wallecx:expense-sort`), reactive search, category MultiSelect filter, inclusive date-range picker, confirm-delete ConfirmDialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-uat-sweep/30-CONTEXT.md
@.planning/milestones/v4.0-phases/25-read-path-list-view/25-HUMAN-UAT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk through Phase 25 Expenses UAT</name>
  <read_first>
    - .planning/milestones/v4.0-phases/25-read-path-list-view/25-HUMAN-UAT.md (7 scenarios verbatim — archived path)
    - .planning/STATE.md §Phase 25 Decisions (DatePicker @update:model-value cast pattern, categoryOptions from RAW expenses, v-if chain order locked)
  </read_first>
  <how-to-verify>
    Setup:
    - `npm run dev` and `/projects/wallecx` → Expenses tab → List sub-tab
    - Seed at least 5-8 expenses across:
      - 2+ different categories
      - Different `expense_date` values (spread across at least 2 months for date-range testing)
      - At least 1 with an image receipt attachment
      - At least 1 with a PDF receipt attachment
      - At least 1 with NO receipt
      - At least 1 with an unknown-MIME receipt (or test scenario 1 with image + PDF and mark unknown-MIME as `skipped` if no unknown-type attachment seeded)

    1. **Scenario 1 — Attachment MIME branches:**
       - Tap an image-receipt paperclip → image renders via `<img>`
       - Tap a PDF-receipt paperclip → PDF renders via vue-pdf-embed
       - Tap an unknown-MIME paperclip → download fallback shows (or mark skipped)
       - Verify token from `pb.files.getToken()` makes URLs accessible (Network tab — no 403s)
       - Pass/fail

    2. **Scenario 2 — Dialog vs Drawer responsive switch:**
       - Resize viewport across 640px breakpoint with preview open
       - At ≥640px: Dialog (centered)
       - At <640px: Drawer position='bottom' (bottom sheet)
       - Confirm AttachmentPreview renders correctly in both
       - Pass/fail

    3. **Scenario 3 — Sort mode persists across reload:**
       - Change sort to 'amount-high' via toolbar dropdown
       - Reload page (Ctrl+R)
       - Confirm list returns in amount-high order BEFORE any interaction
       - DevTools → Application → sessionStorage → confirm `wallecx:expense-sort` key
       - Pass/fail

    4. **Scenario 4 — Search filters reactively:**
       - Type into search box → list filters on each keystroke (no perceived delay)
       - Click X (clear button) → input clears + list restores
       - Pass/fail

    5. **Scenario 5 — Category MultiSelect chip filter:**
       - Select one or more category chips → list narrows
       - Click "Clear filters" button → all chips clear + list restores
       - Verify categoryOptions persists even when filter narrows list to zero matches (Phase 25 invariant — categoryOptions derives from RAW expenses, not filtered output)
       - Pass/fail

    6. **Scenario 6 — Date-range picker filters inclusively (no new PocketBase queries):**
       - Open DevTools Network tab
       - Set From=2026-05-01 and To=2026-05-15 (adjust dates to your seeded data)
       - Confirm list shows ONLY expense_date values within inclusive range
       - Verify Network tab shows NO additional `/api/collections/wallecx_expenses/records` calls (client-side filter only)
       - Pass/fail

    7. **Scenario 7 — Confirm Delete dialog + success toast:**
       - Click trash icon on a row → ConfirmDialog opens (rendered at WallecxApp.vue shell level — locked invariant)
       - Click "Delete Expense" button → `pb.delete()` fires → success toast "Expense deleted." → row removed
       - Pass/fail

    Report:
    ```
    Scenario 1: passed
    Scenario 2: passed
    Scenario 3: passed
    Scenario 4: passed
    Scenario 5: passed
    Scenario 6: passed
    Scenario 7: passed
    ```
  </how-to-verify>
  <acceptance_criteria>
    - All 7 scenarios walked through
    - Failures noted with one-line description
  </acceptance_criteria>
  <resume-signal>Type "approved: X/7 passed" or describe failures by scenario number.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record results in archived 25-HUMAN-UAT.md + 30-UAT-REPORT.md</name>
  <action>
    Note the archived path per CONTEXT D-06: `.planning/milestones/v4.0-phases/25-read-path-list-view/25-HUMAN-UAT.md` (NOT `.planning/phases/25-*/`).

    Per D-05 (structured-format file — update result fields in place):
    1. Replace each `result: [pending]` with the user's outcome
    2. Update Summary counts
    3. Update frontmatter status + bump updated timestamp
    4. Append 7 rows to 30-UAT-REPORT.md Per-Scenario table + 1 Summary row
    5. Note in 30-UAT-REPORT.md that Phase 25's HUMAN-UAT.md is at the archived path
    6. Commit: `test(30-08): record Phase 25 expenses UAT results`
  </action>
  <verify>
    <automated>grep -c "result: \[pending\]" .planning/milestones/v4.0-phases/25-read-path-list-view/25-HUMAN-UAT.md returns 0</automated>
  </verify>
  <acceptance_criteria>
    - Archived 25-HUMAN-UAT.md has zero pending entries
    - 30-UAT-REPORT.md has 7 Phase 25 rows + archived-path note
    - test:unit passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-30-08-01 | Information Disclosure | accept | Phase 25 reads PocketBase via existing listRule (user = @request.auth.id) |
| T-30-08-02 | DoS | accept | Client-side date-range filter bounded by user's own data |
</threat_model>

<verification>
- Archived 25-HUMAN-UAT.md updated with non-pending results
- 30-UAT-REPORT.md contains Phase 25 entries (with archived-path note)
- test:unit passes
</verification>

<success_criteria>
- All 7 Phase 25 expenses scenarios resolved
- Phase 30 sweep complete — orchestrator can now finalize 30-UAT-REPORT.md and run any batched fix plans before marking Phase 30 complete
</success_criteria>

<output>
Create `30-08-SUMMARY.md` with per-scenario breakdown + note: this is the last in-scope UAT plan. Next step is the orchestrator's roll-up — if any failures, create 30-09-FIX-PLAN.md (or multiple). If all passed, mark Phase 30 complete and close v4.1.
</output>
