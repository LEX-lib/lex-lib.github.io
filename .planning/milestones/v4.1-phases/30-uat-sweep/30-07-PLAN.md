---
phase: 30-uat-sweep
plan: 07
type: execute
wave: 7
depends_on: []
files_modified:
  - .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md
  - .planning/phases/30-uat-sweep/30-UAT-REPORT.md
autonomous: false
requirements: [QA-01]
requirements_addressed: [QA-01]
tags: [uat, qa, human-verify, phase-22, dark-mode, wallecx-audit]

must_haves:
  truths:
    - "All 6 Phase 22 audit vectors walked through (toggle interaction, route persistence, FOUC, plus 3 more from 22-HUMAN-UAT.md)"
    - "Pre-flight (NavBar toggle wired up correctly + localStorage contract honored) passes"
    - "Phase 30 UAT Results appendix at bottom of 22-HUMAN-UAT.md"
    - "Phase 22 row in 30-UAT-REPORT.md"
    - "npm run test:unit still passes"
  artifacts:
    - path: ".planning/phases/22-wallecx-audit/22-HUMAN-UAT.md"
      provides: "Phase 30 UAT Results appendix"
      contains: "Phase 30 UAT Results"
    - path: ".planning/phases/30-uat-sweep/30-UAT-REPORT.md"
      provides: "Phase 22 row"
      contains: "Phase 22"
---

<objective>
Walk through Phase 22's v3.0 milestone-closer audit — a 6-vector regression spot-check (not a re-walk of Phase 18's full sweep, per 22-CONTEXT D-01). Verifies that Phase 18's Wallecx dark mode survives Phase 19's NavBar toggle integration.

The 6 vectors cover: live toggle while dialog open, theme persistence across routes, FOUC on hard reload, and 3 more documented in 22-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-uat-sweep/30-CONTEXT.md
@.planning/phases/22-wallecx-audit/22-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk through Phase 22 Wallecx audit 6-vector spot-check</name>
  <read_first>
    - .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md (pre-flight + 6 vectors)
  </read_first>
  <how-to-verify>
    Setup:
    - `npm run dev` and open `/projects/wallecx` in Chromium
    - Have at least 1 vaccination and 1 membership seeded

    1. **Pre-flight (4 checks):**
       - Wallecx loads with NavBar sun/moon visible
       - Click NavBar toggle → `<html>` gains `.my-app-dark`; page flips dark; icon swaps
       - Hard refresh (Ctrl+F5) while dark → no white flash on first paint
       - DevTools Application tab → `localStorage` key `lexarium:theme` = `"dark"`
       - If pre-flight FAILS, abort and report — Phase 19 regressed independently

    2. **Vector 1 — Toggle interaction mid-dialog (SC-1):**
       - Open `/projects/wallecx` in LIGHT
       - Click NavBar toggle → page goes dark; Vaccination group cards switch surfaces; Membership tiles keep `card_color` with luminance-aware text
       - Open Add Vaccination dialog; leave it open
       - Toggle theme again → dialog body re-paints immediately; no white flash; inputs adapt; CTAs legible; backdrop dims correctly
       - Close dialog. Repeat with Add Membership; verify ColorPicker swatch panel adapts; user's chosen `card_color` is NOT mutated
       - Pass/fail

    3. **Vector 2 — Route transitions theme persistence (SC-2):**
       - Wallecx in dark → navigate to `/projects` → still dark, no flash
       - Back to `/projects/wallecx` → still dark on re-entry; no FOUC
       - Full circuit: `wallecx (dark)` → `/` → `/blog` → `/projects/lextrack` → back to `wallecx` → dark across all; no transient white
       - Repeat from light → light persists across the circuit
       - Pass/fail

    4. **Vector 3 — FOUC on hard reload (SC-2):**
       - Wallecx in dark → hard refresh (Ctrl+F5) → first paint already dark; no white flash; NavBar moon icon already shown; Wallecx grid shell dark
       - Vaccinations tab → hard refresh → no FOUC
       - Memberships tab → hard refresh → no FOUC; tiles paint with `card_color` + luminance-aware text immediately
       - Toggle to light → hard refresh → first paint already light; NavBar sun icon already shown
       - Pass/fail

    5. **Vectors 4-6:** Continue with the remaining 3 vectors documented in 22-HUMAN-UAT.md (read the file for the exact scope of vectors 4, 5, 6 — they cover additional regression spot-checks beyond the first three)
       - Pass/fail per vector

    Report:
    ```
    Pre-flight: passed
    Vector 1 (toggle mid-dialog): passed
    Vector 2 (route persistence): passed
    Vector 3 (FOUC hard reload): passed
    Vector 4 (...): passed/failed
    Vector 5 (...): passed/failed
    Vector 6 (...): passed/failed
    Overall: PASS / PARTIAL
    ```
  </how-to-verify>
  <acceptance_criteria>
    - Pre-flight + 6 vectors all walked through
    - Failures itemized
  </acceptance_criteria>
  <resume-signal>Type "approved: pass / partial — list / fail — list".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Phase 30 results to 22-HUMAN-UAT.md and update 30-UAT-REPORT.md</name>
  <action>
    Per CONTEXT D-05:
    1. Append `## Phase 30 UAT Results` to bottom of 22-HUMAN-UAT.md with: pre-flight row + 6 vector rows + sign-off
    2. Update 30-UAT-REPORT.md with Phase 22 Summary row + 7 Per-Scenario rows (pre-flight + 6 vectors)
    3. Commit: `test(30-07): record Phase 22 Wallecx audit UAT results`
  </action>
  <acceptance_criteria>
    - 22-HUMAN-UAT.md has Phase 30 appendix
    - 30-UAT-REPORT.md has Phase 22 entries
    - test:unit passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-30-07-01 | Tampering | block | If pre-flight fails (Phase 19 regression), abort UAT — fix Phase 19 first |
</threat_model>

<verification>
- 22-HUMAN-UAT.md has Phase 30 appendix
- 30-UAT-REPORT.md has Phase 22 entries
- test:unit passes
</verification>

<success_criteria>
- Pre-flight + 6 vectors walked through
- Phase 19 toggle integration confirmed not to regress Phase 18 dark mode
</success_criteria>

<output>
Create `30-07-SUMMARY.md` with vector-by-vector breakdown + note: next plan is 30-08 (Phase 25 — archived; 7 expense list scenarios).
</output>
