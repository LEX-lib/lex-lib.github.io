---
phase: 30-uat-sweep
plan: 03
type: execute
wave: 3
depends_on: []
files_modified:
  - .planning/phases/12-read-path-card-grid-barcode-display-and-detail/12-HUMAN-UAT.md
  - .planning/phases/30-uat-sweep/30-UAT-REPORT.md
autonomous: false
requirements: [QA-01]
requirements_addressed: [QA-01]
tags: [uat, qa, human-verify, phase-12, memberships, barcode]

must_haves:
  truths:
    - "All 6 pending scenarios in 12-HUMAN-UAT.md have a non-pending result"
    - "12-HUMAN-UAT.md Summary pending count equals 0"
    - "Phase 12 row appended to 30-UAT-REPORT.md with per-scenario detail"
    - "npm run test:unit still passes"
  artifacts:
    - path: ".planning/phases/12-read-path-card-grid-barcode-display-and-detail/12-HUMAN-UAT.md"
      provides: "Filled-in pass/fail results for Phase 12's 6 scenarios"
      contains: "result: passed"
    - path: ".planning/phases/30-uat-sweep/30-UAT-REPORT.md"
      provides: "Phase 12 rows in the master report"
      contains: "Phase 12"
---

<objective>
Walk through the 6 pending UAT scenarios for Phase 12 (read-path card grid + barcode display + detail) and record results.

Phase 12 shipped the membership card grid rendering, MembershipDetail dialog with all 7 fields, QR/linear barcode rendering via QrcodeVue + JsBarcode, full-screen scan overlay with wake lock, invalid barcode fallback, and empty/error states. The 6 scenarios cover the full read-path + scan flow + vaccinations regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-uat-sweep/30-CONTEXT.md
@.planning/phases/12-read-path-card-grid-barcode-display-and-detail/12-HUMAN-UAT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk through Phase 12 UAT scenarios in the running app</name>
  <read_first>
    - .planning/phases/12-read-path-card-grid-barcode-display-and-detail/12-HUMAN-UAT.md (all 6 scenarios verbatim)
    - .planning/STATE.md (Phase 12 architectural invariants — JsBarcode try/catch, card_color hex format)
  </read_first>
  <how-to-verify>
    Pre-flight: have at least 2-3 membership cards seeded (different `card_color` values; at least one each of: qr, code128, ean13). For invalid-barcode test, you may need to temporarily seed a card with a deliberately-malformed EAN-13 value (e.g., wrong check digit) via the PocketBase admin UI.

    1. `npm run dev` and navigate to `/projects/wallecx` → Memberships tab

    2. **Scenario 1 — Coloured card grid rendering:**
       - Confirm cards render with backgrounds matching `card_color` (default `#002244` if unset)
       - Confirm expiry badges: yellow "Expiring soon" for cards expiring soon, red "Expired" for past dates
       - Record: pass / fail / blocked

    3. **Scenario 2 — MembershipDetail 7-field dialog + barcode rendering:**
       - Click a card with `barcode_type: qr` → confirm dialog opens with all 7 fields (Card Name, Issuer, Card Number, Barcode Type, Expiry Date, Card Colour swatch + hex, Notes)
       - Confirm QrcodeVue renders a QR SVG
       - Open a card with `barcode_type: code128` (or ean13/code39) → confirm JsBarcode renders the linear barcode SVG
       - Record: pass / fail / blocked

    4. **Scenario 3 — Full-screen scan overlay:**
       - Tap/click the barcode area in MembershipDetail
       - Confirm full-screen WHITE overlay opens (z-index 9999, brightness 1.4)
       - Confirm close button appears (top-right)
       - Confirm wake lock prevents screen dim (battery indicator stops dimming on phone)
       - Confirm overlay is ABOVE the PrimeVue Dialog
       - Press Escape → confirm overlay closes, Dialog remains open
       - Record: pass / fail / blocked

    5. **Scenario 4 — Invalid barcode fallback (SCAN-02):**
       - Open a card with a malformed barcode_value (e.g., EAN-13 with wrong check digit)
       - Confirm: barcode area shows `card_number` as plain text + "Barcode unavailable" caption (no component crash, no console error)
       - Record: pass / fail / blocked. If no malformed card exists, mark `skipped: no malformed barcode available to test`

    6. **Scenario 5 — Empty state + error toast:**
       - Log out + create a fresh user with no membership records, OR temporarily clear memberships for the test user
       - Confirm: empty state shows icon + "No membership cards yet." + a disabled "Add your first card" button
       - To test error toast: cut network in DevTools → reload the Wallecx Memberships tab
       - Confirm: `toast.error("Failed to load membership cards.")` fires
       - Restore network
       - Record: pass / fail / blocked

    7. **Scenario 6 — Vaccinations tab regression:**
       - Switch to Vaccinations tab
       - Confirm: vaccination records load
       - Open a vaccination with a card image attachment → confirm AttachmentPreview shows the image
       - Open a vaccination with a PDF attachment (if any exists) → confirm AttachmentPreview shows the PDF
       - Open VaccinationDetail dialog → confirm no errors
       - Record: pass / fail / blocked

    8. Report results per scenario:
       ```
       Scenario 1: passed
       Scenario 2: passed
       Scenario 3: failed — <description>
       ...
       ```
  </how-to-verify>
  <acceptance_criteria>
    - Each of the 6 scenarios has a non-pending outcome reported
    - Failures include a one-line description
  </acceptance_criteria>
  <resume-signal>Type "approved: X/6 passed" or describe failures by scenario number.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record results in 12-HUMAN-UAT.md and 30-UAT-REPORT.md</name>
  <action>
    Follow the recording protocol from .planning/phases/30-uat-sweep/30-CONTEXT.md §D-05:
    1. Update each of the 6 `result: [pending]` entries with the user's outcome
    2. Update Summary counts (passed/failed/skipped/blocked/pending=0)
    3. Update frontmatter status + updated timestamp
    4. Append 6 rows (one per scenario) + 1 summary row to 30-UAT-REPORT.md
    5. If failures: append entries to "Deferred Items" referencing a future fix-plan name
    6. Commit: `test(30-03): record Phase 12 UAT results`
  </action>
  <verify>
    <automated>grep -c "result: \[pending\]" .planning/phases/12-read-path-card-grid-barcode-display-and-detail/12-HUMAN-UAT.md returns 0</automated>
  </verify>
  <acceptance_criteria>
    - 12-HUMAN-UAT.md has zero pending entries
    - 30-UAT-REPORT.md contains 6 Phase 12 scenario rows
    - `npm run test:unit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-30-03-01 | Information Disclosure | accept | Phase 12 reads PocketBase data with listRule already enforced server-side |
| T-30-03-02 | DoS | accept | Wake-lock test bounded to one screen + voluntary close |
</threat_model>

<verification>
- 12-HUMAN-UAT.md pending count = 0
- 30-UAT-REPORT.md contains 6 Phase 12 rows
- npm run test:unit passes
</verification>

<success_criteria>
- All 6 Phase 12 scenarios resolved
- Any regressions surfaced for batched fix plans
</success_criteria>

<output>
Create `30-03-SUMMARY.md` with per-scenario breakdown, total pass/fail counts, fix-plan references if any, note: next plan is 30-04 (Phase 18 — free-form Wallecx dark-mode sweep).
</output>
