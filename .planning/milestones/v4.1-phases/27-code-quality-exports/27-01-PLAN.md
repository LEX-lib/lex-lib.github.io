---
phase: 27-code-quality-exports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - CQ-01
  - CQ-02

must_haves:
  truths:
    - "Running `npm run test:unit` exits 0 with all 49+ tests green"
    - "The expense schema test suite confirms `expense_date` rejects invalid calendar dates (e.g. Feb 31)"
    - "The expenseMapper test suite confirms `mapToUpdateExpense` drops the `notes` key entirely when notes is undefined"
    - "REQUIREMENTS.md marks CQ-01 and CQ-02 as complete with [x]"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "CQ-01 and CQ-02 marked [x] complete"
      contains: "- [x] **CQ-01**"
  key_links:
    - from: "src/lib/wallecx/expenseSchema.ts"
      to: "expense_date Zod field"
      via: ".refine(val => dayjs(val, 'YYYY-MM-DD', true).isValid()...)"
      pattern: "refine.*dayjs.*YYYY-MM-DD.*true"
    - from: "src/lib/pocketbase/expenseMapper.ts"
      to: "notes field"
      via: "conditional spread ...(record.notes !== undefined ? { notes: record.notes } : {})"
      pattern: "record\\.notes !== undefined"
    - from: "src/lib/pocketbase/__tests__/expenseMapper.spec.ts"
      to: "drops notes when undefined test"
      via: "expect(minimalPayload).not.toHaveProperty('notes')"
      pattern: "not\\.toHaveProperty.*notes"
---

<objective>
Confirm that both deferred code-quality fixes (CQ-01 and CQ-02) are live and passing in the current codebase by running the full test suite. Update REQUIREMENTS.md to mark both items as verified-complete. No code changes to production files — verification and documentation only.

Purpose: Establishes a clean baseline before export work begins (Wave 2). Ensures that the two advisor-flagged quality issues from Phase 23 are provably resolved before v4.1 ships.
Output: Green test run (all 49+ tests pass), REQUIREMENTS.md updated with [x] on CQ-01 and CQ-02.
</objective>

<execution_context>
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run test suite and verify CQ-01 and CQ-02 are passing</name>
  <files>
    src/lib/wallecx/expenseSchema.ts
    src/lib/pocketbase/expenseMapper.ts
    src/lib/pocketbase/__tests__/expenseMapper.spec.ts
  </files>
  <read_first>
    - `src/lib/wallecx/expenseSchema.ts` — confirm `expense_date` field has `.refine(val => dayjs(val, 'YYYY-MM-DD', true).isValid(), ...)` at line 21 (CQ-01)
    - `src/lib/pocketbase/expenseMapper.ts` — confirm `mapToUpdateExpense` uses `...(record.notes !== undefined ? { notes: record.notes } : {})` conditional spread (CQ-02)
    - `src/lib/pocketbase/__tests__/expenseMapper.spec.ts` — confirm the "drops notes when undefined" test uses `expect(minimalPayload).not.toHaveProperty("notes")` (not `toBeUndefined()`)
  </read_first>
  <action>
    This task is verification-only. Do NOT modify any production files.

    1. Read `src/lib/wallecx/expenseSchema.ts` and confirm line 21 contains:
       ```typescript
       .refine(val => dayjs(val, 'YYYY-MM-DD', true).isValid(), { message: 'Date is not a valid calendar date.' })
       ```
       If missing: STOP and report — do NOT implement it here; this is a verification task.

    2. Read `src/lib/pocketbase/expenseMapper.ts` and confirm the return block in `mapToUpdateExpense` contains:
       ```typescript
       ...(record.notes !== undefined ? { notes: record.notes } : {}),
       ```
       If missing: STOP and report.

    3. Read `src/lib/pocketbase/__tests__/expenseMapper.spec.ts` and confirm the "drops notes when undefined" test contains:
       ```typescript
       expect(minimalPayload).not.toHaveProperty("notes");
       ```
       If it uses `toBeUndefined()` instead: STOP and report.

    4. Run the full test suite:
       ```bash
       cd C:/GitRepos/lex-lib.github.io && npm run test:unit
       ```
       All tests must exit 0. The output must show 49 or more passing tests with 0 failures.

    5. From the test output, confirm:
       - The expenseMapper spec suite passes all 9 tests (look for "expenseMapper" in the output)
       - The expense schema validation tests pass (look for "expenseSchema" or "expense schema" in the output)

    Record the exact test counts from the output (e.g. "49 passed") for the summary.
  </action>
  <verify>
    <automated>cd C:/GitRepos/lex-lib.github.io && npm run test:unit -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` exits with code 0
    - Test output contains "0 failed" or shows all tests as passing
    - Test output shows at least 49 tests passing
    - `src/lib/wallecx/expenseSchema.ts` contains the string `dayjs(val, 'YYYY-MM-DD', true).isValid()`
    - `src/lib/pocketbase/expenseMapper.ts` contains the string `record.notes !== undefined`
    - `src/lib/pocketbase/__tests__/expenseMapper.spec.ts` contains the string `not.toHaveProperty("notes")`
  </acceptance_criteria>
  <done>All 49+ tests pass green; CQ-01 refine() confirmed live; CQ-02 conditional spread + correct assertion confirmed live.</done>
</task>

<task type="auto">
  <name>Task 2: Mark CQ-01 and CQ-02 as complete in REQUIREMENTS.md</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read the full file to locate the exact lines for CQ-01 and CQ-02 before editing. The lines currently read `- [ ] **CQ-01**:` and `- [ ] **CQ-02**:`
  </read_first>
  <action>
    Update `.planning/REQUIREMENTS.md` to mark CQ-01 and CQ-02 as verified-complete. Change ONLY the checkbox characters from `[ ]` to `[x]` on these two specific lines. Do not alter any other content.

    Line to change (CQ-01):
    BEFORE: `- [ ] **CQ-01**: Expense date field rejects invalid calendar dates (e.g. Feb 31, Apr 31) with a clear validation error`
    AFTER:  `- [x] **CQ-01**: Expense date field rejects invalid calendar dates (e.g. Feb 31, Apr 31) with a clear validation error`

    Line to change (CQ-02):
    BEFORE: `- [ ] **CQ-02**: \`mapToUpdateExpense\` omits the \`notes\` key entirely when notes is undefined; mapper test asserts \`not.toHaveProperty('notes')\``
    AFTER:  `- [x] **CQ-02**: \`mapToUpdateExpense\` omits the \`notes\` key entirely when notes is undefined; mapper test asserts \`not.toHaveProperty('notes')\``

    Also update the Traceability table at the bottom: change the Status column for CQ-01 and CQ-02 from "Pending" to "Complete (verified 2026-05-22)".

    No other lines in the file should change.
  </action>
  <verify>
    <automated>cd C:/GitRepos/lex-lib.github.io && grep -n "CQ-01\|CQ-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `- [x] **CQ-01**:`
    - `.planning/REQUIREMENTS.md` contains `- [x] **CQ-02**:`
    - `.planning/REQUIREMENTS.md` does NOT contain `- [ ] **CQ-01**:` (no unchecked CQ-01)
    - `.planning/REQUIREMENTS.md` does NOT contain `- [ ] **CQ-02**:` (no unchecked CQ-02)
    - Traceability table rows for CQ-01 and CQ-02 show "Complete" status
  </acceptance_criteria>
  <done>REQUIREMENTS.md updated: CQ-01 and CQ-02 show [x] and "Complete" in the traceability table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → source files | Vitest reads source and spec files; no external network calls in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | REQUIREMENTS.md manual edit | accept | Markdown file under git version control; changes are reviewable in diff. No security-sensitive data. |
| T-27-01-02 | Information Disclosure | Test output in CI logs | accept | Unit tests run locally; no credentials or PII in test output. Test data uses synthetic fixtures. |
</threat_model>

<verification>
- `npm run test:unit` exits 0 with all tests passing
- Grep confirms `.planning/REQUIREMENTS.md` has `[x]` on both CQ-01 and CQ-02
</verification>

<success_criteria>
- All 49+ Vitest tests pass (0 failures, 0 errors)
- `expenseSchema.ts` line 21 contains `.refine(val => dayjs(val, 'YYYY-MM-DD', true).isValid(), ...)`
- `expenseMapper.ts` uses conditional spread `...(record.notes !== undefined ? { notes: record.notes } : {})`
- `expenseMapper.spec.ts` "drops notes when undefined" asserts `not.toHaveProperty("notes")`
- `.planning/REQUIREMENTS.md` shows `[x]` for CQ-01 and CQ-02
</success_criteria>

<output>
After completion, create `.planning/phases/27-code-quality-exports/27-01-SUMMARY.md`
</output>
