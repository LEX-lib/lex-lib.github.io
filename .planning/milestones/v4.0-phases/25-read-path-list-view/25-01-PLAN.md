---
phase: 25-read-path-list-view
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/wallecx/ExpenseItem.vue
  - src/components/projects/wallecx/ExpensesToolbar.vue
autonomous: true
requirements:
  - EXP-07
  - EXP-08
  - EXP-09

must_haves:
  truths:
    - "ExpenseItem.vue renders a single compact row: amount (bold, brand colour, 96px) | date+category (12px muted) + description (14px body, truncated) | optional paperclip icon | pencil icon | trash icon"
    - "All three action buttons (paperclip if receipt, pencil, trash) are at least 44×44px touch targets"
    - "ExpensesToolbar.vue emits update:searchQuery, update:sortMode, update:selectedCategories, update:dateFrom, update:dateTo — no internal state kept"
    - "ExpensesToolbar has two visible rows: Row 1 (search IconField + sort Select), Row 2 (category MultiSelect + two DatePickers)"
    - "Dark mode scoped CSS overrides for p-inputtext, p-select, p-multiselect, p-datepicker present in ExpensesToolbar"
  artifacts:
    - path: src/components/projects/wallecx/ExpenseItem.vue
      provides: Compact expense list row component
      exports: [default component]
    - path: src/components/projects/wallecx/ExpensesToolbar.vue
      provides: Search + sort + category filter + date range toolbar
      exports: [default component]
  key_links:
    - from: ExpenseItem.vue
      to: Expenses interface
      via: "import type { Expenses } from '@/types/wallecx/expenses/types'"
      pattern: "import.*Expenses.*types"
    - from: ExpenseItem.vue
      to: formatCurrency
      via: "import { formatCurrency } from '@/lib/wallecx/currency'"
      pattern: "formatCurrency"
    - from: ExpensesToolbar.vue
      to: ExpensesTab.vue
      via: defineProps + defineEmits v-model shape
      pattern: "update:searchQuery|update:sortMode|update:selectedCategories"
---

<objective>
Create the two new child components needed by ExpensesTab.vue before the tab itself is wired in Plan 02.

Purpose: These components must exist before ExpensesTab.vue imports them. Building them first in Wave 1 enables Plan 02 to reference real, stable contracts.
Output: `ExpenseItem.vue` (compact list row) and `ExpensesToolbar.vue` (search + sort + category + date range toolbar), both ready for import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-read-path-list-view/25-CONTEXT.md
@.planning/phases/25-read-path-list-view/25-RESEARCH.md
@.planning/phases/25-read-path-list-view/25-PATTERNS.md
@.planning/phases/25-read-path-list-view/25-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. All verified in RESEARCH.md. -->

From src/types/wallecx/expenses/types.d.ts:
```typescript
export interface Expenses extends RecordModel {
  id: string
  created: string       // ISO datetime
  updated: string
  user: string
  amount: number
  expense_date: string  // YYYY-MM-DD
  category: string      // denormalized name
  description: string
  notes?: string
  receipt?: string      // filename; PocketBase returns "" for empty — use falsy check
}
```

From src/lib/wallecx/currency.ts:
```typescript
export function formatCurrency(amount: number): string  // returns "₱1,234.56"
```

From src/components/projects/wallecx/WallecxToolbar.vue (pattern to mirror):
Uses defineProps + defineEmits (NOT defineModel).
emit names: 'update:searchQuery', 'update:sortMode'

From src/composables/useIsMobile.ts:
```typescript
export function useIsMobile(): ComputedRef<boolean>  // true when viewport < 640px
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExpenseItem.vue — compact expense list row</name>
  <files>src/components/projects/wallecx/ExpenseItem.vue</files>
  <read_first>
    - src/components/projects/wallecx/MembershipsTab.vue (action button pattern: min-w-[44px] min-h-[44px] touch-manipulation, iconify-icon usage)
    - src/types/wallecx/expenses/types.d.ts (Expenses interface — verify field names before writing)
    - src/lib/wallecx/currency.ts (formatCurrency signature)
    - src/assets/base.css (CSS variable names: --color-brand-primary, --color-typo-body, --color-typo-muted, --color-typo-heading, --color-surface-divider, --color-status-error)
  </read_first>
  <action>
Create `src/components/projects/wallecx/ExpenseItem.vue` as a new file. This is the compact list row for a single expense record.

**Script setup block:**
```typescript
import type { Expenses } from '@/types/wallecx/expenses/types'
import { formatCurrency } from '@/lib/wallecx/currency'
import dayjs from 'dayjs'

defineProps<{ record: Expenses }>()

const emit = defineEmits<{
  edit:    [record: Expenses]
  delete:  [record: Expenses]
  preview: [record: Expenses]
}>()
```

**Template — exact layout (copy verbatim from PATTERNS.md §ExpenseItem row layout):**
```html
<template>
  <div
    class="flex items-center gap-3 py-3 border-b"
    style="border-color: var(--color-surface-divider)"
  >
    <!-- Amount: fixed 96px, bold, brand colour -->
    <div class="w-24 shrink-0 font-bold text-base"
         style="color: var(--color-brand-primary)">
      {{ formatCurrency(record.amount) }}
    </div>

    <!-- Meta + description: fills remaining space -->
    <div class="flex-1 min-w-0">
      <div class="text-xs" style="color: var(--color-typo-muted)">
        {{ dayjs(record.expense_date).format('D MMM YYYY') }} · {{ record.category }}
      </div>
      <div class="text-sm truncate" style="color: var(--color-typo-body)">
        {{ record.description }}
      </div>
    </div>

    <!-- Paperclip icon — only when receipt is truthy (falsy check handles "" and undefined) -->
    <button
      v-if="record.receipt"
      @click.stop="emit('preview', record)"
      class="min-w-[44px] min-h-[44px] flex items-center justify-center touch-manipulation"
      aria-label="View receipt"
    >
      <iconify-icon icon="mdi:paperclip" width="20" height="20"
        style="color: var(--color-typo-muted)" aria-hidden="true" />
    </button>

    <!-- Edit button -->
    <button
      @click.stop="emit('edit', record)"
      class="min-w-[44px] min-h-[44px] flex items-center justify-center touch-manipulation"
      aria-label="Edit expense"
    >
      <iconify-icon icon="mdi:pencil-outline" width="20" height="20"
        style="color: var(--color-typo-muted)" aria-hidden="true" />
    </button>

    <!-- Delete button -->
    <button
      @click.stop="emit('delete', record)"
      class="min-w-[44px] min-h-[44px] flex items-center justify-center touch-manipulation"
      aria-label="Delete expense"
    >
      <iconify-icon icon="mdi:trash-can-outline" width="20" height="20"
        style="color: var(--color-status-error)" aria-hidden="true" />
    </button>
  </div>
</template>
```

**Style block:** Add an empty `<style scoped>` block with a comment explaining no `:deep` overrides are needed because all colours reference CSS variables that auto-switch under `.my-app-dark`:
```css
<style scoped>
/*
 * No PrimeVue form components in ExpenseItem — all colour values reference
 * CSS variables from base.css that auto-switch when .my-app-dark is active.
 * No :deep overrides required.
 */
</style>
```

**Critical constraints:**
- Use `v-if="record.receipt"` (falsy check) — NOT `record.receipt !== undefined`. PocketBase returns `""` for empty file fields; both `""` and `undefined` are falsy. Using `!== undefined` would show the icon on records with an empty string receipt.
- All three action buttons (paperclip, pencil, trash) must have `min-w-[44px] min-h-[44px] touch-manipulation` — Phase 15 invariant.
- Paperclip icon color is `var(--color-typo-muted)` (same as pencil), NOT `var(--color-brand-primary)`.
- Trash icon color is `var(--color-status-error)`.
- Description text must have `truncate` class to prevent long descriptions from breaking the row layout.
- `@click.stop` on all buttons prevents event bubbling (the row div has no click handler, but this is defensive per MembershipsTab pattern).
  </action>
  <verify>
    <automated>
      grep -n "min-w-\[44px\]" src/components/projects/wallecx/ExpenseItem.vue
      grep -n "formatCurrency" src/components/projects/wallecx/ExpenseItem.vue
      grep -n "v-if=\"record.receipt\"" src/components/projects/wallecx/ExpenseItem.vue
      grep -n "emit('preview'" src/components/projects/wallecx/ExpenseItem.vue
      grep -n "emit('edit'" src/components/projects/wallecx/ExpenseItem.vue
      grep -n "emit('delete'" src/components/projects/wallecx/ExpenseItem.vue
      npm run type-check
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/projects/wallecx/ExpenseItem.vue
    - `grep "min-w-\[44px\]" src/components/projects/wallecx/ExpenseItem.vue` returns at least 3 matches (one per action button)
    - `grep "formatCurrency(record.amount)" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match
    - `grep 'v-if="record.receipt"' src/components/projects/wallecx/ExpenseItem.vue` returns 1 match (NOT `record.receipt !== undefined`)
    - `grep "mdi:paperclip" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match
    - `grep "mdi:pencil-outline" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match
    - `grep "mdi:trash-can-outline" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match
    - `grep "color-status-error" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match (on trash icon)
    - `grep "color-surface-divider" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match (border-b style)
    - `grep "truncate" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match (on description div)
    - `grep "touch-manipulation" src/components/projects/wallecx/ExpenseItem.vue` returns at least 2 matches
    - `grep "D MMM YYYY" src/components/projects/wallecx/ExpenseItem.vue` returns 1 match (dayjs format)
    - `npm run type-check` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>ExpenseItem.vue exists with the exact row layout per CONTEXT.md D-01/D-02 and UI-SPEC Component Inventory. All 3 action buttons are 44×44px. Receipt icon is conditional on falsy check. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create ExpensesToolbar.vue — search + sort + category + date range toolbar</name>
  <files>src/components/projects/wallecx/ExpensesToolbar.vue</files>
  <read_first>
    - src/components/projects/wallecx/WallecxToolbar.vue (full file — copy the defineProps + defineEmits shape and search IconField pattern exactly; this is the direct analog)
    - src/assets/wallecx-overrides.css (understand what's already global vs. what needs scoped overrides)
    - src/components/projects/wallecx/ManageExpense.vue (lines 510–540 — dark mode scoped CSS override pattern for PrimeVue form inputs)
  </read_first>
  <action>
Create `src/components/projects/wallecx/ExpensesToolbar.vue` as a new file. This component owns NO state — it is a pure v-model relay: parent owns all refs, toolbar emits updates.

**Script setup block — defineProps + defineEmits (NOT defineModel; mirrors WallecxToolbar.vue pattern exactly):**
```typescript
defineProps<{
  searchQuery: string
  sortMode: string
  sortOptions: { value: string; label: string }[]
  selectedCategories: string[]
  categoryOptions: string[]
  dateFrom: Date | null
  dateTo: Date | null
}>()

const emit = defineEmits<{
  'update:searchQuery': [value: string]
  'update:sortMode': [value: string]
  'update:selectedCategories': [value: string[]]
  'update:dateFrom': [value: Date | null]
  'update:dateTo': [value: Date | null]
}>()
```

**Template — two-row layout with flex-wrap on Row 2:**
```html
<template>
  <div class="flex flex-col gap-2 mb-4">

    <!-- Row 1: Search input + Sort select -->
    <div class="flex items-center gap-2">
      <!-- Search (mirrors WallecxToolbar.vue search pattern exactly) -->
      <IconField class="flex-1">
        <InputIcon class="pi pi-search" />
        <InputText
          :value="searchQuery"
          placeholder="Search by description…"
          class="w-full"
          @input="emit('update:searchQuery', ($event.target as HTMLInputElement).value)"
        />
        <InputIcon
          v-if="searchQuery"
          class="pi pi-times cursor-pointer"
          role="button"
          tabindex="0"
          aria-label="Clear search"
          @click="emit('update:searchQuery', '')"
          @keydown.enter="emit('update:searchQuery', '')"
          @keydown.space.prevent="emit('update:searchQuery', '')"
        />
      </IconField>
      <!-- Sort select (mirrors WallecxToolbar.vue sort Select exactly) -->
      <Select
        :model-value="sortMode"
        :options="sortOptions"
        option-label="label"
        option-value="value"
        class="w-36 min-h-[44px]"
        @update:model-value="emit('update:sortMode', $event)"
      />
    </div>

    <!-- Row 2: Category MultiSelect + two DatePickers; flex-wrap for narrow viewports -->
    <div class="flex items-center gap-2 flex-wrap">
      <MultiSelect
        :model-value="selectedCategories"
        :options="categoryOptions"
        placeholder="All categories"
        class="flex-1 min-h-[44px]"
        display="chip"
        @update:model-value="emit('update:selectedCategories', $event)"
      />
      <DatePicker
        :model-value="dateFrom"
        placeholder="From date"
        dateFormat="dd M yy"
        class="w-32 min-h-[44px]"
        showButtonBar
        @update:model-value="emit('update:dateFrom', $event)"
      />
      <DatePicker
        :model-value="dateTo"
        placeholder="To date"
        dateFormat="dd M yy"
        class="w-32 min-h-[44px]"
        showButtonBar
        @update:model-value="emit('update:dateTo', $event)"
      />
    </div>

  </div>
</template>
```

**Dark mode scoped CSS (per D-18 + PATTERNS.md §Dark mode scoped CSS):**
Target `.my-app-dark` prefixed selectors inside `<style scoped>` for all PrimeVue form inputs used in this component:
```css
<style scoped>
:deep(.my-app-dark .p-inputtext) {
  background-color: var(--color-surface-card);
  color: var(--color-typo-body);
  border-color: var(--color-surface-divider);
}
:deep(.my-app-dark .p-select),
:deep(.my-app-dark .p-multiselect),
:deep(.my-app-dark .p-datepicker) {
  background-color: var(--color-surface-card);
  color: var(--color-typo-body);
  border-color: var(--color-surface-divider);
}
</style>
```

**Critical constraints:**
- Do NOT use `defineModel` — WallecxToolbar uses `defineProps + defineEmits` and ExpensesToolbar must match that pattern for consistency (per RESEARCH.md Pattern 5).
- Do NOT use `@vueuse/core` for debounce — `@vueuse/core` is NOT installed. Search filters reactively on every keystroke (matching existing VaccinationsTab + MembershipsTab pattern). No `useDebounce`, no `setTimeout` debounce — just direct emit on `@input`.
- `categoryOptions` is a prop passed from ExpensesTab (derived from raw `expenses.value` there). The toolbar does NOT compute its own options.
- `sortOptions` is a prop array (`{ value: string; label: string }[]`). The toolbar does NOT define the options — the parent (ExpensesTab) will pass the array.
- Row 2 uses `flex-wrap` so DatePickers drop to a new line on narrow viewports rather than overflowing horizontally.
- All interactive controls (Select, MultiSelect, DatePicker) must have `min-h-[44px]` — Phase 15 touch target invariant.
  </action>
  <verify>
    <automated>
      grep -n "defineEmits" src/components/projects/wallecx/ExpensesToolbar.vue
      grep -n "update:searchQuery" src/components/projects/wallecx/ExpensesToolbar.vue
      grep -n "update:selectedCategories" src/components/projects/wallecx/ExpensesToolbar.vue
      grep -n "update:dateFrom" src/components/projects/wallecx/ExpensesToolbar.vue
      grep -n "update:dateTo" src/components/projects/wallecx/ExpensesToolbar.vue
      grep -n "my-app-dark" src/components/projects/wallecx/ExpensesToolbar.vue
      grep -n "defineModel" src/components/projects/wallecx/ExpensesToolbar.vue
      npm run type-check
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/projects/wallecx/ExpensesToolbar.vue
    - `grep "defineModel" src/components/projects/wallecx/ExpensesToolbar.vue` returns 0 matches (must NOT use defineModel)
    - `grep "update:searchQuery" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "update:sortMode" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "update:selectedCategories" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "update:dateFrom" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "update:dateTo" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "MultiSelect" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "DatePicker" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 2 matches (From and To)
    - `grep "From date" src/components/projects/wallecx/ExpensesToolbar.vue` returns 1 match (placeholder text)
    - `grep "To date" src/components/projects/wallecx/ExpensesToolbar.vue` returns 1 match (placeholder text)
    - `grep "All categories" src/components/projects/wallecx/ExpensesToolbar.vue` returns 1 match (MultiSelect placeholder)
    - `grep "my-app-dark" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 4 matches (scoped CSS block)
    - `grep "p-multiselect" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "p-datepicker" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 1 match
    - `grep "useDebounce\|vueuse/core" src/components/projects/wallecx/ExpensesToolbar.vue` returns 0 matches
    - `grep "flex-wrap" src/components/projects/wallecx/ExpensesToolbar.vue` returns 1 match (Row 2)
    - `grep "min-h-\[44px\]" src/components/projects/wallecx/ExpensesToolbar.vue` returns at least 3 matches
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>ExpensesToolbar.vue exists with two-row layout (search+sort / category+dates), pure v-model relay pattern (no internal state, no defineModel, no @vueuse/core), and dark mode scoped CSS overrides for all PrimeVue inputs. TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → ExpensesToolbar inputs | Search text and date inputs accept arbitrary user-typed strings — used only in client-side computed filtering, never sent to PocketBase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | ExpenseItem.vue receipt button | accept | Receipt data is from the authenticated user's own records; button opens AttachmentPreview which requires a fresh `pb.files.getToken()` fetched in ExpensesTab — no token = no access; no disclosure risk to other users |
| T-25-02 | Tampering | ExpensesToolbar search input | accept | Input is used purely for client-side `.includes()` filtering on an already-loaded in-memory array; never sent to PocketBase; no server-side tampering risk |
| T-25-03 | Denial of Service | ExpensesToolbar DatePicker | accept | DatePicker returns a JavaScript Date object; invalid input is handled by the PrimeVue component itself; no custom parsing that could cause a crash |
</threat_model>

<verification>
After both tasks complete:
1. `npm run type-check` passes with no errors referencing ExpenseItem.vue or ExpensesToolbar.vue
2. `npm run lint` passes (or only reports pre-existing warnings unrelated to Phase 25 files)
3. Both files exist at their declared paths
4. ExpenseItem.vue: amount column is `w-24 shrink-0`, meta column is `flex-1 min-w-0`, all icon buttons are `min-w-[44px] min-h-[44px]`
5. ExpensesToolbar.vue: uses `defineProps` + `defineEmits` (NOT `defineModel`), no `@vueuse/core` import
</verification>

<success_criteria>
- ExpenseItem.vue and ExpensesToolbar.vue both exist and TypeScript-compile clean
- ExpenseItem renders the exact row layout per CONTEXT.md D-01 and UI-SPEC Component Inventory
- ExpensesToolbar has all 5 v-model bindings (search, sort, selectedCategories, dateFrom, dateTo) wired as emit-only
- No `@vueuse/core` dependency introduced
- Dark mode scoped CSS present in ExpensesToolbar for all 4 PrimeVue input types
</success_criteria>

<output>
After completion, create `.planning/phases/25-read-path-list-view/25-01-SUMMARY.md` using the summary template.
</output>
