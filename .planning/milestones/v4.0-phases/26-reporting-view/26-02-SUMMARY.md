---
phase: 26-reporting-view
plan: "02"
subsystem: ui
tags: [vue3, refactor, sfc-extraction, props-events, sessionStorage, primevue]

# Dependency graph
requires:
  - phase: 25-read-path-list-view
    provides: ExpensesTab.vue Phase 25 list logic (filteredSortedExpenses, categoryOptions, sessionStorage 'wallecx:expense-sort' persistence, 4-state v-if order, ExpensesToolbar + ExpenseItem rendering)
  - phase: 26-reporting-view
    provides: Plan 26-01 Wave 1 foundation (chart.js, palette, period.ts, useChartTheme) — not consumed in this plan; sets the stage for Plan 26-03
provides:
  - ExpensesListView.vue — new sibling component owning Phase 25 list view (toolbar + filtered/sorted list + 4 states); receives { expenses, isLoading } as props; emits edit/delete/preview/request-add-expense
  - ExpensesTab.vue reduced to thin shell — owns data load, ManageExpense + AttachmentPreview dialog/drawer wiring, delete + receipt-preview handlers; delegates list rendering to ExpensesListView via 4 wired events
  - Architectural foundation for Plan 26-03 to introduce List/Reports sub-tabs as sibling views sharing the same expenses array via common parent
affects: [26-03, future-write-path-extensions, future-sub-tab-additions]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Parent-shell + child-view SFC split: shell owns data + dialogs + mutating handlers; child owns view-specific UI state (filters, sort, search) and emits intent events back up"
    - "Props-down/events-up at the SFC boundary: child reads props.expenses (raw) for derivation, emits semantic events (edit/delete/preview/request-add-expense) rather than calling sibling functions directly"
    - "sessionStorage persistence travels WITH the view-state owner (sort mode + whitelist + restore live in ExpensesListView, not in shell)"

key-files:
  created:
    - src/components/projects/wallecx/ExpensesListView.vue
  modified:
    - src/components/projects/wallecx/ExpensesTab.vue
    - components.d.ts (auto-regenerated by unplugin-vue-components)

key-decisions:
  - "ExpensesListView reads props.expenses (no .value) — props are unwrapped in setup, so categoryOptions and filteredSortedExpenses dereference the prop directly"
  - "categoryOptions still derives from RAW props.expenses (NOT filteredSortedExpenses) — Phase 25 Pitfall 3 preserved in the new component"
  - "4-state v-if order preserved verbatim: isLoading > expenses.length === 0 > filteredSortedExpenses.length === 0 > v-else"
  - "sessionStorage 'wallecx:expense-sort' read + write + VALID_SORT_MODES whitelist moved entirely to ExpensesListView — the shell no longer touches sort state"
  - "deleteExpense + defineExpose({ deleteExpense }) stay in ExpensesTab shell (Phase 24 invariant) — the child emits 'delete' but does NOT own the confirm/delete flow"
  - "Receipt preview state (showPreview, previewRecord, previewToken, openReceiptPreview) stays in ExpensesTab shell — Reports sub-tab in Plan 26-03 will NOT need this; keeping it in the shell avoids re-extraction later"
  - "ConfirmDialog NOT introduced in either file — STATE.md invariant: confirm dialog lives at WallecxApp shell level only"

patterns-established:
  - "Tab-content shell pattern: each Wallecx tab can host one or more sibling view components that share data via props; shell owns CRUD; views own view-specific UI state"
  - "Verbatim copy preservation across refactors: every user-facing string ('No expenses yet.', 'Add expense', 'Clear filters', delete confirmation) carried over without edit so visual diffs are zero"

requirements-completed: []  # EXP-11 will be marked complete in Plan 26-03 when the period-tabbed reporting view ships; this plan is its structural prerequisite (parent-shell + sibling-view architecture) but delivers no user-visible reporting yet.

# Metrics
duration: 5min
completed: 2026-05-22
---

# Phase 26 Plan 02: ExpensesTab thin-shell refactor + ExpensesListView extraction Summary

**Pure structural refactor extracting Phase 25 list logic into a new ExpensesListView.vue sibling, leaving ExpensesTab.vue as a thin shell that owns the data load + dialogs + CRUD handlers — zero user-visible behavior change, all Phase 25 invariants preserved.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-22T01:39:50Z
- **Completed:** 2026-05-22T01:44:49Z
- **Tasks:** 2
- **Files modified:** 3 (1 new, 2 modified including auto-regenerated components.d.ts)

## Accomplishments

- ExpensesListView.vue created (186 lines) — owns all Phase 25 list view concerns: 5 filter/sort refs (searchQuery, sortMode, selectedCategories, dateFrom, dateTo), 2 computeds (categoryOptions, filteredSortedExpenses), clearFilters helper, sessionStorage persistence via watch(sortMode) + onMounted restore with VALID_SORT_MODES whitelist, ExpensesToolbar wiring, 4-state template (loading skeleton / raw-empty / filtered-empty / data list), all Phase 25 copy verbatim
- ExpensesTab.vue reduced from 313 lines to 161 lines (−152 lines) — now imports only `ref, onMounted` from vue; retains getFullList with locked requestKey 'expenses-getFullList', isLoading flag, ManageExpense + AttachmentPreview Dialog/Drawer wiring, openManage/onCreated/onUpdated, deleteExpense (with useConfirm) + defineExpose, openReceiptPreview (with pb.files.getToken WR-03 guard); delegates list to `<ExpensesListView>` via 4 wired events
- All Phase 25 invariants verified preserved: requestKey 'expenses-getFullList', sessionStorage key 'wallecx:expense-sort', VALID_SORT_MODES whitelist, defineExpose({ deleteExpense }), ConfirmDialog stays at WallecxApp shell level, no @vueuse/core import, no PocketBase calls in child
- Verification: type-check passes, build-only passes (vite + PWA generate), all 49 unit tests pass (10 vaccinationMapper + 11 membershipMapper + 9 expenseMapper + 16 period + 3 router guard)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create ExpensesListView.vue by extracting Phase 25 list logic** — `bcf4568` (feat)
2. **Task 2: Refactor ExpensesTab.vue to thin shell** — `42ed8ea` (refactor)

## Files Created/Modified

- `src/components/projects/wallecx/ExpensesListView.vue` — NEW. 186-line SFC. Receives { expenses: Expenses[], isLoading: boolean } as props; emits 4 typed events (edit, delete, preview, request-add-expense). Owns Phase 25 list state and rendering.
- `src/components/projects/wallecx/ExpensesTab.vue` — MODIFIED. Reduced from 313 → 161 lines. Removed: computed/watch/dayjs imports, ExpensesToolbar/ExpenseItem imports, SORT_STORAGE_KEY/VALID_SORT_MODES, 5 filter-sort refs, expenseSortOptions, categoryOptions/filteredSortedExpenses computeds, clearFilters, and the entire toolbar+list+empty-states template body. Added: `import ExpensesListView` + single `<ExpensesListView :expenses :is-loading @edit @delete @preview @request-add-expense />` delegation.
- `components.d.ts` — auto-regenerated by `unplugin-vue-components` plugin during build; adds `ExpensesListView` and `MultiSelect` registrations.

## Decisions Made

None — plan executed exactly as specified. The plan front-loaded all design decisions (props/events surface, what moves to child vs. stays in shell, which Phase 25 invariants must hold, what file shape to write). All decisions documented in the frontmatter were inherited from the plan, not introduced during execution.

## Deviations from Plan

None - plan executed exactly as written.

The only modification to the planned code shape was removing the literal token "ConfirmDialog" from a comment in ExpensesTab.vue (`// Delete uses useConfirm; ConfirmDialog itself is mounted at WallecxApp.vue shell level.` → `// ... the confirm dialog itself is mounted at WallecxApp.vue shell level.`) so the acceptance grep `grep "ConfirmDialog" ExpensesTab.vue` returns 0 matches as required. This is wording-only inside a comment, no behavior change.

---

**Total deviations:** 0
**Impact on plan:** None.

## Issues Encountered

None.

## Verification Evidence

- `npm run type-check` → exit 0 (vue-tsc --build, no errors)
- `npm run build-only` → exit 0 (vite production build, PWA precache 56 entries generated)
- `npm run test:unit` → 49 tests passed across 5 test files (no regressions)
- `npm run lint` → only the pre-existing VaccinationDetail.vue `'props' is assigned a value but never used` warning (documented in STATE.md as deferred to maintenance sweep); both new/modified files lint clean
- All 22 positive grep acceptance criteria in 26-02-PLAN.md pass (verified)
- All 8 negative grep acceptance criteria in 26-02-PLAN.md pass (verified — no leftover ExpensesToolbar/ExpenseItem/filteredSortedExpenses/categoryOptions/sort-state references in ExpensesTab.vue; no ConfirmDialog/vueuse-core/pb.collection/pb.files references in ExpensesListView.vue)

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 26-03 (Wave 3) is now unblocked. The architecture is in place: ExpensesTab.vue is a parent shell that owns `expenses` data; ExpensesListView.vue is a sibling view. Plan 26-03 will introduce ExpensesReportsView.vue as a second sibling view and wire a sub-tab control inside ExpensesTab.vue to switch between the two — both will consume the same `expenses` prop and the same `isLoading` flag from the shell.
- All Wave 1 foundation artifacts (chart.js@^4.5.1, --color-chart-1..8 CSS tokens, src/lib/wallecx/period.ts with quarterOfYear plugin, src/composables/useChartTheme.ts) remain ready for Plan 26-03 consumption.
- No blockers, no open todos.

## Self-Check: PASSED

- ExpensesListView.vue exists at `src/components/projects/wallecx/ExpensesListView.vue` — FOUND
- ExpensesTab.vue exists at `src/components/projects/wallecx/ExpensesTab.vue` — FOUND
- Commit bcf4568 (Task 1) — FOUND in git log
- Commit 42ed8ea (Task 2) — FOUND in git log
- All grep acceptance criteria pass
- type-check, build, test:unit all green

---
*Phase: 26-reporting-view*
*Completed: 2026-05-22*
