---
phase: 13-write-path-managemembership-crud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/wallecx/ManageMembership.vue
autonomous: true
requirements:
  - MWRITE-01
  - MWRITE-02
  - MWRITE-03
  - MWRITE-04
  - MWRITE-05

must_haves:
  truths:
    - "User can open an 'Add Card' dialog, fill in card name and colour, and submit to create a new membership record"
    - "User can open an 'Edit Card' dialog pre-filled with existing record values, modify fields, and submit to update the record"
    - "Submitting the form during a slow network fires only one request even on double-click (isSaving guard)"
    - "ColorPicker starts pre-filled with navy 002244 for new cards and with the record's card_color for edit mode"
    - "barcode_value field disappears entirely from the DOM when barcode_type is 'number'; switching to Number only clears the ref"
    - "Uploading a card image strips EXIF metadata via canvas re-encode before the file is stored in pendingFile"
    - "Edit mode shows a 100x100 thumbnail of the existing card_image above the FileUpload button"
    - "Session expiry during create is caught with an explicit null guard — no unhandled TypeError"
    - "pendingFile is reset in both the success branch and onHide so stale files never carry to the next dialog open"
  artifacts:
    - path: "src/components/projects/wallecx/ManageMembership.vue"
      provides: "Create/edit dialog for membership cards"
      exports: ["defineModel visible", "defineModel record", "emit created", "emit updated"]
      min_lines: 200
  key_links:
    - from: "src/components/projects/wallecx/ManageMembership.vue"
      to: "src/lib/pocketbase/membershipMapper.ts"
      via: "import { mapToUpdateMembership }"
      pattern: "import.*mapToUpdateMembership.*from.*membershipMapper"
    - from: "src/components/projects/wallecx/ManageMembership.vue"
      to: "src/types/wallecx/memberships/types.d.ts"
      via: "import type { Memberships }"
      pattern: "import type.*Memberships.*from.*types"
    - from: "src/components/projects/wallecx/ManageMembership.vue"
      to: "pocketbase (pb.collection('wallecx_memberships').create / .update)"
      via: "onSubmit FormData"
      pattern: "wallecx_memberships"
---

<objective>
Create ManageMembership.vue — the create/edit dialog for membership cards. Structural analog of ManageVaccination.vue with three key differences: direct v-model refs instead of @primevue/forms (per D-07, locked due to PrimeVue issue #8135), ColorPicker field with live swatch, and conditional barcode_value field.

Purpose: Provides the primary CRUD write surface for membership cards — all create and edit operations flow through this dialog.

Output: src/components/projects/wallecx/ManageMembership.vue — fully functional, type-safe, ready to be imported by MembershipsTab.vue in Plan 03.
</objective>

<execution_context>
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-write-path-managemembership-crud/13-CONTEXT.md
@.planning/phases/13-write-path-managemembership-crud/13-RESEARCH.md
@.planning/phases/13-write-path-managemembership-crud/13-PATTERNS.md
@.planning/phases/13-write-path-managemembership-crud/13-UI-SPEC.md

<interfaces>
<!-- Direct structural analog — read before writing ManageMembership.vue -->
<!-- From src/components/projects/wallecx/ManageVaccination.vue (full file, key lines) -->

Script setup imports (lines 1-11):
```typescript
import { ref, computed } from "vue";
import { zodResolver } from "@primevue/forms/resolvers/zod";  // DO NOT include — D-07
import { Form, type FormSubmitEvent } from "@primevue/forms"; // DO NOT include — D-07
// ManageMembership.vue adds: import { ref, computed, watch } from "vue";
```

defineModel + emit pattern (lines 13-19):
```typescript
const visible = defineModel("visible", { type: Boolean, default: false, required: true });
const record = defineModel<Memberships | null>("record", { default: null });
const emit = defineEmits<{
  created: [record: Memberships];
  updated: [record: Memberships];
}>();
```

isSaving guard (lines 21-22):
```typescript
const isSaving = ref(false);
const pendingFile = ref<File | null>(null);
```

isEditMode + dialogHeader (lines 24-25):
```typescript
const isEditMode = computed(() => record.value !== null);
const dialogHeader = computed(() => isEditMode.value ? "Edit Card" : "Add Card");
```

Dialog shell template (lines 190-198):
```html
<Dialog modal v-model:visible="visible" :header="dialogHeader"
  :style="{ width: '40vw' }" :breakpoints="{ '960px': '75vw', '641px': '92vw' }"
  :closable="!isSaving" @hide="onHide">
```

<!-- Memberships type — from src/types/wallecx/memberships/types.d.ts -->
```typescript
export interface Memberships extends RecordModel {
  id: string; created: string; updated: string; user: string;
  card_name: string;
  issuer?: string; barcode_value?: string; barcode_type?: string;
  card_number?: string; expiry_date?: string; notes?: string;
  card_color?: string; card_image?: string;
}
```

<!-- mapToUpdateMembership — from src/lib/pocketbase/membershipMapper.ts -->
```typescript
export function mapToUpdateMembership(record: Memberships): {
  card_name: string; issuer?: string; barcode_value?: string;
  barcode_type?: string; card_number?: string; expiry_date?: string;
  notes?: string; card_color?: string;
}
```

<!-- MembershipsTab.vue current state — ManageMembership.vue will be imported here in Plan 03 -->
<!-- Props needed: v-model:visible, v-model:record, :token (fileToken for edit thumbnail) -->
<!-- Emits consumed: @created, @updated -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ManageMembership.vue — script setup (all logic)</name>
  <files>src/components/projects/wallecx/ManageMembership.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageVaccination.vue (full file — structural analog; read entirely before writing)
    - src/types/wallecx/memberships/types.d.ts (Memberships interface)
    - src/lib/pocketbase/membershipMapper.ts (mapToUpdateMembership signature)
    - .planning/phases/13-write-path-managemembership-crud/13-PATTERNS.md (Pattern 1 through 8 — direct copy targets)
    - .planning/phases/13-write-path-managemembership-crud/13-RESEARCH.md (Anti-Patterns section — mandatory reading)
  </read_first>
  <behavior>
    The script setup block is the most logic-dense part. Write and verify all logic before the template task.

    Expected behaviors verifiable before template:
    - BARCODE_TYPE_OPTIONS constant defined with 5 entries: qr, code128, ean13, code39, number
    - record watcher resets all refs to create-mode defaults when record.value is null
    - record watcher sets all refs from record fields when record.value is a Memberships object
    - cardColor initializes to '002244' (no #) in both create mode and edit mode fallback (per D-01)
    - barcodeType watcher clears barcodeValue to '' immediately when newType === 'number' (per D-02)
    - thumbnailUrl computed returns null when isEditMode is false OR record.card_image is falsy
    - onSubmit returns early (sets error ref) when card_name.value is empty string
    - onSubmit does NOT append barcode_value to FormData when barcodeType.value === 'number' (per D-02 guard)
    - onSubmit does NOT append card_image to FormData when pendingFile.value is null (per D-03)
    - onSubmit null-guards pb.authStore.record?.id and returns with toast.error on null (HIGH-01)
    - pendingFile.value = null in success branch (HIGH-02 first reset)
    - pendingFile.value = null in onHide() (HIGH-02 second reset)
    - isSaving.value = false in finally block
  </behavior>
  <action>
    Create src/components/projects/wallecx/ManageMembership.vue.

    SCRIPT SETUP — complete implementation:

    Imports (adapted from ManageVaccination.vue, D-07 removes zodResolver/Form):
    ```typescript
    import { ref, computed, watch } from "vue";
    import { z } from "zod";
    import imageCompression from "browser-image-compression";
    import { toast } from "vue-sonner";
    import dayjs from "dayjs";
    import { pb } from "@/lib/pocketbase";
    import { mapToUpdateMembership } from "@/lib/pocketbase/membershipMapper";
    import type { Memberships } from "@/types/wallecx/memberships/types";
    ```

    Props (token for edit-mode thumbnail — D-03):
    ```typescript
    const props = defineProps<{
      token?: string;
    }>();
    ```

    defineModel + emit:
    ```typescript
    const visible = defineModel("visible", { type: Boolean, default: false, required: true });
    const record = defineModel<Memberships | null>("record", { default: null });
    const emit = defineEmits<{
      created: [record: Memberships];
      updated: [record: Memberships];
    }>();
    ```

    Saving + file state:
    ```typescript
    const isSaving = ref(false);
    const pendingFile = ref<File | null>(null);
    ```

    Computed guards:
    ```typescript
    const isEditMode = computed(() => record.value !== null);
    const dialogHeader = computed(() => isEditMode.value ? "Edit Card" : "Add Card");
    ```

    Barcode type options constant:
    ```typescript
    const BARCODE_TYPE_OPTIONS = [
      { label: "QR Code",     value: "qr"      },
      { label: "Code 128",    value: "code128" },
      { label: "EAN-13",      value: "ean13"   },
      { label: "Code 39",     value: "code39"  },
      { label: "Number only", value: "number"  },
    ] as const;
    ```

    Direct v-model refs (D-07 — NOT @primevue/forms):
    ```typescript
    const cardName = ref<string>("");
    const barcodeType = ref<string | null>(null);
    const barcodeValue = ref<string>("");
    const cardNumber = ref<string>("");
    const cardColor = ref<string>("002244");  // D-01: navy default, no #
    const expiryDate = ref<Date | null>(null);
    const issuer = ref<string>("");
    const notes = ref<string>("");
    ```

    Error refs:
    ```typescript
    const cardNameError = ref<string>("");
    const cardColorError = ref<string>("");
    ```

    Record watcher — initializes all refs when record prop changes:
    ```typescript
    watch(
      () => record.value,
      (rec) => {
        if (rec) {
          cardName.value = rec.card_name;
          barcodeType.value = rec.barcode_type ?? null;
          barcodeValue.value = rec.barcode_value ?? "";
          cardNumber.value = rec.card_number ?? "";
          cardColor.value = rec.card_color ?? "002244";  // D-01: fallback navy
          expiryDate.value = rec.expiry_date ? new Date(rec.expiry_date) : null;
          issuer.value = rec.issuer ?? "";
          notes.value = rec.notes ?? "";
        } else {
          cardName.value = "";
          barcodeType.value = null;
          barcodeValue.value = "";
          cardNumber.value = "";
          cardColor.value = "002244";  // D-01
          expiryDate.value = null;
          issuer.value = "";
          notes.value = "";
        }
      },
      { immediate: true },
    );
    ```

    barcode_type watcher — D-02: clear barcodeValue immediately when switching to 'number':
    ```typescript
    watch(barcodeType, (newType) => {
      if (newType === "number") {
        barcodeValue.value = "";
      }
    });
    ```

    Thumbnail computed — D-03: returns URL only in edit mode with existing card_image:
    ```typescript
    const thumbnailUrl = computed(() =>
      isEditMode.value && record.value?.card_image
        ? pb.files.getURL(record.value, record.value.card_image, {
            thumb: "100x100",
            token: props.token,
          })
        : null
    );
    ```

    Zod schema (manual safeParse — D-07):
    ```typescript
    const schema = z.object({
      card_name: z.string().min(1, { message: "Card name is required." }),
      card_color: z
        .string()
        .regex(/^[0-9a-fA-F]{6}$/, { message: "Must be a valid 6-character hex colour." })
        .optional(),
      barcode_value: z.string().optional(),
      barcode_type: z.string().optional().nullable(),
      card_number: z.string().optional(),
      expiry_date: z.union([z.date(), z.string()]).optional().nullable(),
      issuer: z.string().optional(),
      notes: z.string().optional(),
    });
    ```

    onFileSelect (adapted from ManageVaccination.vue — PDF branch removed per Phase 11 D-02):
    - Allowed types: ["image/jpeg", "image/png", "image/webp"] — no application/pdf
    - Max size: 10 * 1024 * 1024
    - EXIF strip: canvas re-encode → imageCompression({ maxSizeMB: 1.5, maxWidthOrHeight: 2048, useWebWorker: true })
    - On success: pendingFile.value = compressed; toast.info("Location data removed.")
    - On error: toast.error("Failed to process image. Please try again."); pendingFile.value = null
    - (Copy ManageVaccination.vue lines 58-124 verbatim but remove the PDF early-return branch at lines 73-76)

    onSubmit (D-07, D-08, D-09, D-10, HIGH-01, HIGH-02):
    ```typescript
    async function onSubmit(): Promise<void> {
      cardNameError.value = "";
      cardColorError.value = "";

      const result = schema.safeParse({
        card_name: cardName.value,
        card_color: cardColor.value || undefined,
        barcode_value: barcodeValue.value || undefined,
        barcode_type: barcodeType.value,
        card_number: cardNumber.value || undefined,
        expiry_date: expiryDate.value,
        issuer: issuer.value || undefined,
        notes: notes.value || undefined,
      });

      if (!result.success) {
        // Zod 4: flatten().fieldErrors for per-field messages
        const fieldErrors = result.error.flatten().fieldErrors;
        cardNameError.value = fieldErrors.card_name?.[0] ?? "";
        cardColorError.value = fieldErrors.card_color?.[0] ?? "";
        return;
      }

      isSaving.value = true;
      try {
        const formData = new FormData();
        formData.append("card_name", cardName.value);
        if (barcodeType.value) formData.append("barcode_type", barcodeType.value);
        // D-02: only append barcode_value when type is not 'number'
        if (barcodeType.value !== "number" && barcodeValue.value)
          formData.append("barcode_value", barcodeValue.value);
        if (cardNumber.value) formData.append("card_number", cardNumber.value);
        formData.append("card_color", cardColor.value);  // always set (default '002244')
        if (expiryDate.value)
          formData.append("expiry_date", dayjs(expiryDate.value).format("YYYY-MM-DD"));
        if (issuer.value) formData.append("issuer", issuer.value);
        if (notes.value) formData.append("notes", notes.value);
        // D-03: only append card_image when a new file was selected
        if (pendingFile.value) formData.append("card_image", pendingFile.value);

        if (!isEditMode.value) {
          // CREATE — D-09: Object.assign contract; HIGH-01: null guard
          const userId = pb.authStore.record?.id;
          if (!userId) {
            toast.error("Session expired. Please log in again.");
            isSaving.value = false;
            return;
          }
          formData.append("user", userId);
          const created = await pb
            .collection("wallecx_memberships")
            .create<Memberships>(formData);
          emit("created", created);
        } else {
          // UPDATE — D-10: FormData mirrors mapToUpdateMembership writable fields
          void mapToUpdateMembership;  // confirms writable field set
          const updated = await pb
            .collection("wallecx_memberships")
            .update<Memberships>(record.value!.id, formData);
          emit("updated", updated);
        }

        toast.success(isEditMode.value ? "Card updated." : "Card added.");
        pendingFile.value = null;  // HIGH-02: explicit reset before close
        visible.value = false;
      } catch (e: unknown) {
        toast.error("Failed to save. Please try again.");
        console.error("ManageMembership: save failed", e);
      } finally {
        isSaving.value = false;
      }
    }

    function onHide(): void {
      pendingFile.value = null;  // HIGH-02: also reset on ESC / programmatic close
    }
    ```

    CRITICAL ANTI-PATTERNS to avoid (from RESEARCH.md):
    - Do NOT use @primevue/forms or zodResolver — ColorPicker ignores initialValues inside Form (issue #8135)
    - Do NOT append card_image to FormData unless pendingFile.value is truthy
    - Do NOT use pb.authStore.record!.id — use optional chaining with null guard
    - Do NOT clear pendingFile only in @hide — also clear in success branch
    - Do NOT use v-show for barcode_value — must use v-if (D-02: DOM removal required)
    - Do NOT prepend # to cardColor ref — store and read without hash
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/projects/wallecx/ManageMembership.vue exists
    - grep "defineModel" ManageMembership.vue returns 2 lines (visible + record)
    - grep "defineEmits" ManageMembership.vue returns 1 line containing "created" and "updated"
    - grep "002244" ManageMembership.vue returns at least 3 lines (initial value + two watcher branches)
    - grep "v-if.*barcodeType.*number" ManageMembership.vue returns 1 line (D-02 field conditional)
    - grep "watch(barcodeType" ManageMembership.vue returns 1 line (D-02 watcher)
    - grep "authStore.record?.id" ManageMembership.vue returns 1 line (HIGH-01 null guard)
    - grep "pendingFile.value = null" ManageMembership.vue returns 2 lines (HIGH-02 dual reset)
    - grep "wallecx_memberships" ManageMembership.vue returns at least 2 lines (create + update)
    - grep "mapToUpdateMembership" ManageMembership.vue returns 2 lines (import + void reference)
    - npm run type-check exits 0
  </acceptance_criteria>
  <done>ManageMembership.vue script setup complete with all form logic: direct v-model refs, Zod safeParse, ColorPicker, barcode_type watcher, EXIF strip pipeline, create/update submit flow, isSaving guard, pendingFile dual-reset, auth null guard.</done>
</task>

<task type="auto">
  <name>Task 2: Write ManageMembership.vue template</name>
  <files>src/components/projects/wallecx/ManageMembership.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageVaccination.vue (template section lines 189-298 — Dialog shell + form fields pattern)
    - .planning/phases/13-write-path-managemembership-crud/13-UI-SPEC.md (§ManageMembership.vue — field order, copywriting contract, ColorPicker layout)
    - .planning/phases/13-write-path-managemembership-crud/13-PATTERNS.md (Pattern 3, 4, 5 template snippets)
  </read_first>
  <action>
    Append the template block to the ManageMembership.vue created in Task 1.

    Dialog shell (mirror ManageVaccination.vue template lines 190-198):
    ```html
    <template>
      <Dialog
        modal
        v-model:visible="visible"
        :header="dialogHeader"
        :style="{ width: '40vw' }"
        :breakpoints="{ '960px': '75vw', '641px': '92vw' }"
        :closable="!isSaving"
        @hide="onHide"
      >
        <form @submit.prevent="onSubmit" class="space-y-4">
    ```

    IMPORTANT: Use `<form @submit.prevent="onSubmit">` NOT `<Form zodResolver>` — D-07 forbids @primevue/forms.

    Field order per UI-SPEC §Component Inventory field order table:

    Field 1 — card_name (required):
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Card Name *</label>
      <InputText v-model="cardName" fluid :disabled="isSaving" />
      <Message v-if="cardNameError" severity="error" size="small" variant="simple">
        {{ cardNameError }}
      </Message>
    </div>
    ```

    Field 2 — barcode_type (Select with BARCODE_TYPE_OPTIONS):
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Barcode Type</label>
      <Select
        v-model="barcodeType"
        :options="BARCODE_TYPE_OPTIONS"
        option-label="label"
        option-value="value"
        fluid
        :disabled="isSaving"
      />
    </div>
    ```

    Field 3 — barcode_value (v-if conditional — D-02, NOT v-show):
    ```html
    <div v-if="barcodeType !== 'number'" class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Barcode Value</label>
      <InputText v-model="barcodeValue" fluid :disabled="isSaving" />
    </div>
    ```

    Field 4 — card_number:
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Card Number</label>
      <InputText v-model="cardNumber" fluid :disabled="isSaving" />
    </div>
    ```

    Field 5 — card_color (ColorPicker + live swatch — D-01, D-03):
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Card Colour</label>
      <div class="flex items-center gap-3">
        <ColorPicker
          v-model="cardColor"
          aria-label="Card colour picker"
          :disabled="isSaving"
        />
        <span
          class="inline-block w-8 h-8 rounded"
          :style="{ backgroundColor: '#' + cardColor }"
          aria-hidden="true"
        ></span>
      </div>
      <Message v-if="cardColorError" severity="error" size="small" variant="simple">
        {{ cardColorError }}
      </Message>
    </div>
    ```
    CRITICAL: v-model="cardColor" stores 6-char hex WITHOUT #. Swatch prepends # in :style only.

    Field 6 — expiry_date (DatePicker):
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Expiry Date</label>
      <DatePicker v-model="expiryDate" fluid dateFormat="dd M yy" :disabled="isSaving" />
    </div>
    ```

    Field 7 — issuer:
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Issuer</label>
      <InputText v-model="issuer" fluid :disabled="isSaving" />
    </div>
    ```

    Field 8 — notes (Textarea, 3 rows, no auto-resize per UI-SPEC):
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Notes</label>
      <Textarea v-model="notes" fluid :rows="3" :auto-resize="false" :disabled="isSaving" />
    </div>
    ```

    Field 9 — card_image (FileUpload with edit-mode thumbnail — D-03):
    ```html
    <div class="flex flex-col gap-1">
      <label class="text-sm" style="color: var(--color-typo-heading)">Card Image</label>
      <div v-if="thumbnailUrl" class="flex flex-col gap-1 mb-1">
        <img
          :src="thumbnailUrl"
          class="w-24 h-24 object-cover rounded"
          alt="Current card image"
        />
        <p class="text-sm" style="color: var(--color-typo-muted)">
          Current image — select a new file to replace it
        </p>
      </div>
      <FileUpload
        mode="basic"
        :auto="false"
        accept="image/jpeg,image/png,image/webp"
        :maxFileSize="10485760"
        :disabled="isSaving"
        :chooseLabel="isEditMode && record?.card_image ? 'Replace image' : undefined"
        @select="onFileSelect"
      />
      <p v-if="pendingFile" class="text-sm" style="color: var(--color-typo-muted)">
        {{ pendingFile.name }} selected
      </p>
    </div>
    ```

    Submit button (fluid, loading + disabled on isSaving):
    ```html
    <Button
      type="submit"
      :label="isEditMode ? 'Save Changes' : 'Add Card'"
      :loading="isSaving"
      :disabled="isSaving"
      fluid
    />
    ```

    Close tags:
    ```html
        </form>
      </Dialog>
    </template>
    ```

    All PrimeVue components (Dialog, InputText, Select, ColorPicker, DatePicker, Textarea, FileUpload, Button, Message) are auto-resolved by PrimeVueResolver — no explicit imports needed in the template.
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "v-model:visible=\"visible\"" ManageMembership.vue returns 1 line (Dialog binding)
    - grep "ColorPicker" ManageMembership.vue returns 1 line (field 5)
    - grep "v-if=\"barcodeType !== 'number'\"" ManageMembership.vue returns 1 line (D-02 conditional)
    - grep "v-show" ManageMembership.vue returns 0 lines (v-if required per D-02)
    - grep "thumbnailUrl" ManageMembership.vue returns 2 lines (computed definition + v-if in template)
    - grep "chooseLabel" ManageMembership.vue returns 1 line (Replace image per D-03)
    - grep "space-y-4" ManageMembership.vue returns 1 line (form class)
    - grep "@submit.prevent" ManageMembership.vue returns 1 line (form submit — NOT @primevue/forms)
    - grep "Form.*zodResolver\|zodResolver.*Form" ManageMembership.vue returns 0 lines (D-07 — no @primevue/forms)
    - npm run type-check exits 0
    - npm run build exits 0
  </acceptance_criteria>
  <done>ManageMembership.vue complete — Dialog with 9 form fields (card_name, barcode_type, barcode_value conditional, card_number, card_color ColorPicker, expiry_date, issuer, notes, card_image with thumbnail), submit button, all per MWRITE-01 through MWRITE-04 and D-01 through D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → ManageMembership form | Untrusted text input (card_name, barcode_value, card_number, issuer, notes) enters via form fields |
| User → FileUpload | Untrusted file content crosses boundary before EXIF strip pipeline |
| ManageMembership → PocketBase | FormData sent to pb.create() / pb.update() — server enforces per-user rules |
| pb.authStore → create FormData | Session identity asserted via pb.authStore.record?.id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | card_name / card_color Zod validation | mitigate | schema.safeParse() validates card_name min-1 and card_color hex regex before any pb call; error shown inline |
| T-13-02-02 | Information Disclosure | card_image EXIF GPS metadata | mitigate | Canvas re-encode strips all EXIF before pendingFile is set; toast.info("Location data removed.") confirms strip ran |
| T-13-02-03 | Tampering | Double-submit duplicate records | mitigate | isSaving ref set true at start of try block; all form fields + submit button disabled while true; Dialog :closable="!isSaving" |
| T-13-02-04 | Elevation of Privilege | create FormData user field spoofing | mitigate | userId comes from pb.authStore.record?.id (server-authenticated session) with null guard; PocketBase createRule also enforces @request.body.user = @request.auth.id server-side |
| T-13-02-05 | Denial of Service | Session expiry at submit time | mitigate | Explicit null guard on userId returns early with toast.error("Session expired. Please log in again.") — no unhandled TypeError |
| T-13-02-06 | Tampering | XSS via card_name in template | accept | Vue template {{ }} auto-escapes all interpolations; no v-html in this file; Zod validates string type |
| T-13-02-07 | Tampering | Stale pendingFile from previous session | mitigate | pendingFile reset in both success branch and onHide() — HIGH-02 dual-reset pattern |
| T-13-02-08 | Tampering | card_image cleared on text-only edit | mitigate | formData.append("card_image", ...) guarded by if (pendingFile.value) — no append when null |
</threat_model>

<verification>
1. TypeScript clean: `npm run type-check`
2. Build passes: `npm run build`
3. Grep checks from acceptance_criteria above
4. Manual spot-check: Open the dialog in dev server, verify ColorPicker initializes to navy, switch to "Number only" and confirm barcode_value field disappears
</verification>

<success_criteria>
- ManageMembership.vue exists with complete script setup and template
- Direct v-model refs used (no @primevue/forms) — D-07 satisfied
- ColorPicker pre-fills navy 002244 — D-01 satisfied
- barcode_value uses v-if (not v-show) and clears on 'number' — D-02 satisfied
- Edit mode shows thumbnail above FileUpload — D-03 satisfied
- isSaving guard on all fields and submit button — D-08 satisfied
- Object.assign create contract via emit('created', created) — D-09 satisfied
- mapToUpdateMembership referenced in update branch — D-10 satisfied
- Auth null guard present — HIGH-01 satisfied
- pendingFile dual-reset present — HIGH-02 satisfied
- MWRITE-01, MWRITE-02, MWRITE-03, MWRITE-04, MWRITE-05 (create/update flows) satisfied
- npm run type-check exits 0
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-write-path-managemembership-crud/13-02-SUMMARY.md`
</output>
