---
phase: 03-meeting-admin-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/views/LexTrackView.vue
autonomous: true
requirements: [UI-ADMIN-03, BUG-04, BUG-05]
tags: [vue, lextrack, cleanup, label]
must_haves:
  truths:
    - "The third ActivityCard label is 'Admin' (was 'Admin Tasks and Support')"
    - "No console.log statements remain in LexTrackView.vue"
    - "The large commented-out Dialog block at lines ~200-245 is gone"
    - "The commented `<AddMeeting ...>` block at lines ~242-245 is gone"
    - "Save handler / fetch watcher are unchanged in behavior (Phase 4 boundary)"
  artifacts:
    - path: "src/views/LexTrackView.vue"
      provides: "Section label rename + dead-code cleanup"
      contains: 'label="Admin"'
  key_links:
    - from: "src/views/LexTrackView.vue"
      to: "ActivityCard.vue"
      via: "v-model:section"
      pattern: 'label="Admin"'
---

<objective>
Three surgical edits in `LexTrackView.vue`:
1. Rename the third ActivityCard's `label` from `"Admin Tasks and Support"` → `"Admin"` (D-13,
   UI-ADMIN-03).
2. Remove every `console.log` statement (BUG-04, D-21).
3. Remove the large commented-out `<Dialog>` block at the bottom of the template plus the
   surrounding `<!--  <AddMeeting ...-->` comment (BUG-05, D-22). If `AddMeeting` is no longer
   referenced after the cleanup, remove its import too (D-23 — only if unused).

Per D-23 stay surgical: do NOT refactor any persistence logic, watcher, or save handler. Phase 4
owns those.

Output: Updated `LexTrackView.vue`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meeting-admin-ui/03-CONTEXT.md
@CLAUDE.md
@src/views/LexTrackView.vue

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Surgical cleanup + label rename in LexTrackView.vue</name>
  <read_first>
    - src/views/LexTrackView.vue (current full file — script + template — every line referenced below)
    - .planning/phases/03-meeting-admin-ui/03-CONTEXT.md (D-13, D-21, D-22, D-23)
    - .planning/REQUIREMENTS.md (BUG-04 wording, BUG-05 wording, UI-ADMIN-03 wording)
  </read_first>
  <files>src/views/LexTrackView.vue</files>
  <action>
Apply exactly these edits and nothing else.

**Edit 1 — Section label rename (D-13, UI-ADMIN-03)**

Around current line 190:
```vue
<ActivityCard v-model:section="supports" label="Admin Tasks and Support" @update="updateSupport" @remove="removeSupport"/>
```
becomes:
```vue
<ActivityCard v-model:section="supports" label="Admin" @update="updateSupport" @remove="removeSupport"/>
```

This must match exactly. Plan 03-02 keys its inline-add Admin defaults off `props.label === 'Admin'`,
so the spelling MUST be "Admin" (capitalized, no trailing space).

**Edit 2 — Remove every `console.log` from this file (BUG-04, D-21)**

Current `console.log` occurrences in the file (per the read, line numbers from current state):
- Line 34: `console.log(support.value);` — inside `updateSupport`
- Line 40: `console.log(supports.value);` — inside `removeSupport`
- Line 58: `console.log(meeting.value);` — inside `updateMeeting`
- Line 64: `console.log(meetings.value);` — inside `removeMeeting`
- Line 84: `console.log(task.value);` — inside `updateTask`
- Line 90: `console.log(tasks.value);` — inside `removeTask`

Delete each line entirely (not just comment it out). Do not collapse adjacent blank lines —
preserve readability. Also remove the single-line commented diagnostic `// console.log(supportsList);`
at the bottom of the watcher (around line 122) and `//console.log(supports.value);` at the end of
`save()` (around line 160), and the `//console.log('Selected date changed:', newDate);` at line 98.
These are leftover debug detritus per BUG-04 / D-21.

After this edit `grep -n "console" src/views/LexTrackView.vue` should return nothing.

**Edit 3 — Remove the commented-out Dialog block (BUG-05, D-22)**

Delete the entire block from current line 198 through line 245, inclusive. That covers:
- The `<!--  <Button label="Show" @click="visible = true" />-->` comment (line 198)
- The blank line(s) before/after if they exist
- The full `<!--  <Dialog v-model:visible="visible" ...> ... </Dialog>-->` block (lines 200-240)
- The `<!--  <AddMeeting ... v-model:meetings="meetings"-->` comment (lines 242-245)

After deletion the section between `</Card>` (around line 194) and `<ManageMeeting ...>` (currently
line 247) should contain at most one blank line for breathing room. Be careful: do NOT delete the
real, functional `<ManageMeeting>`, `<ManageTask>`, `<ManageSupport>` elements that follow at
lines 247-256. Those are live components with v-model bindings.

Also delete the leftover comment `<!--  <LexTrackApp/>-->` at line 165 — same rationale (dead
commented code per D-23 surgical cleanup).

**Edit 4 — Remove unused `AddMeeting` import IF and ONLY IF** the import exists in the script and
no live template reference to `<AddMeeting>` remains after Edit 3. (Per the read, the current file
does NOT import `AddMeeting` — the only reference was inside the commented block. So this edit is
expected to be a no-op grep verification, but check anyway and remove if present.)

**Things NOT to change in this plan:**
- The `watch(selectedDate, async (newDate) => { ... })` body that fetches collections — leave the
  `Promise.all` block, the commented-out single-fetch alternatives (lines 106-110), and the
  `supports.value = supportsList; tasks.value = tasksList; meetings.value = meetingsList;`
  assignments. Phase 4 (BUG-01 — initial load) will refactor this path.
  - Exception: the trailing `//console.log(supportsList);` at the very end of the watcher IS
    deleted per Edit 2 (it's a console statement, not the logic).
- The `save()` function body — Phase 4 (UI-SAVE-01) replaces this with per-item persistence.
  Leave the for-loops and `pb.collection(...).update/create` calls intact.
- The double `/** SUPPORTS */` / `/** SUPPORTS */` repeats — those are the section separators
  per the project's CONVENTIONS.md and stay (they're not console.log debris).
- The empty `<style scoped></style>` block.
- Imports of `ManageMeeting`, `ManageTask`, `ManageSupport`, mappers, `pb`, `dayjs` — all kept.
- The two commented `// supportsList = await pb.collection...` blocks at lines 106-110 — these are
  algorithmic alternates the developer left for reference; they predate this milestone and are
  NOT in scope (D-23 — surgical only).
  </action>
  <verify>
    <automated>npm run type-check && grep -q 'label="Admin"' src/views/LexTrackView.vue && ! grep -q "Admin Tasks and Support" src/views/LexTrackView.vue && ! grep -q "console.log" src/views/LexTrackView.vue && ! grep -q '<Dialog v-model:visible="visible" header="Add DSU Update"' src/views/LexTrackView.vue && ! grep -q "<AddMeeting" src/views/LexTrackView.vue && npx oxlint src/views/LexTrackView.vue -D correctness</automated>
  </verify>
  <acceptance_criteria>
    - Label is "Admin": `grep -q 'label="Admin"' src/views/LexTrackView.vue`
    - Old label gone (UI-ADMIN-03): `! grep -q "Admin Tasks and Support" src/views/LexTrackView.vue`
    - All console.log removed (BUG-04 / D-21): `! grep -q "console.log" src/views/LexTrackView.vue`
    - Commented Dialog block gone (BUG-05 / D-22): `! grep -q '<Dialog v-model:visible="visible" header="Add DSU Update"' src/views/LexTrackView.vue`
    - Commented AddMeeting block gone (BUG-05 / D-22): `! grep -q "<AddMeeting" src/views/LexTrackView.vue`
    - Stale `<!--  <LexTrackApp/>-->` removed: `! grep -q "<LexTrackApp/>" src/views/LexTrackView.vue`
    - Live ManageMeeting still present (regression guard): `grep -q "<ManageMeeting" src/views/LexTrackView.vue`
    - Live ManageTask still present: `grep -q "<ManageTask" src/views/LexTrackView.vue`
    - Live ManageSupport still present: `grep -q "<ManageSupport" src/views/LexTrackView.vue`
    - Save handler still present (Phase 4 boundary): `grep -q "const save = async" src/views/LexTrackView.vue`
    - Watcher still present (Phase 4 boundary): `grep -q "watch(selectedDate" src/views/LexTrackView.vue`
    - No AddMeeting import remaining: `! grep -q "import.*AddMeeting" src/views/LexTrackView.vue`
    - No manual PrimeVue **component** import added: `! grep -E "^import (Button|Card|DatePicker|Dialog) from 'primevue/" src/views/LexTrackView.vue`
    - Type-check passes: `npm run type-check` exits 0
    - oxlint correctness pass: `npx oxlint src/views/LexTrackView.vue -D correctness` exits 0
  </acceptance_criteria>
  <done>The view shows "Admin" as the third section label, no console statements anywhere, no commented-out Dialog/AddMeeting blocks; live components and save logic untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure refactor / cleanup; no new I/O paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | I (Information disclosure) | console.log debug output | mitigate | Removing `console.log` lines eliminates leaking entity payloads to the browser console (defense-in-depth for a future shared-device scenario) |
</threat_model>

<verification>
- `npm run type-check` exits 0
- `npx oxlint src/views/LexTrackView.vue -D correctness` exits 0
- All grep acceptance criteria pass
- Live components (`<ManageMeeting>`, `<ManageTask>`, `<ManageSupport>`, `<ActivityCard ... label="Admin">`) all present
</verification>

<success_criteria>
LexTrackView is visibly clean of dead code; the Admin section label matches the requirements doc;
the watcher and save logic are byte-identical aside from the deleted console.log lines (verify by
viewing the diff focused on those specific lines).
</success_criteria>

<output>
After completion, create `.planning/phases/03-meeting-admin-ui/03-04-SUMMARY.md`.
</output>
