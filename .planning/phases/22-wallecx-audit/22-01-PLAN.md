---
phase: 22-wallecx-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md
  - .planning/phases/22-wallecx-audit/22-01-SUMMARY.md
autonomous: true
requirements:
  - THEME-13
tags:
  - dark-mode
  - audit
  - wallecx
  - pwa
  - milestone-closer

must_haves:
  truths:
    - "Toggling theme from the NavBar while viewing /projects/wallecx immediately re-themes every Wallecx surface (card grids, dialogs, bottom sheets) without manual refresh (SC-1)"
    - "Navigating away from /projects/wallecx and back preserves the chosen theme; Wallecx renders correctly on return (SC-2)"
    - "Installing Wallecx as a PWA and toggling dark mode behaves identically to the in-browser flow; choice persists across close/re-open (SC-3)"
    - "BarcodeDisplay renders black-on-white in BOTH themes; BR-2 invariant preserved (SC-4)"
    - "Zero Wallecx source files are modified by this plan; audit is verification-only by default"
  artifacts:
    - path: ".planning/phases/22-wallecx-audit/22-HUMAN-UAT.md"
      provides: "6-vector regression spot-check checklist + sign-off block for the human tester"
      contains: "DevTools toggle, Vector 1..6, Sign-off"
    - path: ".planning/phases/22-wallecx-audit/22-01-SUMMARY.md"
      provides: "Plan outcome record — documents zero source changes and THEME-13 satisfied / v3.0 ready to close"
      contains: "THEME-13"
  key_links:
    - from: "22-HUMAN-UAT.md Vector 5"
      to: "src/components/projects/wallecx/BarcodeDisplay.vue"
      via: "BARCODE_FOREGROUND / BARCODE_BACKGROUND constants (BR-2 invariant)"
      pattern: "BARCODE_FOREGROUND.*#000000"
    - from: "22-HUMAN-UAT.md Vector 1"
      to: "src/composables/useTheme.ts + .my-app-dark on <html>"
      via: "NavBar sun/moon toggle (Phase 19)"
      pattern: "my-app-dark"
---

<objective>
Phase 22 is the v3.0 milestone closer. Confirm that Phase 18's Wallecx dark mode work still holds now that `.my-app-dark` is driven by the site-wide NavBar toggle (Phase 19) rather than manual DevTools toggling. This plan is verification-only by default — zero Wallecx source changes are expected.

Purpose: Close v3.0 by demonstrating THEME-13 is satisfied. Generate the regression-spot-check UAT artifact for the human tester and the SUMMARY documenting the audit outcome.

Output:
- `.planning/phases/22-wallecx-audit/22-HUMAN-UAT.md` — 6-vector spot-check checklist
- `.planning/phases/22-wallecx-audit/22-01-SUMMARY.md` — audit outcome (expected: zero source changes, all vectors PASS pending human visual)

Anti-regression posture: NO Wallecx source files are modified in this plan. If the executor discovers a regression during UAT-doc-driven self-review, fixing it via `wallecx-overrides.css` is OPPORTUNISTIC (per D-03) — it is not pre-allocated here. Any such fix would land in its own atomic commit, NOT as a planned task in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-wallecx-audit/22-CONTEXT.md
@.planning/phases/18-dark-mode-fixes/18-CONTEXT.md
@.planning/phases/19-theme-infrastructure/19-CONTEXT.md
@.planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md

<interfaces>
<!-- Locked constants the audit verifies — DO NOT MODIFY -->

From src/components/projects/wallecx/BarcodeDisplay.vue (BR-2 invariant, locked by Phase 12):
```typescript
const BARCODE_FOREGROUND = '#000000'; // ALWAYS black, in BOTH themes
const BARCODE_BACKGROUND = '#ffffff'; // ALWAYS white, in BOTH themes
```

From src/composables/useTheme.ts (Phase 19, locked):
- Module-singleton `Ref<'light' | 'dark'>`
- Applies/removes `.my-app-dark` on `document.documentElement`
- Persists to `localStorage.lexarium:theme`

From index.html (Phase 19, locked):
- Inline FOUC script applies `.my-app-dark` before first paint
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate 22-HUMAN-UAT.md regression spot-check checklist</name>
  <files>.planning/phases/22-wallecx-audit/22-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/22-wallecx-audit/22-CONTEXT.md (D-01..D-09, especially §Specific Ideas spot-check script)
    - .planning/phases/21-mini-app-dark-mode-sweep/21-HUMAN-UAT.md (format/structure to mirror)
    - .planning/phases/18-dark-mode-fixes/18-CONTEXT.md (BR-2 invariant context, 15-component sweep that 22 does NOT re-walk)
    - .planning/ROADMAP.md §Phase 22 (SC 1..4)
  </read_first>
  <action>
    Create `.planning/phases/22-wallecx-audit/22-HUMAN-UAT.md` mirroring the structure of `21-HUMAN-UAT.md`. This is the v3.0 milestone-closer UAT — a SPOT-CHECK on 6 regression vectors, NOT a re-walk of Phase 18's full 15-component sweep (D-01).

    Required sections (in order):

    1. **Header / Metadata**
       - Title: `# Phase 22 — Wallecx Audit · Human UAT`
       - Phase, status (`Ready for human verification`), generated date (2026-05-19), tester field
       - One-line statement: "v3.0 milestone closer — confirms Phase 18's Wallecx dark mode still holds with the NavBar toggle (Phase 19) wired up."

    2. **How to toggle the theme** (DevTools instructions)
       - Click the NavBar sun/moon button (Phase 19) — primary method
       - Alternative: DevTools → Elements → `<html>` → toggle the `my-app-dark` class manually
       - Note: localStorage key is `lexarium:theme`; OS preference is the first-visit default

    3. **Vector 1: Toggle interaction (mid-Wallecx, with dialog open)** — maps to SC-1
       - Open `/projects/wallecx` in LIGHT mode
       - Click the NavBar sun/moon button
       - Expect: NavBar icon flips; page background goes dark; vaccination tab cards switch to dark surface; membership tab cards keep user `card_color` with contrast-aware text (Phase 18 `pickTextColor` luminance helper)
       - Open any CRUD dialog (Add Vaccination OR Add Membership)
       - Click the NavBar toggle while the dialog is open
       - Expect: dialog re-paints immediately to the new theme; no white flash; form inputs adapt; Save/Cancel buttons remain legible

    4. **Vector 2: Route transitions (theme persistence)** — maps to SC-2
       - From `/projects/wallecx` in dark, navigate to `/projects` (still dark)
       - Navigate back to `/projects/wallecx` (still dark, no flash on re-entry)
       - Navigate to `/` (home), to `/blog`, to `/projects/lextrack`, back to `/projects/wallecx`
       - Expect: theme persists across every route; no FOUC; no transient white background

    5. **Vector 3: FOUC on hard reload while dark** — maps to SC-2
       - With Wallecx in dark mode, hard refresh (Ctrl+F5 / Cmd+Shift+R)
       - Expect: first paint is already dark — no white flash, no FOUC (Phase 19 inline FOUC script + Phase 18 token overrides do their job)
       - Repeat with vaccination tab AND membership tab active

    6. **Vector 4: Bottom sheets in dark mode** (Phase 17 carry-forward) — maps to SC-1
       - Resize browser to < 640px (or use DevTools device toolbar, iPhone SE / 375px)
       - In dark mode: tap a vaccination group card → bottom sheet slides up from the bottom
       - Expect: drawer body has dark background; drag handle pill (`bg-gray-300 dark:bg-gray-600` from Phase 17) is visible against dark drawer; group records list legible; edit/delete actions visible
       - Repeat for membership card detail bottom sheet
       - Toggle theme WHILE bottom sheet is open → expect re-paint without dismissing the sheet

    7. **Vector 5: BarcodeDisplay BR-2 invariant** — maps to SC-4
       - Open any membership card that has a barcode (open the detail dialog or bottom sheet)
       - In LIGHT mode: barcode renders BLACK on WHITE
       - Toggle to DARK mode: barcode STILL renders BLACK on WHITE (constants are theme-independent per BR-2)
       - Expect: surrounding dialog/sheet chrome adapts to the theme, but the barcode rectangle itself is unchanged
       - Confirm scan overlay (`/projects/wallecx` scan flow) also keeps the barcode black-on-white in both themes

    8. **Vector 6: PWA standalone install + toggle + reopen** — maps to SC-3
       - Install Wallecx as a PWA (Chrome DevTools → Application → Manifest → Install, OR phone install via "Add to Home Screen")
       - Open the standalone window/app
       - Confirm NavBar toggle button is reachable (Phase 19 D-toggle visible on every route)
       - Toggle dark → close standalone (hard-close, force quit on mobile) → re-open standalone
       - Expect: chosen theme persists; first paint is dark; no FOUC
       - Note (informational, not a fail): iOS standalone has a known 7-day localStorage eviction window — if testing across eviction, theme should fall back gracefully to OS-preference + first-visit default per Phase 19 D-06 (this is documented, not a regression)

    9. **Sign-off block**
       - Table or checkboxes: each vector (1..6) with Pass / Fail / Notes columns
       - "Approved by: ______  Date: ______"
       - Closing line: "On approval, run `/gsd-complete-milestone` to archive v3.0."

    10. **Anti-regression reminder (footer)**
        - Note that no Wallecx source files were modified by Phase 22's plan
        - If any vector fails, the executor should open an opportunistic fix in `wallecx-overrides.css` (Phase 18-style `.my-app-dark .p-*` selector pattern) per D-03 — NOT modify any locked file (D-04)

    Format: Markdown. Length: comparable to `21-HUMAN-UAT.md` (concise, scannable). No code blocks for vectors — use numbered/bulleted steps. Include "Expect:" lines so the tester knows the pass criteria.

    Reference D-01..D-09 inline where appropriate (e.g., footer can mention "Per 22-CONTEXT D-01, this is a spot-check; not a re-walk of Phase 18's 15-component sweep").
  </action>
  <verify>
    <automated>test -f .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md &amp;&amp; grep -q "Vector 1" .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md &amp;&amp; grep -q "Vector 6" .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md &amp;&amp; grep -q "BR-2" .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md &amp;&amp; grep -q "BarcodeDisplay" .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md &amp;&amp; grep -q "PWA" .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md &amp;&amp; grep -qi "sign-off\|sign off" .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md</automated>
  </verify>
  <done>
    `22-HUMAN-UAT.md` exists with all 6 vectors (toggle interaction, route transitions, FOUC reload, bottom sheets, BarcodeDisplay BR-2, PWA standalone), DevTools toggle instructions, and a sign-off block. Document references SC-1..SC-4 from ROADMAP §Phase 22. Format mirrors `21-HUMAN-UAT.md`.

    Anti-regression check (must pass):
    - `git diff --name-only` shows ONLY `.planning/phases/22-wallecx-audit/22-HUMAN-UAT.md` (and possibly STATE.md if updated separately)
    - `git diff --name-only` does NOT include any of: `src/main.ts`, `src/composables/useTheme.ts`, `src/composables/useIsMobile.ts`, `src/assets/base.css`, `index.html`, `src/components/CustomNavBar.vue`, `src/components/HeroSection.vue`, `src/components/AboutMeSection.vue`, `src/components/Login.vue`, any `src/views/**`, any `src/components/projects/{lextrack,larga,gift-exchange,api-playground}/**`, `src/components/projects/wallecx/BarcodeDisplay.vue`, or any `src/components/projects/wallecx/**` file
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 22-01-SUMMARY.md documenting the audit outcome</name>
  <files>.planning/phases/22-wallecx-audit/22-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/22-wallecx-audit/22-CONTEXT.md (D-08, D-09 — deliverables + milestone-closure preparation)
    - .planning/phases/22-wallecx-audit/22-HUMAN-UAT.md (just created in Task 1)
    - $HOME/.claude/get-shit-done/templates/summary.md (SUMMARY structure)
    - .planning/ROADMAP.md §Phase 22 (SC 1..4 to reference)
  </read_first>
  <action>
    Create `.planning/phases/22-wallecx-audit/22-01-SUMMARY.md` documenting the plan outcome. Follow the standard GSD SUMMARY structure.

    Required content:

    1. **Frontmatter / header**
       - Phase 22 / Plan 01
       - Status: `code complete — awaiting human UAT`
       - Date: 2026-05-19

    2. **Outcome statement (lead)**
       - "Phase 22 is the v3.0 milestone closer — verification only. Wallecx was already dark-mode-ready as of Phase 18; Phases 19/20/21 wired up the site-wide toggle without modifying any Wallecx source. This plan produced the human UAT artifact for a 6-vector regression spot-check (per D-01); zero Wallecx source files were modified."

    3. **What this plan produced**
       - `22-HUMAN-UAT.md` (created in Task 1) — 6 regression vectors covering SC-1..SC-4
       - This SUMMARY

    4. **What was NOT modified (anti-regression evidence)**
       - List the locked files from 22-CONTEXT §Files Untouched: `src/main.ts`, `src/composables/useTheme.ts`, `src/composables/useIsMobile.ts`, `src/assets/base.css`, `index.html`, `src/components/CustomNavBar.vue`, `src/components/HeroSection.vue`, `src/components/AboutMeSection.vue`, `src/components/Login.vue`, all `src/views/**`, all `src/components/projects/{lextrack,larga,gift-exchange,api-playground}/**`, `src/components/projects/wallecx/BarcodeDisplay.vue` (BR-2 constants), and all other `src/components/projects/wallecx/**` files
       - Note: BarcodeDisplay constants `BARCODE_FOREGROUND = '#000000'` and `BARCODE_BACKGROUND = '#ffffff'` are unchanged (BR-2 invariant preserved per D-05)

    5. **Requirement coverage**
       - THEME-13: covered by the 6-vector UAT (SC-1..SC-4); awaiting human visual sign-off
       - Cross-reference each vector to its SC

    6. **Milestone-closure preparation (per D-09)**
       - Once `22-HUMAN-UAT.md` is signed off:
         - Run `/gsd-verify-phase 22` to lock SC-1..SC-4 verdict
         - Run `/gsd-complete-milestone` to archive v3.0 (generates `v3.0-ROADMAP.md`)
         - STATE.md / ROADMAP.md milestone-shipped updates happen in the orchestrator's post-phase commit, NOT in this plan
       - Deferred Phase 21 items (LexTrack DatePicker/TabView contrast; API Playground sign-off) are NOT addressed here — they are not Wallecx scope (D-01 boundary, 22-CONTEXT §Out of scope)

    7. **Acceptance**
       - "Per D-02, the expected outcome of this plan is zero Wallecx source changes. If the human UAT in Task 1's artifact surfaces a concrete regression, the executor MAY add a fix to `wallecx-overrides.css` (Phase 18-style `.my-app-dark .p-*` selector, per D-03/D-06) as an opportunistic atomic commit — NOT as a planned task in this plan."

    Length: brief — ~50-100 lines. Concise, scannable, milestone-closer in tone.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-wallecx-audit/22-01-SUMMARY.md &amp;&amp; grep -q "THEME-13" .planning/phases/22-wallecx-audit/22-01-SUMMARY.md &amp;&amp; grep -q "BR-2" .planning/phases/22-wallecx-audit/22-01-SUMMARY.md &amp;&amp; grep -q "BarcodeDisplay" .planning/phases/22-wallecx-audit/22-01-SUMMARY.md &amp;&amp; grep -qi "v3.0\|v3\\.0" .planning/phases/22-wallecx-audit/22-01-SUMMARY.md &amp;&amp; grep -q "22-HUMAN-UAT" .planning/phases/22-wallecx-audit/22-01-SUMMARY.md</automated>
  </verify>
  <done>
    `22-01-SUMMARY.md` exists and references THEME-13, BR-2, BarcodeDisplay, v3.0, and the `22-HUMAN-UAT.md` artifact. Documents the audit's verification-only posture, the anti-regression file list, and the milestone-closure preparation steps.

    Anti-regression check (must pass — same as Task 1):
    - `git diff --name-only` after this task shows ONLY the two `.planning/phases/22-wallecx-audit/` markdown files (`22-HUMAN-UAT.md`, `22-01-SUMMARY.md`)
    - `git diff --name-only` does NOT include any source file under `src/**`
    - Specifically, `git diff --name-only` does NOT include any of: `src/main.ts`, `src/composables/useTheme.ts`, `src/composables/useIsMobile.ts`, `src/assets/base.css`, `index.html`, `src/components/CustomNavBar.vue`, `src/components/HeroSection.vue`, `src/components/AboutMeSection.vue`, `src/components/Login.vue`, any `src/views/**`, any `src/components/projects/{lextrack,larga,gift-exchange,api-playground}/**`, `src/components/projects/wallecx/BarcodeDisplay.vue`, or any other `src/components/projects/wallecx/**` file
  </done>
</task>

</tasks>

<verification>
This plan is verification-only — there is no code to run. Plan-level checks:

1. Both deliverables exist:
   - `.planning/phases/22-wallecx-audit/22-HUMAN-UAT.md`
   - `.planning/phases/22-wallecx-audit/22-01-SUMMARY.md`

2. Anti-regression assertion (run after both tasks): `git diff --name-only` must NOT include any of the following locked paths:
   - `src/main.ts`, `src/composables/useTheme.ts`, `src/composables/useIsMobile.ts`
   - `src/assets/base.css`, `index.html`
   - `src/components/CustomNavBar.vue`, `src/components/HeroSection.vue`, `src/components/AboutMeSection.vue`, `src/components/Login.vue`
   - Any `src/views/**` file
   - Any `src/components/projects/{lextrack,larga,gift-exchange,api-playground}/**` file
   - `src/components/projects/wallecx/BarcodeDisplay.vue` (BR-2 constants must stay `#000000` / `#ffffff`)
   - Any other `src/components/projects/wallecx/**` file (per D-02 default; an opportunistic fix to `wallecx-overrides.css` is the ONLY allowed exception, and is not a planned task in this plan)

3. The UAT document covers all 4 ROADMAP §Phase 22 success criteria (SC-1..SC-4) via 6 vectors.

4. BarcodeDisplay constants unchanged (verifiable via `grep "BARCODE_FOREGROUND.*#000000" src/components/projects/wallecx/BarcodeDisplay.vue`).
</verification>

<success_criteria>
This plan succeeds when:

1. `22-HUMAN-UAT.md` exists with 6 regression vectors mapped to ROADMAP §Phase 22 SC-1..SC-4, DevTools toggle instructions, and a sign-off block
2. `22-01-SUMMARY.md` exists documenting the audit's verification-only posture, references THEME-13 / BR-2 / BarcodeDisplay / v3.0, and prepares for milestone closure (per D-09)
3. Zero Wallecx source files modified (anti-regression assertion passes)
4. BR-2 invariant preserved — BarcodeDisplay constants untouched
5. Plan is committed to git ahead of human UAT

The plan is COMPLETE when the deliverables exist and pass the anti-regression check. THEME-13 is satisfied PENDING human UAT sign-off on `22-HUMAN-UAT.md`. The v3.0 milestone-closure commit (STATE.md + ROADMAP.md updates) happens AFTER UAT, in the orchestrator's post-phase commit — NOT inside this plan (per D-09).
</success_criteria>

<output>
After completion, both deliverables already serve as the plan's artifacts. No additional SUMMARY file beyond `22-01-SUMMARY.md` is required.
</output>
