---
phase: 04-bugs-and-save-ux
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/lextrack/ManageSupport.vue
autonomous: true
requirements: [UI-SAVE-01, UI-SAVE-03]
tags: [vue3, primevue, dialog, refactor]

must_haves:
  truths:
    - "ManageSupport dialog emits a `save` event carrying the current support object"
    - "ManageSupport Save button shows a spinner and is disabled while `saving` prop is true"
    - "ManageSupport no longer fires a toast itself, no longer closes itself on save click — both responsibilities move to the parent"
  artifacts:
    - path: "src/components/projects/lextrack/ManageSupport.vue"
      provides: "Presentational support/admin dialog that emits `save` and accepts `saving` prop"
      contains: "defineEmits<{ save: [item: AddDsuSupport & { id?: string }] }>"
    - path: "src/components/projects/lextrack/ManageSupport.vue"
      provides: "PrimeVue Button :loading binding for UI-SAVE-03"
      contains: ":loading=\"props.saving\""
  key_links:
    - from: "ManageSupport.vue Save button click"
      to: "parent's @save event handler in LexTrackView.vue"
      via: "emit('save', support.value)"
      pattern: "emit\\('save'"
    - from: "ManageSupport.vue Save button"
      to: "PrimeVue Button :loading prop"
      via: "props.saving binding"
      pattern: ":loading=\"props.saving\""
---

<objective>
Refactor `ManageSupport.vue` from a local toast-only stub (which also self-closes the dialog) into a presentational dialog that delegates persistence AND visibility-on-success to its parent. The dialog will:
1. Accept a new `saving?: boolean` prop and bind it to the Save button's `:loading` (UI-SAVE-03, D-08).
2. Emit a typed `save` event carrying the current support object (UI-SAVE-01, D-22a).
3. Drop the local `toast.success(...)` call AND the local `visible.value = false` line — D-04 dictates that close happens AFTER the parent's PB call resolves successfully, not on the click itself.

Purpose: This plan is the dialog-side half of UI-SAVE-01 for Admin/Support items; symmetrical with Plans 04-01 and 04-02. Note the extra step here vs. the Meeting/Task plans: ManageSupport currently closes itself in the toast stub (line 22) — this Phase-3 holdover must be removed so D-04 (parent owns close-on-success) can land cleanly in Plan 04-04.

Output: `ManageSupport.vue` ready to be wired to the parent's `handleSupportSave` handler that lands in Plan 04-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bugs-and-save-ux/04-CONTEXT.md
@.planning/phases/04-bugs-and-save-ux/04-RESEARCH.md
@.planning/phases/04-bugs-and-save-ux/04-PATTERNS.md
@src/components/projects/lextrack/ManageSupport.vue
@src/types/lextrack/dsu_supports/types.ts

<interfaces>
<!-- Key shapes the executor needs. Extracted from codebase. -->

From src/types/lextrack/dsu_supports/types.ts (Phase 2 final, includes Phase 1 link field):
```typescript
export type AddDsuSupport = {
  date: string;
  title: string;
  link?: string;
  description?: string;
};
```

From src/views/LexTrackView.vue:22 (already in place):
```typescript
type DsuSupportItem = AddDsuSupport & { id?: string };
```

Existing `defineModel` pattern in ManageSupport.vue:5-17 (DO NOT change):
```typescript
const visible = defineModel('visible', { type: Boolean, default: false, required: true });
const support = defineModel<AddDsuSupport>('support', { required: true });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor ManageSupport.vue script — add saving prop, save emit, drop toast stub AND drop self-close</name>
  <files>src/components/projects/lextrack/ManageSupport.vue</files>
  <read_first>
    - src/components/projects/lextrack/ManageSupport.vue (full file — 51 lines, current state — note line 22's `visible.value = false` self-close that must be removed)
    - .planning/phases/04-bugs-and-save-ux/04-PATTERNS.md (section "src/components/projects/lextrack/ManageSupport.vue")
    - .planning/phases/04-bugs-and-save-ux/04-RESEARCH.md (section "Dialog emits `save`")
    - src/types/lextrack/dsu_supports/types.ts (verify `AddDsuSupport` shape includes `link?: string`)
  </read_first>
  <behavior>
    - Behavior 1: ManageSupport accepts a `saving?: boolean` prop.
    - Behavior 2: When the Save button is clicked, the component emits exactly one event `save` with `support.value` as its single argument.
    - Behavior 3: The component does NOT call `visible.value = false` on save click — D-04: close happens in the parent AFTER the PB call resolves successfully.
    - Behavior 4: The component no longer imports `toast` from `vue-sonner` and no longer calls `toast.success(...)`.
    - Behavior 5: `defineModel` declarations for `visible` and `support` (lines 5-17) are unchanged.
  </behavior>
  <action>
**Step 1 — Replace the import block (current lines 1-3) with this exact block:**

```typescript
<script setup lang="ts">
import type { AddDsuSupport } from "@/types/lextrack/dsu_supports/types";
```

(Removes ONLY the `import { toast } from "vue-sonner";` line.)

**Step 2 — After the `support` defineModel block (current line 17, just before the empty line and `const updateSupport`), add:**

```typescript

const props = defineProps<{ saving?: boolean }>();

const emit = defineEmits<{
  save: [item: AddDsuSupport & { id?: string }];
}>();
```

**Step 3 — Replace the `updateSupport` function (current lines 19-23) with:**

```typescript
const onSaveClick = () => emit('save', support.value as AddDsuSupport & { id?: string });
```

**Critical:** This step removes BOTH `toast.success('Support is updated successfully!');` AND `visible.value = false;`. The self-close was a Phase 3 holdover; D-04 requires the parent (LexTrackView) to set `visible.value = false` only AFTER the PB call resolves successfully. If we leave the self-close in, the dialog disappears immediately on click and a save failure surfaces a toast AFTER the dialog is already gone — broken UX.

**Step 4 — Update the Save button in the template (current line 44). Replace:**

```html
<Button label="Save" @click="updateSupport" class="w-full bg-indigo-600 hover:bg-indigo-700" />
```

**With:**

```html
<Button label="Save" :loading="props.saving" @click="onSaveClick" class="w-full bg-indigo-600 hover:bg-indigo-700" />
```

Per RESEARCH.md Pattern 4: PrimeVue Button `:loading` shows the spinner AND auto-disables — no separate `:disabled` needed.

**Do NOT change:**
- Dialog `header="Edit Admin"`, position, modal, style, or any template body other than the Save button line.
- `defineModel` shapes for `visible` and `support`.
- The three input blocks: `<InputText v-model="support.title">`, `<InputText v-model="support.link" type="url">`, `<Editor v-model="support.description">`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <grep_checks>
      - File contains `defineProps<{ saving?: boolean }>`
      - File contains `save: [item: AddDsuSupport & { id?: string }]`
      - File contains `:loading="props.saving"`
      - File contains `@click="onSaveClick"`
      - File does NOT contain `import { toast } from "vue-sonner"`
      - File does NOT contain `toast.success`
      - File does NOT contain `const updateSupport`
      - File does NOT contain `visible.value = false` inside the script section (template's `@close="visible = false"` line 31 is OK and stays — that's the Dialog's own close-X handler)
    </grep_checks>
  </verify>
  <acceptance_criteria>
    - `grep -n "defineProps<{ saving?: boolean }>" src/components/projects/lextrack/ManageSupport.vue` returns exactly 1 match
    - `grep -n "save: \[item: AddDsuSupport & { id\?: string }\]" src/components/projects/lextrack/ManageSupport.vue` returns exactly 1 match
    - `grep -c ':loading="props.saving"' src/components/projects/lextrack/ManageSupport.vue` outputs `1`
    - `grep -c '@click="onSaveClick"' src/components/projects/lextrack/ManageSupport.vue` outputs `1`
    - `grep -c 'vue-sonner' src/components/projects/lextrack/ManageSupport.vue` outputs `0`
    - `grep -c 'toast.success' src/components/projects/lextrack/ManageSupport.vue` outputs `0`
    - `grep -c 'const updateSupport' src/components/projects/lextrack/ManageSupport.vue` outputs `0`
    - `grep -c 'visible.value = false' src/components/projects/lextrack/ManageSupport.vue` outputs `0` (the template `@close="visible = false"` is fine — different syntax, doesn't match this grep)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>
    - ManageSupport.vue compiles cleanly via `npm run type-check`
    - Component emits `save` with typed payload, accepts `saving` prop, renders spinner via PrimeVue `:loading`
    - Self-close (`visible.value = false` from the script) and toast call both removed
    - Phase 3 URL input + dialog header preserved exactly
    - File ready for parent to wire `@save="handleSupportSave"` and `:saving="isSavingSupport"` in Plan 04-04
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dialog component → Parent view | Emit-only contract; parent owns all PB calls AND visibility-on-success. User-typed strings (`title`, `link`, `description`) flow upward via `defineModel`. |
| `link` input → DOM render | `<InputText type="url">` provides browser-level URL hint; PB validates URL server-side per Phase 1 D-12 / `pocketbase-schema.md`. No client-side regex (intentional). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | `onSaveClick` rapid double-click | accept | PrimeVue `:loading` auto-disables the Save button while parent's save is in flight (RESEARCH.md Pattern 4). |
| T-04-03-02 | Information Disclosure | Toast text leaking PB error details | mitigate | Component fires NO toasts post-refactor. All toast strings owned by parent (Plan 04-04). |
| T-04-03-03 | Spoofing/Repudiation | Stale auth token (401 on save) | accept (transferred to parent) | Parent's `handleSupportSave` catch block (Plan 04-04) handles 401. |
| T-04-03-04 | Injection (URL) | Malicious `link` value submitted to PB | mitigate | PocketBase URL field validates server-side (Phase 1 D-12). Browser `type="url"` is best-effort hint. ActivityCard renders the link via `window.open(url, '_blank', 'noopener,noreferrer')` per Phase 3 D-02 — T-3-01 mitigation in place. |
| T-04-03-05 | Tampering (UI state) | Self-close on save click hides failed-save state from user | mitigate | Phase 4 Plan 04-03 explicitly removes `visible.value = false` from the script (Step 3 of Task 1) per D-04. Close moves to parent's success path only. |
</threat_model>

<verification>
- `npm run type-check` exits 0
- `npm run lint` exits 0 (no new warnings vs Phase 3 baseline)
- File-level grep checks (in `<acceptance_criteria>`) all PASS
- Manual visual smoke deferred to Plan 04-05 (phase gate)
</verification>

<success_criteria>
- ManageSupport.vue is a presentational dialog: emits `save`, accepts `saving` prop, no PB calls, no toast calls, no self-close on save.
- `defineModel` shapes preserved exactly.
- Phase 3 URL input + `header="Edit Admin"` preserved.
- Type-check clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bugs-and-save-ux/04-03-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md template.
</output>
