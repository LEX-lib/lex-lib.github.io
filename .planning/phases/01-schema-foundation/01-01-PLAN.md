---
phase: 01-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/pocketbase-schema.md
autonomous: true
requirements:
  - SCHEMA-04
must_haves:
  truths:
    - "A reader can follow .planning/pocketbase-schema.md from top to bottom and apply all three schema changes in the PocketBase admin UI without thinking"
    - "Each of the three schema changes (duration_unit, link, dsu_day_status) has a rollback note"
    - "All five access rules (listRule, viewRule, createRule, updateRule, deleteRule) on dsu_day_status are explicitly set to @request.auth.id != \"\" — none are left blank"
    - "The unique index SQL for dsu_day_status.date is shown verbatim so the user can paste it"
    - "The doc is idempotent — re-reading it after partial application does not cause destructive re-execution"
  artifacts:
    - path: ".planning/pocketbase-schema.md"
      provides: "Step-by-step admin UI migration instructions"
      contains: "duration_unit, link, dsu_day_status, @request.auth.id, CREATE UNIQUE INDEX"
      min_lines: 80
  key_links:
    - from: ".planning/pocketbase-schema.md"
      to: "PocketBase admin UI"
      via: "explicit click-through steps with field names, types, values"
      pattern: "duration_unit|link|dsu_day_status"
    - from: ".planning/pocketbase-schema.md"
      to: ".planning/phases/01-schema-foundation/verify-schema.ts"
      via: "field names and access rule strings match exactly so the smoke script asserts what the doc instructs"
      pattern: "@request.auth.id != \"\""
---

<objective>
Write `.planning/pocketbase-schema.md`, the step-by-step PocketBase admin UI migration document covering all three schema changes for Phase 1: add `duration_unit` to `dsu_meetings`, add `link` to `dsu_supports`, and create the `dsu_day_status` collection with a unique index on `date`.

Purpose: The user applies schema changes manually against their live PocketBase instance (D-01). The doc must be precise enough that the user can follow it without thinking, and complete enough that no rule is accidentally left blank (which would expose the collection publicly per T-1-01). This document is the canonical spec that the verify-schema.ts smoke script (Plan 02) will assert against.

Output: A single Markdown file at `.planning/pocketbase-schema.md` with three numbered sections (one per schema change), each containing pre-flight verification, click-through steps, and a rollback note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-foundation/01-CONTEXT.md
@.planning/phases/01-schema-foundation/01-RESEARCH.md

<interfaces>
<!-- Locked decisions from CONTEXT.md (D-01 through D-14) — see file for rationale -->

Schema spec to encode in the doc (verbatim from CONTEXT.md + RESEARCH.md):

`dsu_meetings.duration_unit` (per D-09, D-10, SCHEMA-01):
- Type: select
- Required: false (optional)
- Max select: 1
- Values: ["minutes", "hours"]

`dsu_supports.link` (per D-12, SCHEMA-02):
- Type: url
- Required: false (optional)

`dsu_day_status` collection (per D-06, D-07, D-08, D-11, D-13, SCHEMA-03):
- Collection type: base
- Field `date`:
  - Type: text
  - Required: true
  - Format: YYYY-MM-DD (frontend convention; no PB pattern enforcement)
- Field `status`:
  - Type: select
  - Required: true
  - Max select: 1
  - Values: ["sl", "vl", "holiday"]
- Access rules (ALL FIVE must be set explicitly — leaving any blank means PUBLIC access per T-1-01):
  - listRule: `@request.auth.id != ""`
  - viewRule: `@request.auth.id != ""`
  - createRule: `@request.auth.id != ""`
  - updateRule: `@request.auth.id != ""`
  - deleteRule: `@request.auth.id != ""`
- Indexes:
  - `CREATE UNIQUE INDEX idx_dsu_day_status_date ON dsu_day_status (date)`

Admin UI navigation pattern (from RESEARCH.md Pattern 1, targeting PB 0.22-0.26):
- Edit existing collection: sidebar -> settings cog -> Fields tab -> "+ New field"
- Create new collection: sidebar -> "New collection" -> Base type -> add fields -> Rules tab -> Indexes tab -> Save
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write `.planning/pocketbase-schema.md` with all three migrations and rollbacks</name>
  <files>.planning/pocketbase-schema.md</files>
  <read_first>
    - .planning/phases/01-schema-foundation/01-CONTEXT.md (D-01 through D-14 — locked decisions)
    - .planning/phases/01-schema-foundation/01-RESEARCH.md sections: §Architecture Patterns (Patterns 1-6), §Common Pitfalls (Pitfall 1, 5, 6), §State of the Art (PB 0.23+ field changes), §Security Domain (T-1-01 rule blank = public access)
    - .planning/REQUIREMENTS.md §Schema (SCHEMA-01 through SCHEMA-04 acceptance text)
  </read_first>
  <action>
    Create `.planning/pocketbase-schema.md` with the following structure. Use concrete values verbatim — do not paraphrase or shorten. The verify-schema.ts script (Plan 02) will assert these exact field names, types, and rule strings.

    ## Document outline

    ### Header
    - Title: `# PocketBase Schema Migration — LexTrack v1`
    - One-paragraph preamble: this doc is the canonical migration runbook for Phase 1 schema changes; user applies these manually via the PocketBase admin UI; changes are additive (no destructive migrations); after applying, run the smoke test (`npm run verify:schema`).
    - "Targeting PocketBase server v0.22-0.26 (admin UI with sidebar collection list, settings cog per collection, Fields/Rules/Indexes tabs). If your admin UI looks substantially different (e.g. dark theme + new layout = likely 0.37+), the underlying field types and SQL syntax remain valid; consult current PocketBase docs for navigation differences." (per RESEARCH.md Pitfall 6)

    ### Prerequisites section
    - User has admin/superuser credentials for the PocketBase instance at `VITE_API_BASE_URL`
    - User can reach `<VITE_API_BASE_URL>/_/` in a browser (admin dashboard)
    - `npm install` has been run in this repo (so the verify script can import `pocketbase`) — per RESEARCH.md Pitfall 4

    ### Section 1: Add `duration_unit` field to `dsu_meetings` (covers SCHEMA-01)
    - **Verify first** (idempotency per RESEARCH.md Pitfall 5): "In the dsu_meetings field list, check if `duration_unit` already exists. If it exists AND it is a Select field with values `minutes`, `hours` — skip this step."
    - Numbered steps:
      1. Navigate to `<VITE_API_BASE_URL>/_/` and log in.
      2. In the left sidebar, find `dsu_meetings`. Click the settings cog next to it.
      3. The collection edit drawer opens on the **Fields** tab.
      4. Scroll to the bottom of the field list. Click **+ New field**.
      5. Field type: **Select**.
      6. Field name: `duration_unit` (exact, lowercase, underscore).
      7. In the field's Options panel:
         - Max select: `1`
         - Add value: `minutes` (type, press Enter)
         - Add value: `hours` (type, press Enter)
         - **Critical** (per RESEARCH.md Pitfall 1): values MUST be added before saving — saving an empty select field returns `validation_required`.
      8. Leave **Required** unchecked (optional per D-09 — existing rows do not get backfilled, the Phase 2 mapper treats undefined as `'minutes'`).
      9. Click **Save** on the field editor.
      10. Click **Save** on the collection editor to commit.
    - **Rollback:** Open `dsu_meetings`, find the `duration_unit` field, click delete on the field, click Save on the collection. Existing meeting records are unaffected; they simply no longer carry the field. Additive-only — no data loss.

    ### Section 2: Add `link` field to `dsu_supports` (covers SCHEMA-02)
    - **Verify first:** "In the dsu_supports field list, check if `link` already exists. If it exists AND it is a URL field — skip this step."
    - Numbered steps:
      1. Navigate to `<VITE_API_BASE_URL>/_/` and log in (skip if continuing from Section 1).
      2. In the sidebar, click the settings cog next to `dsu_supports`.
      3. **Fields** tab -> **+ New field**.
      4. Field type: **URL**.
      5. Field name: `link`.
      6. Leave **Required** unchecked (optional per D-12 — frontend treats `""` and missing key as "no link").
      7. Click **Save** on the field editor.
      8. Click **Save** on the collection editor.
    - **Rollback:** Open `dsu_supports`, delete the `link` field, save. Existing support records are unaffected.

    ### Section 3: Create `dsu_day_status` collection (covers SCHEMA-03)
    - **Verify first:** "In the sidebar, check if `dsu_day_status` already exists. If it exists AND has fields `date` (text, required) and `status` (select with values sl/vl/holiday, required) AND all five access rules equal `@request.auth.id != \"\"` AND has a UNIQUE index on `date` — skip this step."
    - Numbered steps:
      1. Navigate to `<VITE_API_BASE_URL>/_/` and log in.
      2. In the sidebar, click **+ New collection** (or the `+` near the Collections heading).
      3. Collection name: `dsu_day_status` (exact, lowercase, underscores).
      4. Collection type: **Base** (NOT Auth, NOT View).
      5. **Fields** tab — click **+ New field** for each:

         **Field A — `date`:**
         - Type: **Text**
         - Field name: `date`
         - Required: **checked** (every day-status record must have a date)
         - Min length / Max length / Pattern: leave blank (the unique index enforces uniqueness; format `YYYY-MM-DD` is enforced by the frontend per D-13 to avoid PB `date` type timezone coercion)
         - Click **Save** on the field editor.

         **Field B — `status`:**
         - Type: **Select**
         - Field name: `status`
         - Max select: `1`
         - Values: type `sl`, press Enter; type `vl`, press Enter; type `holiday`, press Enter.
         - Required: **checked** (a record without a status is nonsensical)
         - Click **Save** on the field editor.

      6. Switch to the **Rules** tab. **Critical security step (per T-1-01): set ALL FIVE rules. Leaving any rule blank in PocketBase means UNRESTRICTED PUBLIC ACCESS for that operation.** Paste this exact string into each of the five rule fields:

         ```
         @request.auth.id != ""
         ```

         The five fields:
         - **List rule:** `@request.auth.id != ""`
         - **View rule:** `@request.auth.id != ""`
         - **Create rule:** `@request.auth.id != ""`
         - **Update rule:** `@request.auth.id != ""`
         - **Delete rule:** `@request.auth.id != ""`

         Verify each field is non-empty before proceeding.

      7. Switch to the **Indexes** tab. Click **+ New index**. Paste this exact SQL:

         ```sql
         CREATE UNIQUE INDEX idx_dsu_day_status_date ON dsu_day_status (date)
         ```

         Click **Save** on the index.

      8. Click **Create** (or **Save**) on the collection editor to commit.

    - **Rollback:** In the sidebar, click the settings cog next to `dsu_day_status`, choose **Delete collection**, confirm. The collection is new in this phase, so deletion is non-destructive to other LexTrack data.

    ### Closing section — Verify with the smoke script
    - "After applying all three sections, run the smoke test from the project root:
      ```sh
      npm run verify:schema
      ```
      The script (`.planning/phases/01-schema-foundation/verify-schema.ts`) authenticates against `VITE_API_BASE_URL` using `VITE_LOGIN_EMAIL` / `VITE_LOGIN_PASSWORD` (both must be admin/superuser credentials per A2 in RESEARCH.md), reads the live schema, and asserts every field, type, value, rule, and index above. Exit code 0 = phase ready for Phase 2."
    - "If the smoke script fails, the printed FAIL line tells you which assertion failed; re-open the relevant collection in the admin UI and fix the field/rule/index, then re-run."

    ## Concrete-values check before saving the doc

    Before writing, confirm the following strings appear verbatim in the doc body:
    - `@request.auth.id != ""` — must appear at least 5 times (one per rule on dsu_day_status)
    - `CREATE UNIQUE INDEX idx_dsu_day_status_date ON dsu_day_status (date)` — must appear at least once
    - `duration_unit`, `link`, `dsu_day_status`, `dsu_meetings`, `dsu_supports` — verbatim collection/field names
    - `minutes`, `hours` — duration_unit values
    - `sl`, `vl`, `holiday` — status values
    - `**Rollback:**` (or `### Rollback`) — must appear at least 3 times (one per section)
  </action>
  <verify>
    <automated>test -f .planning/pocketbase-schema.md && [ "$(grep -c '@request\.auth\.id != \"\"' .planning/pocketbase-schema.md)" -ge 5 ] && [ "$(grep -ci 'rollback' .planning/pocketbase-schema.md)" -ge 3 ] && grep -q 'CREATE UNIQUE INDEX idx_dsu_day_status_date ON dsu_day_status (date)' .planning/pocketbase-schema.md && grep -q 'duration_unit' .planning/pocketbase-schema.md && grep -q 'dsu_day_status' .planning/pocketbase-schema.md && grep -q 'minutes' .planning/pocketbase-schema.md && grep -q 'hours' .planning/pocketbase-schema.md && grep -q 'sl' .planning/pocketbase-schema.md && grep -q 'holiday' .planning/pocketbase-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/pocketbase-schema.md`
    - Contains the literal string `@request.auth.id != ""` at least 5 times (one per rule on dsu_day_status) — `grep -c '@request\.auth\.id != ""' .planning/pocketbase-schema.md` returns >= 5
    - Contains the word "rollback" (case-insensitive) at least 3 times — `grep -ci rollback .planning/pocketbase-schema.md` returns >= 3
    - Contains the exact unique index SQL: `grep -q 'CREATE UNIQUE INDEX idx_dsu_day_status_date ON dsu_day_status (date)' .planning/pocketbase-schema.md` succeeds
    - Contains all field names: `duration_unit`, `link`, `dsu_day_status`, `dsu_meetings`, `dsu_supports`
    - Contains all enum values: `minutes`, `hours`, `sl`, `vl` (as `vl` — bare letters), `holiday`
    - File is at least ~80 non-blank lines (substantive doc, not a stub)
  </acceptance_criteria>
  <done>
    A reader unfamiliar with this codebase can open `.planning/pocketbase-schema.md`, follow its three numbered sections in the PocketBase admin UI, and end up with a live schema that the verify-schema.ts smoke script (Plan 02) will pass against. Every rule is explicitly set, every change has a rollback note, and the doc is safe to re-read after partial application.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User -> PocketBase admin UI | User pastes rule strings into the admin UI; if any rule is left blank, that operation becomes publicly accessible |
| Migration doc -> User cognition | Doc is the only source of truth at apply time; ambiguous or incomplete instructions translate directly into misconfigured production schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | dsu_day_status access rules | mitigate | Doc Section 3 step 6 explicitly enumerates ALL FIVE rule fields with the exact rule string `@request.auth.id != ""`, and includes a "Verify each field is non-empty before proceeding" checkpoint. The acceptance criterion `grep -c '@request.auth.id != ""' >= 5` enforces the doc cannot ship missing any rule. |
| T-1-04 | Tampering | dsu_day_status.date uniqueness | mitigate | Doc Section 3 step 7 mandates the UNIQUE SQL index. Without it, two day-status records could exist for the same date and frontend code (per D-08) cannot rely on the constraint. Acceptance criterion enforces the verbatim CREATE UNIQUE INDEX statement is present. |
| T-1-05 | Repudiation | Migration application state | accept | No automated audit log of which steps the user applied — manual migration. Mitigated by the smoke script (Plan 02), which is the source of truth for "the schema matches the spec" regardless of how it got there. |
</threat_model>

<verification>
- `test -f .planning/pocketbase-schema.md` — file exists
- `grep -c '@request\.auth\.id != ""' .planning/pocketbase-schema.md` >= 5 — all five access rules explicitly set
- `grep -ci rollback .planning/pocketbase-schema.md` >= 3 — rollback note per change
- `grep -q 'CREATE UNIQUE INDEX idx_dsu_day_status_date ON dsu_day_status (date)' .planning/pocketbase-schema.md` — unique index SQL verbatim
- All field/value names present (duration_unit, link, dsu_day_status, minutes, hours, sl, vl, holiday)
</verification>

<success_criteria>
The migration doc is complete enough that:
1. The user can apply all three schema changes by following it without consulting CONTEXT.md or RESEARCH.md.
2. The smoke script in Plan 02 will pass against a PocketBase instance migrated solely from this doc.
3. No access rule is silently left blank (publicly accessible).
4. Each change has an explicit rollback note.
5. Re-reading the doc after partial application does not cause destructive re-execution (idempotency check at the top of each section).
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-foundation/01-01-SUMMARY.md` capturing: file written, key sections, grep counts achieved, any deviations from RESEARCH.md spec.
</output>
