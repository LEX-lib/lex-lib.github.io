---
phase: 37-pwa-install-standalone-polish
plan: 05
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - vite.config.ts
  - src/components/projects/wallecx/WallecxApp.vue
  - src/components/projects/wallecx/ExpensesTab.vue
  - src/components/projects/wallecx/VaccinationsTab.vue
  - src/components/projects/wallecx/MembershipsTab.vue
  - src/router/__tests__/guard.spec.ts
autonomous: true
requirements: [PWA-06, PWA-09]
must_haves:
  truths:
    - "vite.config.ts manifest.shortcuts array declares 4 entries (Add Expense / Add Vaccination / Add Membership / Open Reports) targeting /projects/wallecx?action=..."
    - "registerType: 'prompt' and scope: '/' LOCKED comments byte-intact (NFR-PWA-AUTOUPDATE / CON-PWA-SCOPE)"
    - "WallecxApp.vue reads route.query.action on mount, maps to activeTab via ACTION_TAB_MAP, awaits nextTick, sets pendingAction ref, then calls router.replace({ query: {} })"
    - "pendingAction is passed as a prop to ExpensesTab/VaccinationsTab/MembershipsTab (NO new Pinia store)"
    - "Each tab component watches props.pendingAction with { immediate: true } and opens the appropriate Manage dialog (or sub-tab) on match"
    - "SW-update toast at WallecxApp.vue includes style: { paddingBottom: 'env(safe-area-inset-bottom)' } (PWA-06)"
    - "Auth-expired toast at WallecxApp.vue uses eviction-aware copy when isIosSafari() || isStandalone.value is true (NFR-IOS-EVICTION-UX)"
    - "guard.spec.ts contains a test asserting router.currentRoute.value.query.redirect === '/projects/wallecx?action=add-expense' after redirect from unauthenticated /projects/wallecx?action=add-expense (D-37-16)"
  artifacts:
    - path: "vite.config.ts"
      provides: "manifest.shortcuts array — 4 entries"
      contains: "shortcuts:"
    - path: "src/components/projects/wallecx/WallecxApp.vue"
      provides: "pendingAction dispatch + SW toast safe-area + iOS eviction-aware copy"
      contains: "pendingAction"
      contains_also: "ACTION_TAB_MAP"
    - path: "src/components/projects/wallecx/ExpensesTab.vue"
      provides: "pendingAction prop + watcher opens ManageExpense or sub-tab"
      contains: "pendingAction"
    - path: "src/components/projects/wallecx/VaccinationsTab.vue"
      provides: "pendingAction prop + watcher opens ManageVaccination"
      contains: "pendingAction"
    - path: "src/components/projects/wallecx/MembershipsTab.vue"
      provides: "pendingAction prop + watcher opens ManageMembership"
      contains: "pendingAction"
    - path: "src/router/__tests__/guard.spec.ts"
      provides: "Query-preservation test (D-37-16)"
      contains: "preserves query string"
  key_links:
    - from: "vite.config.ts manifest.shortcuts urls"
      to: "WallecxApp ACTION_TAB_MAP keys"
      via: "URL ?action= matches add-expense / add-vaccination / add-membership / open-reports"
      pattern: "action=add-expense"
    - from: "WallecxApp.vue pendingAction"
      to: "ExpensesTab/VaccinationsTab/MembershipsTab props.pendingAction"
      via: ":pending-action=\"pendingAction\""
      pattern: ":pending-action"
    - from: "router.beforeEach query preservation"
      to: "WallecxApp.vue dispatch after login"
      via: "to.fullPath includes ?action=..., redirect query string roundtrips"
      pattern: "query.redirect"
---

<objective>
Wire the Android Quick Actions surface end-to-end (PWA-09 / D-37-13/14/15/16) and apply the two remaining WallecxApp.vue polish edits (PWA-06 SW-update toast safe-area, NFR-IOS-EVICTION-UX eviction-aware auth-expired copy). vite.config.ts gains a `manifest.shortcuts` array; WallecxApp.vue reads `route.query.action` on mount and dispatches via a `pendingAction` ref propagated as a prop to each tab; ExpensesTab / VaccinationsTab / MembershipsTab each grow a `pendingAction` prop + immediate watcher that opens their Manage dialog; the router guard spec gains a query-preservation test that proves the existing redirect mechanism works for the auth-fallback shortcut path.

Purpose: Long-press the Wallecx home-screen icon on Android shows 4 Quick Action shortcuts; tapping one deep-links into `/projects/wallecx?action=<action>` and either dispatches immediately (logged in) or routes through the login page preserving the action via `to.fullPath` (logged out). No new routes, no new Pinia store (D-37-14 local ref). The SW-update toast becomes fully tappable above the iPhone home indicator, and a session-expired toast on iOS / standalone explains 7-day eviction and offers the home-screen mitigation tip.
Output: 6 modified files; no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md
@.planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md
@.planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md
@.planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md
@vite.config.ts
@src/components/projects/wallecx/WallecxApp.vue
@src/components/projects/wallecx/ExpensesTab.vue
@src/components/projects/wallecx/VaccinationsTab.vue
@src/components/projects/wallecx/MembershipsTab.vue
@src/router/__tests__/guard.spec.ts
@src/composables/useMobileEnv.ts
@src/router/index.ts

<interfaces>
<!-- vite.config.ts manifest object (existing, lines 35-79) -->
<!-- LOCKED line 28 byte-intact: registerType: "prompt", // LOCKED: never 'autoUpdate' — CRUD forms have unsaved state -->
<!-- LOCKED line 42 byte-intact: scope: "/", // LOCKED: scope "/" per STATE.md — NOT "/projects/wallecx" -->
<!-- Insertion site: after `screenshots` array (~line 78), before workbox block (line 80). New `shortcuts` key. -->

<!-- WallecxApp.vue current state (127 lines, key spots) -->
- line 2: import { ref, onMounted, watch, defineAsyncComponent } from "vue";
- line 3: import { useRouter } from "vue-router";
- line 16: const activeTab = ref<string>("vaccinations");
- lines 23-38: watch(needRefresh, ...) -> toast.info("A new version of Wallecx is available.", { duration: Infinity, action: { label: "Refresh", onClick: () => updateServiceWorker(true) }, cancel: { label: "Later", onClick: () => { needRefresh.value = false } } });
- line 60: toast.info("Your session has expired. Please sign in again.");
- lines 95-118: <TabPanel> blocks wrap <VaccinationsTab/>, <MembershipsTab/>, <ExpensesTab/> in <Suspense> — these tab elements get :pending-action="pendingAction"

<!-- ExpensesTab existing surface (lines 17-103, 86-103 openManage) -->
- import + defineAsyncComponent for ManageExpense
- const showManage = ref(false)
- const activeSubTab = ref<SubTab>('list')   // 'list' | 'reports'
- async function openManage(record: Expenses | null)  // record=null -> create mode

<!-- VaccinationsTab existing surface (lines 10, 26, 204-206) -->
- import + defineAsyncComponent for ManageVaccination
- const showManage = ref<boolean>(false)
- function openManage(record: Vaccinations | null)  // record=null -> create mode

<!-- MembershipsTab existing surface (lines 8, 22, 153-168) -->
- import + defineAsyncComponent for ManageMembership
- const showManage = ref<boolean>(false)
- async function openManage(record: Memberships | null): Promise<void>  // record=null -> create mode

<!-- useMobileEnv (Phase 33) -->
import { useMobileEnv } from '@/composables/useMobileEnv'
const { isStandalone } = useMobileEnv()  // Ref<boolean>

<!-- router/index.ts guard (Phase 33+) -->
router.beforeEach((to, _from, next) => {
  if (to.matched.some(r => r.meta?.requiresAuth)) {
    if (!useAuthStore().isLoggedIn) {
      next({ name: "login", query: { redirect: to.fullPath } });  // to.fullPath includes ?action=...
      return;
    }
  }
  next();
});

<!-- guard.spec.ts existing pattern (lines 66-73 — copy structure for new test) -->
it("redirects to /login when not authenticated", async () => { ... });
// helpers buildRouter() / addGuard() already defined; mock for useAuthStore at top of file

<!-- Locked invariants -->
- NO new Pinia store (STATE.md). pendingAction is a local ref in WallecxApp.vue passed as prop.
- LOCKED registerType: "prompt" and scope: "/" comments byte-intact.
- ACTION_TAB_MAP keys are EXACT strings: 'add-expense', 'add-vaccination', 'add-membership', 'open-reports'.
- ACTION_TAB_MAP values map to existing activeTab values: 'expenses', 'vaccinations', 'memberships', 'expenses' (open-reports also targets expenses tab).
- 'open-reports' additionally sets the expenses sub-tab to 'reports' inside ExpensesTab (NOT inside WallecxApp).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend vite.config.ts manifest.shortcuts + add query-preservation test in guard.spec.ts</name>
  <files>vite.config.ts, src/router/__tests__/guard.spec.ts</files>
  <read_first>
    - vite.config.ts (full file — LOCKED lines 28 and 42 must remain byte-intact; identify insertion point ~line 78 after `screenshots` array, before `workbox` block)
    - src/router/__tests__/guard.spec.ts (full file — find existing `redirects to /login when not authenticated` test at line 66; copy its structure)
    - .planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md §"vite.config.ts (config, transform) — EXTEND" and §"src/router/__tests__/guard.spec.ts (test, request-response) — EXTEND"
    - .planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md §"Pattern 4: Manifest Shortcuts in vite.config.ts" and §"Guard Spec — Query Preservation Test"
    - .planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md D-37-13, D-37-15, D-37-16
    - public/shortcuts/*.png (must exist — Plan 01 dependency)
  </read_first>
  <behavior>
    - Test 1 (new in guard.spec.ts): When unauthenticated user pushes `/projects/wallecx?action=add-expense`, router redirects to `name: 'login'` and `currentRoute.value.query.redirect === '/projects/wallecx?action=add-expense'` (D-37-16).
    - Test 2 (existing in guard.spec.ts): regressions blocked — existing `redirects to /login when not authenticated`, `allows navigation when authenticated`, `allows unauthenticated access to non-guarded routes` all still pass.
  </behavior>
  <action>
    EDIT 1 — `vite.config.ts`: extend the `manifest` object with a `shortcuts: [...]` array containing 4 entries. Insertion site: AFTER the `screenshots` array (currently the last manifest property, ~line 78) and BEFORE the closing `}` of the `manifest:` object. Each entry has the shape `{ name, short_name, url, icons: [{ src, sizes, type, purpose }] }` per D-37-15:
    - `{ name: 'Add Expense', short_name: 'Add Expense', url: '/projects/wallecx?action=add-expense', icons: [{ src: 'shortcuts/shortcut-add-expense.png', sizes: '96x96', type: 'image/png', purpose: 'any' }] }`
    - `{ name: 'Add Vaccination', short_name: 'Add Vaccination', url: '/projects/wallecx?action=add-vaccination', icons: [{ src: 'shortcuts/shortcut-add-vaccination.png', sizes: '96x96', type: 'image/png', purpose: 'any' }] }`
    - `{ name: 'Add Membership', short_name: 'Add Membership', url: '/projects/wallecx?action=add-membership', icons: [{ src: 'shortcuts/shortcut-add-membership.png', sizes: '96x96', type: 'image/png', purpose: 'any' }] }`
    - `{ name: 'Open Reports', short_name: 'Open Reports', url: '/projects/wallecx?action=open-reports', icons: [{ src: 'shortcuts/shortcut-open-reports.png', sizes: '96x96', type: 'image/png', purpose: 'any' }] }`

    Do NOT modify:
    - Line 28 `registerType: "prompt",` LOCKED comment
    - Line 42 `scope: "/",` LOCKED comment
    - `manifest.name`, `short_name`, `description`, `theme_color`, `background_color`, `display`, `start_url`, `icons`, `screenshots`
    - workbox block (`globPatterns`, `globIgnores`, `maximumFileSizeToCacheInBytes`, `cleanupOutdatedCaches`, `navigateFallback`, `navigateFallbackDenylist`, `runtimeCaching`)
    - `includeAssets` (RESEARCH §Pattern 4 confirms `globPatterns: ["**/*.{...,png,...}"]` auto-precaches PNGs in public/ subdirs without adding to `includeAssets`)
    - rolldownOptions.codeSplitting groups (Phase 36 invariant)
    - visualizer spread-conditional (Phase 33 FND-03 invariant)

    EDIT 2 — `src/router/__tests__/guard.spec.ts`: ADD a new `it()` block INSIDE the existing `describe("requiresAuth guard — /projects/wallecx", () => { ... })` block, AFTER the existing `it("redirects to /login when not authenticated", ...)` test (after line 73, before the `it("allows navigation when authenticated", ...)` test on line 75). Test body:
    - Mock `useAuthStore` to return `{ isLoggedIn: false }` (same pattern as existing tests)
    - `const router = buildRouter(); addGuard(router);`
    - `await router.push("/projects/wallecx?action=add-expense");`
    - `expect(router.currentRoute.value.name).toBe("login");`
    - `expect(router.currentRoute.value.query.redirect).toBe("/projects/wallecx?action=add-expense");`
    - Test name: `"preserves query string in redirect when not authenticated"`

    Do NOT modify any other test, any helper (`buildRouter`, `addGuard`), or any import.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>node -e "const fs=require('fs');const v=fs.readFileSync('vite.config.ts','utf8');const errs=[];if(!v.includes('registerType: \"prompt\",          // LOCKED'))errs.push('LOCKED registerType comment regressed');if(!v.includes('scope: \"/\",                    // LOCKED'))errs.push('LOCKED scope comment regressed');if(!v.includes('shortcuts:'))errs.push('shortcuts array missing');if(!v.includes(\"action=add-expense\"))errs.push('add-expense url missing');if(!v.includes(\"action=add-vaccination\"))errs.push('add-vaccination url missing');if(!v.includes(\"action=add-membership\"))errs.push('add-membership url missing');if(!v.includes(\"action=open-reports\"))errs.push('open-reports url missing');if(!v.includes('shortcuts/shortcut-add-expense.png'))errs.push('add-expense icon src missing');if(!v.includes('shortcuts/shortcut-add-vaccination.png'))errs.push('add-vaccination icon src missing');if(!v.includes('shortcuts/shortcut-add-membership.png'))errs.push('add-membership icon src missing');if(!v.includes('shortcuts/shortcut-open-reports.png'))errs.push('open-reports icon src missing');const shortcutCount=(v.match(/url:\\s*['\"]\\/projects\\/wallecx\\?action=/g)||[]).length;if(shortcutCount!==4)errs.push('shortcut URL count='+shortcutCount+' expected 4');const g=fs.readFileSync('src/router/__tests__/guard.spec.ts','utf8');if(!g.includes('preserves query string in redirect when not authenticated'))errs.push('new guard test missing');if(!g.includes('/projects/wallecx?action=add-expense'))errs.push('test URL with action= missing in guard spec');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK');"</automated>
    <automated>npm run test:unit -- --run src/router/__tests__/guard.spec.ts</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts line 28 contains `registerType: "prompt",          // LOCKED:` (byte-intact)
    - vite.config.ts line 42 contains `scope: "/",                    // LOCKED:` (byte-intact)
    - vite.config.ts manifest object contains a `shortcuts:` key with exactly 4 entries
    - Exactly 4 URLs matching `/projects/wallecx?action=...` (one each for add-expense, add-vaccination, add-membership, open-reports)
    - Each entry has icon `src` matching `shortcuts/shortcut-<action>.png`, `sizes: '96x96'`, `type: 'image/png'`, `purpose: 'any'`
    - vite.config.ts does NOT modify workbox, rolldownOptions, or visualizer blocks
    - guard.spec.ts contains the new `it("preserves query string in redirect when not authenticated", ...)` block
    - New test asserts `currentRoute.value.query.redirect === "/projects/wallecx?action=add-expense"`
    - `npm run type-check` exits 0
    - `npm run test:unit -- --run src/router/__tests__/guard.spec.ts` exits 0 with the new test passing (count grows from 3 to 4)
    - `npm run build` exits 0 with 0 lines containing "exceeds" or "Skipping precaching"
  </acceptance_criteria>
  <done>
    Manifest shortcuts declared with valid URLs + icons; guard spec proves the auth fallback preserves the query string for shortcut dispatch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WallecxApp.vue — pendingAction dispatch + SW toast safe-area + iOS eviction-aware copy</name>
  <files>src/components/projects/wallecx/WallecxApp.vue</files>
  <read_first>
    - src/components/projects/wallecx/WallecxApp.vue (full 127 lines — current imports + watch + onMounted + template)
    - src/composables/useMobileEnv.ts (useMobileEnv() shape and isStandalone ref)
    - .planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md §"WallecxApp.vue (component, request-response) — EXTEND"
    - .planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md §"Pattern 5: pendingAction Dispatch in WallecxApp.vue" (note its Planner decision -> we choose PROP, NOT module-scope ref) and §"Pattern 6: iOS Eviction-Aware Toast" and §"Pattern 7: SW-Update Toast Safe-Area"
    - .planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md §"Interaction Contracts — Manifest Shortcuts" and §"Copywriting Contract" (eviction copy exact string)
    - .planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md D-37-14 (pendingAction local ref + nextTick ordering, NO Pinia store) and Pitfall 4 (immediate watcher required on consumer)
  </read_first>
  <behavior>
    - Test 1: When mounted with `route.query.action === 'add-expense'`, WallecxApp sets `activeTab.value` to `'expenses'`, awaits nextTick, sets `pendingAction.value` to `'add-expense'`, then calls `router.replace({ query: {} })`.
    - Test 2: When mounted with `route.query.action === 'open-reports'`, WallecxApp sets `activeTab.value` to `'expenses'` and `pendingAction.value` to `'open-reports'`.
    - Test 3: When mounted without `route.query.action`, WallecxApp does NOT modify `activeTab` from its default and does NOT set `pendingAction`.
    - Test 4: Invalid action string (not in ACTION_TAB_MAP) is silently ignored — neither `activeTab` nor `pendingAction` is touched (V5 input validation allowlist).
    - Test 5: SW-update toast call includes a `style: { paddingBottom: 'env(safe-area-inset-bottom)' }` option.
    - Test 6: When `isIosSafari()` returns true OR `isStandalone.value` is true at the auth-expired check site, the toast message is the eviction-aware copy; otherwise the generic copy.
  </behavior>
  <action>
    EDIT in place. Multiple coherent edits to a single file. The 4 distinct change zones:

    ZONE A — Imports block (top of `<script setup lang="ts">`):
    - Add `nextTick` to the existing `import { ref, onMounted, watch, defineAsyncComponent } from "vue";` -> `import { ref, onMounted, watch, defineAsyncComponent, nextTick } from "vue";`
    - Add `useRoute` to the existing vue-router import -> `import { useRoute, useRouter } from "vue-router";`
    - Add new import line below the existing imports: `import { useMobileEnv } from "@/composables/useMobileEnv";`

    ZONE B — Top-level setup (after existing `const router = useRouter();` and `const activeTab = ...` declarations, before the SW register block ~line 21):
    - `const route = useRoute();`
    - `const { isStandalone } = useMobileEnv();`
    - `const pendingAction = ref<string | null>(null);`
    - `const ACTION_TAB_MAP: Record<string, string> = { 'add-expense': 'expenses', 'add-vaccination': 'vaccinations', 'add-membership': 'memberships', 'open-reports': 'expenses' };` (D-37-14: open-reports also targets expenses tab; ExpensesTab will switch sub-tab to reports based on its own watcher)
    - Add local helper `function isIosSafari(): boolean { const ua = navigator.userAgent; return /iPad|iPhone|iPod/.test(ua) && !/CriOS|FxiOS|OPiOS|mercury/i.test(ua); }` (RESEARCH §Pattern 6 inline-duplicate per planner note; matches PwaInstallBanner helper byte-for-byte)

    ZONE C — Watch needRefresh -> SW toast (existing lines 23-38):
    - PRESERVE the entire watch(needRefresh, ...) structure: outer message string `"A new version of Wallecx is available."` byte-intact, `duration: Infinity`, `action: { label: "Refresh", onClick: () => updateServiceWorker(true) }`, `cancel: { label: "Later", onClick: () => { needRefresh.value = false } }`.
    - ADD ONE PROPERTY to the toast options object: `style: { paddingBottom: 'env(safe-area-inset-bottom)' }` (PWA-06, per-call approach per RESEARCH §Pattern 7 — targeted scope, does not affect other toasts; do NOT add a CSS override to wallecx-overrides.css — `[data-sonner-toaster]` global override would change every toast site-wide).
    - Place `style` between `duration` and `action` for readability.

    ZONE D — onMounted (existing lines 41-66): TWO changes inside the existing block.

    Change D1 — Replace line 60 `toast.info("Your session has expired. Please sign in again.")` with branching:
    ```
    const evictionMessage = (isIosSafari() || isStandalone.value)
      ? "iOS may have cleared local data after 7 days without opening the app. Please sign in again. Tip: pin Wallecx to your home screen to prevent this."
      : "Your session has expired. Please sign in again.";
    toast.info(evictionMessage);
    ```
    The eviction-aware copy MUST be EXACTLY the string above (per UI-SPEC §Copywriting Contract auth-expired iOS eviction path). PRESERVE the surrounding `await router.push({ name: "login", query: { redirect: "/projects/wallecx" } })` block byte-intact.

    Change D2 — Add the shortcut dispatch block AFTER the auth-expired branch (after the existing `}` closing the `if (!pb.authStore.isValid) { ... }` block, before the `}` closing the `onMounted(async () => { ... })`):
    ```
    const action = route.query.action;
    if (typeof action === 'string' && ACTION_TAB_MAP[action]) {
      activeTab.value = ACTION_TAB_MAP[action];
      await nextTick();   // Pitfall 4: let Suspense begin resolving the active tab before pendingAction set
      pendingAction.value = action;
      router.replace({ query: {} });
    }
    ```
    The dispatch runs AFTER the auth check so a logged-out user is redirected to /login first; the action survives in `to.fullPath` -> `query.redirect` and re-fires after login when the user lands back on `/projects/wallecx?action=...`.

    ZONE E — Template (existing lines 95-118 TabPanel blocks):
    - For each of the 3 `<VaccinationsTab />`, `<MembershipsTab />`, `<ExpensesTab />` elements inside their respective `<Suspense>`, add a prop binding: `:pending-action="pendingAction"`.
    - PRESERVE the `<Suspense>` wrappers, the `<template #fallback><WallecxSkeleton ...></template>` fallbacks, and all `<TabPanel value="...">` shells (Phase 36 invariant).

    LOCKED INVARIANTS:
    - `const { needRefresh, updateServiceWorker } = useRegisterSW();` (line 21) byte-intact
    - SW toast user-visible copy `"A new version of Wallecx is available."` + `"Refresh"` + `"Later"` byte-intact (UI-SPEC §Copywriting Contract)
    - `navigator.storage.persist()` mitigation block (lines 45-51) byte-intact
    - `pb.authStore.isValid` check pattern (line 59) and `router.push({ name: "login", query: { redirect: "/projects/wallecx" } })` byte-intact
    - `<PwaInstallBanner />` mount on line 125 byte-intact
    - `<ConfirmDialog />` mount on line 122 byte-intact (CON-CONFIRMDIALOG-SINGLETON)
    - `wallecx-main-tabs` class and `wallecx-root overscroll-none` Card class byte-intact
    - 3 TabPanel `value=` strings (`vaccinations`/`memberships`/`expenses`) byte-intact
    - NO new Pinia store created (`pendingAction` is a local ref only)
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/projects/wallecx/WallecxApp.vue','utf8');const errs=[];if(!c.includes('nextTick'))errs.push('nextTick import missing');if(!c.includes('useRoute'))errs.push('useRoute import missing');if(!c.includes(\"from '@/composables/useMobileEnv'\")&&!c.includes('from \"@/composables/useMobileEnv\"'))errs.push('useMobileEnv import missing');if(!c.includes('ACTION_TAB_MAP'))errs.push('ACTION_TAB_MAP missing');if(!c.includes(\"'add-expense': 'expenses'\")&&!c.includes('\"add-expense\": \"expenses\"'))errs.push('add-expense map entry missing');if(!c.includes(\"'add-vaccination': 'vaccinations'\")&&!c.includes('\"add-vaccination\": \"vaccinations\"'))errs.push('add-vaccination map entry missing');if(!c.includes(\"'add-membership': 'memberships'\")&&!c.includes('\"add-membership\": \"memberships\"'))errs.push('add-membership map entry missing');if(!c.includes(\"'open-reports': 'expenses'\")&&!c.includes('\"open-reports\": \"expenses\"'))errs.push('open-reports map entry missing');if(!c.includes('pendingAction'))errs.push('pendingAction ref missing');if(!c.includes('router.replace'))errs.push('router.replace clear missing');if(!c.includes('await nextTick()'))errs.push('await nextTick() missing (Pitfall 4 violation)');if(!c.includes(\"style: { paddingBottom: 'env(safe-area-inset-bottom)' }\"))errs.push('SW toast safe-area style option missing');if(!c.includes('A new version of Wallecx is available.'))errs.push('SW toast copy regressed');if(!c.includes('Refresh'))errs.push('SW Refresh label regressed');if(!c.includes('Later'))errs.push('SW Later label regressed');if(!c.includes('iOS may have cleared local data after 7 days without opening the app. Please sign in again. Tip: pin Wallecx to your home screen to prevent this.'))errs.push('eviction-aware copy missing/wrong');if(!c.includes('Your session has expired. Please sign in again.'))errs.push('generic auth-expired copy regressed (must still be available for non-iOS branch)');if(!c.includes('isIosSafari'))errs.push('isIosSafari helper missing');if(!c.includes(':pending-action=\"pendingAction\"'))errs.push('pending-action prop not bound on tab elements');const pendingActionPropCount=(c.match(/:pending-action=\"pendingAction\"/g)||[]).length;if(pendingActionPropCount!==3)errs.push('pending-action prop count='+pendingActionPropCount+' expected 3 (VaccinationsTab, MembershipsTab, ExpensesTab)');if(!c.includes('navigator.storage?.persist'))errs.push('navigator.storage.persist mitigation regressed');if(!c.includes('<PwaInstallBanner />'))errs.push('PwaInstallBanner mount regressed');if(!c.includes('<ConfirmDialog />'))errs.push('ConfirmDialog singleton mount regressed');if(!c.includes('wallecx-main-tabs'))errs.push('wallecx-main-tabs class regressed');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK');"</automated>
    <automated>npm run test:unit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - WallecxApp.vue imports `nextTick` from vue and `useRoute` from vue-router
    - WallecxApp.vue imports `useMobileEnv` from `@/composables/useMobileEnv` and destructures `isStandalone`
    - File contains `ACTION_TAB_MAP` typed `Record<string, string>` with exactly 4 keys matching `add-expense`/`add-vaccination`/`add-membership`/`open-reports`
    - Mapping values: `add-expense` -> `expenses`, `add-vaccination` -> `vaccinations`, `add-membership` -> `memberships`, `open-reports` -> `expenses`
    - `pendingAction` declared as `ref<string | null>(null)`
    - File contains `await nextTick()` between `activeTab.value = ...` and `pendingAction.value = ...` (Pitfall 4)
    - File contains `router.replace({ query: {} })` after pendingAction is set
    - File contains a local `isIosSafari()` function with the exact UA regex `/iPad|iPhone|iPod/` and exclusion regex `/CriOS|FxiOS|OPiOS|mercury/i`
    - SW-update `toast.info(...)` options object contains `style: { paddingBottom: 'env(safe-area-inset-bottom)' }`
    - SW toast user-visible strings byte-intact: `A new version of Wallecx is available.`, `Refresh`, `Later`
    - Auth-expired branch uses the EXACT eviction-aware copy: `iOS may have cleared local data after 7 days without opening the app. Please sign in again. Tip: pin Wallecx to your home screen to prevent this.`
    - Auth-expired branch still includes the generic copy `Your session has expired. Please sign in again.` for non-iOS / non-standalone path
    - Eviction branch gated on `(isIosSafari() || isStandalone.value)`
    - Template has exactly 3 occurrences of `:pending-action="pendingAction"` (one per tab)
    - `<PwaInstallBanner />`, `<ConfirmDialog />`, `wallecx-main-tabs`, `wallecx-root overscroll-none`, `navigator.storage?.persist` all preserved
    - NO new Pinia store created
    - `npm run type-check` exits 0
    - `npm run test:unit` exits 0 (59 + 1 new guard test = 60/60 from Task 1, preserved here)
    - `npm run build` exits 0 with 0 "exceeds" / 0 "Skipping precaching"
  </acceptance_criteria>
  <done>
    WallecxApp.vue dispatches shortcut deep-links, eviction copy lands at the auth-expired toast site, and the SW-update toast has safe-area clearance — all while preserving every Phase 33/34/35/36 invariant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ExpensesTab/VaccinationsTab/MembershipsTab — accept pendingAction prop + immediate watcher dispatches dialog</name>
  <files>src/components/projects/wallecx/ExpensesTab.vue, src/components/projects/wallecx/VaccinationsTab.vue, src/components/projects/wallecx/MembershipsTab.vue</files>
  <read_first>
    - src/components/projects/wallecx/ExpensesTab.vue (lines 17-103 — `openManage(null)` signature for create mode + `activeSubTab` ref + 'list' / 'reports' values)
    - src/components/projects/wallecx/VaccinationsTab.vue (lines 10-26, 204-206 — `openManage(record: Vaccinations | null)` for create mode)
    - src/components/projects/wallecx/MembershipsTab.vue (lines 8-22, 153-168 — `openManage(record: Memberships | null): Promise<void>` for create mode)
    - .planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md §"WallecxApp.vue — Tab-side watcher pattern (example for ExpensesTab — applies to all three tabs)"
    - .planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md §"Pitfall 4: pendingAction Fires Before Tab Watcher is Registered"
    - .planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md D-37-14
  </read_first>
  <behavior>
    - Test 1 (ExpensesTab): When `props.pendingAction === 'add-expense'`, the tab calls `openManage(null)` once (creates a new expense).
    - Test 2 (ExpensesTab): When `props.pendingAction === 'open-reports'`, the tab sets `activeSubTab.value = 'reports'`.
    - Test 3 (ExpensesTab): When `props.pendingAction === null` or any unrecognized value, the watcher does nothing.
    - Test 4 (VaccinationsTab): When `props.pendingAction === 'add-vaccination'`, the tab calls `openManage(null)`.
    - Test 5 (MembershipsTab): When `props.pendingAction === 'add-membership'`, the tab calls `openManage(null)`.
    - Test 6 (all three): The watcher uses `{ immediate: true }` so a pendingAction value set BEFORE the watcher registers (race) is still observed (Pitfall 4).
  </behavior>
  <action>
    For EACH of the 3 tab components (`ExpensesTab.vue`, `VaccinationsTab.vue`, `MembershipsTab.vue`), make the same shape of edit:

    1. ADD `defineProps`. The current files do NOT declare any props (they receive no props from WallecxApp today). Insert a `defineProps` call near the top of `<script setup lang="ts">`, after the existing imports, BEFORE the existing reactive state declarations:
       ```
       const props = defineProps<{ pendingAction?: string | null }>();
       ```
       Use a TYPE-ONLY `defineProps<...>()` form (Vue 3 standard, matches CLAUDE.md "Vue 3 Composition API throughout" conventions).

    2. ADD an immediate watcher. Insert AFTER `openManage` is defined (so it is in scope at watcher registration). The watcher pattern differs per tab:

       For `ExpensesTab.vue` (handles BOTH `add-expense` AND `open-reports`):
       ```
       watch(
         () => props.pendingAction,
         (action) => {
           if (action === 'add-expense') {
             openManage(null);
           } else if (action === 'open-reports') {
             activeSubTab.value = 'reports';
           }
         },
         { immediate: true }
       );
       ```

       For `VaccinationsTab.vue` (handles `add-vaccination`):
       ```
       watch(
         () => props.pendingAction,
         (action) => {
           if (action === 'add-vaccination') {
             openManage(null);
           }
         },
         { immediate: true }
       );
       ```

       For `MembershipsTab.vue` (handles `add-membership`):
       ```
       watch(
         () => props.pendingAction,
         (action) => {
           if (action === 'add-membership') {
             openManage(null);
           }
         },
         { immediate: true }
       );
       ```

    3. Ensure `watch` is in the existing `import { ... } from "vue"` line. ExpensesTab and MembershipsTab already import `watch` (per the existing `watch(showManage, ...)` usage). VaccinationsTab: check the existing vue import; if `watch` is not in it, ADD `watch` to the import.

    LOCKED INVARIANTS (apply to all 3 tabs):
    - `defineAsyncComponent` imports for Manage* dialogs byte-intact (Phase 36 invariant)
    - `showManage` ref and `openManage` function bodies byte-intact (do NOT refactor)
    - Template-level UI byte-intact (the watcher is the ONLY behavioral addition)
    - `requestKey` strings (`'expenses-getFullList'`, `'expense-categories-getFullList'`, `'expense-budgets-getFullList'`, `'vaccinations-getFullList'`, `'memberships-getFullList'`) byte-intact
    - In ExpensesTab: existing `activeSubTab` ref of type `'list' | 'reports'` and sessionStorage persistence (if any) byte-intact
    - The 4-state v-if chain (loading/empty/filtered-empty/data) byte-intact

    Edge case: if the user lands on `/projects/wallecx?action=open-reports` directly (logged in), WallecxApp sets activeTab='expenses' + pendingAction='open-reports', ExpensesTab Suspense resolves, watcher runs with immediate:true, action === 'open-reports', activeSubTab flips to 'reports'. If the user is on a different tab and clicks the Open Reports shortcut: activeTab changes to 'expenses' first (Suspense resolves ExpensesTab on demand because all 3 tabs are async), then pendingAction='open-reports' triggers the watcher in the freshly-mounted ExpensesTab. This is correct per the immediate:true gate.

    Do NOT add any new Pinia store, ref-via-module, provide/inject, or event-bus pattern. Props in / function call out.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>node -e "const fs=require('fs');const files=['src/components/projects/wallecx/ExpensesTab.vue','src/components/projects/wallecx/VaccinationsTab.vue','src/components/projects/wallecx/MembershipsTab.vue'];const errs=[];files.forEach(f=>{const c=fs.readFileSync(f,'utf8');if(!c.includes('defineProps'))errs.push(f+': defineProps missing');if(!c.includes('pendingAction'))errs.push(f+': pendingAction prop missing');if(!c.includes('immediate: true'))errs.push(f+': watcher immediate:true missing (Pitfall 4)');if(!c.includes('=> props.pendingAction'))errs.push(f+': watch source `() => props.pendingAction` missing');if(!c.includes('openManage'))errs.push(f+': openManage function regressed');});const ex=fs.readFileSync(files[0],'utf8');if(!ex.includes(\"action === 'add-expense'\")&&!ex.includes('action === \"add-expense\"'))errs.push('ExpensesTab missing add-expense branch');if(!ex.includes(\"action === 'open-reports'\")&&!ex.includes('action === \"open-reports\"'))errs.push('ExpensesTab missing open-reports branch');if(!ex.includes(\"activeSubTab.value = 'reports'\")&&!ex.includes(\"activeSubTab.value = \\\"reports\\\"\"))errs.push('ExpensesTab activeSubTab=reports dispatch missing');const vac=fs.readFileSync(files[1],'utf8');if(!vac.includes(\"action === 'add-vaccination'\")&&!vac.includes('action === \"add-vaccination\"'))errs.push('VaccinationsTab missing add-vaccination branch');const mem=fs.readFileSync(files[2],'utf8');if(!mem.includes(\"action === 'add-membership'\")&&!mem.includes('action === \"add-membership\"'))errs.push('MembershipsTab missing add-membership branch');files.forEach(f=>{const c=fs.readFileSync(f,'utf8');if(!c.includes('defineAsyncComponent'))errs.push(f+': defineAsyncComponent regressed (Phase 36 invariant)');});if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK');"</automated>
    <automated>npm run test:unit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ExpensesTab.vue, VaccinationsTab.vue, MembershipsTab.vue each contain `defineProps<{ pendingAction?: string | null }>()` (or compatible type-only props syntax)
    - Each contains a `watch(() => props.pendingAction, ..., { immediate: true })` block
    - ExpensesTab watcher handles BOTH `'add-expense'` (calls `openManage(null)`) AND `'open-reports'` (sets `activeSubTab.value = 'reports'`)
    - VaccinationsTab watcher handles `'add-vaccination'` (calls `openManage(null)`)
    - MembershipsTab watcher handles `'add-membership'` (calls `openManage(null)`)
    - Each watcher uses `{ immediate: true }` (Pitfall 4)
    - Existing `defineAsyncComponent` Manage imports and `openManage` function definitions byte-intact in all 3 files
    - Existing requestKey strings (`'expenses-getFullList'`, `'vaccinations-getFullList'`, `'memberships-getFullList'`, etc.) byte-intact
    - `npm run type-check` exits 0
    - `npm run test:unit` exits 0 (test count >= 60: 59 prior + 1 new guard test from Task 1; tabs do not add new tests)
    - `npm run build` exits 0 with 0 "exceeds" / 0 "Skipping precaching"
  </acceptance_criteria>
  <done>
    Each tab is now a pendingAction consumer; WallecxApp's prop flows in, the tab reacts immediately, and `openManage(null)` / sub-tab dispatch fires for the correct action.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route.query.action -> WallecxApp dispatch | Untrusted user-controlled URL parameter (Tampering risk) |
| pendingAction prop -> child tab watcher | App-internal; trusted |
| router.beforeEach -> to.fullPath including ?action=... | Untrusted URL string preserved through redirect |
| manifest.shortcuts URLs -> browser launcher invocation | Build-time static; trusted at install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Tampering | route.query.action arbitrary string | mitigate | ACTION_TAB_MAP allowlist gates dispatch: `if (typeof action === 'string' && ACTION_TAB_MAP[action])` — unknown keys silently ignored (RESEARCH Security Domain V5 Input Validation) |
| T-37-05-02 | Tampering | route.query.action XSS attempt | mitigate | action string is never rendered in template — only used as an object-key lookup; never passed to v-html or interpolated; auto-escaped by Vue if it were |
| T-37-05-03 | Information Disclosure | router.replace({ query: {} }) clears action from URL | mitigate | Prevents shoulder-surfer / browser-history retention of the action keyword post-dispatch |
| T-37-05-04 | Repudiation | dispatch fires before auth check | accept | Dispatch placed AFTER pb.authStore.isValid auth gate inside onMounted; logged-out users redirected first; action survives via to.fullPath redirect query |
| T-37-05-05 | Spoofing | manifest.shortcuts URLs | accept | Build-time static URLs; signed by code review |
| T-37-05-06 | Denial of Service | Pitfall 4 — pendingAction set before watcher registers | mitigate | nextTick await in WallecxApp + immediate:true on tab watchers catches both racing orders |
| T-37-05-07 | Tampering | SW-update toast user-controlled style | accept | style is static literal `{ paddingBottom: 'env(safe-area-inset-bottom)' }`; vue-sonner sanitizes inline style values |
| T-37-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — all libs (vue, vue-router, vue-sonner, @vueuse/core via useMobileEnv) are existing direct deps |
</threat_model>

<verification>
- `npm run type-check` 0
- `npm run test:unit` >= 60 tests pass (59 prior + 1 new query-preservation guard test)
- `npm run build` 0 exceeds / 0 Skipping precaching
- LOCKED comments `registerType: "prompt"` line 28 and `scope: "/"` line 42 in vite.config.ts byte-intact
- ACTION_TAB_MAP has exactly 4 entries matching the shortcut URLs
- 3 occurrences of `:pending-action="pendingAction"` in WallecxApp.vue template
- 3 tab files each declare defineProps and an immediate watcher
- guard.spec.ts grows from 3 tests to 4 (new: `preserves query string in redirect when not authenticated`)
- SW-update toast options object contains `style: { paddingBottom: 'env(safe-area-inset-bottom)' }`
- WallecxApp.vue eviction copy string `iOS may have cleared local data after 7 days without opening the app. Please sign in again. Tip: pin Wallecx to your home screen to prevent this.` byte-exact
- WallecxApp.vue still contains the generic auth-expired copy for non-iOS path
- No new Pinia store files in `src/stores/` (locked invariant)
- `<PwaInstallBanner />`, `<ConfirmDialog />`, `<OfflineBanner />` (added by Plan 02) all coexist without z-index collision (cross-plan consistency — verify in build manually if needed; no new automated test)
</verification>

<success_criteria>
- 4 manifest shortcuts declared, valid URLs + 96x96 icons
- WallecxApp dispatches deep-link via local pendingAction ref (no Pinia store, no module ref)
- 3 tabs accept the prop and react with immediate watchers — Pitfall 4 mitigated
- SW toast safe-area landed via per-call style option — scoped, does not affect other toasts
- iOS eviction-aware copy lands at the auth-expired toast site with correct gating
- guard.spec.ts proves query-preservation through redirect (D-37-16)
- All LOCKED invariants byte-intact: registerType, scope, viewport-fit, ConfirmDialog singleton, PwaInstallBanner mount, no new Pinia store
- All gates green: type-check, test:unit, build (0 exceeds / 0 Skipping precaching)
</success_criteria>

<output>
Create `.planning/phases/37-pwa-install-standalone-polish/37-05-SUMMARY.md` documenting:
- vite.config.ts shortcuts array shape (4 entries × name/short_name/url/icons)
- guard.spec.ts test count before/after (3 -> 4)
- WallecxApp.vue diff scope (5 zones: imports + setup + watch needRefresh + onMounted eviction-aware + onMounted dispatch + template props)
- ACTION_TAB_MAP mapping confirmation
- 3 tab files diff scope (defineProps + watch immediate:true)
- LOCKED line verifications (registerType "prompt", scope "/", no new Pinia store)
- Eviction copy gating logic confirmation
- Gate results
- Cross-reference: this plan completes the visible PWA-09 + PWA-06 surface and ships NFR-IOS-EVICTION-UX. Phase 37 is now ready for Phase 38 UAT (real iOS + Android device install + shortcut long-press tests).
</output>
</content>
</invoke>