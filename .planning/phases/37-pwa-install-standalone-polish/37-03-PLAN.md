---
phase: 37-pwa-install-standalone-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/wallecx/PwaInstallBanner.vue
autonomous: true
requirements: [PWA-04]
must_haves:
  truths:
    - "PwaInstallBanner.vue contains TWO branches in one component: existing iOS v-if and new Android v-else-if (D-37-02)"
    - "Android branch shows when installPromptEvent !== null && !isStandalone && !isDismissed()"
    - "Android Install button click calls installPromptEvent.prompt() synchronously then awaits userChoice (Pitfall 1)"
    - "After prompt() resolves on either outcome, clearInstallPromptEvent() is called (M-9 single-use)"
    - "Dismissal record uses JSON schema { dismissedAt: ISO8601, platform: 'ios'|'android' } (D-37-06)"
    - "Legacy 'true' value migrates lazily on first read to JSON schema (D-37-07)"
    - "30-day re-show gate: Date.now() - Date.parse(parsed.dismissedAt) >= 30 * 86_400_000"
    - "isStandalone === true ALWAYS suppresses (D-37-08)"
  artifacts:
    - path: "src/components/projects/wallecx/PwaInstallBanner.vue"
      provides: "Two-branch install banner (iOS instructional + Android prompt())"
      contains: "useMobileEnv"
      contains_also: "handleAndroidInstall"
  key_links:
    - from: "PwaInstallBanner.vue"
      to: "useMobileEnv.installPromptEvent singleton"
      via: "import { useMobileEnv, clearInstallPromptEvent } from '@/composables/useMobileEnv'"
      pattern: "clearInstallPromptEvent"
    - from: "PwaInstallBanner.vue Android handler"
      to: "BeforeInstallPromptEvent.prompt()"
      via: "event.prompt() called synchronously before await"
      pattern: "event\\.prompt\\(\\)"
---

<objective>
Extend `src/components/projects/wallecx/PwaInstallBanner.vue` IN PLACE (D-37-05 — mount stays at WallecxApp.vue line 125, banner is Wallecx-scoped because start_url = /projects/wallecx) with the Android branch (PWA-04 / D-37-01..04) consuming the Phase 33 `installPromptEvent` module singleton, and upgrade the dismissal record from the Phase 14 `'true'` string schema to the JSON schema with 30-day gate and lazy migration (D-37-06/07/08, NFR-PWA-BANNER-FREQUENCY).

Purpose: Phase 33 captured the Android `beforeinstallprompt` event at App.vue scope. This plan is the FIRST consumer of `.prompt()`. The iOS instructional branch is preserved verbatim (A-43-5 — extended, not split). The dismissal record migration is fair to legacy iOS dismissers — `'true'` users get a fresh 30-day clock on first read, not an immediate re-show (Pitfall 3) and not permanent suppression.
Output: Single modified component file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md
@.planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md
@.planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md
@.planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md
@src/components/projects/wallecx/PwaInstallBanner.vue
@src/composables/useMobileEnv.ts
@src/App.vue

<interfaces>
<!-- useMobileEnv module singleton (Phase 33 FND-01 / FND-02) -->
<!-- File: src/composables/useMobileEnv.ts -->

export interface BeforeInstallPromptEvent extends Event {
  readonly platforms: string[]
  readonly userChoice: Promise<{ outcome: 'accepted' | 'dismissed'; platform: string }>
  prompt(): Promise<void>
}

export function useMobileEnv(): MobileEnv  // returns { isMobile, isTablet, isStandalone, installPromptEvent, safeAreaInsets }
export function setInstallPromptEvent(event: BeforeInstallPromptEvent | null): void  // used by App.vue, NOT here
export function clearInstallPromptEvent(): void  // CALLED by this plan after prompt() resolves

<!-- Current PwaInstallBanner.vue (76 lines) preserves: -->
- BANNER_DISMISSED_KEY = 'wallecx_pwa_banner_dismissed'  (unchanged key per D-37-06)
- isIosSafari() local helper lines 8-11 (UNCHANGED)
- isInStandaloneMode() local helper lines 13-19 (UNCHANGED — keep alongside useMobileEnv.isStandalone for backward-compat per Pitfall 2)
- iOS branch template div (existing v-if isVisible) — copy structure as new v-if isIosVisible
- Bottom-fixed safe-area pattern: `paddingBottom: 'calc(env(safe-area-inset-bottom) + 0.75rem)'`
- z-50, role="complementary", aria-label="Install Wallecx"

<!-- App.vue beforeinstallprompt capture (DO NOT modify here) -->
// App.vue lines 14-37: window.addEventListener("beforeinstallprompt", handleBeforeInstallPrompt)
// handleBeforeInstallPrompt: event.preventDefault() + setInstallPromptEvent(event)
// handleAppInstalled: clearInstallPromptEvent()
// → installPromptEvent singleton is reactive; PwaInstallBanner reads it via useMobileEnv()

<!-- UI-SPEC copy contract -->
- iOS body (UNCHANGED): "Tap **Share** then **Add to Home Screen** to install Wallecx"
- Android body (LOCKED D-37-03): "Install Wallecx for faster access and home-screen shortcuts."
- Android button label (LOCKED, UI-SPEC §Copywriting Contract): "Install"
- Android button aria-label: "Install Wallecx"
- Dismiss aria-label (BOTH branches): "Dismiss install banner"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PwaInstallBanner.vue — Android branch + dismissal JSON schema + lazy migration</name>
  <files>src/components/projects/wallecx/PwaInstallBanner.vue</files>
  <read_first>
    - src/components/projects/wallecx/PwaInstallBanner.vue (current 76-line implementation — full file)
    - src/composables/useMobileEnv.ts (BeforeInstallPromptEvent interface, useMobileEnv() shape, clearInstallPromptEvent export)
    - src/App.vue (Phase 33 capture pattern — context only; do NOT modify)
    - .planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md §"PwaInstallBanner.vue (component, event-driven) — EXTEND" — contains the complete dismissal schema upgrade, handleAndroidInstall, and template two-branch layout
    - .planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md D-37-01 through D-37-08
    - .planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md §"Interaction Contracts — Install Banner" and §"Color Details by Surface — PwaInstallBanner.vue Android branch"
    - .planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md §"Pitfall 1: prompt() Called Outside User-Gesture Context" and §"Pitfall 3: Legacy Dismissal Record Always Triggers Re-show"
  </read_first>
  <behavior>
    - Test 1: When `installPromptEvent.value !== null && !isStandalone.value && !isDismissed()`, the Android branch div renders.
    - Test 2: When `isIosSafari() === true && installPromptEvent.value === null && !isStandalone.value && !isDismissed()`, the iOS branch div renders. The Android branch does NOT render simultaneously.
    - Test 3: When `isStandalone.value === true`, NEITHER branch renders regardless of dismissal record (D-37-08 hard gate).
    - Test 4: `isDismissed()` reads `wallecx_pwa_banner_dismissed`. If raw === `'true'` (legacy), it writes `{ dismissedAt: <now ISO>, platform: detected }` and returns true (D-37-07).
    - Test 5: `isDismissed()` returns true when `Date.now() - Date.parse(record.dismissedAt) < 30 * 86_400_000`, false otherwise.
    - Test 6: Clicking the Android "Install" button calls `event.prompt()` SYNCHRONOUSLY (before any `await`), then awaits `event.userChoice`. On outcome `dismissed`, writes the JSON dismissal record with `platform: 'android'`. On either outcome, calls `clearInstallPromptEvent()`.
    - Test 7: After dismiss button click on either branch, the JSON dismissal record is written with the correct platform.
  </behavior>
  <action>
    Rewrite `src/components/projects/wallecx/PwaInstallBanner.vue` in place. PRESERVE: `<script setup lang="ts">`, `BANNER_DISMISSED_KEY = 'wallecx_pwa_banner_dismissed'` (D-37-06 key unchanged), `isIosSafari()` helper (lines 8-11 verbatim), `isInStandaloneMode()` helper (lines 13-19 verbatim — kept alongside `useMobileEnv.isStandalone` so the in-file logic stays defensive per Pitfall 2). REPLACE the existing single `isVisible` ref with two refs (`isIosVisible` + Android visibility derived reactively from `installPromptEvent`).

    SCRIPT additions:

    1. Import `useMobileEnv` and `clearInstallPromptEvent` from `@/composables/useMobileEnv` at the top imports block. Destructure `const { installPromptEvent, isStandalone } = useMobileEnv()` at module scope of the setup function. These are module-singleton refs — reactive across all consumers.

    2. Add `interface DismissalRecord { dismissedAt: string; platform: 'ios' | 'android' }` and `const THIRTY_DAYS_MS = 30 * 24 * 60 * 60 * 1000`.

    3. Replace the existing localStorage `dismissed === 'true'` check with two functions:
       - `function isDismissed(): boolean` — try/catch wrapped. Reads `BANNER_DISMISSED_KEY`. If raw is `null`/`undefined`/empty -> return false. If raw === `'true'` (Phase 14 legacy schema), construct migrated record `{ dismissedAt: new Date().toISOString(), platform: isIosSafari() ? 'ios' : 'android' }`, write it via `localStorage.setItem(BANNER_DISMISSED_KEY, JSON.stringify(migrated))`, and return `true` (the 30-day clock starts NOW for legacy dismissers per D-37-07 / Pitfall 3). Otherwise JSON.parse(raw) as `DismissalRecord` and return `Date.now() - Date.parse(parsed.dismissedAt) < THIRTY_DAYS_MS`. On any thrown error (JSON parse failure, localStorage unavailable in private browsing), return `true` to silently suppress (D-37-08).
       - `function writeDismissalRecord(platform: 'ios' | 'android'): void` — try/catch wrapped. Constructs `{ dismissedAt: new Date().toISOString(), platform }` and writes it as JSON. Degrades silently on failure.

    4. Add `async function handleAndroidInstall(): Promise<void>`. Read `installPromptEvent.value`. If null, return. Call `event.prompt()` SYNCHRONOUSLY (no `await` before this call — Pitfall 1 / Chrome user-gesture). Then `const { outcome } = await event.userChoice`. If outcome === `'dismissed'`, call `writeDismissalRecord('android')`. In ALL paths after `prompt()` resolves, call `clearInstallPromptEvent()` to honor M-9 single-use and hide the banner this session (D-37-04).

    5. Add `function dismissIos(): void` and `function dismissAndroid(): void`. Each writes its platform's dismissal record then hides its branch:
       - `dismissIos`: `writeDismissalRecord('ios')`; `isIosVisible.value = false`.
       - `dismissAndroid`: `writeDismissalRecord('android')`; `clearInstallPromptEvent()` (clears the singleton so the v-else-if branch self-extinguishes).
       Replace the existing `dismiss()` function in the iOS branch with `dismissIos` (D-37-04: dismiss writes the new JSON schema, not the legacy `'true'`).

    6. In `onMounted`, replace lines 21-31 with: if `isStandalone.value === true` -> return (D-37-08 hard gate). If `isDismissed()` -> return. If `isIosSafari()` -> `isIosVisible.value = true`. The Android branch visibility flows from `installPromptEvent.value !== null` REACTIVELY in the template (no separate flag) — App.vue's listener has already captured the event by the time WallecxApp mounts the banner, but the template guard handles the race where the event arrives after mount.

    TEMPLATE rewrite (entire `<template>` block):

    - Keep `<Teleport to="body">` wrapper.
    - First child div: existing iOS branch with `v-if="isIosVisible"`. Use the EXACT same styling (`#002244` bg, `paddingBottom: calc(env(safe-area-inset-bottom) + 0.75rem)`, z-50, role="complementary", aria-label="Install Wallecx"). Body text UNCHANGED: `Tap <strong>Share</strong> then <strong>Add to Home Screen</strong> to install Wallecx`. Dismiss button calls `dismissIos`. mdi:share-variant icon with `color: #e89820` preserved.
    - Second child div: NEW Android branch with `v-else-if="installPromptEvent && !isStandalone"`. Same `#002244` bg, same `paddingBottom: calc(env(safe-area-inset-bottom) + 0.75rem)`, z-50, role="complementary", aria-label="Install Wallecx". Layout differs:
      - `<span class="flex-1 text-sm" style="color: #ffffff;">Install Wallecx for faster access and home-screen shortcuts.</span>` (D-37-03 EXACT copy, includes period)
      - `<button>` with `min-w-[44px] min-h-[44px]` (LT-01 touch target), inline `style="color: #ffffff; background: none; border: none; cursor: pointer;"`, padding via Tailwind `px-3`, aria-label="Install Wallecx", `@click="handleAndroidInstall"`. Text content: `Install` (one word, UI-SPEC §Copywriting Contract).
      - Dismiss button mirrors the iOS one structurally: `min-w-[44px] min-h-[44px]`, inline `style="color: rgba(255,255,255,0.7); background: none; border: none; cursor: pointer;"`, aria-label="Dismiss install banner", `@click="dismissAndroid"`, contains `<iconify-icon icon="mdi:close" width="20" height="20" aria-hidden="true"></iconify-icon>`.

    LOCKED INVARIANTS:
    - `BANNER_DISMISSED_KEY` value `'wallecx_pwa_banner_dismissed'` byte-intact (D-37-06 — key unchanged).
    - `isIosSafari()` body byte-intact.
    - `isInStandaloneMode()` body byte-intact (NOT removed when adding useMobileEnv.isStandalone — Pitfall 2 belt-and-suspenders).
    - iOS branch user-visible copy `Tap <strong>Share</strong> then <strong>Add to Home Screen</strong> to install Wallecx` byte-intact.
    - z-50, role="complementary", aria-label="Install Wallecx" on both branches.
    - `<Teleport to="body">` wraps everything (PATTERNS Shared Patterns).
    - NO new import of any package not already in package.json. `@/composables/useMobileEnv` is the only new import.
  </action>
  <verify>
    <automated>npm run type-check</automated>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('src/components/projects/wallecx/PwaInstallBanner.vue','utf8');const errs=[];if(!c.includes(\"from '@/composables/useMobileEnv'\")&&!c.includes('from \"@/composables/useMobileEnv\"'))errs.push('useMobileEnv import missing');if(!c.includes('clearInstallPromptEvent'))errs.push('clearInstallPromptEvent missing');if(!c.includes('installPromptEvent'))errs.push('installPromptEvent destructure missing');if(!c.includes('isStandalone'))errs.push('isStandalone destructure missing');if(!c.includes('handleAndroidInstall'))errs.push('handleAndroidInstall handler missing');if(!c.includes('event.prompt()'))errs.push('event.prompt() call missing');if(!c.includes('event.userChoice')&&!c.includes('userChoice'))errs.push('userChoice await missing');if(!c.includes('DismissalRecord'))errs.push('DismissalRecord interface missing');if(!c.includes('dismissedAt'))errs.push('dismissedAt field missing');if(!c.includes(\"platform: 'ios'\")&&!c.includes('platform: \"ios\"'))errs.push('platform ios literal missing');if(!c.includes(\"platform: 'android'\")&&!c.includes('platform: \"android\"'))errs.push('platform android literal missing');if(!c.includes('THIRTY_DAYS_MS')&&!c.includes('30 * 24 * 60 * 60 * 1000')&&!c.includes('30 * 86_400_000'))errs.push('30-day threshold constant missing');if(!c.includes(\"raw === 'true'\")&&!c.includes('raw === \"true\"'))errs.push('legacy true migration branch missing');if(!c.includes('writeDismissalRecord'))errs.push('writeDismissalRecord helper missing');if(!c.includes('wallecx_pwa_banner_dismissed'))errs.push('BANNER_DISMISSED_KEY changed (D-37-06 violation)');if(!c.includes('isIosSafari'))errs.push('isIosSafari helper missing');if(!c.includes('isInStandaloneMode'))errs.push('isInStandaloneMode helper missing (defensive helper kept per Pitfall 2)');if(!c.includes('Tap <strong>Share</strong> then <strong>Add to Home Screen</strong> to install Wallecx'))errs.push('iOS copy regressed');if(!c.includes('Install Wallecx for faster access and home-screen shortcuts.'))errs.push('Android body copy missing/wrong');if(!c.match(/>\\s*Install\\s*<\\/button>/))errs.push('Android Install button label missing');if(!c.includes('v-else-if'))errs.push('v-else-if Android branch missing');if(!c.includes('@click=\"handleAndroidInstall\"'))errs.push('Android Install click handler not wired');if(!c.includes('mdi:close'))errs.push('mdi:close dismiss icon regressed');if(c.match(/\\.prompt\\(\\).*\\.prompt\\(\\)/s))errs.push('prompt() called twice — M-9 violation');if(c.match(/await[^;]+event\\.prompt\\(\\)/))errs.push('event.prompt() called after await — Pitfall 1 violation');if(errs.length){console.error(errs.join('\\n'));process.exit(1)}console.log('OK');"</automated>
    <automated>npm run test:unit</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Component imports `useMobileEnv` and `clearInstallPromptEvent` from `@/composables/useMobileEnv`
    - Component destructures `{ installPromptEvent, isStandalone } = useMobileEnv()`
    - `BANNER_DISMISSED_KEY` value is exactly `'wallecx_pwa_banner_dismissed'` (D-37-06 key unchanged)
    - File contains `interface DismissalRecord` with `dismissedAt: string` and `platform: 'ios' | 'android'` fields
    - File contains a 30-day threshold constant (any of: `THIRTY_DAYS_MS`, `30 * 24 * 60 * 60 * 1000`, `30 * 86_400_000`)
    - `isDismissed()` function exists with the legacy `raw === 'true'` migration branch and returns true after writing migrated record (D-37-07)
    - `writeDismissalRecord(platform: 'ios' | 'android')` helper exists
    - `handleAndroidInstall` is an async function that calls `event.prompt()` BEFORE any `await` (Pitfall 1) and calls `clearInstallPromptEvent()` after `userChoice` resolves
    - `event.prompt()` is called at most once per click handler invocation (M-9)
    - Template has two branches: `v-if="isIosVisible"` (iOS) and `v-else-if="installPromptEvent && !isStandalone"` (Android)
    - iOS body copy `Tap <strong>Share</strong> then <strong>Add to Home Screen</strong> to install Wallecx` byte-intact
    - Android body copy `Install Wallecx for faster access and home-screen shortcuts.` exact (with period)
    - Android Install button label is `Install` (one word)
    - Both branches use `#002244` background and `paddingBottom: 'calc(env(safe-area-inset-bottom) + 0.75rem)'`
    - Both branches use z-50, role="complementary", aria-label="Install Wallecx"
    - Both branches use 44x44 min touch targets on icon buttons
    - `isIosSafari` and `isInStandaloneMode` helper functions still present (Pitfall 2 belt-and-suspenders)
    - `npm run type-check` exits 0
    - `npm run test:unit` exits 0 (59/59 floor)
    - `npm run build` exits 0 with 0 "exceeds"/"Skipping precaching" lines
  </acceptance_criteria>
  <done>
    PwaInstallBanner.vue now has two branches in one component (A-43-5 honored), the dismissal schema migrates lazily for legacy users, and the Android Install button is wired to the Phase 33 module singleton via the single-use prompt() pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser BeforeInstallPromptEvent -> Vue handler | Browser-emitted event; trusted source |
| localStorage `wallecx_pwa_banner_dismissed` -> banner suppression logic | Attacker with localStorage write access can suppress or restore banner (no PII) |
| useMobileEnv.installPromptEvent module singleton -> .prompt() call | Singleton write happens in App.vue (Phase 33 substrate); read here is trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03-01 | Tampering | localStorage `wallecx_pwa_banner_dismissed` | accept | Worst case: attacker suppresses or unsuppresses install banner. No PII, no auth state. Same trust boundary as Phase 14 |
| T-37-03-02 | Tampering | JSON.parse of dismissal record | mitigate | try/catch wraps JSON.parse; on parse failure isDismissed() returns true (silent suppression — fails closed) |
| T-37-03-03 | Repudiation | dismissedAt timestamp | accept | Client-side timestamp; not auth-bearing. Worst case: clock skew shifts the re-show window by hours |
| T-37-03-04 | Information Disclosure | UA detection in isIosSafari() | accept | Client-side UA read; navigator.userAgent is already accessible to any script on the page |
| T-37-03-05 | Denial of Service | event.prompt() rejected by Chrome user-gesture rule | mitigate | Pitfall 1 explicit gate: prompt() called synchronously in @click handler before any await |
| T-37-03-06 | Elevation of Privilege | manifest install path | accept | Browser-controlled install dialog; our code cannot bypass user choice |
| T-37-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — `@vueuse/core` already direct dep (FND-01) |
</threat_model>

<verification>
- `npm run type-check` 0
- `npm run test:unit` 59/59
- `npm run build` 0 exceeds / 0 Skipping precaching
- grep `\.prompt\(\)` returns exactly 1 call site inside `handleAndroidInstall` (M-9)
- grep `'true'` in PwaInstallBanner.vue returns the legacy migration branch only (not setItem('...', 'true'))
- iOS branch user-visible string `Tap <strong>Share</strong> then <strong>Add to Home Screen</strong> to install Wallecx` byte-intact
- Android branch user-visible string `Install Wallecx for faster access and home-screen shortcuts.` exact
- BANNER_DISMISSED_KEY value `'wallecx_pwa_banner_dismissed'` byte-intact (D-37-06)
</verification>

<success_criteria>
- Single-file modification, no new files, no new packages
- Both branches coexist in one component (A-43-5 / D-37-02)
- Dismissal schema migrates legacy 'true' lazily, fairly (D-37-07 / Pitfall 3)
- prompt() called once per click, synchronously, with cleanup (Pitfall 1 / M-9 / D-37-04)
- Standalone always suppresses (D-37-08)
- All gates green
</success_criteria>

<output>
Create `.planning/phases/37-pwa-install-standalone-polish/37-03-SUMMARY.md` documenting:
- Final PwaInstallBanner.vue line count
- handleAndroidInstall code shape (synchronous prompt + await userChoice)
- DismissalRecord interface
- Migration branch path for legacy 'true' values
- Gate results
- Note that this plan does NOT modify App.vue or useMobileEnv.ts (substrate untouched)
</output>
</content>
</invoke>