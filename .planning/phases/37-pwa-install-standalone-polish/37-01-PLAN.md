---
phase: 37-pwa-install-standalone-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pwa-assets.config.ts
  - package.json
  - public/splash/apple-splash-portrait-1179x2556.png
  - public/splash/apple-splash-portrait-1080x2340.png
  - public/splash/apple-splash-portrait-1536x2048.png
  - public/shortcuts/shortcut-add-expense.png
  - public/shortcuts/shortcut-add-vaccination.png
  - public/shortcuts/shortcut-add-membership.png
  - public/shortcuts/shortcut-open-reports.png
autonomous: false
requirements: [PWA-02, PWA-09]
must_haves:
  truths:
    - "Three iOS splash PNGs exist on disk for the 3 v4.3 test viewports (390x844, 360x780, 768x1024)"
    - "Four 96x96 shortcut PNGs exist on disk for the 4 manifest shortcuts"
    - "pwa-assets.config.ts at project root drives @vite-pwa/assets-generator with navy #002244 background"
    - "An npm script regenerates the PWA assets on demand"
  artifacts:
    - path: "pwa-assets.config.ts"
      provides: "Asset generator config (preset 2023 + Apple splash combine + #002244 bg)"
      contains: "combinePresetAndAppleSplashScreens"
    - path: "public/splash/apple-splash-portrait-1179x2556.png"
      provides: "iPhone 14 Pro 390x844@3x splash"
    - path: "public/splash/apple-splash-portrait-1080x2340.png"
      provides: "Android-class 360x780@3x splash"
    - path: "public/splash/apple-splash-portrait-1536x2048.png"
      provides: "iPad 768x1024@2x splash"
    - path: "public/shortcuts/shortcut-add-expense.png"
      provides: "96x96 shortcut icon for Add Expense"
    - path: "public/shortcuts/shortcut-add-vaccination.png"
      provides: "96x96 shortcut icon for Add Vaccination"
    - path: "public/shortcuts/shortcut-add-membership.png"
      provides: "96x96 shortcut icon for Add Membership"
    - path: "public/shortcuts/shortcut-open-reports.png"
      provides: "96x96 shortcut icon for Open Reports"
  key_links:
    - from: "pwa-assets.config.ts"
      to: "public/branding_logo.svg"
      via: "images: ['public/branding_logo.svg']"
      pattern: "branding_logo\\.svg"
    - from: "package.json"
      to: "@vite-pwa/assets-generator CLI"
      via: "generate-pwa-assets npm script"
      pattern: "generate-pwa-assets"
---

<objective>
Generate the iOS splash PNGs (PWA-02 / NFR-IOS-SPLASH) and Android shortcut icons (PWA-09 / D-37-15) consumed by later plans in Phase 37, by creating a `pwa-assets.config.ts` at project root that drives the already-installed `@vite-pwa/assets-generator@^1.0.2` CLI.

Purpose: Provide the static PNG assets that Plan 04 (`index.html` `<link rel="apple-touch-startup-image">`) and Plan 05 (`vite.config.ts` `manifest.shortcuts` array) reference by filename. Without these files on disk, the manifest shortcuts render iconless and the iOS install shows a white-flash instead of a branded splash.
Output: `pwa-assets.config.ts` config file, `public/splash/` directory with 3 splash PNGs, `public/shortcuts/` directory with 4 shortcut PNGs, `generate-pwa-assets` npm script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-pwa-install-standalone-polish/37-CONTEXT.md
@.planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md
@.planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md
@.planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md
@package.json
@vite.config.ts

<interfaces>
<!-- @vite-pwa/assets-generator/config entrypoint exports (verified RESEARCH §Pattern 1) -->
<!-- Source: https://vite-pwa-org.netlify.app/assets-generator/cli -->

Exports from `@vite-pwa/assets-generator/config`:
- defineConfig(config) — typed config helper
- minimal2023Preset — preset matching `headLinkOptions.preset: '2023'`
- combinePresetAndAppleSplashScreens(preset, splashOptions, deviceNames?) — attaches splash to preset

Config shape used here:
{
  headLinkOptions: { preset: '2023' },
  preset: combinePresetAndAppleSplashScreens(minimal2023Preset, { padding: 0.3, resizeOptions: { background: '#002244', fit: 'contain' }, linkMediaOptions: { log: true, basePath: '/' } }),
  images: ['public/branding_logo.svg']
}

Source SVG: public/branding_logo.svg (confirmed exists, RESEARCH §Environment Availability)
Background color: #002244 (matches manifest.background_color in vite.config.ts line 40)

Available CLI invocation:
- `npx @vite-pwa/assets-generator` reads pwa-assets.config.ts at project root automatically.
- The CLI generates assets to the project root next to the source image by default. If --output flag is unavailable, generate then move files post-hoc (RESEARCH §Open Questions Q1).

Required physical PNGs (per UI-SPEC Interaction Contracts §iOS Splash Screens and §Manifest Shortcuts):
- public/splash/apple-splash-portrait-1179x2556.png (iPhone 14 Pro 390x844 @3x)
- public/splash/apple-splash-portrait-1080x2340.png (Android-class 360x780 @3x)
- public/splash/apple-splash-portrait-1536x2048.png (iPad 768x1024 @2x)
- public/shortcuts/shortcut-add-expense.png (96x96)
- public/shortcuts/shortcut-add-vaccination.png (96x96)
- public/shortcuts/shortcut-add-membership.png (96x96)
- public/shortcuts/shortcut-open-reports.png (96x96)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pwa-assets.config.ts and generate-pwa-assets npm script</name>
  <files>pwa-assets.config.ts, package.json</files>
  <read_first>
    - pwa-assets.config.ts (current state — does NOT yet exist; confirm before creating)
    - package.json (current scripts block; `@vite-pwa/assets-generator@^1.0.2` already in devDependencies — confirm)
    - .planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md §"Architecture Patterns — Pattern 1: @vite-pwa/assets-generator Config File"
    - .planning/phases/37-pwa-install-standalone-polish/37-PATTERNS.md §"pwa-assets.config.ts (config, transform) — NEW"
    - .planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md §"iOS Splash Screens (PWA-02)"
    - vite.config.ts (lines 35-79 manifest block — confirm `background_color: "#002244"` and `theme_color: "#002244"` so splash bg matches)
    - public/branding_logo.svg (confirm it exists at this exact path — RESEARCH Environment Availability)
  </read_first>
  <action>
    Create `pwa-assets.config.ts` at the project root. The file uses `defineConfig` from `@vite-pwa/assets-generator/config`, with `headLinkOptions: { preset: '2023' }`, `preset: combinePresetAndAppleSplashScreens(minimal2023Preset, { padding: 0.3, resizeOptions: { background: '#002244', fit: 'contain' }, linkMediaOptions: { log: true, basePath: '/' } })`, and `images: ['public/branding_logo.svg']`. Omit `deviceNames` (third argument of combinePresetAndAppleSplashScreens) so the tool generates splash PNGs for ALL Apple devices — Plan 04 will reference only the 3 needed filenames. Background color `#002244` MUST match `manifest.background_color` and `manifest.theme_color` in vite.config.ts (per UI-SPEC §Color brand navy). Source image is `public/branding_logo.svg` (confirmed exists per RESEARCH §Environment Availability).

    In `package.json`, add a new script `"generate-pwa-assets": "pwa-assets-generator"` directly after the existing `"analyze"` script (preserve existing ordering — Phase 33 added `analyze` after `build-only` per A-43 discipline). Do NOT modify any other script. The script invokes the CLI which reads `pwa-assets.config.ts` automatically.

    Type-check must remain clean (`pwa-assets.config.ts` is a TS file at project root; ensure imports resolve). Do NOT modify `vite.config.ts`, `tsconfig*.json`, or any source file. Do NOT install any new packages — `@vite-pwa/assets-generator@^1.0.2` is already in devDependencies per RESEARCH §Standard Stack.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const cfg=fs.readFileSync('pwa-assets.config.ts','utf8');if(!cfg.includes('combinePresetAndAppleSplashScreens'))process.exit(1);if(!cfg.includes('#002244'))process.exit(2);if(!cfg.includes('public/branding_logo.svg'))process.exit(3);const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));if(!pkg.scripts['generate-pwa-assets'])process.exit(4);console.log('OK');"</automated>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - `pwa-assets.config.ts` exists at project root
    - File contains the literal string `combinePresetAndAppleSplashScreens`
    - File contains the literal string `'#002244'` (splash background — matches manifest.background_color)
    - File contains the literal string `'public/branding_logo.svg'` (source image path)
    - File contains `headLinkOptions: { preset: '2023' }`
    - File contains `padding: 0.3` and `fit: 'contain'`
    - `package.json` scripts object has a new key `generate-pwa-assets` with value `pwa-assets-generator`
    - Existing scripts (`dev`, `build`, `preview`, `test:unit`, `build-only`, `analyze`, `type-check`, `lint*`, `format`) are unchanged
    - `@vite-pwa/assets-generator` is NOT moved from devDependencies to dependencies
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>
    `pwa-assets.config.ts` is committed and ready to drive the CLI. `npm run generate-pwa-assets` invocation resolves (next task runs it).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run asset generator, place outputs in public/splash and public/shortcuts</name>
  <files>public/splash/apple-splash-portrait-1179x2556.png, public/splash/apple-splash-portrait-1080x2340.png, public/splash/apple-splash-portrait-1536x2048.png, public/shortcuts/shortcut-add-expense.png, public/shortcuts/shortcut-add-vaccination.png, public/shortcuts/shortcut-add-membership.png, public/shortcuts/shortcut-open-reports.png</files>
  <read_first>
    - pwa-assets.config.ts (created in Task 1 — confirm it exists)
    - .planning/phases/37-pwa-install-standalone-polish/37-RESEARCH.md §"Pattern 1" Important note and §"Open Questions" Q1, Q3
    - .planning/phases/37-pwa-install-standalone-polish/37-UI-SPEC.md §"iOS Splash Screens (PWA-02)" filename table
    - public/ directory listing (confirm splash/ and shortcuts/ subdirs do NOT yet exist before running)
  </read_first>
  <action>
    Run `npm run generate-pwa-assets`. The CLI writes Apple splash PNGs and icon PNGs to the project root or to `public/` next to the source image (per RESEARCH §Open Question Q1, the exact output path may not be configurable). After generation, identify the 3 splash PNGs whose physical pixel dimensions match the 3 required viewports:
    - 1179x2556 (iPhone 14 Pro 390x844 @3x) -> rename/move to `public/splash/apple-splash-portrait-1179x2556.png`
    - 1080x2340 (Android-class 360x780 @3x) -> rename/move to `public/splash/apple-splash-portrait-1080x2340.png`
    - 1536x2048 (iPad 768x1024 @2x) -> rename/move to `public/splash/apple-splash-portrait-1536x2048.png`

    Create `public/splash/` and `public/shortcuts/` directories first (PowerShell: `New-Item -ItemType Directory -Force public\splash` / `public\shortcuts`).

    For the 4 shortcut icons, the assets-generator's `minimal2023Preset` produces favicon/PWA icon PNGs (e.g. `pwa-64x64.png`, `pwa-192x192.png`, `apple-touch-icon-180x180.png`). It does NOT differentiate per shortcut. Take the existing `public/branding_logo.svg` and produce 4 distinct 96x96 PNG variants — for v4.3 scope, the 4 shortcuts use the SAME branded logo PNG (per UI-SPEC §Manifest Shortcuts: "icon" column points to 4 distinct filenames but the visual content is the same branding logo at 96x96; Android renders them with their own labels). Copy the same 96x96 PNG (derived from the assets-generator output, picking the closest size and rescaling/copying if needed via the CLI's `--preset minimal-2023` 96x96 variant or by selecting the generated `pwa-192x192.png` and producing a 96x96 via image tooling) to all 4 shortcut filenames:
    - `public/shortcuts/shortcut-add-expense.png`
    - `public/shortcuts/shortcut-add-vaccination.png`
    - `public/shortcuts/shortcut-add-membership.png`
    - `public/shortcuts/shortcut-open-reports.png`

    If the generator does not emit a 96x96 variant natively, run an inline `sharp`-based resize via `node -e` (sharp is a transitive dep of @vite-pwa/assets-generator and is available in node_modules). Acceptable alternative: take any generated PWA icon PNG >= 96x96 and use it; Android scales icons.

    After all 7 files are placed, delete the stray PNGs that the CLI emitted to the project root or elsewhere (any `apple-splash-*.png` / `apple-icon-*.png` files NOT in `public/splash/` or `public/shortcuts/` that are NOT the existing `public/apple-touch-icon-180x180.png` / `pwa-*.png` / `maskable-*.png` set listed in `public/`).

    Do NOT overwrite the existing `public/apple-touch-icon-180x180.png`, `public/pwa-192x192.png`, `public/pwa-512x512.png`, `public/maskable-icon-512x512.png`, `public/pwa-64x64.png`, `public/wallecx-icon.svg`, or `public/favicon.ico` — these are referenced by `vite.config.ts` `includeAssets` and `manifest.icons`, and overwriting them risks regressing existing PWA install behavior.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['public/splash/apple-splash-portrait-1179x2556.png','public/splash/apple-splash-portrait-1080x2340.png','public/splash/apple-splash-portrait-1536x2048.png','public/shortcuts/shortcut-add-expense.png','public/shortcuts/shortcut-add-vaccination.png','public/shortcuts/shortcut-add-membership.png','public/shortcuts/shortcut-open-reports.png'];let bad=0;files.forEach(f=>{if(!fs.existsSync(f)){console.error('MISSING:',f);bad++;return}const s=fs.statSync(f);if(s.size<1024){console.error('TOO_SMALL:',f,s.size);bad++}});if(bad)process.exit(bad);console.log('OK',files.length,'files');"</automated>
    <automated>node -e "const fs=require('fs');const orig=['apple-touch-icon-180x180.png','branding_logo.svg','favicon.ico','maskable-icon-512x512.png','pwa-192x192.png','pwa-512x512.png','pwa-64x64.png','wallecx-icon.svg'];let bad=0;orig.forEach(f=>{if(!fs.existsSync('public/'+f)){console.error('REGRESSED MISSING:',f);bad++}});process.exit(bad);"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 splash PNG files exist under `public/splash/` with the exact filenames listed in `<files>` above
    - All 4 shortcut PNG files exist under `public/shortcuts/` with the exact filenames listed
    - Every generated PNG is at least 1 KiB (sanity floor) — empty/zero-byte files fail acceptance
    - Pre-existing public/ assets are NOT modified or deleted: apple-touch-icon-180x180.png, branding_logo.svg, favicon.ico, maskable-icon-512x512.png, pwa-192x192.png, pwa-512x512.png, pwa-64x64.png, wallecx-icon.svg all still exist
    - No stray apple-splash-* or apple-icon-* PNGs are left at the project root or inside public/ root (cleanup of CLI side-output)
    - Splash PNG dimensions match filename suffix: 1179x2556, 1080x2340, 1536x2048 (verified by inspecting PNG file header bytes — first 24 bytes contain IHDR with width and height)
  </acceptance_criteria>
  <done>
    7 PNG assets placed at the exact paths Plan 04 (index.html splash links) and Plan 05 (vite.config.ts manifest.shortcuts icons) will reference by name.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human visual check — splash + shortcut PNGs are branded, not blank</name>
  <files>public/splash/*.png, public/shortcuts/*.png (no source-file edits — visual inspection only)</files>
  <action>HUMAN ACTION ONLY — open the 7 generated PNGs and visually confirm they are branded (see how-to-verify). No code changes in this task.</action>
  <verify>
    <human-check>Each of the 7 PNGs renders Wallecx branding on a solid navy #002244 background, no white-flash, no cropped logo, all under 3 MiB.</human-check>
  </verify>
  <done>Human types "approved"; PNG assets are confirmed ready for Plan 04 (index.html splash links) and Plan 05 (vite.config.ts manifest.shortcuts).</done>
  <what-built>
    7 PNG assets generated from `public/branding_logo.svg` against the navy `#002244` background:
    - 3 splash PNGs (1179x2556, 1080x2340, 1536x2048) under `public/splash/`
    - 4 shortcut icons (96x96 each) under `public/shortcuts/`
  </what-built>
  <how-to-verify>
    1. Open each of the 7 generated PNGs in an image viewer (Windows Photos, VS Code's image preview, or open in browser via file://).
    2. Confirm each splash PNG shows the Wallecx branding logo centered on a SOLID navy `#002244` background (NOT white, NOT transparent, NOT cropped).
    3. Confirm each shortcut PNG (96x96) shows a recognizable branded icon (logo or logo-derived mark) — NOT a blank/empty PNG, NOT a default placeholder.
    4. Confirm none of the splash PNGs exceed 3 MiB (visible in file properties) — Workbox precache cap is 3 MiB per RESEARCH Pitfall 5.
    5. Confirm the navy color in splash PNGs matches the existing app navy (open WallecxApp in dev and compare to the install banner background `#002244`).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 PNGs render correctly with navy bg and branded content. If any PNG is blank/white/oversized, describe the issue and which file is affected; the planner/executor will rerun the generator with adjusted `padding` or `fit` options.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> @vite-pwa/assets-generator CLI | The CLI is a local devDep; runs against `public/branding_logo.svg` and writes PNGs locally |
| static asset -> browser at install time | PNGs are precached by Workbox and served from same-origin; trusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Tampering | `@vite-pwa/assets-generator` package install | accept | Already in devDependencies (RESEARCH §Package Legitimacy Audit slopcheck [OK]); no new install in this plan |
| T-37-01-02 | Information Disclosure | `public/splash/` and `public/shortcuts/` PNGs | accept | PNGs are derived from already-public branding_logo.svg; no PII or secret content |
| T-37-01-03 | Denial of Service | Workbox 3 MiB precache cap | mitigate | Acceptance criterion verifies each splash PNG < 3 MiB; Pitfall 5 covered. Plan 04 follow-up checks `npm run build` log for "Skipping precaching" |
| T-37-01-SC | Tampering | npm/pip/cargo installs | mitigate | NO new package installs in this plan — `@vite-pwa/assets-generator@^1.0.2` already locked in devDependencies (Plan 33-03 ancestor); slopcheck [OK] per RESEARCH |
</threat_model>

<verification>
- `pwa-assets.config.ts` typechecks under `npm run type-check`
- 7 PNG assets exist at the exact paths referenced by Plan 04 and Plan 05
- Original public/ assets (apple-touch-icon, branding_logo, favicon, pwa-*, maskable-*, wallecx-icon) are byte-intact
- Human confirms splash PNGs are branded (not blank) and under 3 MiB
</verification>

<success_criteria>
- pwa-assets.config.ts at project root with the locked config shape (combinePresetAndAppleSplashScreens + #002244 + branding_logo.svg + preset 2023)
- `generate-pwa-assets` npm script added to package.json
- 3 splash PNGs (1179x2556 / 1080x2340 / 1536x2048) under public/splash/
- 4 shortcut PNGs (96x96 each) under public/shortcuts/
- All pre-existing public/ assets unchanged
- Human visual check passes (Task 3 checkpoint)
</success_criteria>

<output>
Create `.planning/phases/37-pwa-install-standalone-polish/37-01-SUMMARY.md` documenting:
- pwa-assets.config.ts shape (preset, splash padding, background hex, source image)
- 7 generated PNG paths and dimensions
- Generator CLI version actually run (`npx @vite-pwa/assets-generator --version`)
- Any side-output files cleaned up during Task 2
- Human checkpoint outcome
- Files Plan 04 / Plan 05 will reference (cross-reference for downstream wave)
</output>
</content>
</invoke>