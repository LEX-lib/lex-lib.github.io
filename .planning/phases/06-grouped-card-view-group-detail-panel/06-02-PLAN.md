---
phase: 06-grouped-card-view-group-detail-panel
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/projects/wallecx/VaccinationGroupPanel.vue
  - src/components/projects/wallecx/WallecxApp.vue
autonomous: true
requirements:
  - GROUP-06
  - GROUP-07

must_haves:
  truths:
    - "User clicks a group card and a Drawer slides in from the right showing all records in that group"
    - "The Drawer header shows the vaccine type name (e.g. COVID-19 or Uncategorized)"
    - "The Drawer DataTable shows 4 columns: Vaccine, Date, Dose, Lot plus a View Record button"
    - "Clicking View Record opens the existing VaccinationDetail.vue Dialog on top of the Drawer — the Drawer stays open"
    - "Closing the Drawer resets selectedGroup; the panel unmounts cleanly via v-if"
    - "Clicking View Record from the Drawer does NOT close the Drawer (D-02)"
  artifacts:
    - path: src/components/projects/wallecx/VaccinationGroupPanel.vue
      provides: "DataTable Drawer content — 4 columns + View Record button emitting view event"
      exports: [default component]
    - path: src/components/projects/wallecx/WallecxApp.vue
      provides: "Drawer element wired with v-model:visible, header, @hide, VaccinationGroupPanel slot"
      contains: "v-model:visible=\"showGroupPanel\""
  key_links:
    - from: src/components/projects/wallecx/WallecxApp.vue (Drawer)
      to: src/components/projects/wallecx/VaccinationGroupPanel.vue
      via: "v-if=selectedGroup slot inside Drawer"
      pattern: "VaccinationGroupPanel"
    - from: src/components/projects/wallecx/VaccinationGroupPanel.vue
      to: src/components/projects/wallecx/WallecxApp.vue (openDetail)
      via: "@view emit wired to openDetail in parent"
      pattern: "@view=\"openDetail\""
    - from: openDetail in WallecxApp.vue
      to: showDetail ref (Dialog)
      via: "showDetail.value = true — Drawer NOT closed (D-02)"
      pattern: "showDetail.value = true"
---

<objective>
Create `VaccinationGroupPanel.vue` (Drawer content DataTable) and wire the Drawer element into `WallecxApp.vue`, completing GROUP-06 (drilldown panel) and GROUP-07 (record row opens VaccinationDetail dialog).

Purpose: The group card grid from Plan 1 is clickable but the Drawer is not yet wired. This plan creates the Drawer content component and adds the Drawer element so clicking a group card opens the Drawer. Clicking View Record in the Drawer opens the existing Dialog on top — the Drawer stays open (D-02 locked).

Output: `VaccinationGroupPanel.vue` (new), `WallecxApp.vue` (modified — Drawer element added after card grid, VaccinationGroupPanel import confirmed).
</objective>

<execution_context>
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/CedrickJhanDeferia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-grouped-card-view-group-detail-panel/06-CONTEXT.md
@.planning/phases/06-grouped-card-view-group-detail-panel/06-RESEARCH.md
@.planning/phases/06-grouped-card-view-group-detail-panel/06-PATTERNS.md
@.planning/phases/06-grouped-card-view-group-detail-panel/06-UI-SPEC.md
@.planning/phases/06-grouped-card-view-group-detail-panel/06-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Read WallecxApp.vue before editing. -->

From Plan 1 output already in WallecxApp.vue:
```typescript
interface VaccineGroup {
  vaccineType: string;
  records: Vaccinations[];
  latestRecord: Vaccinations;
}
const showGroupPanel = ref(false);
const selectedGroup = ref<VaccineGroup | null>(null);
function openGroupPanel(group: VaccineGroup): void {
  selectedGroup.value = group;
  showGroupPanel.value = true;
}
```

From WallecxApp.vue — existing openDetail (must NOT be changed — D-02):
```typescript
async function openDetail(record: Vaccinations): Promise<void> {
  selectedRecord.value = record;
  if (record.card) {
    try {
      fileToken.value = await pb.files.getToken();
    } catch (e: unknown) {
      toast.error("Failed to load attachment. Refresh to try again.");
      console.error("WallecxApp: getToken failed", e);
      selectedRecord.value = null;
      return;
    }
  }
  showDetail.value = true; // ONLY sets showDetail — never touches showGroupPanel
}
```

VaccinationGroupPanel props/emits contract (to create in Task 1):
```typescript
defineProps<{
  records: Vaccinations[];
  listToken: string;
}>()
defineEmits<{
  view: [record: Vaccinations];
}>()
```

Drawer block to insert in WallecxApp.vue template (after the grouped card grid div, before ConfirmDialog):
```html
<Drawer
  v-model:visible="showGroupPanel"
  position="right"
  :header="selectedGroup?.vaccineType ?? ''"
  :style="{ width: '30rem' }"
  :breakpoints="{ '641px': '92vw' }"
  @hide="selectedGroup = null"
>
  <VaccinationGroupPanel
    v-if="selectedGroup"
    :records="selectedGroup.records"
    :list-token="listToken"
    @view="openDetail"
  />
</Drawer>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VaccinationGroupPanel.vue</name>
  <files>src/components/projects/wallecx/VaccinationGroupPanel.vue</files>

  <read_first>
    - src/components/projects/wallecx/VaccinationList.vue — DataTable + Column + Button pattern to replicate (reduced column set); null-coalescing display pattern for dose_number and lot_number
    - src/types/wallecx/vaccinations/types.d.ts — Vaccinations fields referenced in DataTable columns
    - .planning/phases/06-grouped-card-view-group-detail-panel/06-PATTERNS.md — the VaccinationGroupPanel.vue section for the exact template structure
  </read_first>

  <action>
Create `src/components/projects/wallecx/VaccinationGroupPanel.vue` with this exact structure:

script setup lang="ts" block — two imports only (DataTable/Column/Button are PrimeVueResolver auto-imports; no pb import needed — no thumbnail in Drawer columns):

```typescript
import dayjs from "dayjs";
import type { Vaccinations } from "@/types/wallecx/vaccinations/types";
```

Props (no `const props =` alias needed):
```typescript
defineProps<{
  records: Vaccinations[];
  listToken: string;  // included for API consistency; unused in Drawer columns
}>();
```

Emits:
```typescript
const emit = defineEmits<{
  view: [record: Vaccinations];
}>();
```

displayDate helper (identical to VaccinationList.vue):
```typescript
function displayDate(iso: string): string {
  return dayjs(iso).format("DD MMM YYYY");
}
```

Template — single DataTable, striped rows, narrower min-width for Drawer context:

```html
<template>
  <DataTable :value="records" striped-rows table-style="min-width: 24rem">
    <Column field="vaccine_name" header="Vaccine" />
    <Column header="Date" style="width: 8rem">
      <template #body="{ data }">{{ displayDate(data.date_administered) }}</template>
    </Column>
    <Column header="Dose" style="width: 4rem">
      <template #body="{ data }">{{ data.dose_number ?? '—' }}</template>
    </Column>
    <Column header="Lot" style="width: 6rem">
      <template #body="{ data }">{{ data.lot_number || '—' }}</template>
    </Column>
    <Column header="" style="width: 8rem">
      <template #body="{ data }">
        <Button size="small" label="View Record" @click="emit('view', data)" />
      </template>
    </Column>
  </DataTable>
</template>
```

Key differences from VaccinationList.vue — do NOT copy these:
- No loading skeleton block (Drawer only mounts when selectedGroup is set — data is already available)
- No empty-state block (a group card only exists when it has records)
- No thumbnail column
- No Edit or Remove buttons — only View Record (GROUP-06)

CRITICAL: Do NOT add manual imports for DataTable, Column, or Button — PrimeVueResolver auto-imports them. Do NOT use v-html anywhere.
  </action>

  <verify>
    <automated>npm run type-check 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `src/components/projects/wallecx/VaccinationGroupPanel.vue` exists
    - Contains `defineProps` with `records: Vaccinations[]` and `listToken: string`
    - Contains `defineEmits` with `view: [record: Vaccinations]`
    - Contains `displayDate` function using `dayjs(iso).format("DD MMM YYYY")`
    - Contains `DataTable` with `:value="records"` and `striped-rows`
    - Contains exactly 5 Column elements (vaccine_name, Date, Dose, Lot, action)
    - Contains `data.dose_number ?? '—'` for Dose (not || — 0 is a valid dose number)
    - Contains `data.lot_number || '—'` for Lot
    - Contains `Button size="small" label="View Record"` with `@click="emit('view', data)"`
    - Does NOT contain `import { DataTable }` or `import { Column }` or `import { Button }`
    - Does NOT contain `v-html`
    - Does NOT contain a Skeleton element or empty-state div
    - `npm run type-check` exits 0
  </acceptance_criteria>

  <done>VaccinationGroupPanel.vue exists and type-checks clean. It provides a 4-column DataTable (Vaccine, Date, Dose, Lot) plus a View Record button that emits the view event, satisfying GROUP-06 and enabling GROUP-07 wiring in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Drawer element into WallecxApp.vue</name>
  <files>src/components/projects/wallecx/WallecxApp.vue</files>

  <read_first>
    - src/components/projects/wallecx/WallecxApp.vue — FULL FILE read before any edits. Locate: (a) the import block to confirm VaccinationGroupPanel import exists or needs adding; (b) the end of the grouped card grid div (the <!-- Drawer placeholder --> comment from Plan 1, if present); (c) the ConfirmDialog element that the Drawer must be placed before.
    - .planning/phases/06-grouped-card-view-group-detail-panel/06-01-SUMMARY.md — confirm what Plan 1 actually left in the file (the placeholder comment location).
  </read_first>

  <action>
Make two changes to `WallecxApp.vue`. Read the full file first.

**Change 1 — Ensure VaccinationGroupPanel import exists in the import block.**
If Plan 1 deferred the VaccinationGroupPanel import, it will be missing. Add it now alongside VaccinationGroupCard:
```typescript
import VaccinationGroupCard from "./VaccinationGroupCard.vue";
import VaccinationGroupPanel from "./VaccinationGroupPanel.vue";
```
If both lines are already present (Plan 1 added them), skip this change.

**Change 2 — Add the Drawer element in the template.**
Find the location in the template after the grouped card grid closing div (or the Drawer placeholder comment left by Plan 1) and before the `<ConfirmDialog />` element. Insert the following Drawer block there:

```html
<!-- Drawer for group detail panel (GROUP-06, D-01: slides from right) -->
<Drawer
  v-model:visible="showGroupPanel"
  position="right"
  :header="selectedGroup?.vaccineType ?? ''"
  :style="{ width: '30rem' }"
  :breakpoints="{ '641px': '92vw' }"
  @hide="selectedGroup = null"
>
  <VaccinationGroupPanel
    v-if="selectedGroup"
    :records="selectedGroup.records"
    :list-token="listToken"
    @view="openDetail"
  />
</Drawer>
```

The existing `<ConfirmDialog />`, `<ManageVaccination />`, and the `<Dialog> + <VaccinationDetail>` blocks below are left EXACTLY as-is.

CRITICAL (D-02): Do NOT touch `openDetail`. It must NOT reference `showGroupPanel` in any way. Verify the function body does not set `showGroupPanel.value = false` after your edit.

Do NOT add `import { Drawer }` — PrimeVueResolver auto-imports it.
  </action>

  <verify>
    <automated>npm run type-check 2>&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `WallecxApp.vue` template contains `v-model:visible="showGroupPanel"`
    - `WallecxApp.vue` template contains `position="right"`
    - `WallecxApp.vue` template contains `:header="selectedGroup?.vaccineType ?? ''"`
    - `WallecxApp.vue` template contains `:style="{ width: '30rem' }"`
    - `WallecxApp.vue` template contains `:breakpoints="{ '641px': '92vw' }"`
    - `WallecxApp.vue` template contains `@hide="selectedGroup = null"`
    - `WallecxApp.vue` template contains `VaccinationGroupPanel` with `v-if="selectedGroup"`, `:records="selectedGroup.records"`, `:list-token="listToken"`, `@view="openDetail"`
    - `WallecxApp.vue` import block contains `import VaccinationGroupPanel from "./VaccinationGroupPanel.vue"`
    - `openDetail` function body does NOT contain `showGroupPanel` (D-02: Drawer stays open)
    - No `import { Drawer }` in the script block (auto-imported)
    - `npm run type-check` exits 0
  </acceptance_criteria>

  <done>The Drawer is wired in WallecxApp.vue. Clicking a group card opens the Drawer from the right. The Drawer header shows the vaccine type name. VaccinationGroupPanel renders inside with the group's records. Clicking View Record opens VaccinationDetail Dialog on top of the still-open Drawer (D-02). Closing the Drawer resets selectedGroup via @hide.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 1 created VaccinationGroupCard.vue and replaced WallecxApp.vue's VaccinationList with the grouped card grid.
    Plan 2 created VaccinationGroupPanel.vue and wired the Drawer into WallecxApp.vue.
    All four requirements GROUP-04, GROUP-05, GROUP-06, GROUP-07 are now implemented.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to `http://localhost:5173/projects/wallecx` (log in if prompted)
    2. Confirm the flat DataTable is gone — you should see group cards in a 2-column grid (or 1 column on mobile)
    3. Each card shows: vaccine type name (navy, bold), a badge like "2 records", a date "Last: DD MMM YYYY", and either a thumbnail or the mdi:image-off placeholder
    4. If you have records with no vaccine_type, confirm an "Uncategorized" card appears last
    5. Cards should be alphabetically sorted (case-insensitive) — "COVID-19" before "Flu", Uncategorized last
    6. Click any group card — confirm the Drawer slides in from the right with the vaccine type name as the header
    7. Inside the Drawer, confirm columns: Vaccine | Date | Dose | Lot | View Record button
    8. Click "View Record" on any row — confirm the VaccinationDetail Dialog opens on TOP of the Drawer (Drawer stays visible behind the Dialog)
    9. Close the Dialog — confirm you are back in the Drawer (Drawer still open, same group visible)
    10. Close the Drawer (X button or click outside) — confirm it dismisses cleanly
    11. Run `npm run type-check` — confirm it exits 0
    12. Run `git grep "v-html" src/components/projects/wallecx` — confirm zero hits
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PocketBase API → records.value | Established in Phase 1; Phase 6 reads existing refs only — no new fetch |
| records.value → Drawer DataTable | vaccine_name, dates, lot_number, dose_number rendered via template mustache — no v-html path |
| openDetail call from Drawer | Reuses existing openDetail function; no new auth surface |

## STRIDE Threat Register

Phase 6 introduces no new authentication, file upload, or data persistence surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Injection (XSS) | VaccinationGroupPanel.vue — vaccine_name, dates, lot_number rendered in DataTable | mitigate | All cell values rendered via {{ }} mustache interpolation only. No v-html. Acceptance criteria includes grep verification. |
| T-06-05 | Elevation of Privilege | Drawer opens without re-auth | accept | Drawer content is the same per-user records already fetched on mount. PocketBase server rules (BACK-03) enforce ownership; no new elevated access path. |
| T-06-06 | Information Disclosure | selectedGroup.records exposed in Drawer panel | accept | selectedGroup is a client-side ref holding a subset of records.value already rendered on screen. No new data loaded from server. No cross-user data path. |
| T-06-07 | Spoofing | Drawer header shows selectedGroup.vaccineType from user's own record | accept | vaccineType is the user's own data returned by the authenticated PocketBase query. No user-supplied value is unsanitized in the header slot — it renders via PrimeVue's Drawer :header prop (text node, not v-html). |

All prior phase threat mitigations remain in force. No new threat surface introduced.
</threat_model>

<verification>
After both tasks complete and checkpoint passes:
- `src/components/projects/wallecx/VaccinationGroupPanel.vue` exists with 4-column DataTable + View Record button
- `WallecxApp.vue` template contains the Drawer element with `v-model:visible="showGroupPanel"`, `@hide="selectedGroup = null"`, and `VaccinationGroupPanel` inside
- `openDetail` function does NOT set `showGroupPanel.value = false`
- `npm run type-check` exits 0
- `git grep "v-html" src/components/projects/wallecx` returns zero hits
- `git grep "VaccinationList" src/components/projects/wallecx/WallecxApp.vue` returns zero tag usages (component removed from template — file kept on disk)
- GROUP-04: card grid with type name, badge, date, thumbnail — VERIFIED visually
- GROUP-05: Uncategorized card pinned last — VERIFIED visually
- GROUP-06: Drawer panel with 4 columns — VERIFIED visually
- GROUP-07: View Record opens VaccinationDetail Dialog on top of Drawer — VERIFIED visually
</verification>

<success_criteria>
- User clicks a group card and the Drawer slides in from the right (GROUP-06, D-01)
- Drawer header shows the vaccine type name exactly as stored (GROUP-06)
- Drawer DataTable shows Vaccine, Date (DD MMM YYYY), Dose, Lot columns and a View Record button per row (GROUP-06)
- Clicking View Record opens VaccinationDetail.vue Dialog on top of the open Drawer — Drawer does not close (GROUP-07, D-02)
- Closing Drawer resets selectedGroup; reopening a different card shows that card's records (Pitfall 2 guard)
- `npm run type-check` exits 0
- `git grep "v-html" src/components/projects/wallecx` returns zero hits
</success_criteria>

<output>
After completion, create `.planning/phases/06-grouped-card-view-group-detail-panel/06-02-SUMMARY.md`
</output>
