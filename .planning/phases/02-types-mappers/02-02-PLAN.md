---
phase: 02-types-mappers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/lextrack/dsu_day_status/types.ts
  - src/types/lextrack/dsu_day_status/constants.ts
autonomous: true
requirements: [TYPES-03]
must_haves:
  truths:
    - "A DsuDayStatus interface exists with the 5-value status union from the live PB schema"
    - "An AddDsuDayStatus type exists for create payloads (no id/created/updated)"
    - "DSU_DAY_STATUS_VALUES is a runtime as-const tuple containing exactly sl, vl, holiday, bl, others"
    - "DSU_DAY_STATUS_LABELS provides full coverage labels for every status value"
    - "npm run type-check passes after the new module is added"
  artifacts:
    - path: "src/types/lextrack/dsu_day_status/types.ts"
      provides: "DsuDayStatus interface, AddDsuDayStatus type, DsuDayStatusValue type re-export"
      contains: "status: DsuDayStatusValue"
    - path: "src/types/lextrack/dsu_day_status/constants.ts"
      provides: "DSU_DAY_STATUS_VALUES, DSU_DAY_STATUS_LABELS, DsuDayStatusValue"
      contains: "as const"
  key_links:
    - from: "src/types/lextrack/dsu_day_status/types.ts"
      to: "src/types/lextrack/dsu_day_status/constants.ts"
      via: "type DsuDayStatusValue imported from constants"
      pattern: "import type \\{ DsuDayStatusValue \\} from"
---

<objective>
Create the new `dsu_day_status` type module: a fresh directory with `types.ts` (interface + Add type) and `constants.ts` (5-value status tuple + label map). The 5 values are the **live** PB shape per Phase 1 plan 03's deviation note (`sl`, `vl`, `holiday`, `bl`, `others`) — NOT the 3-value version originally specified in Phase 1 CONTEXT.md D-11. Per Phase 2 D-01/D-02/D-03, this 5-value union supersedes that earlier narrowness.

Purpose: Phase 5 (Day Status UI) needs `DsuDayStatus` for fetching/displaying records, `AddDsuDayStatus` for the create-or-replace upsert flow, `DSU_DAY_STATUS_VALUES` to render the dropdown, and `DSU_DAY_STATUS_LABELS` to render the human-readable option text. Plan 02-05 (day status mapper) consumes the types from this plan.

Output: Two new files in a new directory `src/types/lextrack/dsu_day_status/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-types-mappers/02-CONTEXT.md
@.planning/phases/01-schema-foundation/01-03-SUMMARY.md
@.planning/pocketbase-schema.md
@.planning/codebase/CONVENTIONS.md
@src/types/lextrack/dsu_meetings/types.d.ts

<interfaces>
<!-- This plan creates new contracts; nothing exists yet at src/types/lextrack/dsu_day_status/.

     Reference shape from existing modules (e.g. dsu_meetings/types.d.ts) — same Omit/Add pattern
     applies here. The directory snake_case `dsu_day_status/` matches the live PB collection
     name (1:1 mirror, per D-18). -->

<!-- Target shape for src/types/lextrack/dsu_day_status/types.ts: -->
```typescript
import type { RecordModel } from 'pocketbase';
import type { DsuDayStatusValue } from '@/types/lextrack/dsu_day_status/constants';

export interface DsuDayStatus extends RecordModel {
    id: string;
    created: string;
    updated: string;
    date: string;        // 'YYYY-MM-DD' — plain text in PB (D-13 of Phase 1)
    status: DsuDayStatusValue;
}

export type AddDsuDayStatus = Omit<DsuDayStatus, 'id' | 'created' | 'updated'>;
```

<!-- Target shape for src/types/lextrack/dsu_day_status/constants.ts: -->
```typescript
export const DSU_DAY_STATUS_VALUES = ['sl', 'vl', 'holiday', 'bl', 'others'] as const;

export type DsuDayStatusValue = (typeof DSU_DAY_STATUS_VALUES)[number];

export const DSU_DAY_STATUS_LABELS: Record<DsuDayStatusValue, string> = {
    sl: 'Sick Leave',
    vl: 'Vacation Leave',
    holiday: 'Holiday',
    bl: 'Birthday Leave',
    others: 'Other',
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dsu_day_status/constants.ts with the 5-value status tuple, type, and label map</name>
  <files>src/types/lextrack/dsu_day_status/constants.ts</files>
  <read_first>
    - .planning/phases/02-types-mappers/02-CONTEXT.md (decisions D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-18, D-19)
    - .planning/phases/01-schema-foundation/01-03-SUMMARY.md §"Status Enum Has 5 Values in Live Schema" (the canonical source for the 5-value list)
    - .planning/pocketbase-schema.md §"Section 3: Create dsu_day_status collection" (PB field type for status)
  </read_first>
  <action>
    First, create the new directory if it doesn't exist:
    ```sh
    mkdir -p src/types/lextrack/dsu_day_status
    ```

    Then create `src/types/lextrack/dsu_day_status/constants.ts` with EXACTLY this content:

    ```typescript
    /**
     * Runtime values + label map for the dsu_day_status.status field.
     * The 5 values match the live PocketBase schema (see 01-03-SUMMARY.md §Deviations).
     * Phase 5 (Day Status UI) consumes both the values tuple and the labels map.
     */
    export const DSU_DAY_STATUS_VALUES = ['sl', 'vl', 'holiday', 'bl', 'others'] as const;

    export type DsuDayStatusValue = (typeof DSU_DAY_STATUS_VALUES)[number];

    export const DSU_DAY_STATUS_LABELS: Record<DsuDayStatusValue, string> = {
        sl: 'Sick Leave',
        vl: 'Vacation Leave',
        holiday: 'Holiday',
        bl: 'Birthday Leave',
        others: 'Other',
    };
    ```

    Strict requirements:
    - **Order of values matters for UI**: `sl, vl, holiday, bl, others` is the order the dropdown will render in Phase 5. Don't sort alphabetically; preserve this order.
    - **Exactly 5 values** (D-01). Adding a 6th or dropping one to 3 (the original D-11) is incorrect — read 01-03-SUMMARY.md if uncertain.
    - **Label semantics (D-02):**
      - `sl` → `'Sick Leave'`
      - `vl` → `'Vacation Leave'`
      - `holiday` → `'Holiday'`
      - `bl` → `'Birthday Leave'` (the user's HR-specific perk; flag-clear in the label per CONTEXT specifics)
      - `others` → `'Other'` (singular, not plural — the value is `others` because that's what the user typed in PB; the label is `'Other'` because it's a single-item-at-a-time selection)
    - Named exports only (D-19); no default exports.
    - Naming convention from D-06: `DSU_DAY_STATUS_VALUES` and `DSU_DAY_STATUS_LABELS` (no `DSU_DAY_STATUS_STATUS_*` repetition; the field name is implicit because `status` is the only enum-shaped field on this entity).
  </action>
  <verify>
    <automated>
      test -d src/types/lextrack/dsu_day_status \
        && test -f src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "export const DSU_DAY_STATUS_VALUES = \['sl', 'vl', 'holiday', 'bl', 'others'\] as const" src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "export type DsuDayStatusValue = (typeof DSU_DAY_STATUS_VALUES)\[number\]" src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "export const DSU_DAY_STATUS_LABELS: Record<DsuDayStatusValue, string>" src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "sl: 'Sick Leave'" src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "vl: 'Vacation Leave'" src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "holiday: 'Holiday'" src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "bl: 'Birthday Leave'" src/types/lextrack/dsu_day_status/constants.ts \
        && grep -q "others: 'Other'" src/types/lextrack/dsu_day_status/constants.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/types/lextrack/dsu_day_status/` exists
    - File `constants.ts` exists in that directory
    - `DSU_DAY_STATUS_VALUES` is the exact 5-element tuple in the order `sl, vl, holiday, bl, others` with `as const`
    - `DsuDayStatusValue` is derived from `(typeof DSU_DAY_STATUS_VALUES)[number]`
    - `DSU_DAY_STATUS_LABELS` is typed `Record<DsuDayStatusValue, string>` with all 5 keys present
    - All five label values match exactly: `'Sick Leave'`, `'Vacation Leave'`, `'Holiday'`, `'Birthday Leave'`, `'Other'`
    - No default export
  </acceptance_criteria>
  <done>The constants module is in place; `DsuDayStatusValue` type and the values/labels exports are importable from `@/types/lextrack/dsu_day_status/constants`.</done>
</task>

<task type="auto">
  <name>Task 2: Create dsu_day_status/types.ts with DsuDayStatus interface and AddDsuDayStatus type</name>
  <files>src/types/lextrack/dsu_day_status/types.ts</files>
  <read_first>
    - .planning/phases/02-types-mappers/02-CONTEXT.md (decisions D-04, D-16, D-17, D-18, D-19)
    - src/types/lextrack/dsu_day_status/constants.ts (just-created — DsuDayStatusValue is imported from here)
    - src/types/lextrack/dsu_tasks/types.d.ts (reference: this is the file that already imports `RecordModel` from `'pocketbase'` correctly — copy its import pattern)
    - .planning/pocketbase-schema.md §"Section 3" (PB field shape: `date` text required, `status` select required)
  </read_first>
  <action>
    Create `src/types/lextrack/dsu_day_status/types.ts` with EXACTLY this content:

    ```typescript
    import type { RecordModel } from 'pocketbase';
    import type { DsuDayStatusValue } from '@/types/lextrack/dsu_day_status/constants';

    export interface DsuDayStatus extends RecordModel {
        id: string;
        created: string;
        updated: string;
        date: string;
        status: DsuDayStatusValue;
    }

    export type AddDsuDayStatus = Omit<DsuDayStatus, 'id' | 'created' | 'updated'>;
    ```

    Strict requirements:
    - **`.ts` not `.d.ts`** (D-04 / D-05) — even though existing modules use `types.d.ts`, the Phase 2 convention is plain `.ts`. The plain extension lets a file mix types and (potentially future) re-exports without TS treating it as an ambient declaration file.
    - **`date: string`** — plain string, format `'YYYY-MM-DD'`. PB stores it as Text (D-13 of Phase 1), not as a `Date` field.
    - **`status: DsuDayStatusValue`** — required (matches PB `required: true` on the select field per 01-03-SUMMARY.md). No `?`.
    - **Import path (D-16):** `import type { RecordModel } from 'pocketbase'` (not `'pocketbase/dist/pocketbase.es'`).
    - **Omit pattern preserved (D-17):** `Omit<DsuDayStatus, 'id' | 'created' | 'updated'>` — no `Pick`-based refactor.
    - **No `mapToUpdate*` semantics implied here.** Day status uses create-or-replace via the unique-on-date constraint (per Phase 1 D-08); there is no "update existing fields" workflow. The type module here doesn't need to encode that — it just provides the read and create shapes. Plan 02-05 implements the mapper triple minus `mapToUpdate*` (per D-11).
  </action>
  <verify>
    <automated>
      test -f src/types/lextrack/dsu_day_status/types.ts \
        && grep -q "import type { RecordModel } from 'pocketbase';" src/types/lextrack/dsu_day_status/types.ts \
        && ! grep -q "pocketbase/dist/pocketbase.es" src/types/lextrack/dsu_day_status/types.ts \
        && grep -q "import type { DsuDayStatusValue } from '@/types/lextrack/dsu_day_status/constants'" src/types/lextrack/dsu_day_status/types.ts \
        && grep -q "export interface DsuDayStatus extends RecordModel" src/types/lextrack/dsu_day_status/types.ts \
        && grep -q "status: DsuDayStatusValue" src/types/lextrack/dsu_day_status/types.ts \
        && grep -q "date: string" src/types/lextrack/dsu_day_status/types.ts \
        && grep -q "export type AddDsuDayStatus = Omit<DsuDayStatus, 'id' | 'created' | 'updated'>" src/types/lextrack/dsu_day_status/types.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - File `src/types/lextrack/dsu_day_status/types.ts` exists
    - `RecordModel` imported from `'pocketbase'` (no `/dist/`)
    - `DsuDayStatusValue` imported from sibling `constants` file
    - `DsuDayStatus` extends `RecordModel`, has all 5 fields (`id`, `created`, `updated`, `date`, `status`), with `status` required and typed `DsuDayStatusValue`
    - `AddDsuDayStatus` uses the standard `Omit` pattern excluding `id`, `created`, `updated`
    - No default export
  </acceptance_criteria>
  <done>The types module is in place; both `DsuDayStatus` and `AddDsuDayStatus` are importable from `@/types/lextrack/dsu_day_status/types`. Plan 02-05 (day status mapper) is unblocked.</done>
</task>

<task type="auto">
  <name>Task 3: Run type-check and lint to confirm the dsu_day_status module is healthy</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - src/types/lextrack/dsu_day_status/types.ts
    - src/types/lextrack/dsu_day_status/constants.ts
  </read_first>
  <action>
    Run, in order:

    ```sh
    npm run type-check
    npm run lint
    ```

    Both must exit 0. If `lint` auto-fixes formatting in either of the two new files, that's expected — verify with `git status` that no files OUTSIDE `src/types/lextrack/dsu_day_status/` were modified. If unexpected files were touched, STOP and report; pre-existing lint failures are not this plan's scope.

    No files in `src/views/`, `src/components/`, or `src/lib/` should be affected — `dsu_day_status` is a brand-new entity with zero existing consumers, so adding it cannot break anything that already typechecks.
  </action>
  <verify>
    <automated>npm run type-check && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run type-check` exits 0
    - `npm run lint` exits 0
    - No files outside `src/types/lextrack/dsu_day_status/` modified by lint auto-fix
  </acceptance_criteria>
  <done>Both quality gates pass cleanly. The dsu_day_status type module is ready for plan 02-05's mapper work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS compiler ↔ live PB shape | The 5-value `DsuDayStatusValue` union must mirror the PB select field's allowed values; drift here is silent at the type layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | DsuDayStatusValue union vs live PB schema | accept | Phase 1 `npm run verify:schema` confirms the 5 values are present in PB. If a 6th value is added in PB later, the type union must be widened in lockstep — flag for a Phase 6 test. No runtime validation in v1. |
| T-2-02 | Information Disclosure / Tampering | mapFromRecord swallowing malformed PB output (e.g. status=`'banana'`) | accept | Mapper-side; tracked in plan 02-05's threat block. Not in scope here (this plan is types-only). Phase 6 mapper unit tests will cover edge cases. |
</threat_model>

<verification>
- `test -d src/types/lextrack/dsu_day_status`
- `test -f src/types/lextrack/dsu_day_status/types.ts`
- `test -f src/types/lextrack/dsu_day_status/constants.ts`
- `grep -q "export const DSU_DAY_STATUS_VALUES = \['sl', 'vl', 'holiday', 'bl', 'others'\] as const" src/types/lextrack/dsu_day_status/constants.ts`
- `grep -q "export interface DsuDayStatus extends RecordModel" src/types/lextrack/dsu_day_status/types.ts`
- `grep -q "export type AddDsuDayStatus" src/types/lextrack/dsu_day_status/types.ts`
- `npm run type-check` exits 0
- `npm run lint` exits 0
</verification>

<success_criteria>
1. New directory `src/types/lextrack/dsu_day_status/` exists with `types.ts` and `constants.ts` (TYPES-03 acceptance)
2. The status union includes all 5 live values in the order `sl, vl, holiday, bl, others`
3. Labels are present and human-readable for every value
4. The module typechecks and lints cleanly
5. Plan 02-05 (mapper) can import `DsuDayStatus` and `AddDsuDayStatus` without modification
</success_criteria>

<output>
After completion, create `.planning/phases/02-types-mappers/02-02-SUMMARY.md` documenting:
- The new directory and file structure
- The 5-value status tuple and the rationale for using the live (deviated) shape rather than the original 3-value spec
- The label semantics, especially `bl = 'Birthday Leave'` (PH HR-specific) and `others → 'Other'` (singular)
- That this module is consumed by plan 02-05 (day status mapper) and Phase 5 (UI)
</output>
