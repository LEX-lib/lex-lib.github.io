---
phase: 10-tabs-shell-vaccinationstab-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/projects/wallecx/VaccinationsTab.vue
  - src/components/projects/wallecx/MembershipsTab.vue
autonomous: true
requirements:
  - XTAB-01
  - XTAB-02

must_haves:
  truths:
    - "VaccinationsTab.vue exists and contains all vaccination state, computed properties, lifecycle hooks, handlers, and template content from WallecxApp.vue"
    - "VaccinationsTab.vue has no props and no emits — fully self-contained"
    - "VaccinationsTab.vue includes onUnmounted to clear listTokenTimer — no timer leak"
    - "VaccinationsTab.vue imports useConfirm explicitly from primevue/useconfirm but does NOT include a <ConfirmDialog> tag"
    - "MembershipsTab.vue exists as a pure template stub with no script block"
  artifacts:
    - path: "src/components/projects/wallecx/VaccinationsTab.vue"
      provides: "All vaccination logic extracted from WallecxApp.vue"
      exports: []
    - path: "src/components/projects/wallecx/MembershipsTab.vue"
      provides: "Stub empty state for Membership Cards tab"
      exports: []
  key_links:
    - from: "VaccinationsTab.vue"
      to: "primevue/useconfirm"
      via: "explicit import"
      pattern: "import \\{ useConfirm \\} from \"primevue/useconfirm\""
    - from: "VaccinationsTab.vue"
      to: "listTokenTimer"
      via: "onUnmounted clearInterval"
      pattern: "clearInterval\\(listTokenTimer\\)"
---

<objective>
Create VaccinationsTab.vue by extracting all vaccination logic verbatim from WallecxApp.vue, and create MembershipsTab.vue as a no-logic stub.

Purpose: These two new files are the prerequisite for Plan 02, which will gut WallecxApp.vue down to a thin Tabs shell. Creating the destination files first makes the extraction atomic and reversible.
Output: VaccinationsTab.vue (full vaccination feature component) and MembershipsTab.vue (stub).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Source file being extracted from — read before writing VaccinationsTab.vue -->
<!-- File: src/components/projects/wallecx/WallecxApp.vue -->
<!-- Key facts extracted for executor: -->

Script imports to copy verbatim into VaccinationsTab.vue:
```typescript
import { ref, onMounted, onUnmounted, computed, watch } from "vue";
import dayjs from "dayjs";
import { toast } from "vue-sonner";
import { useConfirm } from "primevue/useconfirm";
import { pb } from "@/lib/pocketbase";
import type { Vaccinations } from "@/types/wallecx/vaccinations/types";
import ManageVaccination from "./ManageVaccination.vue";
import VaccinationGroupCard from "./VaccinationGroupCard.vue";
import VaccinationGroupPanel from "./VaccinationGroupPanel.vue";
import WallecxToolbar from './WallecxToolbar.vue';
```

Constants to copy verbatim:
```typescript
const VIEW_MODE_STORAGE_KEY = 'wallecx:view-mode';
const LIST_TOKEN_TTL_MS = 4 * 60 * 1000;
let listTokenTimer: ReturnType<typeof setInterval> | null = null;
```

All 15 refs to copy verbatim: records, isLoading, selectedRecord, showDetail, fileToken, listToken, showManage, manageRecord, confirm (useConfirm()), isExporting, showGroupPanel, selectedGroup, searchQuery, sortMode, viewMode.

VaccineGroup interface + all 3 computed properties (groupedVaccinations, displayedGroups, gridClass) to copy verbatim.

Both lifecycle hooks (onMounted, onUnmounted) and one watcher (watch(viewMode, ...)) to copy verbatim.

All 8 handler functions to copy verbatim: openDetail, openManage, openGroupPanel, exportJson, onCreated, onUpdated, openDelete, deleteRecord.

Template content to copy verbatim (lines 312–448 of WallecxApp.vue, everything INSIDE `<template #content>` EXCEPT the outer `<div class="flex items-center justify-between mb-4">` header row which moves in full, and the `<ConfirmDialog />` tag which STAYS in WallecxApp.vue):
- Header row (Download + Add buttons)
- WallecxToolbar
- Loading skeleton
- Empty state (zero records)
- No-search-results empty state
- Grouped card grid
- Drawer + VaccinationGroupPanel
- ManageVaccination dialog
- Dialog + VaccinationDetail

The <ConfirmDialog /> tag must NOT be in VaccinationsTab.vue — it stays in WallecxApp.vue.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VaccinationsTab.vue — verbatim extraction of all vaccination logic</name>
  <files>src/components/projects/wallecx/VaccinationsTab.vue</files>
  <read_first>
    - src/components/projects/wallecx/WallecxApp.vue — read the ENTIRE file before writing; this is the source of truth for every import, ref, computed, lifecycle hook, handler, and template block to copy
  </read_first>
  <action>
Create `src/components/projects/wallecx/VaccinationsTab.vue` with `<script setup lang="ts">` — no defineProps, no defineEmits.

**Script block** — copy verbatim from WallecxApp.vue:

Imports (all 10):
```typescript
import { ref, onMounted, onUnmounted, computed, watch } from "vue";
import dayjs from "dayjs";
import { toast } from "vue-sonner";
import { useConfirm } from "primevue/useconfirm";
import { pb } from "@/lib/pocketbase";
import type { Vaccinations } from "@/types/wallecx/vaccinations/types";
import ManageVaccination from "./ManageVaccination.vue";
import VaccinationGroupCard from "./VaccinationGroupCard.vue";
import VaccinationGroupPanel from "./VaccinationGroupPanel.vue";
import WallecxToolbar from './WallecxToolbar.vue';
```

Constants (copy verbatim):
```typescript
const VIEW_MODE_STORAGE_KEY = 'wallecx:view-mode';
const LIST_TOKEN_TTL_MS = 4 * 60 * 1000;
let listTokenTimer: ReturnType<typeof setInterval> | null = null;
```

State, interface, computeds, lifecycle hooks, watcher, and handler functions: copy verbatim from WallecxApp.vue lines 14–305.

**Template block** — `<template>` contains the content currently inside WallecxApp.vue's `<Card><template #content>` block, as follows:

```html
<template>
  <div>
    <!-- Header row: Download + Add buttons -->
    <div class="flex items-center justify-between mb-4">
      <div class="flex gap-2">
        <Button ... @click="exportJson" />
        <Button label="Add vaccination" ... @click="openManage(null)" />
      </div>
    </div>

    <!-- WallecxToolbar, loading state, empty states, card grid, Drawer, ManageVaccination, Dialog -->
    <!-- copy all these blocks verbatim from WallecxApp.vue -->
  </div>
</template>
```

CRITICAL rules for the template:
1. The root element of VaccinationsTab.vue's template is a wrapping `<div>` — Vue SFCs require a single root (the Card and `<template #content>` wrapper moves to WallecxApp.vue, not here).
2. Do NOT include `<ConfirmDialog />` — that tag stays in WallecxApp.vue.
3. Copy the header row (Download records button + Add vaccination button) verbatim from WallecxApp.vue lines 312–331.
4. Copy all remaining template blocks verbatim: WallecxToolbar, loading skeleton, zero-records empty state, no-search-results empty state, grouped card grid (VaccinationGroupCard), Drawer + VaccinationGroupPanel, ManageVaccination, and Dialog + VaccinationDetail.
5. Do NOT restructure layout. Do NOT change any class names, Tailwind utilities, or attribute values. Pixel-identical extraction.

End with `<style scoped></style>`.
  </action>
  <verify>
    <automated>cd C:/GitRepos/lex-lib.github.io && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src/components/projects/wallecx/VaccinationsTab.vue` exists
    - File contains `import { useConfirm } from "primevue/useconfirm"`
    - File contains `clearInterval(listTokenTimer)` inside onUnmounted
    - File does NOT contain `<ConfirmDialog`
    - File does NOT contain `defineProps` or `defineEmits`
    - `npm run type-check` passes with no new errors attributable to VaccinationsTab.vue
  </done>
  <acceptance_criteria>
    - `grep -l "useConfirm" src/components/projects/wallecx/VaccinationsTab.vue` returns the file path
    - `grep "from \"primevue/useconfirm\"" src/components/projects/wallecx/VaccinationsTab.vue` returns exactly: `import { useConfirm } from "primevue/useconfirm"`
    - `grep "clearInterval" src/components/projects/wallecx/VaccinationsTab.vue` returns a line containing `clearInterval(listTokenTimer)`
    - `grep "ConfirmDialog" src/components/projects/wallecx/VaccinationsTab.vue` returns no output (zero matches)
    - `grep "defineProps\|defineEmits" src/components/projects/wallecx/VaccinationsTab.vue` returns no output
    - `grep "VIEW_MODE_STORAGE_KEY" src/components/projects/wallecx/VaccinationsTab.vue` returns the constant declaration line
    - `grep "LIST_TOKEN_TTL_MS" src/components/projects/wallecx/VaccinationsTab.vue` returns the constant declaration line
    - `grep "onUnmounted" src/components/projects/wallecx/VaccinationsTab.vue` returns at least one line
    - `grep "exportJson\|onCreated\|onUpdated\|openDelete\|deleteRecord\|openDetail\|openManage\|openGroupPanel" src/components/projects/wallecx/VaccinationsTab.vue` returns 8 distinct function definition lines
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create MembershipsTab.vue — pure template stub</name>
  <files>src/components/projects/wallecx/MembershipsTab.vue</files>
  <read_first>
    - .planning/phases/10-tabs-shell-vaccinationstab-extraction/10-UI-SPEC.md — MembershipsTab.vue Stub Contract section for exact template content
  </read_first>
  <action>
Create `src/components/projects/wallecx/MembershipsTab.vue`.

No `<script setup>` block needed — this is a purely presentational stub.

Template content (exact, per UI-SPEC MembershipsTab.vue Stub Contract):

```html
<template>
  <div class="flex flex-col items-center py-12 gap-3">
    <iconify-icon
      icon="mdi:card-account-details-outline"
      width="48"
      height="48"
      style="color: var(--color-brand-primary)"
      aria-hidden="true"
    ></iconify-icon>
    <p class="text-base font-semibold" style="color: var(--color-typo-heading)">
      Membership Cards
    </p>
    <p class="text-sm text-center max-w-xs" style="color: var(--color-typo-muted)">
      Save your loyalty, insurance, and ID cards here. Coming in the next release.
    </p>
  </div>
</template>

<style scoped></style>
```

Do not add any `<script setup>` block, `ref`, `onMounted`, or PocketBase calls.
  </action>
  <verify>
    <automated>cd C:/GitRepos/lex-lib.github.io && npm run type-check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `src/components/projects/wallecx/MembershipsTab.vue` exists
    - File contains `mdi:card-account-details-outline` icon at width="48" height="48"
    - File contains the stub copy: "Coming in the next release."
    - File does NOT contain any `<script` block
    - `npm run type-check` still passes
  </done>
  <acceptance_criteria>
    - `grep "mdi:card-account-details-outline" src/components/projects/wallecx/MembershipsTab.vue` returns the iconify-icon line
    - `grep "width=\"48\"" src/components/projects/wallecx/MembershipsTab.vue` returns a match
    - `grep "Coming in the next release" src/components/projects/wallecx/MembershipsTab.vue` returns the paragraph line
    - `grep "var(--color-brand-primary)" src/components/projects/wallecx/MembershipsTab.vue` returns the iconify-icon style attribute
    - `grep "var(--color-typo-heading)" src/components/projects/wallecx/MembershipsTab.vue` returns the heading paragraph
    - `grep "var(--color-typo-muted)" src/components/projects/wallecx/MembershipsTab.vue` returns the body paragraph
    - `grep "<script" src/components/projects/wallecx/MembershipsTab.vue` returns no output
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VaccinationsTab → PocketBase | All PocketBase calls authenticated via pb.authStore; per-user rules enforced server-side — unchanged from WallecxApp.vue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | VaccinationsTab.vue — verbatim extraction | accept | Extraction-only refactor; no logic changes; all existing mitigations (parameterised filters, no v-html, server-first delete) transfer unchanged |
| T-10-02 | Information Disclosure | MembershipsTab.vue stub | accept | Pure template, no data access, no PocketBase calls; zero attack surface |
</threat_model>

<verification>
After both tasks complete:

1. Both new files exist:
   - `ls src/components/projects/wallecx/VaccinationsTab.vue` — exists
   - `ls src/components/projects/wallecx/MembershipsTab.vue` — exists

2. VaccinationsTab.vue contains no ConfirmDialog:
   - `grep "ConfirmDialog" src/components/projects/wallecx/VaccinationsTab.vue` — zero matches

3. Timer cleanup present:
   - `grep "clearInterval" src/components/projects/wallecx/VaccinationsTab.vue` — matches `clearInterval(listTokenTimer)`

4. Type check passes:
   - `npm run type-check` — exits 0
</verification>

<success_criteria>
- `src/components/projects/wallecx/VaccinationsTab.vue` created with all 10 imports, 15 refs, VaccineGroup interface, 3 computed properties, onMounted + onUnmounted + watcher, 8 handler functions, and complete template content from WallecxApp.vue
- `src/components/projects/wallecx/MembershipsTab.vue` created as a pure-template stub with exact copy from UI-SPEC
- `npm run type-check` passes with zero new errors
- Neither file contains the other's concerns (VaccinationsTab has no membership content; MembershipsTab has no vaccination logic)
</success_criteria>

<output>
After completion, create `.planning/phases/10-tabs-shell-vaccinationstab-extraction/10-01-SUMMARY.md`
</output>
