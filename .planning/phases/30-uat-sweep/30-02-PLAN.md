---
phase: 30-uat-sweep
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - .planning/phases/11-backend-frontend-foundation/11-HUMAN-UAT.md
  - .planning/phases/30-uat-sweep/30-UAT-REPORT.md
autonomous: false
requirements: [QA-01]
requirements_addressed: [QA-01]
tags: [uat, qa, human-verify, phase-11, pocketbase, memberships]

must_haves:
  truths:
    - "The 1 pending PocketBase Admin UI verification scenario in 11-HUMAN-UAT.md has a non-pending result"
    - "11-HUMAN-UAT.md Summary count for pending equals 0 after this plan"
    - "Phase 11 row appended to 30-UAT-REPORT.md"
    - "npm run test:unit still passes"
  artifacts:
    - path: ".planning/phases/11-backend-frontend-foundation/11-HUMAN-UAT.md"
      provides: "Filled-in pass/fail result for Phase 11's 1 scenario"
      contains: "result: passed"
    - path: ".planning/phases/30-uat-sweep/30-UAT-REPORT.md"
      provides: "Phase 11 row in the master report"
      contains: "Phase 11"
---

<objective>
Verify the `wallecx_memberships` PocketBase collection schema and rules match the locked spec from Phase 11. This is a single Admin-UI visual confirmation — the only Phase 11 UAT scenario.

The collection should expose 10 user-defined fields (user, card_name, issuer, barcode_value, barcode_type, card_number, expiry_date, notes, card_color, card_image) with specific constraints: `barcode_type` is a SELECT field with exactly 5 values (qr, code128, ean13, code39, number); `card_image` is a Protected File with max 10485760 bytes and MIME limited to image/jpeg + image/png + image/webp (no PDF) and thumbs `100x100,400x0`; all 5 per-user API rules use `@request.body.user` (NOT `@request.data.user`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-uat-sweep/30-CONTEXT.md
@.planning/phases/11-backend-frontend-foundation/11-HUMAN-UAT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify wallecx_memberships collection in PocketBase Admin UI</name>
  <read_first>
    - .planning/phases/11-backend-frontend-foundation/11-HUMAN-UAT.md (the scenario, verbatim)
    - .planning/STATE.md §Accumulated Context (PocketBase v0.29.3 rule syntax invariant)
  </read_first>
  <how-to-verify>
    1. Open the PocketBase Admin UI for the live Wallecx instance
    2. Navigate to **Collections → wallecx_memberships**
    3. **Schema tab — confirm 10 user-defined fields exist with expected types:**
       | Field | Type | Notes |
       |-------|------|-------|
       | user | Relation | required, max select 1, users |
       | card_name | Text | required |
       | issuer | Text | optional |
       | barcode_value | Text | optional |
       | barcode_type | **SELECT** | values: `qr, code128, ean13, code39, number` (EXACTLY 5, no more, no less) |
       | card_number | Text | optional |
       | expiry_date | Date | optional |
       | notes | Text/Editor | optional |
       | card_color | Text | hex without `#`, max 6 chars |
       | card_image | **File** | Protected ON, max 10485760 bytes, MIME `image/jpeg`+`image/png`+`image/webp` (no PDF), thumbs `100x100,400x0` |
    4. **API Rules tab — confirm all 5 rules:**
       - List rule: `user = @request.auth.id`
       - View rule: `user = @request.auth.id`
       - Create rule: `@request.auth.id != "" && @request.body.user = @request.auth.id` (uses `@request.body.user`, NOT the deprecated `@request.data.user`)
       - Update rule: `user = @request.auth.id`
       - Delete rule: `user = @request.auth.id`
    5. Record: pass / fail / blocked. If fail, describe which field or rule is wrong.
  </how-to-verify>
  <acceptance_criteria>
    - The single PocketBase schema verification scenario has been walked through
    - Pass / fail / blocked is reported with detail
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what's wrong.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record result in 11-HUMAN-UAT.md and 30-UAT-REPORT.md</name>
  <action>
    Follow the recording protocol from .planning/phases/30-uat-sweep/30-CONTEXT.md §D-05:
    1. Replace `result: [pending]` with the user's outcome in 11-HUMAN-UAT.md
    2. Update Summary counts (pending=0)
    3. Update frontmatter `status:` (approved / partial / failed) and bump `updated:` to today
    4. Append a Phase 11 row to 30-UAT-REPORT.md (create file if it doesn't exist using the schema from 30-01-PLAN.md Task 2)
    5. Commit: `test(30-02): record Phase 11 UAT result`
  </action>
  <verify>
    <automated>grep -c "result: \[pending\]" .planning/phases/11-backend-frontend-foundation/11-HUMAN-UAT.md returns 0</automated>
  </verify>
  <acceptance_criteria>
    - 11-HUMAN-UAT.md has zero pending entries
    - 30-UAT-REPORT.md contains a Phase 11 row
    - `npm run test:unit` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-30-02-01 | Tampering | accept | UAT only verifies existing PocketBase rules; no schema mutations made here |
</threat_model>

<verification>
- 11-HUMAN-UAT.md has zero `result: [pending]` entries
- 30-UAT-REPORT.md contains Phase 11 row
- `npm run test:unit` passes
</verification>

<success_criteria>
- Phase 11's 1 UAT scenario resolved
- Master report updated
</success_criteria>

<output>
Create `.planning/phases/30-uat-sweep/30-02-SUMMARY.md` with pass/fail outcome + note: next plan is 30-03 (Phase 12 — 6 scenarios).
</output>
