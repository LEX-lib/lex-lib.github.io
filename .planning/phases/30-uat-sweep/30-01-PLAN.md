---
phase: 30-uat-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md
  - .planning/phases/30-uat-sweep/30-UAT-REPORT.md
autonomous: false
requirements: [QA-01]
requirements_addressed: [QA-01]
tags: [uat, qa, human-verify, phase-10]

must_haves:
  truths:
    - "Both pending scenarios in 10-HUMAN-UAT.md have a non-pending result (passed / failed / skipped / blocked / deferred)"
    - "10-HUMAN-UAT.md Summary counts reflect the actual outcome (pending=0)"
    - "Any failed scenarios are itemized in 30-UAT-REPORT.md with a proposed fix-plan reference"
    - "npm run test:unit still passes (49+ tests green) after any regression fix"
  artifacts:
    - path: ".planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md"
      provides: "Filled-in pass/fail results for Phase 10's 2 scenarios"
      contains: "result: passed"
    - path: ".planning/phases/30-uat-sweep/30-UAT-REPORT.md"
      provides: "Phase 10 row in the master UAT report (create file if needed)"
      contains: "Phase 10"
---

<objective>
Walk through the 2 pending UAT scenarios in `.planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md`, record pass/fail outcomes in place, and either fix-up any regressions inline (if trivial) or surface them to 30-UAT-REPORT.md for batched fix planning per Phase 30 CONTEXT D-03.

Phase 10 shipped the Wallecx Tabs shell + VaccinationsTab extraction (predating Memberships). The 2 pending scenarios verify:
1. Membership Cards tab stub renders correctly (icon + "Coming in the next release." copy)
2. Vaccinations features still work end-to-end after extraction (data load, search/sort/view toggle, group drawer, CRUD, confirm dialog, toasts)

Scenarios are documented in the originating HUMAN-UAT.md — do NOT re-author them; record results against the existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-uat-sweep/30-CONTEXT.md
@.planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk through Phase 10 UAT scenarios in the running app</name>
  <read_first>
    - .planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md (the 2 scenarios to test, verbatim)
    - .planning/phases/30-uat-sweep/30-CONTEXT.md §Done Criteria (D-07: every scenario must end up with non-pending result)
  </read_first>
  <how-to-verify>
    1. `npm run dev` — start the dev server
    2. Navigate to `/projects/wallecx` in a Chromium browser
    3. **Scenario 1 — Membership Cards Tab Navigation:**
       - Click the "Membership Cards" tab
       - Confirm the stub renders: icon + heading + "Coming in the next release." copy
       - Open DevTools Console — confirm no errors fired during the tab switch
       - Record: pass / fail / blocked
    4. **Scenario 2 — Vaccination Features Regression Check (XTAB-02):**
       - Switch to "Vaccinations" tab
       - Verify: data loads, search filters work, sort modes work, view toggle works
       - Open a vaccination group → confirm group drawer / panel opens
       - Add a vaccination → confirm it persists via PocketBase + toast appears
       - Edit a vaccination → confirm save works + toast
       - Delete a vaccination → confirm ConfirmDialog fires, deletion persists + toast
       - Record: pass / fail / blocked
    5. For each scenario, report the result back to the orchestrator in this format:
       ```
       Scenario 1: passed
       Scenario 2: failed — <one-line description of failure>
       ```
  </how-to-verify>
  <acceptance_criteria>
    - Each of the 2 scenarios has been actively walked through (not skipped without reason)
    - Pass / fail / blocked / skipped is reported per scenario with a one-line note on any failure
  </acceptance_criteria>
  <resume-signal>Type "approved: X/2 passed" or describe failures by scenario number.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record results in 10-HUMAN-UAT.md and 30-UAT-REPORT.md</name>
  <read_first>
    - The user's response to Task 1 (pass/fail per scenario)
    - .planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md (current state)
    - .planning/phases/30-uat-sweep/30-UAT-REPORT.md (create if first plan, else append)
  </read_first>
  <files>
    .planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md
    .planning/phases/30-uat-sweep/30-UAT-REPORT.md
  </files>
  <action>
    1. **Update 10-HUMAN-UAT.md in place:**
       - For each scenario, replace `result: [pending]` with `result: passed` (or `result: failed: <reason>`, `result: skipped: <reason>`, `result: deferred: <reason>` per D-08)
       - Update `## Summary` counts: passed/issues/pending/skipped/blocked
       - Update frontmatter `status:` to `approved` if all passed; `partial` if any failed/deferred; bump `updated:` timestamp to today
       - If any failures, append a row to the `## Gaps` section: `- Scenario N: <fix-plan reference>` (fix plan created in follow-up step)

    2. **Update / create 30-UAT-REPORT.md:**
       - If file does NOT exist yet, create with header:
         ```markdown
         # Phase 30 UAT Sweep — Master Report

         **Generated:** 2026-05-25
         **Scope:** Phases 10, 11, 12, 18, 20, 21, 22, 25 (per ROADMAP §Phase 30)

         ## Summary

         | Plan | Phase | Total | Passed | Failed | Deferred | Status |
         |------|-------|-------|--------|--------|----------|--------|

         ## Per-Scenario Results

         | Plan | Phase | Scenario | Status | Result | Failure Detail | Fix Plan |
         |------|-------|----------|--------|--------|----------------|----------|

         ## Deferred Items

         (none yet)
         ```
       - Append a row to the Summary table for Phase 10
       - Append per-scenario rows to the Per-Scenario Results table
       - If failures, add a "Deferred Items" entry referencing the future fix-plan name (e.g., `30-09-FIX-PLAN.md`)

    3. Commit: `test(30-01): record Phase 10 UAT results`
  </action>
  <verify>
    <automated>grep -c "result: pending" .planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md returns 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "result: \[pending\]" .planning/phases/10-tabs-shell-vaccinationstab-extraction/10-HUMAN-UAT.md` returns 0
    - `grep -c "Phase 10" .planning/phases/30-uat-sweep/30-UAT-REPORT.md` returns at least 1
    - `npm run test:unit` exits 0 (regression floor)
    - Commit landed with conventional message
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UAT findings | Plan 30-01 records results; downstream fix plans address regressions if any |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Repudiation | Pass/fail outcomes recorded only in markdown | accept | Git log preserves history; markdown is the canonical artifact per project convention |
</threat_model>

<verification>
- 10-HUMAN-UAT.md has zero `result: [pending]` entries
- 30-UAT-REPORT.md exists and contains a Phase 10 row
- `npm run test:unit` still passes
- Any failures are logged in 30-UAT-REPORT.md Deferred Items with a fix-plan reference
</verification>

<success_criteria>
- Phase 10's 2 UAT scenarios are resolved (pass/fail/deferred) — no more `pending` entries
- Master UAT report has its first phase entry
- Regression floor (test:unit) intact
</success_criteria>

<output>
Create `.planning/phases/30-uat-sweep/30-01-SUMMARY.md` capturing:
- Pass/fail breakdown for Phase 10 (2 scenarios)
- Any failures + corresponding fix-plan reference
- Confirmation that test:unit still passes
- Note: next plan is 30-02 (Phase 11 — 1 scenario)
</output>
