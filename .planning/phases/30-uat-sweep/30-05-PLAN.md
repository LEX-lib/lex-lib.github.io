---
phase: 30-uat-sweep
plan: 05
type: execute
wave: 5
depends_on: []
files_modified:
  - .planning/phases/20-site-shell-non-app-pages/20-HUMAN-UAT.md
  - .planning/phases/30-uat-sweep/30-UAT-REPORT.md
autonomous: false
requirements: [QA-01]
requirements_addressed: [QA-01]
tags: [uat, qa, human-verify, phase-20, dark-mode, site-shell]

must_haves:
  truths:
    - "All 5 SC sections (Home, Projects, Blog, Login, NavBar) in 20-HUMAN-UAT.md walked through in light + dark + 375px"
    - "Phase 30 UAT Results appendix appended to bottom of 20-HUMAN-UAT.md"
    - "Phase 20 roll-up row in 30-UAT-REPORT.md"
    - "npm run test:unit still passes"
  artifacts:
    - path: ".planning/phases/20-site-shell-non-app-pages/20-HUMAN-UAT.md"
      provides: "Phase 30 UAT Results appendix"
      contains: "Phase 30 UAT Results"
    - path: ".planning/phases/30-uat-sweep/30-UAT-REPORT.md"
      provides: "Phase 20 row"
      contains: "Phase 20"
---

<objective>
Walk through Phase 20's dark-mode UAT covering site-shell (NavBar) + non-app pages (Home, Projects, Blog, Login). Five SC sections corresponding to THEME-04 through THEME-08. Free-form format.

Phase 20 shipped the dark-mode treatment for the marketing/portfolio site. Each SC verifies a specific surface in both themes, both viewports. Failure modes are documented inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-uat-sweep/30-CONTEXT.md
@.planning/phases/20-site-shell-non-app-pages/20-HUMAN-UAT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Walk through Phase 20 site-shell dark-mode UAT</name>
  <read_first>
    - .planning/phases/20-site-shell-non-app-pages/20-HUMAN-UAT.md (5 SC sections + failure-mode catalog)
  </read_first>
  <how-to-verify>
    Setup:
    - `npm run dev` and open `http://localhost:5173/`
    - Toggle theme via NavBar sun/moon button (Phase 19 delivery — should be available)
    - DevTools Device Toolbar: 375px ready
    - For each SC, test in BOTH viewports AND BOTH themes

    1. **SC-1 Home Page (HeroSection + AboutMeSection — THEME-04):**
       - Light mode 375px: hero name visible, body readable, "About Me" CTA legible, navy + amber blobs subtly visible
       - Light mode desktop: same + image-side decoration visible
       - Dark mode 375px: hero name LIGHT (not dark), body readable on dark, CTA legible (amber bg + dark navy text), navy blobs replaced by amber-tinted blobs (NOT invisible)
       - Dark mode desktop: same as above
       - About section: photo visible, "About Me" heading light, blockquote amber accent, social buttons with amber hover
       - Record: pass / fail per row

    2. **SC-2 ProjectsView (THEME-05):**
       - Light 375px: 5 tiles in single column; titles visible, WIP/Active tags clearly colored
       - Light desktop: 2-column grid
       - Dark 375px: tile backgrounds dark; WIP tag chip with LIGHT amber text (NOT dark `#b07010`); Active tag with light green
       - Dark desktop: same
       - "View Project" button: navy + white text legible on both themes
       - Record: pass / fail per row

    3. **SC-3 BlogView (THEME-06):**
       - Light 375px + desktop: branding logo 60% opacity, "Blog coming soon" dark heading, subtitle readable
       - Dark 375px + desktop: logo 80% opacity (via `dark:opacity-80`), heading light, subtitle readable
       - Record: pass / fail

    4. **SC-4 Login (THEME-07):**
       - Light 375px + desktop: glass card centered, h1 black, subtitle visible, email + password float labels work, "Remember me" visible
       - Dark 375px + desktop: glass card readable; h1 WHITE (via `dark:text-white`); subtitle white@80%; decorative gradients subtle; PrimeVue dark tokens for inputs
       - Error states: trigger validation error on empty submit; verify error message visible on both themes
       - Record: pass / fail per row

    5. **SC-5 CustomNavBar (THEME-08):**
       - Light: brand Avatar navy-on-white, menu items readable, moon icon visible, "Log In" button visible
       - Dark: Avatar inverted (light/cream lines on dark navy via `dark:invert`), menu items light, sun icon visible, "Log In" or DiceBear avatar visible
       - Toggle interaction: clicking sun/moon re-themes ALL surfaces with no flash
       - Reload persistence (Phase 19 localStorage contract)
       - Profile dropdown (when logged in): avatar opens dropdown, "Logout" visible on both themes
       - Record: pass / fail per row

    6. Use the Failure-Mode Catalog (in 20-HUMAN-UAT.md §Failure-Mode Catalog) for diagnostic mapping if you spot regressions

    Report per SC:
    ```
    SC-1 Home: passed
    SC-2 Projects: failed — WIP tag dark amber on dark amber (invisible)
    SC-3 Blog: passed
    SC-4 Login: passed
    SC-5 NavBar: passed
    Overall: PARTIAL
    ```
  </how-to-verify>
  <acceptance_criteria>
    - All 5 SC sections walked through
    - Each failure includes which Failure-Mode Catalog row matches (for fix-plan dispatch)
  </acceptance_criteria>
  <resume-signal>Type "approved: pass / partial — list / fail — list".</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Append Phase 30 results to 20-HUMAN-UAT.md and update 30-UAT-REPORT.md</name>
  <action>
    Per CONTEXT D-05 (free-form file protocol):
    1. Append `## Phase 30 UAT Results` section at the bottom of 20-HUMAN-UAT.md with a 5-row table (one per SC), pass/fail per row, notes column for failure mapping to Failure-Mode Catalog rows
    2. Update 30-UAT-REPORT.md Summary table with Phase 20 row; Per-Scenario rows for each of the 5 SCs
    3. Commit: `test(30-05): record Phase 20 site-shell dark-mode UAT results`
  </action>
  <verify>
    <automated>grep -c "## Phase 30 UAT Results" .planning/phases/20-site-shell-non-app-pages/20-HUMAN-UAT.md returns 1</automated>
  </verify>
  <acceptance_criteria>
    - 20-HUMAN-UAT.md has Phase 30 appendix
    - 30-UAT-REPORT.md has 5 Phase 20 SC rows
    - test:unit passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## STRIDE
| Threat ID | Category | Disposition | Mitigation |
|-----------|----------|-------------|------------|
| T-30-05-01 | Information Disclosure | accept | Public marketing site; no auth boundary in scope |
</threat_model>

<verification>
- 20-HUMAN-UAT.md has Phase 30 appendix
- 30-UAT-REPORT.md has Phase 20 entries
- test:unit passes
</verification>

<success_criteria>
- 5 SCs walked through with clear per-section pass/fail
- Failures mapped to Failure-Mode Catalog rows for fix-plan dispatch
</success_criteria>

<output>
Create `30-05-SUMMARY.md` with per-SC breakdown + note: next plan is 30-06 (Phase 21 mini-apps).
</output>
