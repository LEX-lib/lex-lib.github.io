---
phase: 36-mobile-performance
plan: 06
type: execute
wave: 3
depends_on: [36-01]
files_modified:
  - src/components/projects/wallecx/ManageExpense.vue
  - src/components/projects/wallecx/ManageMembership.vue
  - src/components/projects/wallecx/ManageVaccination.vue
autonomous: true
requirements: [PF-07]
tags: [performance, webp, image-compression]

must_haves:
  truths:
    - "ManageExpense.vue, ManageMembership.vue, ManageVaccination.vue all import compressToWebP from '@/lib/wallecx/compressToWebP' and call it in place of the previous inline imageCompression(...) block"
    - "ManageExpense.vue's new File([compressed], file.name, { type: ... }) wrapper passes `{ type: 'image/webp' }` (NOT file.type) — Pitfall 4 mitigation"
    - "ManageMembership.vue and ManageVaccination.vue continue to assign `pendingFile.value = compressed` directly (no File wrapper) — the helper returns a File with type='image/webp' already"
    - "EXIF stripping stays in each call site (canvas re-encode in each onFileSelect) — NOT moved into the helper (D-36-09)"
    - "PDF bypass logic (if file.type === 'application/pdf') stays byte-intact in each file — PDFs never reach compressToWebP"
    - "The direct `import imageCompression from 'browser-image-compression'` is REMOVED from all 3 files (TypeScript will error if the call remains; both line and call are gone together — Anti-Pattern 'Removing import without removing call')"
    - "Existing toast copy ('Location data removed.') and EXIF-error toast paths stay byte-intact"
  artifacts:
    - path: src/components/projects/wallecx/ManageExpense.vue
      provides: "WebP compression via shared helper; new File wrapper updated to image/webp type"
    - path: src/components/projects/wallecx/ManageMembership.vue
      provides: "WebP compression via shared helper; direct compressed assignment preserved"
    - path: src/components/projects/wallecx/ManageVaccination.vue
      provides: "WebP compression via shared helper; direct compressed assignment preserved"
  key_links:
    - from: src/components/projects/wallecx/Manage{Expense,Membership,Vaccination}.vue
      to: src/lib/wallecx/compressToWebP.ts
      via: "import { compressToWebP } from '@/lib/wallecx/compressToWebP'"
---

<objective>
Replace the 3 byte-identical `imageCompression(strippedFile, { maxSizeMB:1.5, maxWidthOrHeight:2048, useWebWorker:true })` blocks in `ManageExpense.vue` / `ManageMembership.vue` / `ManageVaccination.vue` with a single call to the shared `compressToWebP(strippedFile)` helper from Plan 36-01. The helper passes the additional `fileType: 'image/webp'` option (D-36-10). Update `ManageExpense.vue`'s `new File([compressed], file.name, { type: ... })` wrapper to `{ type: 'image/webp' }` (Pitfall 4 — sending wrong MIME to PocketBase would defeat the WebP goal).

Purpose: Centralizes 3 duplicated option blocks into one helper (D-36-09); produces actual WebP-typed File objects for upload (D-36-10); the storage size reduction is recorded post-phase in 36-07-SUMMARY via a sample comparison. EXIF stripping stays in each call site because the 3 implementations differ (createImageBitmap vs Image+objectURL) and unification is out of scope (D-36-09).

Output: 3 modified Manage* files. Parallel-safe (file-disjoint), runs in Wave 3 after the foundation helper exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-mobile-performance/36-CONTEXT.md
@.planning/phases/36-mobile-performance/36-RESEARCH.md
@.planning/phases/36-mobile-performance/36-PATTERNS.md
@src/lib/wallecx/compressToWebP.ts
@src/components/projects/wallecx/ManageExpense.vue
@src/components/projects/wallecx/ManageMembership.vue
@src/components/projects/wallecx/ManageVaccination.vue

<interfaces>
compressToWebP signature (shipped in Plan 36-01):
```typescript
export async function compressToWebP(file: File): Promise<File>
// Returns a File with .type === 'image/webp'
// Preconditions: caller must EXIF-strip first; caller must NOT pass PDFs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ManageExpense.vue — swap imageCompression call + update File wrapper to image/webp</name>
  <files>src/components/projects/wallecx/ManageExpense.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageExpense.vue (current file — line 3 `import imageCompression from 'browser-image-compression'`; the `onFileSelect` flow around lines 191–215 with PDF bypass, EXIF strip via createImageBitmap, imageCompression call, and `new File([compressed], file.name, { type: file.type })` wrapper)
    - .planning/phases/36-mobile-performance/36-PATTERNS.md §"src/lib/wallecx/compressToWebP.ts" → ManageExpense.vue per-call-site change
    - .planning/phases/36-mobile-performance/36-RESEARCH.md §"Pitfall 4: Changing File Type in FormData Without Updating File Object"
    - src/lib/wallecx/compressToWebP.ts (Plan 36-01 output)
  </read_first>
  <action>
    **Edit 1 — Imports:**
    - REMOVE line 3: `import imageCompression from 'browser-image-compression'`
    - ADD: `import { compressToWebP } from '@/lib/wallecx/compressToWebP'`

    **Edit 2 — Replace the imageCompression block AND update the File wrapper (around lines 205–210):**

    Find:
    ```typescript
    const compressed = await imageCompression(strippedFile, {
      maxSizeMB: 1.5,
      maxWidthOrHeight: 2048,
      useWebWorker: true,
    })
    pendingFile.value = new File([compressed], file.name, { type: file.type })
    ```

    Replace with (CRITICAL: change `{ type: file.type }` → `{ type: 'image/webp' }` — Pitfall 4):
    ```typescript
    const compressed = await compressToWebP(strippedFile)
    pendingFile.value = new File([compressed], file.name, { type: 'image/webp' })
    ```

    **Invariants — do NOT touch:**
    - PDF bypass at the top of `onFileSelect`: `if (file.type === 'application/pdf') { pendingFile.value = file; return; }` — byte-intact (PDFs never reach compressToWebP per helper contract).
    - The EXIF-strip step using `createImageBitmap(file)` → canvas drawImage → `canvas.toBlob` → `new File([blob], file.name, { type: file.type })` to produce `strippedFile` — byte-intact (helper does NOT do EXIF stripping; D-36-09).
    - `toast.info('Location data removed.')` — byte-intact.
    - `allowedTypes` validation runs BEFORE EXIF strip and compression — byte-intact.
    - Receipt upload to PocketBase (FormData append) — byte-intact (will receive the WebP-typed File).
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <acceptance_criteria>
    - ManageExpense.vue contains `import { compressToWebP } from '@/lib/wallecx/compressToWebP'`
    - ManageExpense.vue does NOT contain `import imageCompression from 'browser-image-compression'` (grep returns 0)
    - ManageExpense.vue does NOT contain a direct `imageCompression(` call (grep returns 0 — the call AND the import must both be gone, per Anti-Pattern)
    - ManageExpense.vue contains `await compressToWebP(strippedFile)`
    - ManageExpense.vue contains `new File([compressed], file.name, { type: 'image/webp' })` (Pitfall 4 — type changed from file.type to 'image/webp')
    - ManageExpense.vue does NOT contain `new File([compressed], file.name, { type: file.type })` (grep returns 0 — old wrapper gone)
    - ManageExpense.vue STILL contains `if (file.type === 'application/pdf')` (PDF bypass byte-intact)
    - ManageExpense.vue STILL contains `toast.info('Location data removed.')` (EXIF-removed toast byte-intact)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>ManageExpense.vue routes image compression through shared helper; File wrapper type set to 'image/webp'; PDF bypass + EXIF strip + toast invariants preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ManageMembership.vue — swap imageCompression call (direct compressed assign)</name>
  <files>src/components/projects/wallecx/ManageMembership.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageMembership.vue (current file — line 4 imageCompression import; onFileSelect block around lines 214–220)
    - .planning/phases/36-mobile-performance/36-PATTERNS.md §"src/lib/wallecx/compressToWebP.ts" → ManageMembership.vue per-call-site change
    - src/lib/wallecx/compressToWebP.ts (Plan 36-01 output)
  </read_first>
  <action>
    **Edit 1 — Imports:**
    - REMOVE line 4: `import imageCompression from "browser-image-compression";`
    - ADD: `import { compressToWebP } from '@/lib/wallecx/compressToWebP'`

    **Edit 2 — Replace the imageCompression block (around lines 214–220):**

    Find:
    ```typescript
    const compressed = await imageCompression(strippedFile, {
      maxSizeMB: 1.5,
      maxWidthOrHeight: 2048,
      useWebWorker: true,
    });
    pendingFile.value = compressed;
    ```

    Replace with:
    ```typescript
    const compressed = await compressToWebP(strippedFile)
    pendingFile.value = compressed
    ```

    NOTE: ManageMembership does NOT wrap in `new File(...)` — it assigns the helper-returned File directly. The helper's File has `type === 'image/webp'` already (no Pitfall 4 concern here — the type travels with the File).

    **Invariants — do NOT touch:**
    - PDF bypass — Membership cards are images-only per Phase 11 D-02; the validation gate stays.
    - EXIF strip via `Image` + `URL.createObjectURL` approach — byte-intact.
    - ColorPicker direct v-model (`cardColor` ref; PrimeVue #8135) — D-13 invariant; preserve.
    - `card_color` no-hash invariant (CON-CARD-COLOR-NO-HASH) — preserve mapper round-trip.
    - `toast.info('Location data removed.')` — byte-intact.
    - `{ immediate: true }` record watcher — Phase 35 D-35-04 invariant; preserve.
  </action>
  <verify>
    <automated>npm run type-check && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - ManageMembership.vue contains `import { compressToWebP } from '@/lib/wallecx/compressToWebP'`
    - ManageMembership.vue does NOT contain `import imageCompression from "browser-image-compression"` (grep returns 0)
    - ManageMembership.vue does NOT contain a direct `imageCompression(` call (grep returns 0)
    - ManageMembership.vue contains `await compressToWebP(strippedFile)`
    - ManageMembership.vue contains `pendingFile.value = compressed` (direct assign preserved)
    - membershipMapper tests still green (`npm run test:unit` ≥ 59 tests)
    - `npm run type-check` exits 0
  </acceptance_criteria>
  <done>ManageMembership.vue routes image compression through shared helper; direct compressed assign preserved; ColorPicker + card_color + watcher invariants untouched; membership mapper 11 tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ManageVaccination.vue — swap imageCompression call (direct compressed assign)</name>
  <files>src/components/projects/wallecx/ManageVaccination.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageVaccination.vue (current file — line 6 imageCompression import; onFileSelect block around lines 184–190; gallery allowlist includes application/pdf — preserve)
    - .planning/phases/36-mobile-performance/36-PATTERNS.md §"src/lib/wallecx/compressToWebP.ts" → ManageVaccination.vue per-call-site change
    - src/lib/wallecx/compressToWebP.ts (Plan 36-01 output)
  </read_first>
  <action>
    **Edit 1 — Imports:**
    - REMOVE line 6: `import imageCompression from "browser-image-compression";`
    - ADD: `import { compressToWebP } from '@/lib/wallecx/compressToWebP'`

    **Edit 2 — Replace the imageCompression block (around lines 184–190):**

    Find:
    ```typescript
    const compressed = await imageCompression(strippedFile, {
      maxSizeMB: 1.5,
      maxWidthOrHeight: 2048,
      useWebWorker: true,
    });
    pendingFile.value = compressed;
    ```

    Replace with:
    ```typescript
    const compressed = await compressToWebP(strippedFile)
    pendingFile.value = compressed
    ```

    **CRITICAL — PDF bypass MUST stay above the EXIF-strip+compression path:**

    ManageVaccination accepts PDFs from gallery (Phase 35 D-35-05 — "gallery accept includes application/pdf"). The existing PDF bypass `if (file.type === 'application/pdf') { pendingFile.value = file; return; }` MUST execute BEFORE the EXIF strip and before compressToWebP is reached. Verify after the edit that the early-return path is byte-intact.

    **Invariants — do NOT touch:**
    - PDF bypass (gallery-PDF support; Phase 35 D-35-05) — byte-intact.
    - EXIF strip via `Image` + `URL.createObjectURL` approach — byte-intact.
    - `administeredDate` direct v-model + `[visible, record]` tuple watch `{ immediate: true }` (D-33-01-A; PrimeVue Forms #8191) — byte-intact.
    - Camera input images-only; gallery input images+PDF — byte-intact.
    - VaccinationSnapshot isDirty / closeWithoutGuard (Phase 35 D-35-05) — byte-intact.
    - `toast.info('Location data removed.')` — byte-intact.
  </action>
  <verify>
    <automated>npm run type-check && npm run test:unit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - ManageVaccination.vue contains `import { compressToWebP } from '@/lib/wallecx/compressToWebP'`
    - ManageVaccination.vue does NOT contain `import imageCompression from "browser-image-compression"` (grep returns 0)
    - ManageVaccination.vue does NOT contain a direct `imageCompression(` call (grep returns 0)
    - ManageVaccination.vue contains `await compressToWebP(strippedFile)`
    - ManageVaccination.vue contains `pendingFile.value = compressed`
    - ManageVaccination.vue STILL contains `if (file.type === 'application/pdf')` (PDF bypass byte-intact — Phase 35 gallery-PDF support)
    - ManageVaccination.vue STILL contains `administeredDate` direct v-model usage (D-33-01-A preserved)
    - `npm run type-check` exits 0
    - `npm run test:unit` ≥ 59 tests pass
    - `npm run lint` clean except grandfathered VaccinationDetail.vue:5
  </acceptance_criteria>
  <done>ManageVaccination.vue routes image compression through shared helper; PDF bypass + administeredDate + EXIF strip + isDirty invariants untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PocketBase /api/files/wallecx_*/{id}/{filename} | Existing upload path; the only change is the MIME type of the uploaded File object (image/webp vs image/jpeg). PocketBase accepts both. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-15 | Tampering | PDFs accidentally compressed | mitigate | All 3 files keep the PDF early-return BEFORE the EXIF strip + compressToWebP path. Acceptance criteria grep enforces `if (file.type === 'application/pdf')` is still present in ManageExpense and ManageVaccination. |
| T-36-16 | Information Disclosure | EXIF data accidentally retained | mitigate | EXIF stripping stays in each call site (canvas re-encode produces a `strippedFile`); the helper receives the already-stripped File. The 3 different canvas approaches are preserved verbatim per D-36-09. |
| T-36-17 | Tampering | MIME type mismatch between File object and uploaded bytes | mitigate | Pitfall 4: ManageExpense's `new File([compressed], file.name, { type: ... })` wrapper has its `type` changed from `file.type` (e.g. image/jpeg) to `'image/webp'` to match the actual compressed bytes. Acceptance criteria enforce the exact replacement. |

**No HIGH-severity threats introduced.** PocketBase server already validates file uploads via collection rules; this change is purely client-side encoding optimization.
</threat_model>

<verification>
- `npm run type-check` — 0 errors
- `npm run test:unit` — ≥59 tests pass (membership mapper 11 tests still green)
- `npm run lint` — clean except grandfathered VaccinationDetail.vue:5
- Grep audit: `grep -rc "imageCompression(" src/components/projects/wallecx/Manage*.vue` returns 0 (all routed through compressToWebP)
- Grep audit: `grep -rc "import imageCompression from" src/components/projects/wallecx/Manage*.vue` returns 0 (direct imports removed)
- Grep audit: `grep -c "{ type: 'image/webp' }" src/components/projects/wallecx/ManageExpense.vue` returns 1 (Pitfall 4 fixed)
- Grep audit: `grep -c "if (file.type === 'application/pdf')" src/components/projects/wallecx/Manage{Expense,Vaccination}.vue` returns ≥2 (PDF bypass byte-intact)
</verification>

<success_criteria>
- All 3 Manage* dialogs upload WebP-typed Files
- 0 `imageCompression(` calls remaining in Manage* files; 0 direct imports remaining
- ManageExpense `new File(...)` wrapper updated to 'image/webp' type (Pitfall 4 closed)
- PDF bypass + EXIF strip + direct compressed assign + Phase 35/D-33 invariants byte-intact
- Gates green
</success_criteria>

<output>
After completion, create `.planning/phases/36-mobile-performance/36-06-SUMMARY.md`. Record any easily-observed file-size delta for a sample image upload (pre/post WebP) in the summary; the formal sample comparison happens in Plan 36-07.
</output>
