# Phase 36 — Resume Handoff (paused 2026-05-28 at human-verify checkpoint)

## State at pause

- **Plans 36-01 through 36-06: COMPLETE and committed.**
- **Plan 36-07 auto tasks (T1 preconnect, T2 post-analyze + 20/20 grep audits PASS, T3 STATE update): COMPLETE and committed** (`910ff91`, `512b344`, `9358c5c`).
- **Plan 36-07 task 4 (`checkpoint:human-verify`): OPEN — awaiting user re-verification** after the WebP-thumbnail fixes below.
- WallecxApp gzip: **64.09 KB → 2.13 KB (-96.7%)** vs 36-BASELINE; target ≥50% met 15×.

## Regressions found during human-verify + their fixes (already committed)

1. **`79dda03` fix(36): skip ?thumb for WebP files** — PocketBase thumb generator 404s on WebP sources. Patched 5 sites with a `.webp`-filename fallback (ManageExpense receipt, ManageMembership card, VaccinationGroupCard, VaccinationList, AttachmentPreview 400×400).
2. **`1d9747b` fix(36): rename WebP output to .webp + always-skip thumb on ManageExpense receipt** — root cause for the lingering broken receipt thumbnail: `compressToWebP` was storing WebP bytes under the original `.jpg` filename, so the `.webp` fallback check missed them. compressToWebP now renames its output to `.webp`; ManageExpense onFileSelect uses the helper output directly (drops the post-helper File wrapper that re-applied the .jpg name); ManageExpense receipt thumbnail ALWAYS skips `?thumb` to also cover legacy DB records.

## Remaining behaviors to confirm on resume (paste yes/no per the 36-07 checkpoint instructions)

1. Cold-load Vaccinations tab CLS ≤ 0.1
2. Cold-load Memberships tab CLS ≤ 0.1
3. Cold-load Expenses tab + Reports sub-tab CLS ≤ 0.1
4. PF-05 `[wallecx:perf]` console lines + `localStorage['wallecx:perf-baseline']` JSON present
5. WebP upload MIME = `image/webp` AND edit-dialog receipt thumbnail renders (post-fix)
6. Suspense fallback only on first tab switch (or graceful re-show acceptable)

Real-device cellular PF-05 baseline + real-device CLS final → carried to Phase 38 UAT sweep (same precedent as Phase 35).

## Remaining orchestrator work after checkpoint approved

1. Spawn `gsd-executor` continuation to write `36-07-SUMMARY.md` (record T1-T4 done, deviation: WebP thumb fallback fix in `79dda03`/`1d9747b`, audit numbers from `36-07-AUDIT.md`).
2. Regression gate: `npm run test:unit` (already green throughout).
3. Spawn `gsd-verifier` on phase 36 — verify all 6 PF requirements + invariants (LOCKED vite lines, ConfirmDialog singleton, WebP MIME).
4. Spawn `gsd-code-reviewer` on the phase diff (BaseMobileDialog → perfInstrument is the riskiest new code path; the WebP thumb fallback was just hand-applied — review it for missed sites).
5. Write `36-HUMAN-UAT.md` recording emulation pass + Phase 38 deferrals.
6. `gsd-tools.cjs phase complete 36` → updates ROADMAP/STATE/REQUIREMENTS.
7. Commit doc artifacts + present completion summary. Then offer `/gsd-discuss-phase 37` (PWA Install + Standalone Polish).

## Environment caveat

`gsd-sdk` is NOT on PATH. All `gsd-sdk query X.Y ...` workflow calls map to `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" X Y <args>`. The mapping is also saved in this project's memory (`reference_gsd_sdk_invocation.md`).

## Resume command

`/gsd-execute-phase 36` (will detect this open checkpoint and route back to the human-verify task), OR `/gsd-resume-work` from a clean session.
