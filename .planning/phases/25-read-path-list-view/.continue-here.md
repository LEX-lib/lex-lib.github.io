---
context: phase
phase: 25-read-path-list-view
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-05-21
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> No blocking constraints encountered this session. Wave 1 executed cleanly.

_Remove this section if empty — kept for structural completeness._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| @vueuse/core import | Package is NOT installed. D-09 (CONTEXT.md) said to use `useDebounce` from it — do not re-add it. Existing reactive instant filtering is the correct pattern. | advisory | Check `grep "vueuse/core" package.json` returns 0 before any import |
| defineModel on Toolbar | ExpensesToolbar must use `defineProps` + `defineEmits` (NOT `defineModel`) — mirrors WallecxToolbar.vue pattern. Using `defineModel` would break v-model compatibility. | advisory | PATTERNS.md §Pattern 4 documents this explicitly |
| ConfirmDialog in ExpensesTab | Do NOT add `<ConfirmDialog />` to ExpensesTab.vue. STATE.md architectural invariant: ConfirmDialog is at WallecxApp.vue shell level only. | advisory | acceptance_criteria in 25-02-PLAN.md: `grep "ConfirmDialog" ExpensesTab.vue` must return 0 |

<current_state>
Phase 25: Read Path — List View is mid-execution.

Wave 1 DONE (Plan 25-01):
- `src/components/projects/wallecx/ExpenseItem.vue` — created and committed (e05b206)
- `src/components/projects/wallecx/ExpensesToolbar.vue` — created and committed (a8cf273)
- `25-01-SUMMARY.md` — written and committed (f527520)

Wave 2 PENDING (Plan 25-02):
- `src/components/projects/wallecx/ExpensesTab.vue` — Phase 24 stub needs to be extended with full Phase 25 implementation

Session paused due to context exhaustion (90%+) before Wave 2 could start.
</current_state>

<completed_work>

Completed Tasks:
- **25-01 Task 1**: Create ExpenseItem.vue — Done (commit e05b206)
  - Compact flex row: amount (w-24 bold brand) | meta+description (flex-1 min-w-0) | paperclip (conditional) | pencil | trash
  - All 3 action buttons at min-w-[44px] min-h-[44px] touch-manipulation
  - `v-if="record.receipt"` (falsy check — handles "" and undefined)
  - All colours via CSS variables (auto dark mode)

- **25-01 Task 2**: Create ExpensesToolbar.vue — Done (commit a8cf273)
  - Two-row layout: Row 1 (search IconField + sort Select), Row 2 (category MultiSelect + 2 DatePickers, flex-wrap)
  - Pure v-model relay — defineProps + defineEmits, NO defineModel, NO @vueuse/core
  - 5 emit channels: update:searchQuery, update:sortMode, update:selectedCategories, update:dateFrom, update:dateTo
  - Dark mode scoped CSS for p-inputtext, p-select, p-multiselect, p-datepicker
  - Deviation: DatePicker @update:model-value uses `instanceof Date` cast for TypeScript overload safety

Earlier in session (plan-phase):
- RESEARCH.md written (key finding: @vueuse/core not installed)
- PATTERNS.md written (3 files mapped to analogs)
- 25-01-PLAN.md + 25-02-PLAN.md written and plan-checker verified (all 12 dimensions passed)
</completed_work>

<remaining_work>

**Wave 2 — Plan 25-02 (ExpensesTab.vue):**

- **Task 1 (script additions):** Extend the Phase 24 stub with:
  - Add `onMounted, computed, watch` to existing vue import
  - Add imports: `dayjs`, `useIsMobile`, `ExpensesToolbar`, `ExpenseItem`, `AttachmentPreview`
  - Add 5 filter/sort refs: `sortMode`, `searchQuery`, `selectedCategories`, `dateFrom`, `dateTo`
  - Add receipt preview refs: `showPreview`, `previewRecord`, `previewToken`
  - Add `expenseSortOptions` constant (5 options)
  - Add `sessionStorage` watcher for `wallecx:expense-sort`
  - Add `categoryOptions` computed (from raw `expenses.value`, NOT filteredSortedExpenses)
  - Add `filteredSortedExpenses` computed (4-step: search → category → date range → sort)
  - Add `clearFilters()` helper
  - Add `openReceiptPreview()` with `pb.files.getToken()` + token-failure abort pattern
  - Add `onMounted` (restore sort from sessionStorage BEFORE getFullList call)

- **Task 2 (template):** Replace Phase 24 stub template inner content with:
  - ExpensesToolbar (v-if="!isLoading", all 5 v-models + categoryOptions + sortOptions)
  - 3 skeleton rows at height="3rem" (v-if="isLoading")
  - No-records empty state (v-else-if="expenses.length === 0") with mdi:cash-multiple icon
  - Filters-empty state (v-else-if="filteredSortedExpenses.length === 0") with mdi:filter-remove-outline + "Clear filters" button
  - ExpenseItem v-for (v-else) with @edit/@delete/@preview wired
  - AttachmentPreview in Dialog (desktop, v-if="!isMobile") and Drawer bottom (mobile, v-else)
  - Preserve ManageExpense wiring from Phase 24 stub
  - Final build verification: `npm run type-check` + `npm run build-only` must exit 0
</remaining_work>

<decisions_made>

- **No debounce for search** — `@vueuse/core` not installed; reactive instant filtering matches existing VaccinationsTab + MembershipsTab pattern. D-09 in CONTEXT.md is superseded by this research finding.
- **DatePicker instanceof Date cast** — PrimeVue DatePicker emits a union type wider than `Date | null`; `instanceof Date ? $event : null` cast in @update:model-value handlers satisfies TypeScript. Applied in ExpensesToolbar.vue.
- **Two-row toolbar layout with flex-wrap** — Row 2 (category + dates) uses `flex-wrap` so DatePickers drop to new line on narrow viewports instead of overflowing.
- **categoryOptions derived from raw expenses.value** — NOT from filteredSortedExpenses (RESEARCH.md Pitfall 3: feedback loop where selected categories disappear from MultiSelect).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/phases/25-read-path-list-view/25-02-PLAN.md` — the complete plan for Wave 2; read in full before executing
2. `.planning/phases/25-read-path-list-view/25-CONTEXT.md` — 18 locked decisions; D-09 overridden by research finding (no @vueuse/core)
3. `.planning/phases/25-read-path-list-view/25-RESEARCH.md` — critical pitfalls (Pitfall 3 = categoryOptions source, Pitfall 4 = v-if chain order)
4. `.planning/phases/25-read-path-list-view/25-PATTERNS.md` — analog code for ExpensesTab patterns
5. `src/components/projects/wallecx/ExpensesTab.vue` — current Phase 24 stub; read FULLY before touching it
6. `src/components/projects/wallecx/MembershipsTab.vue` — direct analog; copy patterns verbatim

## Critical Anti-Patterns (do NOT repeat these)

- **DO NOT overwrite Phase 24 stub code** — the existing `deleteExpense`, `onCreated`, `onUpdated`, `openManage`, `defineExpose`, `ManageExpense` wiring must be preserved. Task 1 ADDS to the file, not replaces it.
- **DO NOT put `categoryOptions` computed on `filteredSortedExpenses`** — it MUST derive from `expenses.value` (the raw unfiltered array).
- **DO NOT add `<ConfirmDialog />`** to ExpensesTab.vue template — it is at WallecxApp.vue shell level only.
- **DO NOT use `@vueuse/core`** — package is not installed.

## Infrastructure State

- Branch: `feat/wallecx`
- Last commit: `f527520` — docs(25-01): complete ExpenseItem + ExpensesToolbar plan
- `components.d.ts` modified (auto-updated by type-check run during Wave 1) — minor, will be committed by next executor run
- `25-PATTERNS.md` untracked — will be auto-picked up by executor commit step

<context>
This is a clean mid-execution pause. Wave 1 completed without issues. The two new child components (ExpenseItem.vue and ExpensesToolbar.vue) are committed and ready for import. Wave 2 is purely about wiring ExpensesTab.vue — extending the Phase 24 stub with 8 script additions (Steps 1–8 in Task 1) and replacing the template inner content (Task 2). The plan is extremely detailed with verbatim code blocks for every addition, so the executor should be able to work from the plan text alone.
</context>

<next_action>
Run `/clear` then `/gsd-execute-phase 25` — the executor will discover that 25-01 already has a SUMMARY.md and skip it, proceeding directly to Wave 2 (25-02-PLAN.md). No research or planning needed.
</next_action>
