---
phase: 35-forms-dialogs-on-small-screens
plan: 05
type: execute
wave: 5
depends_on: ["35-01", "35-02", "35-03", "35-04"]
files_modified:
  - src/components/projects/wallecx/ManageVaccination.vue
autonomous: true
requirements: [FD-07, FD-03, FD-04, FD-05, FD-09, LT-08, FD-06]
must_haves:
  truths:
    - "ManageVaccination renders through a single BaseMobileDialog instance with exactly ONE <Form> (the two Phase-34 Form instances collapsed)"
    - "administeredDate stays a direct v-model ref OUTSIDE @primevue/forms (#8191 / D-33-01-A)"
    - "The [visible, record] tuple watch with { immediate: true } that seeds administeredDate is preserved; date pre-fills in Edit mode"
    - "Date administered DatePicker uses :inline=isMobile; card upload offers Take photo + Choose from gallery (images + PDF)"
    - "Backdrop-tap on a dirty form prompts 'Discard changes?'; Save/Cancel bypass via closeWithoutGuard()"
  artifacts:
    - path: "src/components/projects/wallecx/ManageVaccination.vue"
      provides: "ManageVaccination migrated to BaseMobileDialog, two-Form collapsed to one, administeredDate invariant preserved"
      contains: "BaseMobileDialog"
  key_links:
    - from: "ManageVaccination DatePicker"
      to: "administeredDate ref (direct v-model, outside Form)"
      via: "v-model='administeredDate' NOT name-bound"
      pattern: "v-model=\"administeredDate\""
    - from: "ManageVaccination [visible, record] watch"
      to: "administeredDate seed"
      via: "tuple watch { immediate: true }"
      pattern: "administeredDate"
---

<objective>
Migrate `ManageVaccination.vue` (LAST in the locked order A-43-2 — highest-risk: two `<Form>` instances must collapse to one, and the `administeredDate` direct-v-model #8191 invariant must survive) onto `BaseMobileDialog`. The two Phase-34 `<Form>` blocks (Dialog branch + Drawer branch) become ONE `<Form>` in the `#default` slot. The `administeredDate` ref and its `[visible, record]` tuple watch stay in the child, OUTSIDE `@primevue/forms`. Apply FD-03/04/05/09.

Purpose: Final migration. The two-Form collapse is safe because both branches rendered identical Form markup; Vue's provide/inject for `@primevue/forms` follows DOM parentage through the slot boundary (RESEARCH Pitfall 4). administeredDate must pre-fill correctly in Edit mode (Pitfall 5).

Output: ManageVaccination.vue migrated with one Form; administeredDate invariant intact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-forms-dialogs-on-small-screens/35-RESEARCH.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-UI-SPEC.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-PATTERNS.md
@.planning/phases/35-forms-dialogs-on-small-screens/35-04-SUMMARY.md

<interfaces>
<BaseMobileDialog v-model:visible :title :is-dirty :is-saving ref="baseDialogRef">
  <Form v-slot="$form" :initialValues :resolver @submit="onSubmit" ...>
    <!-- named fields ... -->
    <!-- administeredDate DatePicker sits OUTSIDE/alongside named fields, direct v-model -->
  </Form>
  <template #actions><!-- Save/Cancel --></template>
</BaseMobileDialog>
baseDialogRef.value?.closeWithoutGuard()

INVARIANTS (must not change):
- administeredDate: ref<Date|null>, DIRECT v-model, NOT name-bound to @primevue/forms (#8191 / D-33-01-A)
- [visible, record] tuple watch with { immediate: true } seeds administeredDate — do not remove (Pitfall 5)
- Manual dateAdministeredError required-check stays in onSubmit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate ManageVaccination.vue to BaseMobileDialog, collapse two Forms to one, preserve administeredDate invariant</name>
  <files>src/components/projects/wallecx/ManageVaccination.vue</files>
  <read_first>
    - src/components/projects/wallecx/ManageVaccination.vue (entire file — the TWO <Form> instances at ~225 (Dialog) and ~343 (Drawer); administeredDate ref ~32; [visible, record] tuple watch ~54-62; onHide reset ~207-210; EXIF path ~75-141; onSubmit FormSubmitEvent ~143; card FileUpload accept ~303-308; date DatePicker direct v-model)
    - src/components/projects/wallecx/ManageMembership.vue (just-migrated reference from Plan 04 — BaseMobileDialog usage, onRawFileChange, dirty snapshot)
    - src/components/projects/wallecx/BaseMobileDialog.vue (slot contract + closeWithoutGuard)
    - 35-PATTERNS.md §ManageVaccination.vue (two-Form→one-Form collapse, administeredDate invariant + tuple watch, onHide, EXIF, FD-05 images+PDF, onSubmit, dirty snapshot, FD-03) + §Invariant Cross-Reference
    - 35-RESEARCH.md §8 ManageVaccination map, Pitfall 4 (Form context in slot), Pitfall 5 (administeredDate reseed on reopen)
    - 35-UI-SPEC.md Contract 4 (administeredDate inline field group), Contract 5 (camera images+PDF), Contract 7 ManageVaccination table
  </read_first>
  <action>
**Composable swap (Pattern S-2):** `useIsMobile` → `const { isMobile } = useMobileEnv()`.

**Collapse branches AND collapse two Forms to one (Pattern S-1 + Pitfall 4):** Replace `<Dialog v-if="!isMobile">...<Form>...</Form>...</Dialog>` + `<Drawer v-else>...<Form>...</Form>...</Drawer>` with ONE `<BaseMobileDialog v-model:visible="visible" :title="dialogHeader" :is-dirty="isDirty" :is-saving="isSaving" ref="baseDialogRef">` whose `#default` slot contains exactly ONE `<Form v-slot="$form" :initialValues="initialValues" :resolver="resolver" @submit="onSubmit" validate-on-submit class="space-y-4">`. The Form must be in the CHILD's slot content (NOT in BaseMobileDialog) so its provide/inject context wraps the named fields. Remove this file's duplicated dark-mode `:deep` scoped block.

**INVARIANT — administeredDate (#8191 / D-33-01-A, Pitfall 5):** The `administeredDate = ref<Date | null>(null)` stays declared in ManageVaccination. Its DatePicker uses DIRECT `v-model="administeredDate"` and is NOT `name`-bound — it sits inside the Form's visual layout but is NOT a `@primevue/forms` field. Preserve the `[visible, record]` tuple watch VERBATIM:
```typescript
watch(() => [visible.value, record.value] as const, ([isVisible, rec]) => {
  if (!isVisible) return
  administeredDate.value = rec ? new Date(rec.date_administered) : null
  dateAdministeredError.value = ''
}, { immediate: true })
```
Do NOT split it into two separate watches and do NOT remove `{ immediate: true }` — this is what makes Edit mode pre-fill the date and prevents stale-date-on-reopen.

**Date field markup (Contract 4):** `<DatePicker v-model="administeredDate" :inline="isMobile" fluid dateFormat="dd M yy" showButtonBar :disabled="isSaving" />` wrapped in the label + Message field group, with the manual `dateAdministeredError` Message.

**Import + ref:** `import BaseMobileDialog from './BaseMobileDialog.vue'`; `const baseDialogRef = ref<InstanceType<typeof BaseMobileDialog> | null>(null)`.

**onSubmit (preserve manual date validation):** keep `async function onSubmit({ valid, values }: FormSubmitEvent)`. Keep the manual `if (!administeredDate.value) { dateAdministeredError.value = 'Date administered is required.'; return }` check. Replace the success-path `visible.value = false` with `baseDialogRef.value?.closeWithoutGuard()`. Add `function onCancel(): void { baseDialogRef.value?.closeWithoutGuard() }`. Keep onHide reset (pendingFile + dateAdministeredError reset) running on close.

**Dirty snapshot (FD-09):** snapshot on `visible` rising edge; `isDirty` computed comparing the named-field values (read from refs or initialValues) + `administeredDate.value?.toISOString() ?? null` + `pendingFile.value !== null`. Use the per-PATTERNS approach (individual field snapshot is acceptable and simpler than initialValues stringify).

**FD-05 two-affordance (Contract 5 — IMAGES + PDF for vaccination):** Replace the card FileUpload with `<template v-if="isMobile">` two label-wrapped hidden inputs — camera (`accept="image/jpeg,image/png,image/webp" capture="environment"` — images only; camera can't make PDFs) + gallery (`accept="image/jpeg,image/png,image/webp,application/pdf"`). Desktop `v-else` keeps existing FileUpload (images+PDF accept). Add `onRawFileChange` bridge calling the existing `onFileSelect` (preserve EXIF pipeline + validation verbatim).

**FD-03 (Contract 7 ManageVaccination):** Vaccine Type / Vaccine Name / Lot Number / Manufacturer / Location InputText `inputmode="text" enterkeyhint="next"`; Dose Number InputNumber `inputmode="numeric" enterkeyhint="next"`; Notes Textarea `inputmode="text" enterkeyhint="done"` (last field); `autocomplete="off"` on all. These are `name`-bound Form fields — the HTML attrs pass through to the underlying input via Vue attr inheritance.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `ManageVaccination.vue` imports `BaseMobileDialog` and contains exactly one `<BaseMobileDialog` — `grep -c "<BaseMobileDialog" ManageVaccination.vue` returns 1
    - `ManageVaccination.vue` contains exactly one `<Form ` — `grep -c "<Form" ManageVaccination.vue` returns 1 (two collapsed to one)
    - `ManageVaccination.vue` does NOT contain `<Dialog ` and does NOT contain `<Drawer `
    - `ManageVaccination.vue` DatePicker uses direct `v-model="administeredDate"` and is NOT inside a `name=` binding
    - `ManageVaccination.vue` retains the `[visible.value, record.value] as const` tuple watch with `{ immediate: true }`
    - `ManageVaccination.vue` gallery input accept contains `application/pdf`; camera input has `capture="environment"` (images only)
    - `ManageVaccination.vue` contains `useMobileEnv`, `:inline="isMobile"`, `inputmode="numeric"`, `enterkeyhint="done"`, `closeWithoutGuard`, the manual `dateAdministeredError` required check
    - `npm run test:unit` passes (total >=59); `npm run type-check` exits 0
  </acceptance_criteria>
  <done>ManageVaccination renders through one BaseMobileDialog with exactly one Form; administeredDate direct-v-model + tuple watch {immediate:true} preserved; FD-03/04/05/09 applied; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device camera/gallery → card input | User-supplied image/PDF crosses into upload pipeline |
| Form + administeredDate → PocketBase | Vaccination record written to wallecx_vaccinations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-12 | Tampering | administeredDate required validation | mitigate | Manual `dateAdministeredError` required-check in onSubmit is preserved; the dirty guard governs dismissal only and cannot bypass this check (Save calls onSubmit → validation → closeWithoutGuard only on success). |
| T-35-13 | Tampering | onRawFileChange card bridge | mitigate | Routes through unchanged onFileSelect (size cap + MIME allowlist incl. PDF + EXIF strip). accept attrs are hints. |
| T-35-14 | Information Disclosure | card EXIF | mitigate | Existing EXIF-strip pipeline preserved before upload. |

No HIGH-severity threats. Required-field validation + EXIF strip + MIME allowlist preserved; dirty guard does not bypass save validation.
</threat_model>

<verification>
- `npm run test:unit` → passes (>=59)
- `npm run type-check` → 0 errors
- `grep -c "<BaseMobileDialog" ManageVaccination.vue` → 1; `grep -c "<Form" ManageVaccination.vue` → 1
- `v-model="administeredDate"` direct; tuple watch `{ immediate: true }` present
</verification>

<success_criteria>
ManageVaccination migrated to one BaseMobileDialog with exactly one Form, administeredDate direct-v-model + tuple watch invariant intact, FD-03/04/05/09 applied, tests green. All 4 dialogs now render through BaseMobileDialog.
</success_criteria>

<output>
After completion, create `.planning/phases/35-forms-dialogs-on-small-screens/35-05-SUMMARY.md`
</output>
